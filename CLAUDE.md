# genesis_pickaplace

Replays in-the-wild Kinova gen3-lite pick-and-place demos in the Genesis simulator.

- Entry point: `python example.py -v` (viewer), `-t <uid>` for specific trials, `-p 0|1|2` to filter by can position.
- Data: `./inthewild_trials/<uid>_episodes.npy` (gitignored; pickled dicts with `vel_cmd` + `gripper_pos`). Regenerable from ROS bags via `trial_reader.py` (needs ROS1).
- Env: use `~/workspace/genesis_sim2real/venv` (python 3.10, genesis-world 0.2.1 editable → `~/workspace/Genesis`, torch 2.7.0+cu126, cv2 4.11). E.g. `~/workspace/genesis_sim2real/venv/bin/python example.py -t 232`.
- See `CAN_STARTING_POSITION.md` for the can-position recovery effort status.

## Agent Status

- **HEAD SESSION (2026-09-08, low token budget — 🟡 paused).** All four subagent lanes STOPPED; their work is committed. Cluster jobs continue unattended: 12 de-confounded `(v)` e2e runs (4 seeds x WM/RLPD/DP), the queued 32 e2e learner jobs, A6 dose curve, 3 remaining place runs. **Nobody is watching them** — readouts must be run manually.
- **TOP PRIORITY ON RESUME: `paper/HANDOFF_episode_record_2026-09-08.md`** — amendment (w). Half committed (`7096fe6`: `full_env.py` emits sticky `ep_*` stage flags from the single exit path both terminate and truncate reach, gated OFF via `FULLENV_EPISODE_RECORD=1`). **Two wires remain and MUST be done together**: (1) the genesis adapter must copy those flags onto the TRANSITION as `log_`-prefixed keys — the trainer reads `trans`, not `info`, so the committed change currently reaches nothing; (2) the launcher must export the gate. Do NOT apply while e2e jobs are queued against the adapter (between-arm code difference). This is what unblocks **e2e placement and slide acquisition curves**, which do not exist today because the reward ladder is the only truncation-proof channel and neither is a rung.
- **`HRI_results/` is the writing-fidelity basis** (audited, committed): `CLAIMS_LEDGER.md` (claims keyed to sentences, with the wrong-sentence-to-avoid), `VERIFICATION.md` (per-row cluster provenance), `REGISTRATIONS.md` (35 predictions: 9 met, **11 FAILED**, 6 not evaluable, 1 demoted, 3 withdrawn), `AUDIT_GUIDE`, `WHAT_STANDS`. **Do not describe our registration record as clean.** Three exposures a writer must not trip: PAPER_PLAN **H4 is recorded FAILED** with a directional human preference and the current null was registered AFTER it — presenting the null as H4 confirmed inverts the record; **no TOST was ever run** on any contrast, so no equivalence sentence rests on its registered test; `slide_success` is "met" twice for a predicate (p) withdrew.
- **Numbers of record after today's re-scores** (all on the rebuilt bank, pinned hardware): place **0.715 v 0.652** (p 0.112), slide **0.581 v 0.598** (p 0.690), symmetry control **0.695 v 0.623** with the bank-of-origin clause at 0.009, e2e picked **0.492 v 0.537**, pick in-distribution **0.878 v 0.873** (DP) / **0.858 v 0.842** (RLPD). **Five of seven e2e stages have MDE above the ±0.10 margin** → "no effect detectable at this sample size", never "equivalent". Every non-significant e2e hint leans MACHINE; `nested_honest` 0.046 v 0.104.
- **The finding that governs interpretation:** most of the machine set's advantage is OUR best-of-3 selection — reward 206 v 131 de-selected, completions 16 v 8, and against the human set the advantage collapses from +75 % to +11 % (picked 63 v 64, indistinguishable). The `(v)` runs test this; until they land, no e2e result isolates demonstration source.
- **Robomimic: source claim WITHDRAWN twice over.** The quantity control killed it (MG718s 0.475 ≈ human 0.455, p 0.886; MGall 0.610 above), and the BC-RNN row compared two different policy classes (GMM head on human, deterministic on machine; interaction +0.667, matched-head gaps 0.834 vs 0.167). Cross-learner ordering withdrawn.

- **EVAL-FIXES / RE-SCORE AGENT (2026-09-08 15:40), 🟢 TOP-PRIORITY WORK LAUNCHED: PHASE_PLAN (v), the DE-CONFOUNDED end-to-end machine arm.** The end-to-end machine set of record keeps the **best of up to 3 attempts per start** (`--one-per-ic-best`) while the human set keeps **every** attempt; measured from the surviving 195-tape harvest, selection lifts Σ reward **131 → 206 (+57 %)**, doubles completed demonstrations **8 → 16**, and `best ≠ first` on **24 of 72** starts — so no end-to-end number (the `nested_honest` gap and the ignition effect included) currently separates demonstration *source* from our own tape filtering. Selection cannot be matched upward, so the machine arm is **de-selected**. Registered as **(v)** (`6d1915b`) BEFORE the build; `--one-per-ic-first` selector (`364e65c`); set `dDPfull_first` built in all three formats by the same converter/selector path as the set of record, with the registered numbers asserted by the build itself (72 starts, Σ 131, completions 8, picked 63 — now *below* human on picked 63 v 64 and contact 18 v 21, ahead only on completions 8 v 3), CHECK-OK row-for-row, sha `ebb7bd84`. **12 runs training: `v1st_wm_s0-3` (3377166-69), `v1st_rlpd_s0-3` (3377153-56), `v1st_dp_s0-3` (3374497-500)**, on `gpu`/**normal QOS** (a separate 10-GPU cap, so they do not queue behind my 65 pending preempt jobs). Two traps hit and fixed: the RLPD/DP launchers bake `--nice=9000` and `-p preempt` (forces the capped QOS); and the world-model adapter's (j)/(l) end-of-episode settle **did not exist when the human arm of record trained**, so it is now skippable (`R2D_EOE`, default unchanged) and the (v) world-model runs train with it off — disclosed in the amendment, with the consequence that their cells must come from the pinned post-hoc re-score. **ETA: Diffusion Policy ~3 h (10k of 100k steps in 10 min), RLPD ~10 h, world model ~13 h (2 seeds start when DP frees slots).** Also done: `HRI_results/AUDIT_GUIDE_2026-09-08.md` (provenance chain, registration check, the ten traps, defensible-vs-indefensible phrasing) + WHAT_STANDS/README/REVIEWER_ENTRY carrying the selection confound; pinned end-to-end re-score **COMPLETE, all 64 cells** (re-harvested; no registered row moved). Blocked on: nothing.

- **HRI_RESULTS AGENT (2026-09-08, branch `4dof-cartesian`, HEAD 7b227e7):** 🟡 IDLE — stood down by the coordinator; directory is in the state they wanted. No processes running, everything committed. **`HRI_results/` is a regenerable audit layer**, not a hand-written table: `make_tables.py [--refresh]` (~2 min, run DETACHED — a foreground call times out and looks like a crash) drives `harvest_cluster.py` + `harvest_nodes.py` on pax and regenerates `results.{csv,md}`, `fig_effects.{png,pdf}`, `METHODOLOGY.md`, and the three audit artefacts. Current: 21 OK, 21 provisional, 0 superseded, 2 withdrawn, 5 empty, 3 floor. **Stats:** exact permutation p (project statistic, unchanged) + MDE@80% + 95% CI, plus Bayesian P(Δ∈ROPE)/P(Δ>0)/BF01 from a SEED-RANDOM-EFFECTS logistic-binomial (pooling understates SE 2.5x), deterministic grid quadrature, 3 priors + separate-sigma. `python3 hri_stats.py` self-tests against published p 0.875 and MDE 0.345/CI ±0.245. **Three audit artefacts (the deliverable of record):** `CLAIMS_LEDGER.md` (per-SENTENCE: cells, strength, mandatory caveat, the sentence that would be WRONG, and for unsupported claims the licensing experiment; two rules enforced mechanically — R1 refuses "established" where MDE > 0.10, R2 forces the set-construction confound inline), `VERIFICATION.md` (per-row cluster paths/bank/hardware/script + KNOWN-STALE paths), `REGISTRATIONS.md` (**9 MET, 11 FAILED, 6 NOT EVALUABLE, 1 DEMOTED, 3 WITHDRAWN, 5 PENDING** — the record is NOT clean). **Row statuses:** OK / provisional / SUPERSEDED (re-score in flight) / WITHDRAWN (measured the wrong thing; unrepairable by re-scoring; inferential columns blanked) / floor / EMPTY-with-reason. **Eight loose claims surfaced** (coordinator has fixed the first two at both sites and logged all eight in the reviewer entry): (1) "all registered predictions met" is FALSE — in-distribution `placed_v2` 0.083 v 0.242 fails the margin, machine-favouring; (2) **the registered TOST equivalence procedure was NEVER RUN, on any contrast**; (3) PAPER_PLAN H4 (the original headline) is recorded FAILED with a directional human preference, and the later null was registered after; (4) amendment (k) spots60 has numbers but no met/not-met verdict, and its WM clause was never run; (5) `slide_success` "met" twice for a predicate the project withdrew; (6) registration-timing exposure (A16 after 3 of 8 seeds, A29 after 2, A27 after the full 8v8); (7) dv3 G3 registration timestamp postdates its submission by 7 min; (8) "world models are source-indifferent" is ONE task — the independent-task replication produced a learner that scored 0/400. **Confounds verified by me, not taken on report:** the machine full-task set is BEST-OF-THREE — human 118 / machine selected 206 / selection removed 131, so its lead collapses +75% → +11%; and the `_cp` re-score non-reproduction IS the hardware class (16/16 on 36-core AVX2, 0/176 elsewhere; no machine cell was ever scheduled there, so re-scoring is NOT directionally biased). My earlier contrary claims (an "unexplained arm-asymmetric defect", and "ISA and cores are not collinear") are both WITHDRAWN — the first was a node-attribution join bug, the second came from Slurm `AvailableFeatures`, which lie here. **Standing rules encoded:** a re-scored row keeps its predecessor and movement is reported in the DIFFERENCE (place SAMPLE: each arm moved ~0.02, Δ went +0.014→+0.056, 4.1x); when an upstream fix makes an as-recorded row unreproducible it becomes a dated STATIC NOTE rather than vanishing. **Pickup if asked:** `cd HRI_results && python3 make_tables.py --refresh` detached; place/e2e DP+RLPD are the empty cells (place RLPD both arms ~0.84 vs WM 0.65–0.70 = possible LEARNER gap worth a look). Blocked on: nothing.
- **EEF ACTION-DISTRIBUTION AGENT (sim box, offline, 2026-09-07; commit db859cf): 🟢 DONE (human half).** `can_pos_recovery/eef_action_dist.py` + `paper/EEF_ACTION_DIST_2026-09-07.md` + `paper/figures/fig_eef_action_dist_human.{png,pdf}`. Measures EEF displacement per 0.12 s decision (dx,dy,dz,dyaw) resampled on the REAL timestamps in `_timed.npz` (`fb_t`/`fb_tool`, clean 40.00 Hz) rather than the recorder's 4-frame stride (row 46 quantified: tapes 29.63-39.13 fps, median 32.79; the naive stride inflates p99 by up to +10.8 %). **Set of record determined locally, not invented: the 74 uids with a `_timed.npz` are SET-EQUAL to the w3 honest-census 74; 0 skipped;** the other 22 bags are 16 `fail`, 5 stubs, and uid 303. **Verified, not assumed:** `tool_pose` = xyz m + (theta_x,y,z) DEGREES, extrinsic-xyz (ROS RPY) - convention chosen by axis agreement with the measured `tool_twist` (base-frame cosine +0.84 vs ~0 for every alternative) and independently confirmed by URDF FK from `joint_pos` (0.0000 m median / 0.0023 m p95 to `tool_frame`); **theta_z IS the world-z yaw** (FK yaw vs theta_z r = 1.0000 median, constant 180.000 deg offset); |d yaw/d q| = [1.000, .069, .069, .820, .130, .013] so joint_1 is unit-sensitivity BUT joint_4 (0.82) also matters - the joint regression alone would have misled, FK settles it; theta_z never wraps here (0/74) though the code wraps anyway; `fb_tool` preferred over `_cartesian.npy` `tool_pose` (window mean, ~32.8 Hz, NO timestamps; they agree to 0.1 mm median). **Headline (n=37 216 decisions):** range is set by the joystick cap (p1/p99 = +-13.2 mm = VCAP*dt), **51.4 % of decisions move nothing**, channels near-independent (max |r| 0.215 = dz-dyaw), large motions axis-aligned (68 % single-axis above 5 mm), dyaw active in only 7.4 %. **Discrete-grid answer: a small grid is a POOR fit.** Quantization error over MOVING decisions is ~0.5 half-bins at every B (= the motion is analog); the flattering '79 % represented at B=3' is the zero atom. Open-loop tape drift p50 68 / 33 / 25 mm at B = 3 / 5 / 7 (can diameter 66 mm); **~15 levels per translation channel + ~6 for yaw** is the first honest setting (10 mm drift, quantization inside the 1 mm still-band). Explains why the ternary result on the COMMANDED joystick (DISCRETE_ACTION_REPLAY) does not transfer to the realized action. **NEW, AMBIGUOUS, for the head agent:** commanded `wz == 0` and `wx == 0` EXACTLY in all 74 tapes - the only commanded rotation channel is `wy`, which drives realized world-z YAW (r -0.55) and NOT realized theta_y (r 0.000), yet `baselines/cartesian_env.py` names it `PITCH_CAP`/`dpitch`; `cartvel_reference_frame` is UNSPECIFIED so the data cannot settle it - no code changed. **Blocked on:** the LEARNER half (needs cluster rollouts; VPN down). Drop-in: `--source <arm> --rollouts <npz> --bins paper/figures/eef_action_bins.npz`, then `--plot --source human --source <arm>` (overlay path smoke-tested). CLAUDE.md entry left UNCOMMITTED (the file carried pre-existing edits from the parent session).
- **CONTACT_PUSH AGENT (delegated 2026-09-07 15:10; DONE 2026-09-08 07:30):** 🟢 complete. `paper/CONTACT_PUSH_2026-09-07.md` (predicate, unit check, tool-vs-wrist bank table, S2-4 lists, full results, verdict, determinism note); PHASE_PLAN amendments (g), (g′), (g″), (g‴); code in `baselines/genesis_can_env.py` + `rl/full_env.py` + the private cluster copies + the r2dreamer adapter/evaluator; `baselines/diagnostics/contact_push_{check,bank_geometry}.py`; tables `~/wm_fix_2026-09-03/contact_push_table_2026-09-08.md`. **Sweep 3354669–76 finished 192/192 (8v8 everywhere).** `contact_push` = picked ∧ can↔goal contact ∧ dot(TOOL−can, goal−can) < 0 ∧ no gripper↔goal contact. **Results:** contact-after-release 0.346 v 0.366 (Δ −0.020, p 0.31); carrycontact 0.285 v 0.250 (Δ +0.035, p 0.55); end-to-end rnd30 MODE 0.204 v 0.212 (Δ −0.008, p 0.94). `slide_success` (l′) ≈ 0 at the phases, 0.017–0.058 end-to-end and mostly settle-only. **Finding: `contact` is not gripper-goal credit (11–33 % of failures) — it is carry-into-goal credit; 37–69 % of `contact` fails the stricter test.** Source null survives everywhere (P1 met); P2's ≤ 0.15 clause violated → registered wrong-side branch fires. **Framing recommendation: keep all three columns — `slide_success` as the task outcome (no arm learns a true slide), `contact_push` as the discriminating statistic, `contact` as the legacy predicate with its failing fraction attached.** Hardware: same physical core count reproduces 119/119; all 31 differing pairs involve a 36-core machine; CPU-family labels on this cluster are unreliable so the ISA question is open, not ruled out.
- **HEAD/ORCHESTRATION SESSION (pop-os, 2026-09-07 23:05):** 🟡 paused on infrastructure — **VPN DOWN since 22:37**, cluster unreachable (no tunnel iface; login DNS resolves only through it). Submitted jobs are unaffected and still running; monitoring, new submissions and rsync are blocked. **READ `paper/OVERNIGHT_STATE_2026-09-07.md` — it now opens with a consolidated morning brief** (decisions waiting, results that CHANGED overnight, what is new, what is running/held) superseding the 13-layer chronology below it. **First action on reconnect: kill up to 30 stray `srun --overlap` probes on the shared login node** (they made ssh itself time out), then the pinned e2e re-score pickup list at the end of that doc. **Decisions waiting for the user:** (1) Slide human set = 15 sim-successful tapes vs ~30 demonstrated-intent (registered as PHASE_PLAN (q); my recommendation: train on 15, disclose the 16 with cause, since machine demos are sim-achievable by construction) — 32 `sl_*` jobs held on this; (2) whether to raise e2e to 16v16 (MDE ≈ 0.21); (3) robomimic framing, but only after the controls land. **Corrections landed this session (commit 11b7c53):** 'deterministic' wording fixed in the four docs of record (mode IS deterministic for RLPD = tanh of mean, is NOT run-to-run deterministic for the WMs, which sample a latent inside `act` with no per-episode reseed; no comparison biased); RESULTS row + §7 item 9 and CONFOUNDS row 44 updated — **dv3 now HAS a working configuration** (state input + the r2dreamer return clamp; G2 PASS 0.700/0.700 human vs 0.5 threshold, machine 0.633/0.633; partial G3 n=2v2 human 0.700 vs machine 0.633, not testable at that n; the clamp diagnosis reproduces on an independent port ⇒ the WM arm is no longer single-port). CONFOUNDS 44 rung 3 (reference-task DMC/Atari validation) STILL OPEN for both ports — do not claim 'validated implementation'. **New this session:** robomimic DP arm complete — MH200 0.863 vs MG200s 0.095, p 0.00016 (complete separation); cross-learner ordering matches the thesis (BC-RNN Δ0.53, DP Δ0.77, RLPD Δ0.31) **but the MGall quantity control reads 0.74/0.60 on two seeds, ABOVE the human arm — do not write the ordering up as a finding until the controls read out.** Also answered: e2e preview/record split is enforced (table builder refuses mixed-role rows); **DP evaluates CPU-only but at 568 s/episode** (≈9.5 h per 60-start cell, ~300 CPU-h for the DP share of the pinned pass — user judgement call whether that beats the confound it removes); pinned RLPD and DP wall-clock on AVX2 are UNKNOWN, measure one cell each before committing 32 runs. Verified AVX2 nodes: pax109, pax154. **Running offline:** an agent building the human EEF per-decision action distribution (dx,dy,dz,dyaw over the 0.12 s decision grid) from local `inthewild_trials/*_cartesian.npy` + `_timed.npz`, incl. the discrete-action-grid answer the user asked for; learner halves blocked on the cluster. 7 tracked files remain modified = the sim-box agent's world-change work, left uncommitted per its own note.

- **END-TO-END (FULL TASK) DP+RLPD AGENT (cluster clone `$LAB/gp_e2e` @ c701f10, branch `e2e-work`; 2026-09-07 21:20):** 🟢 **SUBMITTED — 32 runs, `e2e_rlpd_*` 3355448–63 and `e2e_dp_*` 3355464–79, all PENDING at `--nice=9000`** (deliberately behind the robomimic recovery, dv3 G3 and the contact-phase runs; my GPU jobs also queue behind my own 19/20 GPU cap, so expect a long wait). Registered FIRST as **PHASE_PLAN amendment (n)** (local 68be767, before any job): the amendment-(d) world-model design repeated for the other two learners — `scope='full'`, staged sparse ladder, human `dHfull_all` (74) vs machine `dDPfull` (72), 8 seeds per arm per learner, RLPD 250k decisions / DP 100k grad steps, final checkpoint only, 150 GB disk guard. **Statistic of record: `slide_success` (amendments (l)/(l')), sampled, on `spots60`;** `rnd30` MODE reported as the cell comparable with PHASE_RESULTS §5.1. New code (local 8e66524, c8bc1cb, 36a5e7e): `baselines/rl/full_demos.py` (segment loader for staged-reward FULL-scope tapes + the DP set builder), `baselines/eval_e2e.py` (full-scope evaluator; the IC is injected by redirecting the env's OWN `GenesisCanEnv.reset` call, and ONE `end_of_episode()` per episode gives `slide_success` + honest `nested` beside the proxy), `baselines/e2e_table_all.py`, `cluster/{e2e_build_sets,e2e_eval_cells,sbatch_rlpd_e2e,sbatch_dp_e2e,submit_e2e}.sh`; `train_rlpd.py` gained `--scope full --demo-format segment` (pick/place/contact paths untouched). **Datasets built + verified** (`matched_w3/{dHfull_all,dDPfull}`, shas 70027c89 / 433fda6e, CHECK-OK row-for-row vs `$W/demos_state_full`): 74 tapes / 29 221 decisions / Σ118 / nested 3 vs 72 / 32 851 / Σ206 / nested 16. **Two disclosures measured, not assumed:** (1) **idle fraction 0.365 human (0.393 pre-pick) vs 0.007 machine** — the raw-vs-pruned DP confound amendment (n) disconfirm branch (iii) exists for (a machine-ahead |Δ| ≥ 0.10 for DP triggers a human-PRUNED control before the word "source"); (2) `convert_to_lerobot` drops 2 one-decision tapes per arm, so the DP datasets hold 72/74 and 70/72 episodes while RLPD and the WM train on all — symmetric, 0.007 % of rows, recorded in the manifests. **Smokes all PASS:** RLPD full pipeline (segments → staged reward → ckpts → TRAIN-OK → eval cell → 8 stage columns in `metrics.json`); DP provenance + lerobot gates; DP eval path on the existing full-task teacher (`dp_phase/dHfull_pruned_DP_s0`) picked and ended `nested_honest` on uid 252. Timing: ~29 s per 300-decision episode on CPU. **22:10 addendum (coordinator's node-sensitivity + order-dependence findings, landed BEFORE any of my cells exist — clone @ f46ba66, local 567b1d1):** every episode now stamps node/pid/within-process order and every cell stamps the node set + protocol; `eval_e2e.py --ic-index k` + `merge_e2e_iso.py` give one FRESH PROCESS per start (Genesis allows one world per process, so that is the only available isolation), and each run now writes BOTH the shared-process cell (the §5.1 protocol) and an `_iso` cell for rnd30 + spots60 — read with `e2e_table_all.py --cell-suffix _iso`. Training runs untouched: Slurm spooled the batch scripts, the eval stage is read at run time, and all 32 jobs were still PENDING when this landed. Measured: isolated costs 2.05x per episode (~59 s vs ~28.5 s; RLPD eval ~18 min -> ~50 min wall, DP ~35 -> ~75 min, both far inside their walltimes). Checked rather than assumed: the world-model rnd30/hold15 cells of record enumerate the same starts in the same order as this evaluator (30/30 can_pos, 15/15 uids), so the shared cells really are order-matched to §5.1. **Open decision for the coordinator/user: which protocol the three-learner table uses — shared keeps my rows comparable with the published WM row; `_iso` is more correct but needs §5.1 re-scored under isolation first. Both will exist, so no re-run either way.** **22:35 addendum 2 (hardware axis pinned to AVX2 vs AVX-512; Slurm labels unreliable — clone @ aa543eb, local 9447908, again BEFORE any cell exists and with all 32 jobs still PENDING):** `eval_e2e.py` now reads `/proc/cpuinfo` (never a Slurm feature label — pax001 advertises `broadwell` and is Cascade Lake) and stamps `cpu_model`/`isa`/`avx512f` on every episode plus their sets on every cell; `--require-isa avx2|avx512` refuses to produce a cell on the wrong class and fires before anything else (verified). `merge_e2e_iso.py` FAILS on a cell whose shards span two classes. `e2e_table_all.py` keys its warning off the ISA CLASS, not the node name: several nodes of one class are declared safe to combine, a cross-class comparison prints the arm x ISA balance and states that an even split is variance (inflating the MDE) while a skew is BIAS, and `--isa avx2` gives the clean within-class read. `cluster/isa_probe.sh` builds the real node->class map and emits the `--nodelist` for a pinned re-score (`--constraint` cannot do it). The 32 spooled batch scripts cannot be pinned after submission, so the node->class map my cells stamp is measured, and a pinned AVX2 re-score of the primary cells stays available post hoc (RLPD eval is pure CPU, so that re-score is cheap). **22:50 — VPN DOWN 22:37, no further cluster access attempted. READ `paper/OVERNIGHT_STATE_2026-09-07.md` section `## VPN DOWN 22:37 — pinned e2e re-score pickup` FIRST.** Everything of mine is committed locally (last code commit b7d7fab); the cluster clone is at aa543eb with the newer eval files rsynced-but-uncommitted (they landed ~22:33, so any job that starts will correctly stamp its in-job cells `role: preview`). Third addendum, committed before the drop: evaluation is DECOUPLED from training — in-job cells are `preview` and no table can pick them up (`e2e_table_all.py` refuses a row that mixes roles), the CELLS OF RECORD come from `cluster/sbatch_e2e_rescore.sh`, a pinned CPU-only pass writing under `<run>/rec/` that re-reads `/proc/cpuinfo` on arrival and dies if the node is not AVX2. **09-08 07:35 — link back, census done, both blocking measurements taken.** Clone re-synced + committed (884f643, `isa_probe.sh` removed); login node clean; `isa_map.json` deleted unread. **Census** (`hw_probe.sh` PAR=6, 120 machines → `hw_map.json`): 32c×5, 36c×24, 40c×25, 48c×10, **64c×55**, 96c×1 → **`REQUIRE_CORES=64` chosen** (most common non-36 size, no `--nodelist` needed). **Finding: core count and instruction set are PERFECTLY COLLINEAR here — all 24 AVX2 machines have exactly 36 cores, every other size is AVX-512, zero overlap** — so no comparison on this cluster can separate the two accounts; and since 40c and 64c (same ISA, different sizes) AGREED bit-for-bit, the evidence fits a **two-class split (36c/AVX2 vs the rest)** better than a per-size rule. Guard unchanged (pin 64 satisfies either). **Measurements on pinned 64-core nodes:** RLPD full rnd30 cell (30 eps, shared, 1 process) **858 s = 27.7 s/ep** (pax146); DP one episode CPU-only **425 s (390 s in-loop)** (pax048) — 1.46× faster than the 568 s login-node figure. **Extrapolated pinned pass: RLPD ~69 CPU-h, DP ~352 CPU-h, ~8 h wall for all 32 across 55 machines** — ordinary parallel work; DP's shared `spots60` cell (6.5 h serial) is the irreducible long pole. All 32 training jobs still PENDING at nice=9000, so the pinned pass has nothing to score yet. **Pickup:** `cd $LAB/gp_e2e && python3 baselines/e2e_table_all.py --cell-root rec --strat` once the pinned pass has run → a dated section in `paper/PHASE_RESULTS_2026-09-05.md` (do NOT rewrite §5/§5.1) + review reels; `rnd300` cells are post hoc (SETS=rnd300, ~2.5 h/cell on CPU).
- **SLIDE/CONTACT PHASE DP+RLPD (same agent; 2026-09-07 23:55): ON HOLD under PHASE_PLAN (p); 32 jobs 3355056-87 held (0 running), nothing rebuilt.** My amendment (o) ("the slide phase pays what it scores") is committed as **ce2226c (registration)** and **2b4b83d (implementation + measured yields)** and was deliberately NOT edited away — the coordinator registered the withdrawal as **(p) da228b1**, which withdraws (l)'s `grip_cmd < 0.3` clause (it passes 2 of 74 demonstrations; humans release fully and then push the can home with the fingers re-closed to ~0.4). **My independent full-set measurement corroborates it:** at the registered grip<0.30 clause, **0 of 74 human and 0 of 195 machine tapes** contain a 3-decision slide window; at 0.35/0.40/0.45 the human yield is 3/6/7 tapes and the machine 6/12/13; human contact-frame grip minima are 0.33–0.41 in 5 of the 11 (m) tapes. Also measured and relevant to the 11-vs-18 reconciliation: **7 human tapes reach `contact` with no `placed_v2` at all (7 ICs), and 11 + 7 = 18** — the entry gate, not the scorer, most likely explains the user's eyeball count. **Code hold state (20f49f8, clone synced):** `FullTaskEnv(scope='contact')` now REQUIRES an explicit `contact_grant` (fast pre-check, ~6 s, no default while (p) is uncalibrated); `slide_success` (the withdrawn (o) reward) raises unless `CONTACT_GRANT_ALLOW_WITHDRAWN=1`; `prior_release` ((p) clauses 1–5) raises NotImplementedError pending its clause-5 calibration; `bare_contact` reproduces the (m)/WM-of-record behaviour; kept from (o) — bare contact no longer ends the episode unless it is the named grant. `train_rlpd`/`eval_place` require `--contact-grant`; both contact launchers and the contact eval stage print SLIDE-ON-HOLD and exit 0. **Waiting on:** (p)'s clause-5 threshold calibration and the lineage decision (`dHfull_w3` vs census disagree on 24/74 action streams) before any segment is cut or any job released.
- **PICK + IN-DISTRIBUTION DP TABLES — DEFINITIVE (2026-09-08 14:20, all cells core-homogeneous on 64-core).** The 7 pinned re-runs COMPLETED; repair pass: 64 cells complete, 0 repaired, 0 failed. Both tables now self-report "all arms span the same core classes" (RLPD 1440 v 1440 episodes; DP 960/840/480, all 64-core). **RLPD pick, n=8v8, LAST ckpt, human dHv2raw v machine dDPv2:** sampled rnd30 134/240 (0.558) v 129/240 (0.537), Δ +0.021 p 0.658; deterministic rnd30 144/240 (0.600) v 136/240 (0.567), Δ +0.033 p 0.646; **sampled spots60 412/480 (0.858) v 404/480 (0.842), Δ +0.017 p 0.557**; deterministic spots60 416/480 (0.867) v 415/480 (0.865), Δ +0.002 p 1.000; sampled hold15 104/120 v 99/120 p 0.513; deterministic hold15 p 1.000. Sampling costs ~1 episode of 30 within-arm (human −1.25, machine −0.88). Dead seeds dHv2raw s65 / dDPv2 s55 included and flagged. **DP spots60 (selected ckpt):** dH 527/600 (0.878) v dDP 524/600 (0.873), Δ +0.005 p 0.845; dHv2raw 330/480 (0.688), −0.19 vs both, p 0.000 (the pruned-vs-raw DP gap reproduces in-distribution). 4 runs correctly report `LAST-MISSING` (pruned checkpoint) instead of falling back. **Core-class effect measured directly:** re-running the five 48-core DP seeds on 64-core reproduced their per-seed counts EXACTLY (53/51/55/50/50 both times), while the one 36-core RLPD seed (s61) moved +2/30 on sampled rnd30 and −1/60 on deterministic spots60 — consistent with 36-core boxes being the divergence source; the mixed-core originals are preserved as `*_mixedcore`. Every conclusion is unchanged by the fix (all p > 0.5 on the source comparison). **Three-learner PLACE table is BLOCKED on a bank re-score, not on my runs:** the world-model place cells carry no bank stamp and no rebuilt-bank re-score of that phase exists, so they were scored on the RAW-grip polE bank while my rows use the rebuilt physical-grip bank (30 of 148 entries differ in restore behaviour); `place_table_all.py` prints BANK VERSION NOT COMPARABLE and refuses to merge the rows.
- **PICK + IN-DISTRIBUTION DP TABLES READ OUT (2026-09-08 07:20; VPN back 07:02).** All 16 `rlpdsmp_*` and all 28 `dpspots_*` cells are COMPLETE (28/28 DP runs have a `selected_spots60` sweep.json, zero incomplete cells; 13 cells across the two waves had been lost to killed sweep parents and were recovered by `cluster/sweep_repair.sh`, 0 recomputed episodes). **RLPD pick, n=8v8, LAST ckpt, human dHv2raw vs machine dDPv2:** sampled rnd30 132/240 (0.550) v 129/240 (0.537), Δ +0.013 p 0.813; deterministic rnd30 144/240 (0.600) v 136/240 (0.567), Δ +0.033 p 0.646; **sampled spots60 (amendment (k), in-distribution) 413/480 (0.860) v 404/480 (0.842), Δ +0.019 p 0.540**; deterministic spots60 417/480 (0.869) v 415/480 (0.865), Δ +0.004 p 0.873; sampled hold15 104/120 v 99/120, p 0.513. Sampling costs ~1 episode of 30 within-arm (human −1.50, machine −0.88). Dead seeds dHv2raw s65 / dDPv2 s55 included and flagged. **DP spots60 (selected ckpt):** dH 527/600 (0.878) v dDP 524/600 (0.873), Δ +0.005 p 0.845; dHv2raw (raw human) 330/480 (0.688), −0.19 vs both, p 0.000 — the pruned-vs-raw DP gap reproduces in-distribution. 4 runs (dHv2raw s50/51/53/54) correctly report `final_spots60=LAST-MISSING` (checkpoint pruned) rather than falling back to the selected one. **CORE-HOMOGENEITY FIX:** both comparisons were core-heterogeneous — pick had dHv2raw s60 on 40-core and s61 on **36-core** with every dDPv2 cell on 64-core; DP had dH s25–29 on 48-core with dDP/dHv2raw on 64-core. Those 16 cells were archived as `*_mixedcore` and resubmitted pinned to 64-core (`--exclude` the 101 non-64 nodes): jobs 3362808–09 (pick) and 3362810–14 (DP). **The numbers above are provisional until those land.** Every future cell now stamps `node.cores` (both evaluators). Terminology: these are RLPD/DP cells, so "deterministic" is accurate; no world-model cell is involved and none of my cells is scored on bare `contact` (place scores `placed_v2`; the contact cells are held under (p)).
- **VPN DOWN 2026-09-08 ~00:30 (place/slide agent): cluster unreachable (`login.pax.tufts.edu` NXDOMAIN; general internet fine), all work continues cluster-side.** State at disconnect — **pick re-evals**: 14 of 16 `rlpdsmp_*` finished (2 `dHv2raw` still running); 6 `det_hold15` cells had been silently lost to a killed sweep parent and were RECOVERED with `cluster/sweep_repair.sh` (33 cells already complete, 0 failures, no episode recomputed); `rlpd_eval_sampled.sh` now self-heals via `run_sweep()`. **`dpspots_*`**: all 28 running since 22:27, verified clean on first real execution (RECORD-OK, correct selected/final checkpoints, 0 FATAL/Traceback). **`pl_*`** (32 place runs): queued. **`sl_*`** (32 slide runs): HELD under (p) and additionally refused in code (`SLIDE-ON-HOLD`). Clone `$LAB/gp_place` @ 73f904c. **On reconnect:** (1) `bash cluster/sweep_repair.sh '$LAB/genesis_pickaplace/baselines/rl/checkpoints/rlpd_g99v2fullw3_*'` to aggregate any cell whose parent died; (2) `python3 analysis/rlpd_sampled_table.py --root $LAB/genesis_pickaplace/baselines/rl/checkpoints` (sampled vs deterministic, hold15/rnd30/spots60, dead seeds flagged); (3) `python3 analysis/dp_spots60_table.py --root $LAB/genesis_pickaplace/baselines/outputs` once `dpspots` land; (4) `bash cluster/place_readout.sh` when the place runs finish. Partial pick signal (9–14 seeds, NOT a readout): spots60 56–60/60 vs rnd30 17–21/30; sampled costs ~1 episode of 30 vs deterministic; dead seeds `dHv2raw s65` / `dDPv2 s55` reproduce as dead.
- **PLACE-PHASE DP/RLPD AGENT (cluster clone `$LAB/gp_place` @ df3df3e, 2026-09-07 17:50):** 🟢 RESUBMITTED after the filesystem-full incident (every earlier job died at launch, exit 0:53; nothing of mine was lost — all 28 DP checkpoints the spots60 evals need verified present, the 4 dHv2raw runs missing `100000` were already pruned before the incident and the launcher handles it as LAST-MISSING). **Disk fixes now in the launchers (PHASE_PLAN addendum to (h), registered 17:35):** one lerobot checkpoint = 2.85 GB (949 MB weights + 1.9 GB optimizer), so DP place now uses `save_freq=STEPS/2`, prunes every superseded checkpoint AND the final `training_state` after training (949 MB/run at rest), and a requeue past the budget skips training (`TRAIN-DONE-ALREADY`); RLPD gained `train_rlpd --ckpt-every` (place runs: 0 = no snapshot zips) and archives only ckpt_040 (the registered 100k-decision pick read) + ckpt_100 (LAST) = 36 MB/run; `--no-video` added to wandb_eval/eval_sweep and used by the two bulk pick re-eval launchers (~46 GB of unused mp4s avoided; rollouts byte-identical). Disk guard (hold under 150 GB free) run before each batch: 444 GB free at submission. **Jobs:** batch 1 CPU re-evals `rlpdsmp_*` 3354534–49 (RUNNING) + `dpspots_*` 3354550–77; batch 2 place `pl_rlpd_*` 3354580–95, `pl_dp_*` 3354596–611 (PD). **Registered/validated earlier:** amendment (h) (DP+RLPD place, human 39 v machine matched-39, polE sampled+mode, |Δ|<0.10); data of record `matched_w3/{dH_place,dDP_place_n39}` (+lerobot, shas 38f986be/5473ed08, CHECK-OK vs `$W/demos_state`); smokes RLPD 0/10 S+M, DP 5/10 holdE; `place_table_all.py` reproduces PHASE_RESULTS §2.y exactly (0.703 v 0.647, p 0.227). Amendment (l) `slide_success` noted — does not touch these place cells; any later contact/full-task cell of mine uses it. **Pickup:** `cd $LAB/gp_place && bash cluster/place_readout.sh` (runs the deferred polE cells once `polE_place.json` carries `bank_version`, then all three tables); reels `cluster/place_review.sh`; then PHASE_RESULTS §2.z.
- **DP-GAP AGENT (2026-09-07 ~15:30, delegated by the head session):** 🟢 done — `paper/DP_PRUNED_GAP_2026-09-07.md` committed (20594c4, doc only). Verdict: the 0.520 (LAST rnd30, w3, n=10) is a different cell from every ~0.80 on record (all in-distribution: hdp_s2 0.80 07-31, dH_DP_s0 0.80 08-02, dDP_DP mean 0.80, hold-15 0.913 now); on rnd30, 10/30 ICs lie outside the pruned set's training support (9 at can x ≥ 0.52 > max train 0.513) and are picked 0/280 by every DP arm (RLPD/WM pick 5/4 of them); on the live 20 DP = 0.78. Random-start DP went 0.23 (Aug) → 0.57–0.59 (repeat-4, old world) → 0.52 (w3, −0.07 on far-x ICs). Nothing to rerun; suggested: stratify rnd30 by support in MORNING_TABLE, draw rnd2 from the success-tape box (CONFOUNDS row 31 note). Branch `4dof-cartesian`. Blocked on: nothing.
- **DISCRETE-REPLAY AGENT (sim box, 2026-09-07 02:20, 🟢 DONE):** `paper/DISCRETE_ACTION_REPLAY_2026-09-06.md` + `baselines/discrete_replay_check.py` (outputs `~/wm_fix_2026-09-03/discrete_replay/`). Registered (23:10) then run: raw vs ternary / five-level / held-word / gripper-only / arm-only joystick twist through the commanded-Cartesian path (`CartesianCanEnv` vel, w3 via sim_variant_hook, 74 success uids, 1-thread deterministic, 8 procs). **All predictions MET — every code ≥ raw (tern picked 23/placed 17/contact 6/nested 2 vs raw 17/12/2/1); gripper ternary ramp lossless; 16 words carry 99.1 % (45 used).** **Power caveat FIRED: the raw commanded path picks only 17/74 in w3 (joint path 69)** — diagnosed offline: the integrated twist drifts a median 4.2 cm (p90 24) from the real tool pose by grasp time (the real arm never realised the joystick twist 1:1). Recommendation: define the discrete arm on the REALISED velocity of the joint-path tapes (derive_cartesian_realized.py → snap to 16(+gripper) words), then repeat this check; do not snap `_cartesian.npy` directly. Nothing trained; frozen sets/cluster untouched. Next: head agent decides whether to run the realised-velocity variant (~1 h on 8 procs, same script).
- **ROBOMIMIC LEG (agent, 2026-09-06 22:40 → 09-07 20:30, cluster `ssh pax`):** 🟡 VPN down at handoff; all work queued and self-contained. Plan `paper/ROBOMIMIC_PLAN_2026-09-05.md` (+ A1 DP arm, A2 quantity/failure/budget controls, A4 action-process control, all registered BEFORE their runs); log `paper/ROBOMIMIC_LOG_2026-09-06.md` (every number from tool output, job ids). Envs `$LAB/robo_venv` + `$LAB/r2d_venv_robo` (`cluster/robomimic/verify_env.sh`); data `$LAB/robomimic_data/` (bank sha 72b75550…, arms PH200/MH200/MG200s/MG718s/MH300/MGall/PH200pb + A4 re-execution arms, `arm_counts.json`, `action_stats.json`). **GATES: G0 PASS (4/5 registered form), G2a random 0/50, G2b no-demo RLPD 0/50 on all 8 seeds (PASS at 0.00), G1 BC-RNN PH200 0.92.** **RESULTS (LAST, bank_can50, n=8v8):** BC-RNN MH 0.927 / MG200s 0.393; DP MH200 0.863 v MG200s 0.095 (Δ +0.767, exact p 0.00016 — complete separation); RLPD MH200 0.455 v MG200s 0.147 (Δ +0.307, p 0.008). **The source reading is in doubt: A2 control MGall (all 3,900 rollouts) scored 0.74 and 0.60 on its first two seeds — ABOVE MH200 — which would fire the registered rule 'quantity/coverage, not source'.** A4 found MG tapes are not open-loop reproducible after any action edit (treatment yield 0/200; control 114/200), so the MG smoothing arm was abandoned per its own rule; the MH dose pair was built instead (ε 0.15 → 95 paired tapes, ε 0.20 → 61). **Queued after the 09-07 filesystem-full incident (recovery batches, all with a ≥100 GB launcher disk guard):** r2d MG200s s7 retrain 3354420; A2 controls MGall 3354448-55, MG718s 3354456-63, MG200s@300k 3354464-71; A4 3354850-81. **Standing rule now implemented: final checkpoint only** (RLPD `CKPT_FRACS=1.0`, DP `SAVE_FREQ=$STEPS`). **Pickup on reconnect:** read results by job id from `$LAB/robomimic_runs/{rlpd,r2d,dp}/*/eval_bank50*/metrics.json`, compute the A2 (P1-P3) and A4 (P-A4-2′/3′) contrasts, and note that RLPD PH200 (the originally registered primary contrast) has never run — 8 × ~2 GPU-h.
- **DV3-DEBUG AGENT (delegated 2026-09-05 → 09-07 20:15 cluster clock), 🟢:** **G1 + G2 PASSED — dv3 is fixed by ONE knob, `return_clamp 1.0`** (λ-return target capped at the task's max return; dv3 had the r2dreamer TRIGGER — targets 1.9–4.7 vs max 1.0 — but never its amplifier, its actor already being the stock bounded normal). G1 reach proxy: fresh deterministic hold-15 **15/15 on 4/4 runs** (jobs 3291585–88) vs {1,0}/{15,0}/{11,3}/{4,15} for every previously tried lever; post-ignition wobble 7/9 on record → 0/4. G2 pick (1e6 steps, state demos, corrected world; jobs 3337054–57): **dHv2raw rnd30 0.700 / 0.700, hold15 15/15 + 14/15; dDP 0.633 / 0.633, hold15 15/15 ×2** — same band as r2dreamer (0.617 v 0.608, n=8v8). Partial n=2v2 Δ +0.067, p 0.333 (floor). **G3 RUNNING: seeds 2/3 per arm = 3354433–36** (land ≈ 09-08 17:00) → n=4 v 4 permutation. dv3 can now be reported as a SECOND world-model implementation instead of 'no working configuration' (RESULTS §7 item 9 and CONFOUNDS row 44 will need updating once G3 lands). 09-07 16:00–17:00 the project filesystem hit 100% and killed all jobs cluster-wide; the four G2 checkpoints survived and were re-evaluated (3354217–24); the pick launcher now aborts below 150 GB free. Registrations/readouts: `paper/DV3_DEBUG_2026-09-05.md` §5–§9; code `cluster/dv3dbg/` (tree of record `$W/dv3_dbg` = dv3_fix + a state-only demo-image hunk). Blocked on: nothing.
- **09-07 23:00 — READ FIRST: `paper/OVERNIGHT_STATE_2026-09-07.md`** (decisions waiting, the open reproduction problem, what landed, what runs overnight), then `paper/WHAT_EACH_PHASE_TRAINS_ON_2026-09-07.md` (Pick/Place/Slide reward vs score; Slide's 32 jobs HELD because it pays on bare contact but is scored on release; amendment (o) pending) and `REVIEW_GUIDE_2026-09-07.md` §8 + addendum. Tonight: no-demo control passed at 0.000 (MG helps RLPD less than MH, is not worthless); dv3 G2 passed 0.700/0.700 v 0.633/0.633 (RESULTS §7 item 9 + CONFOUNDS row 44 are now WRONG and need updating); phase evals proven bit-exact across nodes (3488 episodes) which CONTRADICTS the earlier 62%-divergence claim; full-scope episodes look order-dependent, sequence check 3355868 decides whether PHASE_RESULTS §5.1 stands. Disk hit 100% and killed every job; 434 GB freed, launchers now keep finals only and refuse to start under 100 GB.
- **09-07 17:20 — READ FIRST: `paper/REVIEW_GUIDE_2026-09-07.md` §8 (corrections after four Opus adversarial reviews: `ADVERSARIAL_REVIEW_{statistics,eval_env,data,robomimic_ops}_2026-09-07.md`).** Headline S1s: RLPD pick MDE 0.345 (weakest null); the significant in-distribution cells belong to the all-data arm and are deterministic-only (sampled statistic chosen post hoc by the user 09-07 15:20); WM full-scope `nested` = training proxy (honest re-score queued) and the "carry-without-release" claim was BACKWARDS; place cells drew bank entries with replacement (unpinned uid; re-score queued); dumped-bank grip-units bug (banks being rebuilt); `contact`'s ee clause vacuous (ee = wrist) and contact_push must use the tool point; hold15 is NOT held-out (14/15 training starts); end-to-end machine set = best-of-3 on 89 % of starts (Σ reward 206 v 118); robomimic MG actions differ in magnitude/roughness/gripper → action-statistics control A4 required before "source", G2b no-demo gate submitted, "SAC reset distribution" caveat struck. Rule: commit registrations BEFORE submitting. Agents running: eval fixes + re-score (`agent_brief_eval_fixes.md`, amendment (j)), contact_push (tool point), DP+RLPD place (`agent_brief_place_dp_rlpd.md`), robomimic (G2b + A4 draft), DV3 (G2 evals ~17:45); amendment (i) symmetry evals (48 CPU) running; end-to-end human trial renders running locally (`can_pos_recovery/videos_census/<uid>_full_gc_kp4_riser3_shelf6.mp4`). Pending user: test-start set (60-start three-spot proposal), A4 confirmation.
- **WM-FIX / PHASE SESSION (pop-os + cluster, 2026-09-06 22:45), 🟢 active (VPN back), branch `4dof-cartesian`.** ALL 09-05 chains landed and are written up (PHASE_RESULTS §2.y matched place 0.703 v 0.647 p 0.227; §3 matched contact 0.593 v 0.602 p 0.841; §4 carrycontact 0.807 v 0.796 p 0.348; §5 end-to-end 4v4 human ≥ machine every stage, contact +0.117 p 0.49). END-TO-END 8v8 DONE 09-07 08:40 (PHASE_RESULTS §5.1): rnd30 MODE picked 0.500 v 0.537, contact 0.379 v 0.338, nested 0.163 v 0.192, all p > 0.6 → no source effect; all registered predictions met. DV3: G1 PASSED (4/4 seeds 15/15 reach with the clamp); agent resumed for G2 (pick from dHv2raw). **ROBOMIMIC LEG READY (agent report 09-07 01:55; `paper/ROBOMIMIC_LOG_2026-09-06.md`, code `baselines/robomimic/` + `cluster/robomimic/`):** new envs `$LAB/robo_venv` + `$LAB/r2d_venv_robo` (shared envs untouched); data `$LAB/robomimic_data/v1.5/can/` (sha256.txt); G0 PASS (10/10 success flags, 4/5 within 1 cm); bank `bank_can50.npz`; arms PH200/MH200/MG200s (+MH300, MGall, PH200pb) in RLPD/r2d/lerobot formats; smokes end-to-end for RLPD, r2dreamer, DP; BC-RNN control COMPLETE (LAST ckpt, 50-state bank, 3 seeds): PH200 0.92 / MH200 0.927 / MG200s 0.393 — imitation collapses on SAC-derived data, both registered BC predictions met; **ROBOMIMIC MATRIX RUNNING since 09-07 10:50 (user's go): MH200 + MG200s × rlpd/r2d/dp × 8 seeds = 48 runs (PH200 dropped for now); RLPD 13:30: MH 0.455 v MG200s 0.147, p 0.008 — **this READING IS WITHDRAWN 09-08 by its own registered quantity control**: MG718s 0.475 (Δ +0.020, p 0.886, indistinguishable from human) and MGall **0.610 (ABOVE human)**; MGall v MG200s +0.463 p 0.0002; budget extension only +0.140 (p 0.115). The registered 'quantity/coverage, not source' rule FIRES → the robomimic leg now AGREES with the Genesis null instead of contradicting it. DP (0.863/0.095) and BC-RNN (0.927/0.393) rest on the SAME 200-tape draw and inherit the doubt until run on MG718s/MGall. OPEN: that draw was 89 % last-four-checkpoint rows, so a NEUTRAL random-200 draw (~8 GPU-h) is needed to separate 'pathological sample' from 'per-demonstration quality'; registered primary PH200 still never run (~8-12 GPU-h); quantity/budget controls (MGall, MG718s, 300k) being registered + run by the robomimic agent; r2d + dp arms running; results table = the agent's scripts under `cluster/robomimic/` + `paper/ROBOMIMIC_LOG_2026-09-06.md`; gate `GO=1` exactly (a GO=0 mis-fire on 09-07 10:26 submitted 72 jobs, all cancelled before starting — logged).** DP amendment §A1 registered in the plan. Discrete-action replay check DONE (`paper/DISCRETE_ACTION_REPLAY_2026-09-06.md`): snapping the joystick twist to a ternary code costs nothing (ternary ≥ raw at every stage) but the raw commanded path itself reproduces only 17/74 picks (twist drifts 4.2 cm by grasp) → any discrete arm must be built from the REALISED tool velocity (`derive_cartesian_realized.py`) quantised to the 16(+3 gripper)-word codebook, then re-checked; not started. Robomimic prep DONE (above). **MACHINE-FIRST ARM (user 09-07 13:50: "distilled from the human ones" needs testing) is owned by a FORKED session (`WM_FIX_PLAN_2026_09_03 verification ⑂`; registration `paper/MACHINE_FIRST_PLAN_2026-09-07.md` on worktree branch `worktree-machine-first-arm`; jobs prefixed `mf_`; SAC-without-demos teachers RUNNING since 14:03 (3349688–91, QOS normal), r2dreamer no-prefill teachers following; reward-only r2dreamer/SAC teachers → harvest on the 66 human ICs → `matched_w3/dRL` → r2dreamer ×8 + RLPD ×8 vs dHv2raw/dDP). This session does NOT run a parallel effort; fold its readouts into PHASE_RESULTS/REVIEW_GUIDE when it reports.** **PAPER-EVAL BUILD (user 09-07 15:20/15:35): Slide + full-task jobs HELD (slide definition under review; `contact_push` predicate agent running, brief `agent_brief_contact_push.md`); DP+RLPD PLACE phase agent running (brief `agent_brief_place_dp_rlpd.md`, 32 runs, PHASE_PLAN (h)); pending user decision on the test-start set (60-start three-spot set proposed vs the rnd30 box); sampled actions become the statistic for RLPD/r2dreamer; DP gap explained (`DP_PRUNED_GAP_2026-09-07.md`).** **REVIEW MODE (user 09-07 09:00): `paper/REVIEW_GUIDE_2026-09-07.md` (results of record with MDE/CI, threats, falsifiers, checklist, decisions) written + sent; matched-start review reels SENT (12 mp4 + 6 sheets in `~/wm_fix_2026-09-03/review_matched/`; index `paper/REVIEW_REELS_INDEX_2026-09-07.md`; place start-level discordance favours human 192 v 126 — descriptive, registered per-seed p 0.227).** NEXT: DV3 G2/G3 readout (~19:00); relay the matched-reels index; robomimic go/no-go (user); decisions in REVIEW_GUIDE §7. **READ `paper/PROJECT_STATE_2026-09-05.md` (plain-language briefing, §10 = state at disconnect + reconnect pickup list) FIRST.** On reconnect: `python3 phase_table.py runs {place,contact,carrycontact}` (matched-N arms `dDP_n39/n11/n21` = numbers of record), `python3 full_table.py runs rnd30` (end-to-end; post-hoc evals via the login-node loop `full_eval_sweep.sh`), `paper/DV3_DEBUG_2026-09-05.md` (agent), then `./review_sweep.sh` + SendUserFile for the new conditions; write PHASE_RESULTS §2 (matched place), §3 (matched contact), §4 (carrycontact), §5 (end-to-end). Contact human n11 0.593 v machine n25 0.609 on polE_contact (p 0.71, descriptive). Human carrycontact 0.807 polE MODE (8 seeds). Robomimic parked to Monday (user). READ `paper/PHASE_PLAN_2026-09-04.md` (amendments (c′) repeat-1 pilot, (d) end-to-end arm, registered 09:55/10:05) and the end of `paper/WM_FIX_LOG_2026-09-03.md` ("chains of record", 10:10) first. **DONE:** pick n=8v8 null (RESULTS_WM_HUMAN_VS_MACHINE_2026-09-04.md), rnd300 retest MET, all-data dHv2all NOT met (WM 0.588 v 0.617; RLPD 0.554 v 0.600), PLACE phase human 0.703 v machine 0.709 (polE MODE, p 0.878; `paper/PHASE_RESULTS_2026-09-05.md` §2), characterization + positioning notes, robomimic plan `paper/ROBOMIMIC_PLAN_2026-09-05.md` (c7495a5; MG-file probe lines still UNVERIFIED). **RUNNING (all on the cluster, chained; monitors armed):** (1) repeat-1 clock pilot DONE 14:55: rnd30 MODE human 0.642 vs machine 0.650 (Δ −0.008, p 1.000, n=4v4), all seeds learned → P1 met, the action-repeat hold hid nothing (PHASE_PLAN (c′) verdict, PHASE_RESULTS §6). (2) **MATCHED-N rule (user 11:15, amendment (e)): machine sets subsampled to the human tape count are the primary at every phase** — carrycontact human n21 3290191–98 (running) vs machine n21 3291435–42 (the unmatched 36-demo machine runs were cancelled before starting); place machine n39 3291443–50 (matched confirmation of the §2 null; 39 v 63 becomes the uncapped secondary); contact machine n11 3291451–58 vs sub-floor human n11 3290207–14 (running). `matched_n.py` (seed 0, kept files in repeat.json), launcher `DEMO_OVERRIDE`, converter `--max-tapes`. Evaluator records bank-entry restore failures as `restore_failed` (counted failure; cc smoke 5/148 under a random policy, place evals 0). Place polE-dDP symmetry columns DONE (PHASE_RESULTS §2.x: 0.681 v 0.720 MODE, p 0.374). (3) END-TO-END full task (amendment (d)): build 3290389 PASSED (human dHfull_all 74 tapes: pick 64 / contact 21 / nested 3, total reward 118, prefill 117k; machine dDPfull best-per-IC 72: 70 / 36 / 16, total 206, prefill 132k) → smoke 3290390 (180k) → runs 3290391–94 dHfull_all / 3290395–98 dDPfull (2e6, staged reward 1/1/2/4, return_clamp 8, `wmfix_full.sbatch` bakes the actor/entropy recipe in; success-by-stage in `eval_genesis.py` `stages`). **Queue lesson (18:40): jobs default to QOS normal (10-GPU cap); 40 pending runs moved to QOS preempt (20 more) → 44 GPU runs in flight; CPU evals must not request a GPU (`wmfix_phase_eval_cpu.sbatch`). Sub-floor human contact 8/8 DONE (holdE 11/11 all); polE_contact bank (160) built, 30 post-hoc evals running; first cell human s6 76/160 MODE (0.475). Repeat-1 pilot DONE (P1 met). Review reels pipeline: `review_sweep.sh` → SendUserFile.** **Slips disclosed in the log:** the first repeat-1 chain lacked the recipe overrides (cancelled, no result); the smoke budget must exceed the stride-1 prefill. **NEXT:** when smokes pass nothing to do; when runs land: `phase_table.py runs place` (add polE-dDP columns → PHASE_RESULTS §2), contact/carrycontact results → PHASE_RESULTS §3, repeat-1 readout vs P1/P2 → PHASE_PLAN (c′) + log, end-to-end success-by-stage table (new `full_table.py` to write) → PHASE_RESULTS §4; relay the robomimic plan (agent may still patch the two MG lines). Blocked on: nothing (VPN window). Key decisions: like-for-like physical quantities at repeat 1 (cap/discount/updates per sim step equal, batch_length uncompensated); end-to-end machine set = best tape per IC (selection disclosed), human set = every tape incl. no-picks.
- **(superseded detail of the 09-04/05 session, kept for provenance):**  stage-1 comparison DONE (`paper/RESULTS_WM_HUMAN_VS_MACHINE_2026-09-04.md`, `paper/MORNING_TABLE_2026-09-04.md`). **NOW: per-phase comparison (Place, Contact) — READ `paper/PHASE_PLAN_2026-09-04.md` (registered 09:15) first.** Frozen sets are pick-scoped ⇒ full-task recording in w3: human `baselines/demos_v2/dHfull_w3` (array 3256732, 75 uids, from `episodes_all_v2r` rsynced to the cluster). Built + smoke-tested: `~/wm_fix_2026-09-03/{contact_scope_patch.py,make_phase_banks.py}` (cluster copies in `$LAB/wm_fix_2026-09-03/`), `baselines/rl/to_dreamer_native.py --phase --state-only`, `baselines/prune_full_v1.py`, r2dreamer adapter/evaluator contact scope + `--entry-bank` (required), configs `genesis_{place,contact}_state.yaml`; place-scope restore verified live in w3 (survival 1/1, random policy 0). Smoke tape 233: pick→contact with NO placed_v2 (carries into the goal) — the census decides the contact-phase entry (plan amendment if placed_v2 is rare). **13:35 (09-05): PLACE PHASE COMPLETE — human 0.703 vs machine 0.709 on the 148 policy-generated starts (polE MODE, 8 v 8, p 0.878; holdE saturated 104/104 vs 102/104); registered |Δ| < 0.10 MET; contact one-armed dDP holdE 0.98. `paper/PHASE_RESULTS_2026-09-05.md` §2 written; polE-dDP symmetry columns computing (3290111–42). 09:40 (09-05): positioning note `paper/POSITIONING_demo_source_null_2026-09-05.md` (d34006b): MoDem Fig. 8 is the WM precedent (cite, no 'first'); BC idle sensitivity established; RLfD source cell is the first controlled online one found; framing F1 recommended; bar-raiser = robomimic PH-vs-MG leg. 02:10 (09-05): VPN DOWN (12-h window closed); runs finish unattended. MORNING: run the pickup list at the end of paper/WM_FIX_LOG_2026-09-03.md (phase_table → PHASE_RESULTS §2; polE-dDP merge + sweep; gates; status). 22:50: PLACE PHASE RUNNING — dH place 3263097–104 (since 17:18); dDP place s0/s5/s6/s7 = 3270587/92/93/94 + s1–4 RESUBMITTED 3272139–42 after four runs died on the P100 node pax077 (launchers now `--exclude=pax077` and abort before evals if training didn't reach its budget — the old launcher had evaluated an untrained checkpoint); one-tape-per-IC machine set (63 vs 39 human segments; uncapped 104 kept, not run); contact dH 3270529–36 GATE-SKIP (11 < 20), contact dDP 3272143–46 + (preempted, resubmitted) 3276571–74 (one-armed, 25 demos; polE bank optional). Launchers now `--requeue` with clean restart; running runs flagged Requeue=1. Skip cell now 3276575. rnd300 skip cell (dDP s1 mode, ep 269 counted as failure) 3270635. dHv2all post-hoc: holdv2 16/16, rnd300 14/16 (loop running). Morning: `python3 phase_table.py runs place` + `python3 morning_table.py runs` → results docs; refresh this status. Machine harvest 195 tapes (picked 179, contact 46). Two S5 bugs fixed (per-IC keying; holdE dict). Results table: `python3 phase_table.py runs place` (cells holdE / polE / polE-dDP × sample|mode; polE-dDP = machine-policy-generated bank, dumps 3280431–38, scored post hoc by `polE_dDP_sweep.sh` via a detached loop). Early polE: dH 0.75–0.80, first dDP seed 0.61. Contact (one-armed dDP): 11/11 holdE. PHASE_RESULTS_2026-09-05.md §2 to be pasted from phase_table.py.** 20:30 READ-OUTS: (1) ALL-DATA arm dHv2all — registered prediction NOT met: WM rnd30 MODE 0.588 vs dHv2raw 0.617 (p 0.58), in-distribution cells lower; RLPD 8/8 rnd 18,21,1,18,19,18,19,19 = 0.554 vs raw 0.600 (p 0.567; one dead seed per arm) — no gain from failure tapes on either learner. (2) rnd300 retest of phase-1 COMPLETE: prediction MET — dHv2raw 0.602 vs dDP 0.618 (MODE, 8 v 8, Δ −0.017, p 0.546; within 0.02 of rnd30). Both in MORNING_TABLE §5 + the log. 17:45: phase chain RESUBMITTED from the harvest (the DP full-task teacher 3257952 is DONE: hold 14/15, rnd 14/30; its harvest refused `--verify` (pick-scope only) → removed): S4 harvest 3263094 (array 0-7, RUNNING) → S4m 3263095 → S5 3263096 → WM place ×16 3263097–3263120 (+ contact ×16 gated 3263121–36, expected GATE-SKIP: human contact-after-release = 11 < 20). Place smoke 3258751 PASSED the pipeline. Eval stall watchdog running on the login node (`eval_stall_watchdog.sh`, `watchdog.log`).** Earlier:  all-data S8 3257858→S9 3257897→WM dHv2all ×8 RESUBMITTED 3258752–59 (two converter fixes on the way: filename collision on repeated ICs; state-only placeholder image must be (T,64,64,6)); place smoke 3258751 PASSED the pipeline check (5k steps, all four eval cells written; 16 place runs 3257958–73 stay queued behind S5). All 16 dHv2all runs RUNNING past their gates + RLPD dHv2all ×8 RESUBMITTED at GAMMA=0.99 + eval_ics_v2_w3 (recipe of record; the launcher default 0.998 was wrong) = 3258287–95; rnd300 ×36 3257991–3258026. dHv2all = 106 tapes (66 succ + 24 raw-run non-picks + 16 real-fail recordings), pick 73/no-pick 33. Characterization (item 3) DONE: paper/DEMO_CHARACTERIZATION_dDP_vs_dHv2raw_2026-09-04.md (53ac93d), videos/figs sent. Yields: place 39/64, contact-after-release 11. Watch: smoke 3257956 must pass before the place runs start (they wait on S5 ≈ +5 h); RLPD arm untested (DRYRUN after S9). 13:00: chains scripted (`~/wm_fix_2026-09-03/submit_chains.sh`, place first, contact gated, all-data arm dHv2all, rnd300); VPN DOWN — on reconnect: smokes then submit.** NEXT (superseded detail): census → banks/segments → machine teacher (prune_full_v1 → matched_w3/dHfull_pruned + lerobot → `ARM=dHfull_pruned sbatch_dp.sh` → `record_demos --teacher dp --scope full`) → WM phase-2 runs (8×2). RLPD/DP phase support not built yet (plan §3).
- **Status:** 🟢 PAPER MODE. Branch `4dof-cartesian` / dv3 `genesis`. **Read paper/CONFOUNDS.md (living ledger — check every claim against it) and PAPER_PLAN.md first** — it is the living coordination doc (hypotheses H1-H4, condition matrix with statuses, decision log). The paper: human vs model demonstrations for IL, with dv3 as the world-model arm (H4: world models benefit ~equally from all demo sources).
- **SIM-BOX SESSION (pop-os) — 09-07 23:15, 🟢. MORNING ITEM #1 (slide human set): READ `paper/slide_per_uid_2026-09-07.txt` (74-row table) + §7 of `paper/SLIDE_CLAUSE5_LINEAGE_2026-09-07.md`.** Decision is 15 vs 25 vs 30 tapes. **15** = the slide completes in sim [232 233 236 237 255 273 294 297 299 300 305 308 317 325 330] (11 nested); **30** = the human pushed the can ≥3 cm goalward after setting it down; **25** = ≥4 cm middle. **Nothing lands on the user's ~18, and that is proven not to be a set:** two unrelated constructions both total 18 (place agent's 11+7, and "pushed ≥3 cm but sim stopped short") and they share only 3 uids, disagreeing on 15 of 18. ~18 is a REAL-footage judgement; every automated count scores the SIMULATION, and the 15-to-30 gap is the systematic 2–4 cm under-transfer of SLIDE_ANATOMY (sim tool tracks the real tool to 0.2 cm; the can does not follow). Cutting at 15 biases the phase toward the geometry this world reproduces. **Also settled today:** amendment (l)'s `grip_cmd<0.3` clause WITHDRAWN on my evidence (slide_success passed 2/74; 44 grip_closed; the human releases then re-closes to ~0.4 and pushes — user: "sometimes its easier to push with the gripper closed"); clause 5 calibrated as `|can_z − resting| ≤ 1.0 cm`, NO gripper term (released-and-resting population n=897, p99 0.65 cm, max 0.77; a carried can is 2.13 cm up) — and finger position must NOT be used, because a fist reads ABOVE the clamp band (273 → 1.00) and any "gripper must be open" test kills fist-pushes; corrected ENTRY gate proposed (same 1.0 cm, no gripper term) buys 1 uid, not 7. **Lineage: census is authoritative** — `dHfull_w3` and the census are DIFFERENT recordings of the same 74 ICs (actions differ on 24/74, contact flags on 11); census re-executes bit-exact here, dHfull 2/74 and loses the pick on 5 uids. Tools added: `can_pos_recovery/slide_score.py` (calls the landed end_of_episode, never reimplements it), `can_pos_recovery/render_slide_panel.py` (video + synced reward/grip/distance/tilt graph). **Rule learned the hard way: a tape's `stage` field is stale** — `_nested()` only runs when an episode hits max_steps and 52/74 end `adapter_exhausted`, so 13 w3 tapes are honest-nested with a non-nested label [232 233 237 247 251 273 275 294 299 302 304 317 330]; always use the honest json. All 74 end-to-end videos + 20 slide-panel videos are on local disk and listed in `paper/REVIEW_REELS_INDEX_2026-09-07.md` (no VPN needed). Uncommitted: render_census fix, slide_score.py, render_slide_panel.py, SLIDE_ANATOMY, SLIDE_CLAUSE5_LINEAGE, slide_per_uid txt, reels-index section, CONFOUNDS 50/51.
- **(earlier that day) SIM-BOX 09-07 17:00 — owns the SLIDE work + the END-TO-END RENDERS permanently (user, via the genesis-pickaplace-04 handoff).** (1) **All 74 end-to-end videos DONE**: `can_pos_recovery/videos_census/<uid>_full_gc_kp4_riser3_shelf6.mp4` (5.1 GB, real | sim re-execution + wrist cam, max state deviation stamped). The inherited run had failed 27/74; cause was NOT missing tapes (the peer's guess) but PHASE_PLAN amendment (j)'s new `shelf band mismatch` assert landing in `baselines/rl/full_env.py` at 15:21 MID-RUN — the renderer builds the shelf6 world via apply_pre but never exported `GENESIS_SIM_VARIANT`, so every worker launched after the patch aborted. **Fixed in `can_pos_recovery/render_census.py` (worker now sets `os.environ['GENESIS_SIM_VARIANT'] = a.variant`)**; 27 relaunched, all passed. Verified for the user: the mp4 replays the tape's `actions_delta` through FullTaskEnv, and uid 232's census tape is BYTE-IDENTICAL (actions sha 2434a8df96829171) to `~/wm_fix_2026-09-03/fulltapes/dHfull_w3_partial/100000.npz`. (2) **`paper/SLIDE_ANATOMY_2026-09-07.md`** — the slide is CONTROL-limited at stroke level: sim tool tracks the real tool to **0.2 cm median** tool-to-tool at set-down (45 tapes); set-down geometry is statistically identical between tapes that arrive and tapes that don't (can-vs-tool lever 1.5 cm, along-axis +0.3 cm both, exact perm p 0.82); the only separator is remaining distance (10.7 vs 12.3 cm); engaged strokes transfer 0.65 goalward and lose ~2.4 cm/tape laterally. **Self-correction recorded in the doc:** my first pass compared sim CAN centre vs real TOOL and reported "1.7 cm fidelity" — that silently absorbed the 1.5 cm grasp lever arm, error bar = the answer. (3) **CONFOUNDS row 51 (new): uids 234 and 318 are unwinnable by construction** — the only two 90° lying-can entries in `trial_placements.json` (z 0.085, the "2 bogus artifacts" flagged 2026-07-20); the tip rule fires at decision 1, both tapes are 1 decision, stage `none`, in EVERY census. Real footage shows both cans UPRIGHT at t0 (checked by eye vs 233). Every n=74 denominator carries them (picked 69/74 = 69/72 on the winnable set); moves NO comparison (both `none` everywhere, one fit / one holdout). NOT FIXED — changing ICs breaks every frozen set, head-agent/user call; an automated Hough lid-detect I tried is NOT trustworthy (returned nothing on the good control 233, 30 cm off for 234). (4) User correction accepted: **uid 232 IS nested** by the metric of record (dist 6.6 ≤ 8.1 cm, both upright) — I had quoted the tape's stale `stage` field. The env only evaluates `_nested()` when the episode hits max_steps, and 52/74 tapes end `adapter_exhausted`, so 13 w3 tapes are honest-nested with a non-nested recorder label [232 233 237 247 251 273 275 294 299 302 304 317 330]. **Rule: never quote a tape's `stage`; use the honest json.** Uncommitted today: render_census.py fix, SLIDE_ANATOMY doc, CONFOUNDS row 51, this status. Next: nothing blocking; open asks unchanged (weigh the real can, 0.295 vs 0.35 kg).
- **(prior sim-box entry)** 2026-09-04 00:20 — **RESULT OF THAT NIGHT: `gc_kp4_riser3_shelf6_og4` ADOPTED for the recorder path** (RELEASE_OPEN_PREREG §5, CONFOUNDS row 50; passed every pre-registered clause, honest exact 74/74): **picked 69 / contact 28 / tipped 32→20 / honest nested 16→23 / set-down 46→50** vs w3 69/26/32/16/46; holdout tipped 13→5, contact 7→9; CLEAN holdout (17 non-lab uids) confirms (tipped 6→4, nested 4→8, contact 5→7); ognow (K=∞) also passed, og4 preferred by the registered margin rule. It is a causal fast-open grip filter in the RECORDER (record_demos.HumanFollower._filter_grip, keyed off VARIANTS[name]['grip_open_gain']; world bit-identical to w3) — mechanism-UNfaithful (the real release is a 1.7 s ramp, og4 makes it ~0.4 s), outcome-faithful, per-phase customisation under the user's 09-03 12:40 mandate. **CARRIED CONFOUND (row 50): machine-arm harvests don't get this shaping — human-vs-machine comparisons on og4 data must neutralise (apply the filter to teacher grip outputs) or disclose.** Retrain decision = head agent/user. Morning report posted in-chat 00:45; 6 flip videos SENT to the user (243 262 308 242 wins — all three tipped uids land NESTED under og4 in the render — plus 236 276 new-tip losses; `videos_census/*_og4.mp4`). Box idle, no processes left, monitors stopped. Uncommitted: sim_variants og rows, record_demos filter, RELEASE_OPEN prereg, CREEP §5 verdicts, SHELF addendum, CONFOUNDS rows 49/50, this status (commit only if asked). **Open asks to the user:** weigh the real can (rho 852.5 ⇒ 0.295 kg vs 0.35 assumed); shelf tape-measure now optional (camera+arm agree on ~12 cm). **Next when the user is back:** (1) decide the retrain on og4 data (head agent; note row-50 asymmetry — either apply the same filter to teacher grip outputs at harvest or disclose); (2) optional per-phase split beyond the release (slide phase untouched — contact 26→28 is within noise; the slide remains the control-limited phase); (3) optional 2nd rep of the og4 census row for the x3 honesty bar before any paper number cites it. **Creep verdict (CREEP_PREREG §5 + CONFOUNDS row 49, exact 74/74 all rows):** P1 imp99 contact 20/tipped 43, C1 gimp99 23/38, B1 ts5_imp99 20/29 vs w3 26/32 → disconfirm branch (c): the creep (F5) is a real engine property but NOT the recorder-path mechanism; the impedance lever is dead for this grasp (C1 proves the site is the finger–can pair; B1 shows ts5 recovers tips but never contact); w3 stays the world of record; the registered imp 0.95/0.97 ladder was NOT taken (its trigger — pick loss — never fired). **Release lab complete (11 conds):** open_now 17/20 upright / 5 nested vs control 0/20; every physics cond ≤ 10/20; `unload` 0/20 DEAD, unload_ts5 8/20 = plain ts5. **Shelf height ADJUDICATED (SHELF_HEIGHT_PREREG addendum 17:55): the real plate is 11–13 cm = shelf6 to ~1 cm** — my own 16 cm arm-differential argument was WRONG (tool-z differential ≠ pad-z differential; the rotation-invariant |ee−can| measure shows true in-hand slip only +0.6 cm median, the "walk" was wrist pitch) and the camera (goal-top ray 10.7 cm ±2; stand leg feet back-project to x≈0.57 = sim front face; plate edge ≈12 cm on the rods) + the 233 release frames agree; tape measure now a confirmation only. **Bag check 23:15: NO independent gripper command topic exists** (g_frame ≡ fb_grip corr 1.0000); the human release is truly a ~1.7 s ramp, faithfully carried; a "sim follower 3–45 s lag tail" was a MEASUREMENT ARTIFACT (regrasp cycles) — withdrawn before reaching any doc. **RUNNING (launched 23:11, ~50 min/row): `CENSUS_ROOT=baselines/demos_v2/census_og_0903 census_shelf_0903.sh gc_kp4_riser3_shelf6_ognow gc_kp4_riser3_shelf6_og4`** = the causal fast-open release filter (READ `paper/RELEASE_OPEN_PREREG_2026-09-03.md` FIRST — registered 23:55 before any readout; filter in record_demos.HumanFollower._filter_grip keyed off VARIANTS[name]['grip_open_gain'], worlds bit-identical to w3, selftests PASS, unit test 91 streams: closing/holds never altered; disclosed: mechanism-UNfaithful per-phase customisation under the user's 09-03 12:40 mandate; 7 of the 20 lab uids sit in the holdout [243 245 248 257 308 331 333] — contamination disclosed in §4). Monitor bemffo31v watches the census log. **Pickup when both rows land:** `score_shelf_prereg.py --new baselines/demos_v2/census_og_0903 --variants gc_kp4_riser3_shelf6_ognow gc_kp4_riser3_shelf6_og4`; fill prereg §5; adopt rule §4 (tipped ≤ 26 ∧ contact ≥ 21 ∧ picked ≥ 66 ∧ nested ≥ 16 ∧ holdout tipped ≤ 12; prefer og4 on a tie; disconfirm (a) → release-phase-only world, (b) → release control-limited, keep w3, (c) picked drop → deadband-0.10 fallback row); then the MORNING REPORT to the user: success-by-stage tables (w3 / jit / imp99 / gimp99 / ts5_imp99 / ognow / og4), release-lab 11-cond table, shelf adjudication + the two corrected self-errors, flip videos via render_census, re-ask can weight (tape measure now optional). New uncommitted code today: sim_variants og rows, record_demos filter, prereg docs, CONFOUNDS 49 update, SHELF addendum. Census dirs data rsync-only as ever. **SCOPE (user 09-02): this box owns the world change + its improvement loop; NO dataset builds, NO retraining (cluster). Bar: "if you make a world that gives us better real2sim we'll retrain everything". Keep the GPU free.** **MANDATE OF RECORD (user 09-03 12:40): "if you think you can improve any of these phases you should. contact matters more than nested, but it is better if the can doesn't fall over"; per-phase worlds allowed.** Standing format: success-by-stage tables. **READ `paper/CREEP_PREREG_2026-09-03.md` FIRST** (registered 17:05 before any candidate readout), then `paper/SHELF_HEIGHT_PREREG_2026-09-03.md` §6 (all three shelf rows NOT ADOPTED; **F3 "shelf 4 cm too low" WITHDRAWN** — read with the stale `replay_harness.BOTTLE_HEIGHT` 0.075, world can is 0.101; real shelf best estimate 12–13 cm = shelf6; CONFOUNDS row 48 withdrawn). Honest w3 funnel of record (74 uids, bit-exact): **picked 69 / placed 66 / set-down 46 / contact 26 / honest nested 16**, tipped 32, upright-short 21 (CONFOUNDS row 47). Noise floor (w3 + 1 mm IC jitter): contact 27 / tipped 35 / nested 18; per-uid flips contact 7 (sd 2.65), tipped 7, nested 4, picked 0; 10 stage-flip uids [237 244 248 256 259 261 269 316 327 328]. **THE FINDING OF RECORD (CONFOUNDS row 49): contact-constraint CREEP** — Genesis regularises friction constraints by R ∝ (1−imp)/imp, so a pinched can under a sub-limit tangential load creeps linearly in time (pinch lab `can_pos_recovery/pinch_lab.py`: 59° droop/1 s under 0.1 N·m at the engine default 0.9/0.95; 8.5° at impedance 0.99; ∝ timeconst; independent of pinch force and contact count — the split-can/"free hinge" idea was the WRONG lever, refuted). Release lab (20 w3 tips, `release_lab.py`): control 0/20 upright, no friction/knuckle/ts5 condition > 10/20, snap-open 17/20 → the can rotates in the slowly-opening pinch. **RUNNING (`CENSUS_ROOT=baselines/demos_v2/census_creep_0903 nohup can_pos_recovery/census_shelf_0903.sh …`, launched 16:55, ~50 min/row incl. honest, sequential 6-way, idle box; driver log `baselines/demos_v2/census_creep_0903_driver.log`, events `census_creep_0903/census.log`):** (1) **P1 `gc_kp4_riser3_shelf6_imp99`** (impedance 0.99 on all 16 geoms, timeconst untouched); (2) `gc_kp4_riser3_shelf6_gimp99` (finger+can geoms only, scope control); (3) `gc_kp4_riser3_shelf6_ts5_imp99` (bracket). Predictions §4: P1 contact ≥ 32, tipped ≤ 22, picked 65–69, nested ≥ 20; adopt iff contact ≥ 32 ∧ tipped ≤ 24 ∧ picked ≥ 66 ∧ same sign on holdout 24; disconfirm branches (a) per-phase release world, (b) imp 0.95/0.97 ladder if picks drop ≥ 4, (c) within noise → keep w3, report control-limited. Score: `score_shelf_prereg.py --new baselines/demos_v2/census_creep_0903 --variants <rows>`. New code today: `sim_variants.py` `impedance`/`impedance_scope` + `can_segments` (split can, rho-aware URDF with `<inertial>`, kept but not a candidate; all selftests PASS); `census_shelf_0903.sh` honours `CENSUS_ROOT`; scorer `--new`; `can_pos_recovery/{tip,slide}_anatomy.py` promoted from scratchpad with the correct can height (`CENSUS` env + variant args). **Pickup after the runs:** fill prereg §5 + CONFOUNDS row 49 status; `tip_anatomy.py`/`slide_anatomy.py` on w3 (corrected height) and the winner; success-by-stage table + real | w3 | candidate videos of flipped uids (`render_census.py`) to the user; if adopted → head agent decides the retrain (physics change for every arm). **Open asks to the user:** tape-measure the real shelf surface above the table (prediction now ~12–13 cm); weigh the real can (rho 852.5 ⇒ 0.295 kg vs 0.35 assumed). Earlier today (still true): ts5_w4 NOT recommended (user 12:10 "no change needed"); replay-harness history leak fixed (row 45); real2sim fidelity tool (row 46, disclosed not fixed). All of this box's 09-02/03 work is UNCOMMITTED (commit only if asked); census dirs, `_timed.npz`, honest/release/pinch outputs are data (rsync-only, untracked). `git stash@{0}` is disposable.
- **Earlier (sim box, 2026-09-02 evening).** **WORLD SEARCH on the legacy `trial_placements` world — CONFIRM on the sealed holdout but the WRONG world for the paper** (prereg §4): champion (ff 70, goal_fric 1.07, table_fric 0.35) holdout 23/18/11/11 vs θ0 24/17/9/7; not for citation. **THE REAL FINDING: the frozen w3 sets are `gc_kp4_riser3_shelf6` WITHOUT `grasp_timeconst 0.005`** → gripper_lab defect A (8–10 mm finger–can penetration, over-squeeze ejects the can) is live in every corrected-world arm; user's pinch-away/straight-fingers observation on `v2_w3_dHv2` = that defect (attribution unconfirmed by the head agent). Memory note `ts5-gripper-fix-not-in-frozen-sets`.
- **Last session:** 2026-09-02 midday (pre-compact). **New living doc: paper/CONFOUNDS.md (15 rows) — check every claim against it.** Today: A29 FINAL n=8v8 (pruned beats raw DP +0.16 old p .006 / +0.27 corr p<.001; fig11; fig12 = pruning removes time not space, 8 raw-only trials); figures restyled single-column (colstyle.py, `human*` convention, wide kept as *_wide; fig1 shows both worlds; fig13 WM diag); **WM clamp finding: return_clamp=100 mis-set vs shaped returns ~500–1000 → critic saturation → bistability (RESULTS §3.2); r2d NOCLAMP "runaway" WITHDRAWN, dv3 runaway STANDS**; A32 WM pilots submitted (C2000 dH s130-133, RS1 dH s134-137, SPARSE-RS1 dH s138-139; LOGDIR_BASE *_C2000_dH/_RS1_dH/_SPARSE_RS1_dH); **A33 RLPD-dense corrected world submitted (dH/dDP s60-67, wave g99w3dense, frozen matched_w3 sets)**; A31 chain RESUMED from selection after a sourcing bug (select 3162376/84 → shards → merge 3162383/91 → build 3162392 → submit 3162393 = 32 machine-arm learners + A28 release + A27 packs if N≥45); **both RLPD die-offs were MY purge deleting wandb_cache under running jobs** (fixed in ~/purge_0901.sh); the 16 s60-67 dHv2raw RLPD runs are being RE-EVALUATED from archived ckpts by cluster/rlpd_select_confirm.sh (agent; CPU jobs) — no retrain. idle_frac metric fixed (arm columns; machine demos have 0 idle decisions, human pruned ~0.19). Arm names: dH=raw, dHpruned, dDP (vs raw base), dDPpruned (vs pruned base). Train pack ~/train_2026-09-02/ current. **Pickup:** rlpd re-eval headlines → RESULTS §2 v2 row; chain log $LAB/ddpv2_chain.log; A32/A33 first-hour checks; A27 packs after chain submit; update CONFOUNDS rows 3/13 when A33/A32 read out.
- **Last session:** 2026-09-01 night (post power-failure; user offline ~04:00–?). **A31 correction of record: the dDPv2 teacher trains on PRUNED dHv2** (my A25 raw-teacher was an error; that harvest archived as *_rawteacher_archive). Arm names of record (user): dH=dHv2raw (raw), dHpruned=dHv2, dDP=dDPv2 (matched to raw base; RLPD/WM machine arm), dDPpruned=dDPv2p (matched to pruned base; DP machine arm). **Cluster-resident A31 chain running (no laptop needed; scripts cluster/a31_chain/, log $LAB/ddpv2_chain.log):** teachers 3137348/49 (old) 3137358/59 (w3) → select 3137350/60 → harvest shards 3137351-56 / 3137362-67 (attempts 8) → merge 3137357/68 → build 3137369 → submit 3137370 (32 machine-arm learners DP dDPv2p v2fullP{,w3} + RLPD dDPv2 g99v2full{,w3} s50-57; releases A28 3120763-68; A27 12 packs if w3 N≥45). Readouts self-generate: harvest jobs 3137006-08 (+8/20/32h). **Snapshot 21:45:** DP on raw dH BAD (rnd 0.35 old / 0.24 w3, n=8) vs frozen 0.55; first dHpruned DP 0.60; first raw-dH RLPD w3 23/30 — RLPD thrives on raw, DP dies on it. Train pack for user: ~/train_2026-09-02/ (videos, advisor, stats refresh). **Pickup:** read ddpv2_chain.log + newest paper/harvest_*.md; check TEACHER-BAR-FAILED / A27-SKIPPED-LOW-N; score v2 contrasts with ~/train_2026-09-02/3_stats/v2_stats.py; then RESULTS/figs update.
- **Last session:** 2026-09-01 evening (main session, restart for model update). **READ paper/figures/ADVISOR_BRIEF_2026-09-01.md first** — corrected-world triple gradient (DP indifferent +0.06; RLPD pre-registered NULL vs old-world +0.21×3 = world-dependent, coverage mechanism figs6/7; WM n=8v8 +0.246 perm p 0.133, ignition 7/8 v 3/8 — directional, A27 confirmatory). PREREG A20–A28 registered pre-readout. Mechanism: STOPS-vs-CREEP (strict-stop 0.37 v 0.22 d+1.16; pause_frac fragility disclosed; fig10, WM_METRIC+addenda). Drift audit: frozen-set ICs median 2cm from recovered truth (no reruns). v2 (A25/A26 raw-for-everyone, uncapped, dHv2raw N=69/66): human-arm 32 jobs (v2full/g99v2full{,w3}, s50-57) + 4 dDPv2 teachers RUNNING at restart; A28 packs 3120763-68 QUEUED (dDPretimed s110-113/dHsmoothed s114-117/dDPnoised s118-121; sets manipulation-checked 6b324d1). **MANUAL PICKUP (subagents+watchers died at restart):** (1) teacher HEADLINEs → selection → dDPv2 harvest (--ic-mode demo --attempts 3 --mode sample --verify; sim-variant for w3) → make_v2_matched vs dHv2raw + lerobot + r2d conversions (templates V2_BUILD_2026-09-01.md §7/§D+addenda) → machine-arm learners (same waves/seeds) + A27 24 packs (2-seed 90g, s90-101); (2) compose job 3120169 → rsync cluster v2_review/real_vs_sim/ → ~/demo_review_2026-08-31/real_vs_sim/ (real videos local in real/, 84); (3) A28 first-hour checks; (4) ~/purge_0901.sh on cluster possibly unrun (fs 89%). Silent-default sightings: NINE. Token frugality directive in memory.
- **Last session:** 2026-09-01 morning (part 5: OVERNIGHT RESULTS). **DP `dp_v2r_0831` DONE + evaled**: 100k clean (loss 0.003); dual-IC eval n=15/block: **in-dist picked 0.40 / placed 0.33 / contact 0**, random picked 0.07; hand-picked demo-IC videos 4/4 picked+placed (incl recovered 245/262) sent to user; single seed 0 — DP seed variance (0.00–0.47 historical) means no strong conclusion vs the 0.67/0.27 July control without seeds 1/2; in-dist IC support also changed (recovered hard starts now in the set). **RLPD FAILED TWICE at γ0.998 (legacy clock)**: attempt A (auto-ent) Q→3082 by 67k, killed; attempt B (pre-registered ent_coef 0.005 fallback) ran 100k but Q→18,398, final ckpt 0.0 in-dist / 0.1 random (videos+metrics in `baselines/rl/checkpoints/rlpd_v2r_s0b/wandb_eval/`). **Attempt C (`rlpd_v2r_s0_0901-g99` = γ0.99/A17 + ent 0.005) CANCELLED by user ~06:45 at ~25k steps (box being moved) — INCONCLUSIVE, not failed**; partial checkpoints in `rlpd_v2r_s0c/`, resume/relaunch with the same flags if wanted. If a future run at γ0.99 also explodes, the local legacy-clock recipe is dead and the fix is native contract-v1 tapes at the final-rr clock (rerecord_delta_demos path). Local RLPD fps ≈3.7 (200k≈15h — resize accordingly). wandb: all runs in `genesis_pickaplace`. **Status:** 🟡 paused (box moving); no processes left running; everything committed except datasets (rsync-only: `episodes_recovered_v2` 37 tapes, `episodes_all_v2r`, `episodes_pick_v2r`, phase dirs, `lerobot_dataset_pick_v2r`) and the DP/RLPD outputs + eval videos (local only). **Wrap-up next steps:** (1) DP seeds 1/2 if the 0.40-vs-0.67 question matters; (2) RLPD γ0.99 rerun to conclusion; (3) camera-audit PREREG + click pass (unchanged from part 2); (4) commit the 2026-09-01 doc/status updates (uncommitted in working tree).
- **Earlier (part 4: overnight launches).** **DP `dp_v2r_0831`** (lerobot diffusion, state-only, 100k steps @6.4 it/s ≈4.3h, `baselines/outputs/dp_pick_v2r`) on the NEW 73-tape success set; **RLPD `rlpd_v2r_s0_0831`** (RLPD_PLAN pinned recipe: UTD-10, E10/Z2, LN critics, γ0.998, pick scope, delta_joint, legacy clock, 200k ≈7h, `baselines/rl/checkpoints/rlpd_v2r_s0`) on all 91 tapes (81,292 demo transitions, 50%/batch). Both wandb project `genesis_pickaplace`, run from `.venv-eval` (sb3 2.8.0 + genesis 0.2.1 + lerobot 0.4.5 fork, all verified). Datasets: `episodes_all_v2r` (91 = 36 v2 + 55 old), `episodes_pick_v2r` (73 ≥picked) → `lerobot_dataset_pick_v2r`, `episodes_pick_phase_v2r{,_all}` + `pick_entry_states_v2r.json`. DISCLOSED runs on the recovered corpus — frozen paper sets untouched. **MORNING TODO:** eval both + videos to user (standing directive); RLPD eval via wandb_eval/eval_sweep pattern; DP via the funnel eval. **Round-3/late-evening results:** ok_batch sweep 11/12 solved ×3-validated 0 flips (nested 294/299/300/328); stale re-collect 11/11 improved (nested 247/256/320/330); 294's v2-collection no-pick was a LOAD ARTIFACT (solo re-collect: nested, 5363 frames; env-path search 3× nested at the merged xy); 237 = camera-convicted control-limited (0/31, seeds ON the visible can), 303 no-video. **FINAL: success funnel picked 73/75=0.97, placed ~0.89, contact ~0.44, nested ~0.35** (single-collection stages; v2 pool 37 tapes / ~97k transitions). Commit `a478d95` pushed (user-directed agent; datasets rsync-only, v2 pool + backups untracked).
- **Earlier (part 3: RECOVERY EXECUTED — user goal "recover as many demos as possible").** **Read `paper/DEMO_RECOVERY_RESULTS_2026-08-31.md`: 13/14 unsolved successes recovered + fail 240 — solved successes 61 → 74/75, SEVEN through NESTED (233, 255, 262, 267, 275, 301, 321 — all six real nested demos re-nest, plus goal-disturbed 255).** Winners ×2-reproduced (0/14 flips), merged into `trial_placements.json` (backup `.bak_0831_1741`, tag `cpu_research_recovery_0831`; world block untouched — merge_and_validate.py's WORLD is stale kp100, don't use its merge). 303 = the one casualty (control-limited: 0/32 lift, no video). 319/329 cap at picked (round-2 fine+drag-bridge grids failed to upgrade; a lone 329 "contact" was a load-flake rejected by solo reps). The old "fk-seed" label meant NEVER SEARCHED, not unsearchable — 233's FK seed wins verbatim. Datasets/matched sets untouched; new tapes would be a disclosed v2 pool; 3rd validation rep optional (`rescue/driver_val.sh` in session scratchpad). **v2 POOL + VIDEOS DONE (same evening, user-approved):** `baselines/episodes_recovered_v2/` = 14 tapes / 23,191 transitions, `--ee`, integrity-verified, `_stage.json` manifest; env-basin stages ≥ replay-basin for EVERY tape (nested 7 / placed 5 incl 319 / contact 1 = 329 / picked 1 = 240) — the round-2 "flake" was the two-basins gap, not noise. Videos: `can_pos_recovery/videos/<uid>_*.mp4` ×14, suffixes renamed to VALIDATED replay stages (render_videos.py had two stale bugs, both fixed: crashed on goal_pos=None recovery entries — now falls back to static goal; outcome scorer used pre-07-20 hard-contact nested — now proximity). 233+255 videos sent to user. DISCLOSED-V2 only — frozen matched sets untouched; training use needs PREREG amendment. **What's next:** (1) camera-audit PREREG then click-pass scoring (incl. the 4 no-pick fails via t0 camera — valid for them since the can never moves); (2) if v2 trains anything, PREREG amendment first.
- **Earlier same day (part 2: census + calibration gate).** **Read `paper/CAMERA_GATE_2026-08-31.md` — verdict GO at ~1.5–3.5 cm, with a protocol correction that invalidates the naive audit: placements encode GRASP-time positions, so score at the closure-that-initiates-the-lift, NOT t0** (cans nudged pre-close: 286 = 71 s of maneuvering; dragged post-close: 295 = 11 cm; 261 = regrasp case that breaks "first closure"). Pipeline + model + eyeball overlays: `can_pos_recovery/camera_audit/` (README has the env recipe — pure-python rosbags, no ROS). t0 census of all 21 unsolved trials: NO lying start cans; 4/5 unsolved fails + 301/319 = arm-base edge cluster (bad for search AND camera); **255's goal can visibly disturbed at t0 (why it never solved)**; 233/259/266/267/275/278/321/333 clean → camera-seed re-solve candidates; 9 labeled trials have NO video (audit ceiling 82/91; only 303 of them is unsolved). Camera-free stat: |close-xy − placement| median 2.9 cm, 14/15 ok-trials ≤ 5 cm. Goal-top pixel identical ±6 px across ok trials = first visual confirmation of the static goal. Calibration war stories in the README (tape-only fit is 10–20 cm degenerate while looking self-consistent; unconstrained distortion runs away; every largest-blue-blob heuristic failed once — cans/tape/elbow all blue). **What's next:** PREREG the audit (thresholds from measured error; disconfirm-branch decision rules) BEFORE scoring; then the close-time click pass over 80 videoed trials (cyan markers in `camera_audit/overlays/topc_*.jpg` = projected placements); refit; score; camera-seeded re-solves for the 14 unsolved successes. **Blocked on:** nothing. Post-blackout cluster harvest reads still pending from 08-30 handoff.
- **Earlier same day:** Adversarial critique of `paper/DEMO_RECOVERY_PLAN_2026-08-31.md` → `paper/CRITIQUE_demo_recovery_2026-08-31.md`: bags ARE local; **61 solved successes, not 72** (07-20 "61→72" counted 11 solved FAILS; 16 successes lack can_pos incl. user-validated 233); no Hough prototype exists; homography parallax ≈ 4–8 cm > 3 cm threshold (fix = DLT + can-top plane); no prereg/disconfirm branch.
- **Prior session:** 2026-08-30 (blackout handoff). READ `paper/RESULTS_for_writing_2026-08-30.md` §7 and the bottom of `paper/NEXT_2026-08-28.md` first; harvest files on the cluster are the first read after the blackout.
- **2026-08-28..30 — key state:** RLPD recipe restarted at γ 0.99 (A17); RLPD (1a) human>DP final n=8v8 (LAST rnd +0.21, p 0.001; divergence 1/8 vs 6/8); both DreamerV3 torch ports run away without the return clamp (dv3-std, r2d NOCLAMP) → WM arm = clamped r2dreamer, disclosed; r2dreamer corrected-world gate ignites (human>DP n=4, p 0.14, n=8 running); DEMO3/MoDem fallback venv + smoke queued; PREREG A16–A19; packing wrappers (r2d 2–4/GPU, dv3 3/GPU); three dv3 launcher bugs + three silent pack deaths fixed — check pack .out for rc=1 within an hour.
- **Last session (before that):** 2026-07-31 (this box)
- **2026-07-27..31 — key state:** (1) **Positive control CONFIRMED**: joint DP picked 0.67 in-dist / 0.27 random (audit-replicated on rebuilt data; 15-ep dual eval via `wandb_eval --ic-mode both`, headline = demo-IC since 07-30). Human IL set = **66 demos** (2 pick / 38 placed / 6 contact / 20 nested). (2) **Cartesian BC unexplained** after 6 dead hypotheses (all EEF encodings ~0.00 in-dist incl 6-DOF abs6/delta6; joint works) — obs×action 2×2 running on cluster answers it; July30th_Fable.md §2 has the full ledger, DON'T retread killed hypotheses. (3) **Cluster genesis segfault SOLVED**: installer's conda-ffmpeg poisoned libstdc++ (11:38 07-30); every "hardware" theory was an artifact of the poisoned window. Fix = conda rollback + pip-only install; `cluster/verify_env.sh` passes incl. H200. ONE env serves lerobot+genesis (proven both machines). NEVER conda-install into it. (4) **Ouroboros**: end-to-end proven on cluster; lineages (DP+ACT, TARGET_KEPT, IC_MODE=demo) running; eval metrics were going to the WRONG wandb project + failing on poisoned env — fixed; gen-0 post-hoc eval healthy (user-run 07-31). (5) **Stage-wise matrix adopted** (user): isolated per-stage evals via reset-to-recorded-entry-states + chained specialists; placed_v2 (release-based) boundaries; DP-only specialists. Tooling = next build. (6) dv3: local joint pick-scope run = H4 learnability gate; tip penalty now 0.0 (termination only); `train_success_rate` was counting tips as successes (fixed, incl VEC facade); abs6 RL degenerate (13-33 step episodes, open question). (7) **Six silent-default/fallback bugs** this week (grip-column ×3 files, control-mode ×3 boundaries) — pattern documented in AUDIT_REQUEST_Fable.md; every cartesian call site now passes control explicitly; datasets travel by rsync ONLY (never git; 862MB incident in history, cleanup deferred).
- **2026-07-24..26 — CARTESIAN EVERYWHERE + OUROBOROS.** (1) **Best-DP result: state-only WINS** — img legs (top / top+wrist, 100k, 67 pruned pick demos) both eval picked 0.20/placed 0.20/contact 0/nested 0 vs state 0.33/—/0.11/0.07; BUT image legs were confounded: inputs included `environment_state` (ground-truth can pose) so cameras were redundant, and the resnet18 backbone trained from scratch (`pretrained: None`). Honest vision DP (drop env_state + IMAGENET weights + crop aug) queued as wave-2. (2) **lerobot dataset bug family** (each cost hours): converter never called `ds.finalize()` → metadata truncated at interpreter exit (`pa=None` in `__del__`) → IndexError at train; torchcodec 0.10⟷torch 2.7 ABI mismatch (fix: torchcodec==0.3.*, pinned in cluster/install_lerobot.sh); lerobot PNG writer missing mkdir (patched in fork, branch genesis-fixes). dtype=video is the path of record. (3) **Ouroboros pipeline** (cluster/sbatch_ouro_{train,harvest}.sh): train→eval→negctl-gated verified harvest→convert→resubmit; smoke-caught: PICK_CAP 300 truncated closed-loop DP teachers (2.5% yield vs 0.33 eval — `--cap`, cluster default 1200); harvested npz now carry stage/label. (4) **Cartesian arm**: `CartesianFullTaskEnv` (staged reward over CartesianCanEnv, [-1,1]^5, VCAP 0.11/PITCH_CAP 1.0 measured from bags); demos = human's REAL joystick vel_cmds (commanded-replay validated: uid232 picks+contacts); collector records rig images (+ latent max_steps=1200 #26 bug fixed); dv3 task genesis_cartesian (CART=1 sbatch); ouroboros ACTIONS=cartesian (proprio 9); gen-0 = 67 picked-only pruned cartesian demos. (5) **TIP RULE** (user-driven, census-backed): terminate + −0.5 when can lies tipped FREE (tilt>60° AND grip open). Census: past 60° orientation NEVER recovers (31/32); but demos carry the can pitched >60° IN-HAND (4/5 contact demos) → grip-open guard essential; zero nested demos ever tip. My "8 fallen-can demos" belief was STALE (pre-panel placements; current: 2 bogus 90°@z=0.085 artifacts). (6) **dv3 misc**: periodic policy eval baked into sbatch (was a local-only side process — cluster never evaled); 5M steps ≈104k updates (~21k @1M didn't learn; cluster hits train_return 0.3); eval SAMPLES (since user's 365220c; new `eval_action_mode` config); demo reward audit CLEAN (272 total = 17×8+8×4+50×2+4×1; random-policy negctl 0 reward ×3; videos in dreamerv3-torch/demo_videos + random_episodes). (7) **Cartesian SACfD**: relabel_cartesian grant-parity with dreamer converter EXACT (66/63/19/12/16) → reward-identical DV3fD/SACfD comparison; 200k running. (8) ALGORITHM_MIX.md = portfolio plan (wave 1: SACfD/DP/DV3fD; wave 2: ACT, RLPD-style, honest-vision DP). **NEXT:** ouro1 gen-0 eval in wandb `genesis_pickaplace_ouro` decides if the chain earns its generations (scancel if ≪0.33); sacfd_cart eval curve; dv3 cart5m/run5m curves; then SACfD-teacher chain + wave-2.
- **2026-07-20 BB — ROOT CAUSE OF THE ENV-VS-REPLAY GAP (#26 successor, task #7): `env.step` default `max_steps=1200` is an EVAL horizon; the collector feeds whole tapes ignoring `done`, and once `done` goes true env.step runs `_nested()` (100 settle steps) on EVERY subsequent command → demos >1200 frames get 100 phantom sim steps/cmd from t=1200 on (uid 300: ~357k) → tape desyncs → no-pick.** Proven by bisect: call-equivalent traces WITHOUT the done-block are bit-identical to replay (uid 300 picks, stage=nested via fixed collector). Red herrings eliminated en route: per-step obs reads (get_contacts/get_dofs_control_force) DON'T perturb; fp32-truncated arm cmds absorbed (sim is fp32 internally); grip [0,1] clip only bites where recorded gp<0 (kept raw-command pass-through `env.step(grip_motor=, arm_cmd=)` anyway — collector uses it). **Fallout: every episodes_all demo >1200 frames had a corrupted post-1200 tail (41 staged demos + 9 long no-picks) → FULL RE-COLLECT (in flight at session end: run_recollect_all.sh, 16-way; archive episodes_all_maxstepsbug/). Short no-picks 236/237/244 NOT explained — check post-recollect.** **RE-SEARCH (task #8): 11/13 fk-seed no-picks recovered via NEW cpu_research.py (CPU-parallel candidate search, ok-class by construction; also has --env-path mode to search in the collector basin). 4 nested (247,256,320,330), 1 contact, 3 placed, 3 picked; solved 61→72. Unsolved: 255, 303.** trial_placements.json updated (backup .bak_premerge). **GATES (idle box, x3, perfectly deterministic): G2 21/19/7/7 vs old-box 20/15/5/5 (placed +4 = FAIL → same-machine baselines mandatory); G3 picked 0.20 vs 0.40 (FAIL). ALSO: heavy load (24 procs) flips borderline demos (G2 20/18/6/6 loaded vs 21/19/7/7 idle) → official numbers ONLY from idle box, x3.** **Throughput: ~25s/episode serial, 16-way sweet spot (~28 eps/min; 75-demo replay <3min). wandb (task #9): wired both trainers — lerobot --wandb.enable + disable_artifact (run_dp_pick.sh, run_v6_train.sh incl. post-train video-eval stage); SB3 via baselines/rl/wandb_utils.py (scalar cb + async CPU video-eval subprocess cb) + baselines/wandb_eval.py (shared eval worker, smoke-tested); train_sacfd_full.py has --run-name/--eval-freq; wandb auth present on box. NEXT: recollect finishes → merged manifest breakdown (expect success ≥picked 50→~65-70) → finalize_pick_dataset (episodes_pick was NOT copied to BB, rebuild) → re-baseline champions same-machine → DP + SACfD retrains w/ wandb.**
- **2026-07-20 — GOAL FINAL (human eyes beat both statistical fits): (0.672,-0.221).** User validated demos 233+242 ("pick/place/slide perfect") → their finals must TOUCH the goal → two-circle intersection, user picked SOUTH from candidate videos, matches slide directions. My ring-fit (0.662,-0.057) was WRONG: contact coverage was ONE-SIDED (all approaches from north) which fools a fixed-radius circle fit; place-cluster median also biased (wrist-at-release ≠ can rest). Ancient (0.6,-0.2) had y right all along. **Nested metric now PROXIMITY-based** (center-dist ≤ 0.081 = diam+15mm noise floor; 233 verified 0.5mm short of hard contact) + picked + both upright + settle — baked into env/remeasure/render/collect. Neg-control nested FP 2/16 (12.5%) — thin separation, watch. **Replay @ south (75 succ): picked 61 / placed 47 / contact 15 / NESTED 16 (0.21, was ~0.03-0.04).** **NIGHT2 retrains @ south: DP-pick-v2 = FIRST policy with nonzero nested on random ICs (picked 0.33 / contact 0.11 / nested 0.07; seeds 0.40/0.07/0.53 picked — DP seed variance persists). SACfD-v2 picked 0.55 (0.40/0.33/0.93). Pattern: RL leads pick, DP leads downstream.** Old-goal datasets archived as `episodes_*_goal662`. **NO-PICK diagnosis (user asked):** 25 no-picks = ~11 env-path artifacts (one-sided replay-vs-env gap; TRACE: identical commands/world but arm diverges 17mm in FREE SPACE — suspect env._obs() per-step get_contacts/get_dofs_control_force perturbing the solver; scripts+data in `baselines/diagnostics/`; new-box job #1 = rerun trace_env with obs reads disabled) + ~4-6 recoverable by re-search under current world + ~8-10 control-limited hard core (regrasps/nudges — leave as RL negative data). Genesis checkout: committed the local headless-render patch (f41427d); it's v0.2.1+269 upstream commits — NOT pip-reproducible; lerobot is the user's fork (dirkmcpherson/lerobot).
- **Prior session:** 2026-07-18 (night)
- **2026-07-18 HEADLINE RESULT — RL-on-everything BEATS DP-on-success at the pick stage (random ICs, corrected goal):** SACfD trained on ALL 91 demos (58 pick r=1 + 33 fail/no-pick r=0, reward-relabeled) → picked **0.40/0.53/0.47 (mean 0.47)**, far more seed-consistent than DP-successful-picks (38 success-only demos, 100k steps) → picked **0.00/0.07/0.47 (mean 0.18)** with wild seed variance (classic small-data DP overfit; in-distribution the DP does complete the task — videos sent). SACfD checkpoint curve (25k→200k: 0/0/.2/.2/.2/.5/.1/.4) NOT clearly plateaued → hence overnight resume. DP-pick checkpoint: `outputs/dp_pick`; SACfD: `sacfd_all/sacfd_final.zip`. New infra: `--max-steps` on eval_policy+eval_sac (#21 lever), parallel-seed eval chains (`run_pick_eval.sh`, `run_sacfd_eval.sh`), `resume_sacfd.py`, `full_env.py` (staged reward: pick+1 place+1 contact+2 nested+4, training-only nested proxy — EVAL keeps settled honest metric), `train_sacfd_full.py` (staged demo relabel with per-frame proxies GATED by env-measured episode stage; dry-run grants: 58 pick/52 place/45 contact/1 nested — mirrors demo_buffer exactly). Full-task DP legs still pending: v6 (33 contact demos) via `run_v6_train.sh` queued behind overnight.
- **Prior session (same day, earlier):** 2026-07-18
- **2026-07-18 — EXPERIMENT REFRAME (user): RL on everything vs DP per-stage (success only).** Datasets built with the corrected goal (all obs carry goal_xy=(0.662,-0.057)): (1) **v6** = 33 contact-success solved demos (DP contact-stage); (2) **episodes_all** = ALL 91 non-stub demos kept full-length regardless of outcome (RL "everything"), via `collect_all_classified.py` (solved→reset(uid); unsolved→FK-seed); (3) **episodes_pick** = 38 SUCCESS-LABELED solved demos that reached ≥picked (DP pick-stage, NOW TRAINING). Classification: `demo_manifest_auth.json` (env-measured) + `demo_manifest.{csv,json}` (prelim gallery). KEY ACCOUNTING: 93 total = 75 success + 16 fail + 2 stub; only **50 success are "solved" (ok/ok_batch)** = collectable in training env, other 25 are FK-seed-only. Collector fixes: `--max-attempts 3` + no-pick early-abort (8-retry was ~11h; now tractable); parallelized collection 3-6 CPU procs (KeyError bug fixed — chunks must draw from env.solved_uids not all-success). `finalize_pick_dataset.py` filters label==success (7 fail demos 238/249/260/270/310/313/334 excluded — they're the neg control). Pipeline scripts: `run_dp_pick.sh` (convert+train), `run_v6_train.sh`, `run_pick_pipeline.sh` (end-to-end, GPU-gated). FOOTGUN HIT: killed-pipeline had already launched a train → double-launch race; cleaned up. **NEXT:** when dp_pick done → eval random ICs + eval videos (standing directive); then RL-on-everything (SACfD --demo-dir episodes_all) + DP contact-stage (v6) for the per-stage comparison. Video set re-rendered at corrected goal (videos_realsim/ 82, videos_gallery/ 82).
- **Prior session:** 2026-07-17
- **2026-07-17 session — GOAL-CENTER NAILED (#27, done).** User's idea: recover the static goal from real placement slides (picked-can contact points ring around the goal at 1 can-diameter). Built `can_pos_recovery/goal_from_slides.py` (fixed-radius circle-fit = least-biased center) → **goal (0.662,-0.057)**, baked into replay_harness `STATIC_BOTTLE_POSITION` + ic_sampling + genesis_can_env (uid-reset now uses static goal, not stale per-trial `r['goal_pos']`) + example.py (was still (0.6,-0.2)!) + remeasure default. REJECTED a sim goal-free re-fit (`goal_from_sim_slides.py`): goal-removed replay OVERSHOOTS (held can dragged past real stop). Honest full-75 @ new goal: picked 62/75(.83) placed 52/75(.69) contact 48/75(.64) nested 3/75(.04); fail-demo neg-control contact 5/16(.31) nested 0/16. vs old (0.656,-0.103): placed +7, contact -5 (DE-INFLATION not regression; old goal sat on -y edge of can cluster), nested 0 — within noise. **CORRECTED an overstatement mid-session:** stride-3 subset showed 0.72 contact but honest full-75 is 0.64. User chose to KEEP the true data-derived goal (honesty over the flattering metric). **cam0 diameter-ruler idea abandoned** (goal can is clipped in corner, oblique side view). **Gallery metrics.json analysis:** goal-mismatch was NOT the dominant replay failure (only 1/7 pick-fails were goal misses); real ceiling = TIP-OVER (29/67 successes end tilted >45°, incl. 22 that contact then tip → 72%-contact/3%-nested gap) + no-pick (12/67). SACfD-broad 200k teacher verified (loads, 17→7 deterministic, ready for harvest --scope pick). **NB DATASETS STALE:** episodes_raw_v4 etc. encode OLD goal in obs → MUST re-collect with corrected goal before any retrain. **NEXT (when GPU free):** re-collect demo dataset w/ new goal → re-run Milestone-1 gate (teacher past 0.4 full contact random). Throughput (task #21) still blocks the matrix.
- **Prior session:** 2026-07-13
- **Standing user directives:** full autonomy granted (no permission-asking except destructive actions); EVERY trained policy gets eval videos sent to the user; honesty protocol (corrected metrics, x3 reps, negative controls, pre-registered predictions) is mandatory for all reported numbers.
- **2026-07-13 session — NEW EXPERIMENT: human vs AI demonstrations.** Goal: train teacher (on human demos OR reward) → harvest AI demos from it → compare a student (DP/BC/SACfD) trained on human vs AI demos. User bar for "useful policy" = full-task contact ≥0.4 on random ICs; validate scaffolding on PICK subtask first. BUILT + validated (all compile, harness dry-runs): shared eval extractions `baselines/{ic_sampling,eval_core,dp_runner}.py`; `harvest_ai_demos.py` (teacher-agnostic AI-demo generation, --verify open-loop replay guards manufactured success, random-teacher must yield ~0); `bc/{train_bc,eval_bc}.py` (plain MLP BC, trains to MSE 9e-4 on v4); `rl/eval_sac.py` (SAC via shared eval_core); `experiment/run_comparison.py` (matched human/AI sets, x3 seeds, 2 neg-controls). Minimal edits: `convert_to_lerobot.py` MIN_FRAMES 4th argv; `train_sacfd.py` --demo-dir/--steps/--out-dir. **RESULTS:** v4 grip-effort DP did NOT clear gate (random pick ~0.08, contact 0 — overfit unchanged). v5 collect_relaxed FUTILE (killed, 0 kept — dropped demos fail at PLACE not slide). **OVERNIGHT RUNNING:** (1) SACfD `--full` 200k on 50-ep broad buffer → `baselines/rl/checkpoints/sacfd_broad/` (at launch: ep_rew_mean 0.025, agent picking); (2) all-91-episode replay gallery → `can_pos_recovery/videos_gallery/` + `metrics.json` (user wants to eyeball close-vs-hopeless; leading hypothesis: failures are goal-location mismatches — static goal 0.6,-0.2 vs per-demo real goal — see 233: picks cleanly, carries to wrong spot, tips). **MORNING TODO:** analyze metrics.json for systematic pattern; eval sacfd_broad pick + video; fix eval throughput (task #21: 1200-step rollouts ~5min each block the comparison matrix).
- **Current summary:** Replay characterized & plateaued (pick 0.73, contact 22/75, engine gate: stay on genesis 0.2.1 — 1.2.1 fails pinch grasp). DP-v3 trained (in-dist picked 0.49/placed 0.37/contact 0.33/nested 0.05; downstream near-solved, PICK is the bottleneck). In flight at handoff: v3 randomized eval, v4 dataset chain (adds grip-effort obs), SACfD (built+smoke-verified, full 200k run queued). Next after those: real-demo BC (v5, all 75 bag trajectories), vision goal ground-truth, DP size ablation, unlabeled 200-231 ingestion. All numbers/decision rules in HANDOFF_PLAN.md §2-3.
- **Prior status (2026-07-08):** 🟡 remediation (independent panel found real overstatements; fixes landed)
- **Panel review (2026-07-08):** 4 adversarial agents. AFFIRMED: FK position recovery is sound (validated vs bag tool_pose ~1cm). OVERTURNED: (1) negative control was rigged — disabled goal relocation, hiding ~40% nested false-positive rate on fail-labeled demos (11/16 "solved"); (2) coverage 50/75 is a fit not recovery (winner drifts up to 17cm from FK, 60% depend on goal relocation median 12.8cm); (3) 52/61 solved are un-revalidated batch-proxy winners (success_rate:None); (4) nested metric lacked the picked precondition (both DP 0.06 and replay 0.35 inflated). FIXED: nested + batch proxy now require picked. Randomized-IC DP: picked 0.34/contact 0.14/nested 0.04. See CAN_STARTING_POSITION.md "INDEPENDENT PANEL REVIEW" + tasks #3,#11,#12,#13. User is rsyncing all raw bags to enable vision ground truth (Hough circles prototyped, works; no factory calibration — use FK-homography).
- **Prior session:** 2026-07-06
- **Current branch:** main
- **What happened:** Finished the placement pipeline end-to-end: 58/75 legit successful demos solved (77% coverage), validated contact 0.53 / nested 0.28 per run (see CAN_STARTING_POSITION.md FINAL NUMBERS). Key late finds: drag demos (seed at closure-start, from bags), fallen-can demos (8 solved with lying spawns — real videos show cans starting on their side), GPU->CPU winner transfer cliff (CPU re-pick pass recovers), nested metric scored at first-contact + settle (19/19 negative-control specificity). Success gallery in can_pos_recovery/videos (31/39 renders complete the task). lerobot DP baseline scaffolding in baselines/ (collector -> LeRobotDataset v3 w/ proprio+environment_state split -> eval incl --random ICs), smoke-tested.
- **What's next:** (1) full dataset collection + overnight state-only DP train (task list #9), eval on demo ICs + randomized ICs; (2) genesis upgrade in fresh venv (+ lerobot in same venv), match 0.2.1 baseline via probes -> sweep -> validation; (3) deferred: substeps=8 world (rescues 6 more drop-class trials — probe results in scratchpad probe_s8.json ... rerun post-upgrade), failure-video renders.
- **Blocked on:** nothing
- **Key decisions made:** substeps=8 decision deferred to post-upgrade world (probe: 6/8 drop-class rescued at 2x cost). Fail-labeled demos never get relocated goals (would manufacture success); example.py applies placements to success-labeled trials only. 290/322 are mislabeled stubs (gripper never closes) — excluded. Nested metric = upright+contact+at-rest after settle at first contact-success. Videos/datasets gitignored (reproducible).
