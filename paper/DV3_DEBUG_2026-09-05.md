# DV3 debug ladder — 2026-09-05 (delegated agent; brief `~/wm_fix_2026-09-03/agent_brief_dv3.md`)

Goal: make the dreamerv3-torch port (dv3, tree `$W/dv3_fix`, `$W=/cluster/tufts/shortlab/jstale02/wm_fix_2026-09-03`)
learn the sparse Genesis tasks that the r2dreamer port learns under the same protocol. Gates: **G1** reach proxy
(`reach_goal`, R=0.25, state obs, 2 seeds × 300k env steps, fresh-process deterministic eval ≥ 0.8 = 12/15 on the 15
hold ICs @1200 on 2/2 seeds) → **G2** pick smoke (demos, ≥ 500k, picked ≥ 0.5 on rnd30) → **G3** pick n=4 v 4.
Honesty protocol: register before running; no number in this doc before the tool output shows it; fresh-process
evals only; every readout carries job id, checkpoint, IC set, action mode.

## 1. Evidence read before registering (2026-09-05 11:30–11:50, cluster clock)
On record (WM_FIX_LOG 09-03 15:17 → 09-04 00:50; PLAN §7 1d/1e): dv3 reach R=0.25, fresh deterministic hold-15 eval of
the END checkpoint, 150k env steps: baseline (fp16, joint deltas) {1/15, 0/15}; fp32 {15/15, 0/15}; EEF {11/15, 3/15};
EEF+fp32 {4/15, 15/15}; R=0.30 baseline 300k {0/15, 13/15}. Every lever bimodal; in-loop bins wobble after ignition.

New readings from `metrics.jsonl` of the ten `s1b_dv3_reachgoal_state_*` runs (tool output 11:41):

| run | target_max (max / last 5) | value_max max | value_mean last | entropy last 5 | fps | fresh hold15 |
|---|---|---|---|---|---|---|
| r0.25 s0 (fp16) | 3.03 / 2.98 2.27 2.20 2.48 1.80 | 2.01 | 1.59 | −1.8 2.2 1.4 0.6 4.2 | 15.7 | 1/15 |
| r0.25 s1 (fp16) | 0.00 (never ignited) | 0.00 | 0.00 | 9.93 ×5 | 16.2 | 0/15 |
| r0.25 fp32 s0 | 3.36 / 2.45 2.04 2.01 1.55 1.36 | 2.24 | 1.17 | 2.9 0.4 3.5 2.9 6.2 | 16.7 | 15/15 |
| r0.25 fp32 s1 | 4.20 / 3.27 4.04 4.16 4.20 3.11 | 3.60 | 2.89 | −2.1 −1.8 −1.6 −2.3 1.7 | 17.3 | 0/15 |
| r0.25 eef s0 | 2.74 / 1.94 2.20 2.25 2.36 1.46 | 1.97 | 1.23 | 1.9 1.6 −0.3 −1.6 1.8 | 14.4 | 11/15 |
| r0.25 eef s1 | 2.78 / 1.04 1.42 2.52 2.66 2.43 | 2.19 | 1.71 | 5.7 4.3 −1.5 −1.4 −0.2 | 14.9 | 3/15 |
| r0.25 eef_fp32 s0 | 3.19 / 1.69 1.74 1.73 1.59 1.82 | 1.52 | 1.24 | 1.8 2.4 −0.6 −1.4 0.5 | 20.8 | 4/15 |
| r0.25 eef_fp32 s1 | 1.94 / 1.85 1.79 1.23 1.17 1.41 | 1.38 | 0.86 | −0.2 −0.5 3.3 3.8 2.5 | 20.2 | 15/15 |
| r0.30 s0 (300k) | 4.69 / 4.39 3.38 2.99 3.66 3.65 | 4.31 | 2.75 | −3.0 −0.4 −0.3 −2.7 −3.6 | 11.8 | 0/15 |
| r0.30 s1 (300k) | 2.80 / 2.62 2.39 2.10 1.72 1.83 | 2.05 | 1.25 | 2.3 −0.6 3.2 4.9 4.2 | 11.7 | 13/15 |

Readings:
1. **The r2dreamer TRIGGER is present in dv3.** The task's maximum attainable return is 1.0 (one sparse +1, then
   terminate). In every ignited run the imagined λ-return targets run to 1.9–4.7 and the critic to 1.4–4.3 — the
   critic bootstraps through a terminal the model does not represent as terminal, exactly the mechanism §6 of
   `RESULTS_WM_HUMAN_VS_MACHINE_2026-09-04.md` names for r2dreamer (fixed there by `return_clamp = 1.0`).
   dv3's `reward_EMA` only rescales the ADVANTAGE by the 5–95 % percentile range (`max(range, 1)`); it does not
   remove the overestimate. dv3 already carries the lever: `models.py _compute_target` clamps the λ-return list
   when `return_clamp > 0` (08-28 flag, default 0; the tg_clamp list-clamp crash was fixed 08-28 §6).
2. **The r2dreamer AMPLIFIER is absent in dv3.** dv3's actor `dist: 'normal'` = `Normal(tanh(mean), std)` with
   `std = (max_std − min_std)·sigmoid(std + 2) + min_std`, `absmax None` (networks.py:713–720) — the same formula as
   r2dreamer's stock `bounded_normal` (distributions.py:217–221), not the `bounded_normal_clipped` projection that
   turned r2dreamer's entropy excursion into a permanent collapse. ⇒ the distribution half of H2 is moot; the entropy
   coefficient (dv3 stock 3e-4 vs r2dreamer's fix 3e-5) is the only H2 lever left. dv3 differs from r2dreamer in
   ways that are NOT levers in this ladder: actor gradient `dynamics` (backprop through the model) with the
   percentile-normalised advantage; critic `symlog_disc` + slow target.
3. Ignition is stochastic and separate from consolidation: 7 of the 8 R=0.25 runs ignited (first 25k bin ≥ 0.6 by
   40–70k env steps); fp16 s1 never did (entropy 9.93, zero online successes in 172k steps). A critic lever cannot
   help a run that never sees the reward.
4. Budget: 150k env steps ran 2:17–3:22 h (sacct, jobs 3242805–09 / 3244934–35; fps 14–21 on H200/A100); dv3
   divides `--steps` and `--time_limit` by `action_repeat` 4 (run_config: steps 37500 decisions, time_limit 300).
   300k env steps ≈ 5–7 h. Effective recipe of the stage-1b stack (run_config.json of fp32 s0): batch 16×64,
   train_ratio 512 (1 update per 2 decisions), prefill 2500, pretrain 100, discount 0.997, λ 0.95, horizon 15,
   actor lr 3e-5 entropy 3e-4, model lr 1e-4, deter 512 / units 512, delta_joint cap 0.025, scope reach_goal,
   reward_scale 1, `eval_action_mode sample` in-loop (fresh evals are deterministic via `genesis_eval.py`).
5. `dreamer.py:404–426` re-enters an existing timestamped subdir holding `latest.pt` (preemption-safe resume), so a
   `--requeue` restart continues the run (weights + step count; buffer re-read from `train_eps`).

## 2. Round 1 — registered 2026-09-05 11:34 (commit 10c6079, cluster clock; submitted 11:35:02) BEFORE submission
**H1: clamp the λ-return target at the maximum attainable return (1.0).** Two single-lever pairs, each against
its on-record reference at the same rung, budget 300k (the G1 budget; references were 150k):
- **Pair A `clamp1`**: stage-1b stack (fp16, 7-dim joint deltas) + `--return_clamp 1.0`. Reference: baseline {1, 0}.
- **Pair B `clamp1_fp32`**: same + `--precision 32`. Reference: fp32 {15, 0} (fp32 "buys the ceiling" on record).
Seeds 0/1 each; 4 GPU jobs = the brief's cap. R=0.25, world `gc_kp4_riser3_shelf6`, state obs 17-dim, no demos.
Launcher `cluster/dv3dbg/dv3dbg_s1b.sbatch` (= `$W/dv3dbg_s1b.sbatch`; tree `$W/dv3_fix` UNCHANGED — both levers
are CLI flags recorded in the slurm header and `run_config.json`); runs `$W/runs/dv3dbg_reach_r0.25_<tag>_s<seed>`.
Score: fresh-process `genesis_eval.py` (deterministic actor) on the END `latest.pt`, 15 hold ICs @1200, `reached_goal`.

Predictions (numbers fixed now):
- **P1 (mechanism, by construction)**: `target_max ≤ 1.0` in every logged row of every clamp run; `value_max ≤ 1.10`
  in every row after 50k env steps (the critic fits the clamped targets; symlog_disc may overshoot slightly).
- **P2 (consolidation)**: on every seed that ignites (first 25k bin with `train_success_rate ≥ 0.6`), every LATER
  25k bin stays ≥ 0.6 through 300k. Comparison statistic, computed by the same script on the on-record runs: the
  fraction of ignited runs with at least one post-ignition 25k bin < 0.6 (expected high on record, 0/4 under H1).
- **P3 (gate)**: fresh hold-15 ≥ 12/15 on 2/2 seeds for at least one pair; B ≥ A expected (fp32 ceiling effect).
Decision rules:
- Any pair 2/2 ≥ 12/15 → **G1 PASS**; that pair's flags become the G2 recipe (A preferred if both pass: fewer changes).
- P1 ∧ P2 hold, P3 fails on a seed that ignited → end-checkpoint fragility despite a stable loop → round 2 =
  `actor.entropy 3e-5` (the r2dreamer residual-flicker lever) on the better pair, 2 seeds.
- P1 holds, P2 fails (wobble persists with targets ≤ 1) → the λ-return leak is NOT dv3's mechanism → round 2 =
  entropy 3e-5 as the last registered lever; if that also fails P2/P3 → **clean negative at G1**: "dv3's post-ignition
  wobble is not the critic overestimate; levers exhausted: clamp, fp32, EEF, entropy".
- A seed with zero online successes by 100k env steps and entropy ≥ 9.9 = non-ignition (exploration, outside H1):
  replaced by seed 2 of the same pair, disclosed, at most one replacement per pair; the dead seed stays in the table.
- A requeued (preempted) run is reported with its restart count; the resume is dv3's own (weights + steps + buffer).
- fps is read from `metrics.jsonl` within the first 15 min of each run; if < 10 fps the 300k budget is kept and the
  landing time re-estimated (no budget cut).

**Submitted 2026-09-05 11:35:02 (cluster clock)**, launcher `$W/dv3dbg_s1b.sbatch` (= commit 10c6079), all PD (Priority) at submission:
- Pair A `clamp1`: s0 **3291585**, s1 **3291586** (`TAG=clamp1 … 0|1 300000 0.25 --return_clamp 1.0`)
- Pair B `clamp1_fp32`: s0 **3291587**, s1 **3291588** (`… --return_clamp 1.0 --precision 32`)
Run dirs `$W/runs/dv3dbg_reach_r0.25_{clamp1,clamp1_fp32}_s{0,1}`; slurm `$W/slurm/dv3dbg_s1b_<job>.out`.
