# DEMO3 (MoDem-mode) world-model fallback -- cluster smoke attempt, 2026-08-29

**Outcome: NOT RUN. Blocked before the first cluster command by a dropped Tufts VPN.**
No Slurm job was submitted; no install was performed; nothing on the cluster was touched.

## What happened (timeline, local box)
- 18:23-18:26 -- previous cluster commands in `paper/SESSION_LOG_2026-08-23_cluster.md` succeeded (sha 4500059).
- 18:29 -- first command of this attempt: `ssh: Could not resolve hostname login-prod.pax.tufts.edu`.
  Diagnosis: `/opt/cisco/secureclient/bin/vpn state` -> `Disconnected`; `login-prod.pax.tufts.edu` is a
  Tufts-internal name (NXDOMAIN on 8.8.8.8 / 1.1.1.1), so nothing resolves without the VPN.
  The box rebooted at 17:42; the VPN apparently came up and dropped again ~18:27.
- 18:32-18:48 -- polled reachability every 20 s for ~16 min: still down. Reconnecting Cisco Secure Client
  needs the user's Tufts login + Duo push, which the agent cannot do. Per the ground rule ("stop and write
  up" rather than iterate), stopped here.

## What was prepared (local only, ready to rsync)
All in `~/workspace/demo3_prep/` (no genesis_pickaplace file edited except this report):
- Verified prep state: `STATUS.md` gap #4 (offline-buffer capacity sized by the first post-shuffle demo) is
  ALREADY patched in `demo3/demo3/storage/ensemble_buffer.py:27` (`sum(len(t) for t in demo_dataset)`).
  Gap #1 verified: `FullTaskEnv.__init__` (`baselines/rl/full_env.py:273-278`) accepts exactly the kwargs
  `genesis_pick_task.make_env` passes (`action_mode`, `delta_cap`, `delta_leash_mult`, `action_repeat`,
  `delta_ref`, `pick_shaping`, `camera_rig`, `scope`, `backend`, `max_steps`); `reset(seed=, options=)` matches.
  Gap #7 verified: `SeedScheduler.sample()` with enable=False returns `np.random.randint(2**31)` (int) -> fine.
- MoDem mode selection confirmed in `train.py:66-81`: `enable_reward_learning=false` + `demo_sampling_ratio>0`
  -> `ModemTrainer` + `EnsembleBuffer`; `policy_pretraining=false` skips BC pretrain (rule-compatible variant).
  Seeding = `seed_steps = max(1000, 5*300) = 1500` random-action decisions, then a burst of 1500 updates,
  then 1 update/decision. Logger prints train/eval metrics to stdout; `save_csv=true` writes `eval.csv`.
  Hydra work_dir = `demo3/demo3/logs/<task>/<seed>/<exp_name>` (parser.py:66); the smoke script copies it
  to `runs/smoke_s0`.
- NEW `cluster/install_demo3_venv.sh` (sbatch -p batch, 1 h): `condaenv/genesis/bin/python -m venv
  --system-site-packages $LAB/demo3_venv`, then pip-only
  `torchrl==0.8.* tensordict==0.8.* hydra-core==1.3.2 omegaconf==2.3.0 termcolor kornia==0.7.2 pandas`,
  then an import check incl. `from torchrl.data import ReplayBuffer, LazyTensorStorage, SliceSampler` and
  a `RESOLVED ...` version line. Rationale for pins: torchrl 0.8.x / tensordict 0.8.x are the release
  train built against torch 2.7 (torchrl 0.8.0 requires torch>=2.7); 0.6.x (STATUS.md's guess) targets
  torch 2.5 and 0.9.x/0.10.x pull torch 2.8/2.9 which would replace the base env's torch 2.7.0+cu126 inside
  the venv. Still unverified on the cluster -- watch the `RESOLVED` line: torch must stay 2.7.0+cu126.
- NEW `cluster/smoke_demo3.sh` (sbatch -p gpu --gres=gpu:1 -c 8 --mem 48g -t 2:00:00): flag set
  `enable_reward_learning=false policy_pretraining=false demo_sampling_ratio=0.5 steps=5000 eval_episodes=1
  eval_freq=2500 save_freq=2500 save_video=false save_csv=true disable_wandb=true exp_name=smoke_s0 seed=0`,
  env `GENESIS_PICKAPLACE_ROOT=$LAB/genesis_pickaplace MUJOCO_GL=egl`. Success criterion = a `train`
  metric line after step 1500 (first update burst) in `runs/smoke_s0_<jobid>.out`. Budget estimate:
  5000 decisions x 4 sim steps at ~60k sim steps/h + 5000 updates + MPPI planning -> ~1-1.5 h; if it
  times out, rerun with `STEPS=3000`.

## Exact commands to resume (once the VPN is up; each via `~/bin/ccmd tufts '...'`)
```
LAB=/cluster/tufts/shortlab/jstale02
rsync -a --exclude venv --exclude __pycache__ ~/workspace/demo3_prep/ tufts:$LAB/demo3_prep/     # incl. out/dH_demo3.pkl 173 MB
ccmd tufts "LAB=$LAB; mkdir -p \$LAB/demo3_prep/runs; sbatch \$LAB/demo3_prep/cluster/install_demo3_venv.sh"
#   poll: squeue -u jstale02 ; tail -5 $LAB/demo3_prep/runs/install_<jid>.out  -> expect RESOLVED ... INSTALL_OK
ccmd tufts "LAB=$LAB; sbatch --dependency=afterok:<install_jid> \$LAB/demo3_prep/cluster/smoke_demo3.sh"
#   poll: tail -20 $LAB/demo3_prep/runs/smoke_s0_<jid>.out ; grep -c '^train\|^eval' ... ; ls $LAB/demo3_prep/runs/smoke_s0
```
Job ids: none (never submitted).

## Exact command for a real 1M-step run (after a green smoke)
```
sbatch -p gpu --gres=gpu:1 -c 8 --mem 48g -t 7-00:00:00 -J demo3_dH_s0 \
  -o $LAB/demo3_prep/runs/dH_modem_s0_%j.out --wrap "
  export GENESIS_PICKAPLACE_ROOT=$LAB/genesis_pickaplace MUJOCO_GL=egl OMP_NUM_THREADS=8;
  source $LAB/demo3_venv/bin/activate; cd $LAB/demo3_prep &&
  python train_genesis.py --config-name genesis_pick task=genesis-pick-semi obs=rgb num_envs=1 \
    demo_path=$LAB/demo3_prep/out/dH_demo3.pkl n_demos=56 demo_success_only=true \
    enable_reward_learning=false policy_pretraining=false demo_sampling_ratio=0.5 \
    steps=1000000 eval_episodes=10 eval_freq=10000 save_freq=50000 save_video=false save_csv=true \
    disable_wandb=false wandb_project=genesis_pickaplace wandb_entity=jambotime exp_name=dH_modem seed=0"
```
Caveats for the real run: `steps` counts decisions (x4 sim steps); at ~15k decisions/h single-process,
1M decisions is ~3 days of env time alone plus 1 update/decision -> expect to exceed 7 days; the paper-
scale budget is 100k-300k decisions (STATUS.md #8). Run name per convention: `dH_demo3modem_s0`.
Two known footguns still untested on real hardware: `buffer.py:15` hard-codes cuda:0 (GPU node only) and
the `cfg.genesis` dataclass path in `make_env` (never executed under hydra).
