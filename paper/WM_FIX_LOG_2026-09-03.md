# WM fix log — 2026-09-03 (GPU box = pop-os, RTX 3060 12 GB; shared with the sim-box world agent, CPU-loaded)

Companion to `paper/WM_FIX_PLAN_2026-09-03.md`. Dated entries; numbers only from `metrics.jsonl` / fresh-eval JSON.
Run tree (outside the repo, never git): `~/wm_fix_2026-09-03/` — `r2d_record/` (code), `runs/<name>/` (logdirs).

## 2026-09-03 ~08:00 — setup, code-of-record reconstruction, what is NOT verifiable yet

**Cluster unreachable at start** (`ssh tufts`: name not resolved; VPN pending). Everything below about "what runs up
there" is inferred from local artifacts and must be re-checked against `$LAB/r2dreamer` and `$LAB/dreamerv3-torch`
once the VPN is up (see "Cluster diff TODO").

**r2dreamer code of record, reconstructed locally.**
- `cluster/r2dreamer_port.tar.gz` (Aug 15) is a 30-file OVERLAY (core: buffer/dreamer/networks/tools/train/trainer/
  distributions + configs + genesis files), not a full tree — no `rssm.py`, no dmc/model-size configs.
- Base commit found by minimum diff: fork `~/workspace/r2dreamer` @ `1fadce4` (2026-03-05, = upstream import; the
  fork's later `main`/`sigreg` work is NOT in the port). Port-specific edits on top: 268 lines across
  dreamer/trainer/networks/tools.
- `cluster/patches/r2dreamer_final_rr.patch` (Aug 31) does NOT apply cleanly to base+tarball (envs/genesis.py hunk 4,
  envs/__init__.py hunk 1 fail) ⇒ the tarball is stale vs the cluster tree; the exact code of record exists only on
  the cluster. The two failing hunks touch genesis-only files, irrelevant to a DMC run.
- Composed tree = `1fadce4` + tarball (patch NOT applied): `~/wm_fix_2026-09-03/r2d_record/`.
- Port defaults on a DMC task resolve to STOCK values: `_base_.yaml` `act_entropy ${oc.select:env.act_entropy,3e-4}`,
  `actor_bc_lambda …,0.0`, `return_clamp …,0.0`, `horizon …,333`, `actor_dist …,bounded_normal`; `dreamer.py`
  clamp/BC branches are skipped at 0. So `env=dmc_vision` runs the port's code paths under stock settings.
- Port changes that DO touch every task (candidates a DMC run exercises): trainer `is_demo` column added to every
  online row; `demo_reinjector` hooks; periodic `latest.pt`; eval envs optional; `buffer.add_chunk` + TensorDict
  `set_` rewrite; `episode_ids` comment changed ("increment" → "kept constant") but NEITHER version increments it —
  no behavior change there.
- `~/workspace/r2dreamer` fork itself (branch `sigreg`, last commit 2026-04-16) has a prior ManiSkill comparison in
  `logdir/` where "DreamerV3 and R2Dreamer both reach >100 reward by 2e5 steps" (fork CLAUDE.md) — that is the
  FORK, not the port, and ManiSkill, not DMC. Does not count for stage 0.

**dreamerv3-torch code of record: cluster-only.** `cluster/patches/dreamerv3-torch-genesis_final_rr.patch` is
against a `configs.yaml` blob (`c508c32`) that exists in NO commit of the local checkout
`~/workspace/dreamerv3-torch/dreamerv3-torch` (fork @ `1ec0641`, dirty), and its context references
`genesis_pixels_vec`/`genesis_pick_msrecipe` blocks the local file lacks. The local checkout can only smoke the
DMC pipeline; the stage-0 dv3 run of record must use the cluster tree (rsync when the VPN is up).
`return_clamp` default 0 in the patch (models.py `_compute_target` clamp only when > 0).

**Environments.** r2dreamer venv `~/workspace/r2dreamer/.venv` (py3.10, torch 2.11, dm_control 1.0.28, mujoco 3.3.0):
DMC cartpole/walker render smoke OK (EGL). dv3 venv `~/workspace/dreamerv3-torch/dreamerv3-torch/.venv` (torch 2.10)
had NO dm_control despite upstream requirements → `pip install dm_control` (→ dm_control 1.0.45, mujoco 3.12.0).
Box: 12 cores (world agent uses ~5), 31 GB RAM, disk 97% full / 27 GB free → env_num 4, small logdirs, no
video_pred_log.

**Cluster diff TODO (when VPN up):** rsync `$LAB/r2dreamer` and `$LAB/dreamerv3-torch` code (no runs/ckpts) →
diff against `r2d_record/` and the local dv3 checkout; record every differing file here; re-run stage 0 on the
cluster trees if the DMC-relevant core differs.

**Smokes launched 08:20 (pipeline only, not gated):** `runs/smoke_r2d_cartpole` (record tree, env=dmc_vision,
cartpole_balance, env_num 4, 8k steps, eval 2 eps @4k) and `runs/smoke_dv3local_cartpole` (local dv3, same knobs).
Purpose: pipeline works end-to-end, throughput, metric names.

## 2026-09-03 08:00 — smoke findings (pipeline)
- r2dreamer stock `dmc_vision` keeps replay on the GPU (`buffer.storage_device=${device}`, max_size 5e5 images) →
  CUDA OOM on the 12 GB card at startup (tried to allocate 3.8 GiB with 6.9 GiB in use). Relaunched with
  `buffer.storage_device=cpu buffer.max_size=2e5|3e5` — the SAME storage choice the port's genesis config makes
  (`storage_device cpu`), so it is not a deviation from the genesis path. Algorithm unchanged (data identical).
- r2dreamer step-0 eval on cartpole_balance (random policy): eval_score 708.9 / length 500 (2 eps). cartpole_balance
  rewards near-upright regardless of policy, so a high random baseline is expected; the gate (≥ 800 LAST by 200k)
  is only ≈ 100 above it — walker_walk is the discriminating task; cartpole mainly checks the pipeline + no
  regression below random.
- dv3 local checkout: wandb ON by default (`wandb_config.use_wandb: True`, project `dreamer_v3`); the smoke synced
  one run there (name `easy-smoke-740`). Gated runs use `--configs dmc_vision nowandb`. Step-5000 eval:
  eval_return 239.7 (2 eps) — below random-ish, but at 5k steps nothing has trained yet.
- Launcher: `~/wm_fix_2026-09-03/launch_stage0.sh <r2d|dv3> <task> <seed> <steps>`; every command is echoed to
  `runs/COMMANDS.log` and the run's `.out` head; refuses to overwrite a logdir.
- Both ports run `torch.compile` at start (dv3 `compile: True`; r2dreamer "Compiling update function") → first
  minutes show fps 0 / no step progress on this CPU-shared box (load avg ≈ 9 with the world agent's 6 workers).

## 2026-09-03 08:25 — smokes complete (pipeline OK on both ports; box too slow for the gate)
- r2dreamer smoke (record tree, cartpole_balance, 8k steps, env_num 4, CPU replay): ran to completion; evals
  [0] 708.9, [4000] 543.7 (2 eps each); train/opt/updates 1249 at step 5528; `fps/fps 5.2` (includes compile).
  Stock `bounded_normal` samples reach ±4 on DMC too (`train/action_min -3.9 / action_max 4.1`) — upstream
  behavior, not port-specific (the port's `bounded_normal_clipped` is opt-in for genesis only).
- dv3 local-checkout smoke (cartpole_balance, 13k steps, envs 4, wandb on): evals [5000] 239.7, [9000] 179.0
  (2 eps); ~8 env steps/s. Pipeline OK; NOT the code of record (see 08:00 entry).
- Throughput verdict: ≈5–8 env steps/s here (CPU shared with the world agent's 6 workers, load ≈ 9) ⇒ 200k-step
  cartpole ≈ 7–11 h per run locally. User (08:20) authorized interactive cluster GPU sessions (and cancelling a
  running job if necessary) → gated runs move to the cluster once the VPN resolves; one local r2dreamer
  cartpole s0 (200k, RECONSTRUCTED tree) launched 08:25 as fallback + cross-machine check
  (`runs/s0_r2d_cartpole_balance_s0`).

## 2026-09-03 ~08:55 — cluster reachable (alias fix), code-of-record diff DONE, stage-0 cartpole jobs submitted
- **Reachability:** the VPN (cscotun0, Tufts DNS) was up the whole time; this box's `~/.ssh/config` has no `tufts`
  alias (that lives on the laptop) — `ssh pax` / `login.pax.tufts.edu` works. All my scripts use `pax`.
- **r2dreamer cluster tree (`$LAB/r2dreamer`, git c25eb3b + 32 dirty files) vs my reconstruction:** every
  DMC-relevant file is IDENTICAL (dreamer/trainer/buffer/networks/tools/distributions/rssm, configs/configs.yaml,
  model/_base_.yaml, env/dmc_vision.yaml, envs/dmc.py). Differences are genesis/isaaclab/maniskill-only
  (demo_prefill 56, envs/genesis 43, eval_genesis 51 lines; cluster-only: `genesis_pick_v5d4c_delta_shaped.yaml`,
  `genesis_touchgoal_v5d4c_delta.yaml`, maniskill/isaaclab configs+envs, eval_ms/ms_control) plus
  envs/__init__.py (maniskill branch + `pick_shaping` kwargs) and envs/parallel.py (+3 lines moving action/done to
  CPU before dispatch — benign). ⇒ the tarball+patch in the repo is STALE vs the cluster for genesis files; the
  cluster tree is the record. Local copy of it: `~/wm_fix_2026-09-03/cluster_r2d/`.
- **dv3 cluster tree (`$LAB/dreamerv3-torch`, git 2a80452 + 139 dirty) vs local checkout `1ec0641`:** dreamer.py
  391 / models.py 65 / tools.py 29 / configs.yaml 221 changed lines; envs/dmc.py, wrappers, networks, exploration,
  parallel identical. Cluster configs.yaml carries `genesis_pickplace/pixels/pick_msrecipe/…/touchgoal/dv3std`
  blocks + `return_clamp: 0` default. Local copy: `~/wm_fix_2026-09-03/cluster_dv3/` (code files only; the repo
  root on the cluster holds timestamp-named run dirs, excluded). ⇒ the local dv3 checkout is NOT the record; dv3
  stage 0 runs from the cluster tree.
- **Cluster envs:** neither `$LAB/r2d_venv` (mujoco 3.3.7, torch 2.8) nor `$LAB/condaenv/genesis` (mujoco 3.3.7,
  torch 2.7) has dm_control; an unpinned install would upgrade mujoco to 3.12 (genesis-world pins <3.4). Base
  envs left UNTOUCHED (verified 3.3.7 after): built overlay venvs `--system-site-packages` from each base python
  (`$LAB/wm_fix_2026-09-03/r2d_venv_dmc`, `…/dv3_venv`) with dm_control (+ mujoco 3.12 shadowed inside the overlay
  only); `suite.load('cartpole','balance')` OK in both.
- **Queue:** my 4 main-session jobs running (rlpd ×3, dp-round-robin); gpu partition had idle L40S/A100 nodes —
  NO job cancelled. Submitted `wmfix_s0` (sbatch `$LAB/wm_fix_2026-09-03/wmfix_s0.sbatch`, `-p gpu,preempt`, 1 GPU
  l40s|a100|l40|h200, 8 CPUs, env_num/envs 8, eval 5 eps every 10k, 200k steps): r2d cartpole s0 **3232145**,
  s1 **3232146**; dv3 cartpole s0 **3232147**, s1 **3232148**. Logs `$LAB/wm_fix_2026-09-03/{slurm,runs}/`;
  every command appended to `runs/COMMANDS.log`. Batch jobs rather than a held interactive TTY (same GPU use,
  survives my ssh).
- 09:03 — jobs 3232145–48 FAILED at 10 s: MY preflight asserted `dm_control.__version__` (attribute does not exist;
  I had seen that on the login node and still shipped it). No GPU time lost; GPUs were allocated within a minute
  (A100 80GB pax106, A100 40GB pax143 ×2, H200 pax009). Preflight now uses `importlib.metadata.version`.
  Resubmitted 09:04 (job ids in the next entry).
- 09:04 — resubmitted, all RUNNING within 45 s: r2d cartpole s0 **3232173** (pax106 A100-80G), s1 **3232174**
  (pax143 A100-40G); dv3 cartpole s0 **3232175** (pax143), s1 **3232176** (pax009 H200). Preflight prints
  `dm_control 1.0.45 mujoco 3.12.0 torch 2.8.0 (r2d) / 2.7.0 (dv3)`. Commands of record in `runs/COMMANDS.log`.

## 2026-09-03 ~09:06 — stage-1 preparation (code drafted + unit-tested; NOT run — gated on stage 0)
- r2dreamer adapter patch (on my local copy of the cluster tree, `~/wm_fix_2026-09-03/cluster_r2d`, diff in
  `~/wm_fix_2026-09-03/state_obs.patch`, 26 changed lines): `GenesisPick(state_obs=False)` flag; when true the
  adapter also emits `obs["state"]` = FullTaskEnv's 17-dim float32 vector (captured from reset()/step() returns —
  the same vector DP/RLPD consume) and adds `state: Box(17,)` to `observation_space`; `envs/__init__.py` passes
  `state_obs=config.get("state_obs", False)`. Default path byte-identical (observation_space unchanged).
  Standalone test in the genesis venv (world `gc_kp4_riser3_shelf6`, scope touchgoal, delta_joint 0.025/5,
  repeat 4): reset → keys image+state+log_*, state (17,) float32; 30 random steps OK, state evolves, no reward,
  sim_variant line confirmed; default-flag env has NO state key.
- Stage-1 config drafted: `configs/env/genesis_touchgoal_state.yaml` = touchgoal recipe of record with exactly the
  plan's changes (mlp_keys 'state', cnn_keys '$^', reward_scale 1, time_limit 1200, act_entropy 3e-4,
  return_clamp 0, demo_dir null, no reinjection); actor_dist stays bounded_normal_clipped (recipe of record).
- dv3 side needs no adapter work: `envs/genesis.py` already has vector mode with a `state` key and
  `genesis_pickplace` config uses `mlp_keys: 'state'`.
- Local r2d cartpole cross-check (RECONSTRUCTED tree, env_num 4): eval 488.6 @30k → 619.6 @40k → 785.1 @50k.

## 2026-09-03 ~09:10 — touchgoal evidence re-checked at the source (plan §1 item 1 STANDS) + one eval label bug
- `eval_genesis.py:242` hardcodes `success_key = "placed_v2" if scope=="place" else "picked"` → for scope
  `touchgoal` the fresh eval reports **"picked"** (0.00 by construction: the episode terminates at first goal
  contact, a pick can never be granted) instead of `touched_goal`. `policy_eval/metrics.json` of
  `touchgoal_v5d4c_delta_s0` indeed has keys `picked 0.0 / tipped 0.0 / timeout 1.0`. Reporting bug only —
  the same JSON's `mean_reward` is 0.0 (a touch pays +100 and terminates), so no touch happened in the eval.
- Decisive: `metrics.jsonl` of both touchgoal seeds (2,814 rows, 2,599 train episodes each, mean length 96.1
  agent steps): `train/data/reward_frames` max **0.0**, `train/data/reward_sum` max 0.0, `episode/score`
  mean 0.0 / max 0.0, `episode/train_task_success` 0.0 in every 100k bin. Not one goal contact in ~2,600
  episodes per seed. The trainer never saw a reward, so this is exploration/reachability under this
  obs+action parametrization, not credit assignment. (dv3 seed 0 found it from 230k — DV3_DIAGNOSIS §6.)
- Consequence for stage 1: the state-input touchgoal run is exactly the right next probe; its fresh eval must
  use the `task_success` / reward accounting, not the evaluator's `picked` summary (fix goes with the stage-1
  patch: scope-aware success key `{"place": placed_v2, "touchgoal": touched_goal, else picked}`).
- ~09:12 Stage-1 tree STAGED on the cluster: `$LAB/wm_fix_2026-09-03/r2dreamer_fix/` = `$LAB/r2dreamer` +
  exactly 3 patched files (envs/genesis.py, envs/__init__.py, eval_genesis.py) + `configs/env/genesis_touchgoal_state.yaml`
  (diff -rq verified; `$LAB/r2dreamer` itself untouched). Launcher `wmfix_s1.sbatch <seed> <steps>` (base
  `$LAB/r2d_venv`, exports GENESIS_PICKAPLACE_ROOT + R2D_SIM_VARIANT=gc_kp4_riser3_shelf6, refuses to run without
  the patch, proves the `[sim-variant] gc_kp4_riser3_shelf6` line in the slurm .out, then a FRESH-process
  `eval_genesis.py` sample-mode 15 hold ICs @1200). NOT submitted — gated on stage 0.
- Local r2d cartpole s0 (RECONSTRUCTED tree): eval **963.0 @60k** (5 eps) — above the 800 bar early; LAST @200k
  is the registered statistic.
- ~09:14 Stage-2 data path scoped (no code yet — gated on stage 1): r2d native sets
  `matched_w3/r2d/{dH,dDP}/*.npz` carry image/action/reward/discount/is_* only (no state); the contract-v1
  sources `matched_w3/dH/*.npz` carry `states (T,17)` + `final_state (17,)` (+ images T+1). Plan when reached:
  `to_dreamer_native.py --with-state` writes `state (T+1,17)` = concat(states, final_state) into a NEW dir
  (`matched_w3/r2d_state/<arm>`), `demo_prefill._load_episode`/`add_demo_set` pass `state` through when present
  (downsample by the same idx; assert (T,17) float32), so demo rows and online rows share the key set.
- Cluster stage-0 throughput: r2d 61–71 fps (env_num 8, A100) → 200k ≈ 50 min; dv3 ≈ 22 fps → 200k ≈ 2.5 h.
- (09:17) Clock note: earlier entry stamps in this log were written from a wrong mental clock (~1 h ahead); corrected
  to real EDT (cluster and box agree: job stamps 09:02/09:04). Entries from here on use `date`.

## 2026-09-03 09:40 — stage-2 data code DRAFTED (opt-in, untested on real training; nothing submitted)
- `baselines/rl/to_dreamer_native.py --with-state` (repo file, +25/−7 lines, default path byte-identical): emits
  `state (T,17) float32` = concat(`states`, `final_state`) aligned with `image`; refuses tapes without
  states/final_state or with non-finite/mis-shaped state; `repeat.json` gains `with_state`, `state_dim`.
- `demo_prefill.py` (staged copy only, `~/wm_fix_2026-09-03/state_prefill.patch`, 19 lines): loads `state` when
  the tape has it (asserts (T,17), downsampled by the same idx), and adds it to demo rows ONLY when
  `env.state_obs` is true (then every tape must have it — loud assert); dropped otherwise, so pixels-only runs
  are unchanged.
- Cluster staged tree `r2dreamer_fix` now differs from `$LAB/r2dreamer` in exactly demo_prefill.py,
  envs/genesis.py, envs/__init__.py, eval_genesis.py + the new touchgoal_state config.

## 2026-09-03 09:55 — user context (dv3 arm result, DayDreamer) → in-house positive controls FOUND on the cluster
- User: has seen dv3 work on an arm control task; DayDreamer shows legged locomotion (dense). Repo evidence:
  MANISKILL_VS_GENESIS.md (08-10) documents the March dv3 ManiSkill PickCube result (pixels, sparse +100, 10
  teleop demos, takeoff 110–140k, ≈200 wandb runs) and its entropy fingerprint (MS: actor entropy → −2 nats by
  17k updates, before the first success; Genesis: 8–9.5 nats flat for 1M steps — the actor never commits).
  DayDreamer also did UR5/XArm visual pick-and-place with sparse rewards, so sparse visual manipulation is in
  the algorithm's demonstrated envelope.
- NEW to the ledger: **r2dreamer has its own ManiSkill positive control on the cluster**
  (`$LAB/r2dreamer/runs/ms_pickcube_s{0,1}`, 08-18, `$LAB/r2d_ms_venv` mani_skill 3.0.0b21): numbers and the
  STOCK knobs it used are in PLAN §7 Amendment (train success ≈0.9 from 50k on both seeds; eval 0.9/1.0 at
  301k). Plan §1's "neither port was ever run on a reference task" is therefore wrong for ManiSkill (true for
  DMC/Atari); the plan's line should be corrected by the main session's doc owner.
- Decisions: stage 1 runs dv3 as a mandatory reference alongside r2dreamer (PLAN §7 amendment); stage 0b is
  satisfied by the on-record runs (no new MS runs unless dv3 fails stage 1 — the current dv3 tree has no
  ManiSkill-capable env: `condaenv/dreamer4` has mani_skill but lacks gym/tensorboard).

## 2026-09-03 10:05 — NEW CONFOUND: every dv3 genesis run ran in the BASE world; dv3 stage-1 launcher staged
- The cluster dv3 tree (`$LAB/dreamerv3-torch`: envs/genesis.py, genesis_eval.py, dreamer.py) contains no
  sim-variant handling at all and `cluster/sbatch_dv3_pack.sh` exports no variant; `FullTaskEnv` is built
  without `sim_variant_hook.apply_pre/post` ⇒ all dv3 genesis runs (touchgoal probe in plan §1 / DV3_DIAGNOSIS
  §6, dv3std/clamp/fix diagnostics, any A32-era dv3 cells) used the base world (base kp, no riser), not
  `gc_kp4_riser3_shelf6`. CONFOUNDS.md mentions dv3 once and not for this — **not on the ledger**; for the
  main session's doc owner (unreachable from this box: ListAgents shows only the sim-box world agent).
  Impact: none on the r2dreamer arm of record; affects any cross-port claim that assumes a shared world.
- dv3 stage-1 reference: adapter patched in a COPY (`$LAB/wm_fix_2026-09-03/dv3_fix`, `dv3_variant.patch`,
  +10 lines) to apply the hook from `GENESIS_SIM_VARIANT` with NO silent default (asserts if unset);
  launcher `wmfix_s1_dv3.sbatch <seed> <steps>` = configs `genesis_pickplace genesis_pick_msrecipe
  genesis_touchgoal genesis_dv3std usestateonly nowandb --time_limit 1200` (state-only obs, delta_joint
  0.025/5, repeat 4, sparse scale 1, clamp 0, train_ratio 512, stock entropy 3e-4), proves the
  `[sim-variant]` line, then fresh-process `genesis_eval.py` on 15 hold ICs @1200 (dv3 evaluator is
  deterministic-actor; r2dreamer's is sample-mode — noted, gate is per port). NOT submitted (stage 0 gate).

## 2026-09-03 10:02 — STAGE 0 readout begins (cluster, code of record)
- **r2dreamer cartpole_balance s0 (job 3232173, pax106 A100-80G): COMPLETED rc 0, 54 min, 196k env steps.**
  Evals (5 sampled eps each, from metrics.jsonl): 0:695 → 10k:201 → 30k:612 → 70k:967 → 100k:989 → 150k:996 →
  **LAST 190k: 996.4** (best-of-last-3 997.0). Gate (LAST ≥ 800 by 200k): **PASS**.
- **r2dreamer cartpole_balance s1 (job 3232174, pax143 A100-40G): COMPLETED rc 0, 59 min, 196k steps; LAST 190k:
  997.0** (best-of-last-3 998.6; 5 sampled eps). ⇒ **r2dreamer cartpole gate PASS 2/2.** dv3 cartpole s0/s1
  (3232175/76) still running (55k @15 fps / 77k @24 fps; last evals 286 / 334) — endpoint ≈ 2 h away.
- Local cross-check (RECONSTRUCTED tree, env_num 4, this box): r2d cartpole s0 at 108k, evals 994–996 from 90k —
  agrees with the cluster trees.

## 2026-09-03 10:05 — SUBMITTED (per PLAN §7 Amendment 10:05): walker sanity + stage 1 both ports
- r2d walker_walk 500k: s0 **3233329** (pax143), s1 **3233330** (pax105). Gate: ≥600 by 500k on ≥1/2, ≥400 on 2/2.
- Stage 1 r2dreamer touchgoal STATE (env=genesis_touchgoal_state, 300k, world gc_kp4_riser3_shelf6 exported,
  tree r2dreamer_fix): s0 **3233331** (pax008 H200), s1 **3233332** (pax106 A100).
- Stage 1 dv3 reference (configs genesis_pickplace+pick_msrecipe+touchgoal+dv3std+usestateonly+nowandb,
  --time_limit 1200, 300k, GENESIS_SIM_VARIANT exported, tree dv3_fix): s0 **3233333** (pax106), s1 **3233334**
  (pax047 H200). All six RUNNING at 10:04; preflights printed genesis 0.2.1 + torch versions.
- dv3 adapter world-hook standalone test (local genesis venv, 09:58): `[sim-variant] gc_kp4_riser3_shelf6: kp=[800,
  800, 600, 400, 240, 240] gc=1.0 riser=0.03` printed; state (17,) float32; 20 random steps OK; with the variable
  unset the adapter REFUSES (AssertionError) — no silent base world in the fix tree.

## 2026-09-03 10:20 — stage-1 health (5 min in) + stage-2 demo sets converted
- Stage-1 r2dreamer s0/s1 (3233331/32): slurm .out has **6 `[sim-variant] gc_kp4_riser3_shelf6` lines each** (6
  workers), `Encoder CNN shapes: {}`, `Encoder MLP shapes: {'state': (17,)}`, 9.60M params — state-only confirmed.
  dv3 s0/s1 (3233333/34): 1 marker line each (single world), still building; no metrics yet. Walker s0/s1 started
  (step-0 evals 21.6 / 28.7).
- Stage-2 inputs prepared (CPU, new dirs, nothing overwritten): `$LAB/wm_fix_2026-09-03/demos_state/{dH,dDP}` via
  `to_dreamer_native.py --with-state --repeat 4 --terminal-reward 1` from `matched_w3/{dH,dDP}`: dH 58 tapes /
  6,985 rows (T p50 110, max 247), dDP 58 / 7,343 rows (T p50 120, max 236); `state (T,17) float32` finite; repeat.json
  `sim_variant gc_kp4_riser3_shelf6, action_repeat 4, with_state true, terminal_reward 1.0, total_reward 58`.

## 2026-09-03 10:40 — stage 1 at half budget: NO goal touch on either r2dreamer seed; probe launched
- r2dreamer touchgoal STATE (3233331/32) at 150k / 128k env steps (≈150 / 125 episodes of 300 decisions):
  `episode/train_task_success` 0.00 in every 25k bin, `train/data/reward_frames` max 0.00, `train/action_entropy`
  9.93 (the maximum for 7 dims) — the same flat-high-entropy fingerprint as the pixel runs. dv3 reference at 30k /
  10k steps (12.9 fps, single world; endpoint ≈ 6 h), 0 touches so far.
- Walker sanity (r2d): 485 @100k (s0) / 469 @110k (s1) — on track. dv3 cartpole: 530 @85k (s0) / 412 @133k (s1) —
  below r2dreamer's pace; gate (≥800 by 200k) at risk for the current dv3 tree.
- Diagnostic launched (local CPU, background): random-policy reachability of the touchgoal reward under the SAME
  adapter/action parametrization (uniform actions and 25-decision-held actions, 10 episodes × 300 decisions, worlds
  gc_kp4_riser3_shelf6 and base): if random exploration essentially never touches the goal can in 300 decisions,
  sparse touchgoal is not a learnability probe for ANY trainer here, and the stage-1 disconfirm branch should be
  read as "reward unreachable by exploration", not "adapter broken" — a dense/`reach`-scope variant would then be
  the honest stage-1 test.

## 2026-09-03 10:55 — upstream dv3 reference submitted (bisect insurance); stage 1 at 200 episodes, still 0 touches
- Cluster dv3 tree vs upstream NM512 `dreamerv3-torch` @ 6ef8646 (2026-03-08, cloned to
  `$LAB/wm_fix_2026-09-03/dv3_upstream`): dreamer.py 633 / models.py 201 / tools.py 448 / networks.py 30 changed
  lines; envs/dmc.py identical. dv3 cartpole (3232175/76) at 95k: 459 / 145k: 412 — likely to miss the ≥800@200k
  gate. Submitted upstream cartpole s0/s1 200k (port `dv3up` in wmfix_s0.sbatch, overlay dv3_venv) so the
  "fork vs upstream" question is answered in parallel (job ids below).
- Stage 1 r2dreamer at 205k / 174k steps (≈200 / 175 episodes): still 0 goal touches, entropy 9.93. dv3 reference
  30k / 10k steps, 0 touches. Random-policy reachability probe (local, corrected world, uniform actions): first 6
  episodes min tool–goal distance 0.23–0.42 m (goal at z 0.262 on the shelf), no touch.
- 11:00 — upstream dv3 reference: first two submissions died at import (3234220/21: `ruamel.yaml.safe_load` removed in
  ruamel ≥0.18; 3234287/88: upstream forces `MUJOCO_GL=osmesa` at dreamer.py:7, no OSMesa on the nodes). Applied
  exactly TWO documented compatibility lines to `$LAB/wm_fix_2026-09-03/dv3_upstream` (safe_load →
  `YAML(typ='safe', pure=True).load`; `os.environ.setdefault('MUJOCO_GL', …)` — the same two changes the fork made).
  Resubmitted: **3234321** (s0, pax142) / **3234322** (s1, pax141), RUNNING.
- **Random-policy reachability (local, corrected world `gc_kp4_riser3_shelf6`, uniform actions in [−1,1]^7, delta_joint
  0.025/leash 5, repeat 4, 10 episodes × 300 decisions, demo ICs): touched 0/10; min tool–goal distance median 0.354 m,
  best 0.231 m; goal can at (0.672, −0.221, z 0.262).** A max-entropy policy does not get within 23 cm of the goal in
  an episode. Base-world and held-action variants running (outputs `~/wm_fix_2026-09-03/probe_*.txt`).

## 2026-09-03 11:10 — STAGE 1 r2dreamer s0 TRAINING DONE: 0 touches in 300k; probe: goal unreachable by random walk in BOTH worlds
- **s1 r2dreamer touchgoal STATE s0 (3233331): train rc 0, 300k env steps, ≈270 episodes × 300 decisions; `train_task_success`
  0.00 in every 25k bin, `train/data/reward_frames` max 0.00, entropy 9.93 throughout.** The in-job fresh eval crashed
  (`KeyError: 'state'` — eval_genesis' `reset_to_uid`/`reset_to_ic` bypass adapter.reset and built the obs dict without
  `state`); patched (both helpers read `genv._obs()["state"]` when `state_obs`), restaged at 11:09:50 (before s1's eval
  starts), and a standalone CPU eval submitted for s0: **3234609** (hold 15, sample mode, 1200 steps). Its result is the
  registered primary score; it cannot exceed what training never saw.
- **Random-policy reachability, base world (uniform, 10 eps × 300 decisions): touched 0/10, min tool–goal median 0.337 m,
  best 0.268 m.** Corrected world (earlier): 0/10, best 0.231 m. ⇒ In neither world does a max-entropy policy come within
  ≈23 cm of the goal can inside an episode; the sparse touchgoal reward is not discoverable by undirected exploration at
  this action parametrization (0.025 rad/sim-step deltas, leash 0.125 rad). The dv3 base-world pixel success (1/2 seeds
  from 230k, DV3_DIAGNOSIS §6) was a rare-discovery event, not evidence that the reward is routinely reachable.
- **`reach` scope is NOT a usable fallback:** in the base probe the tool is already within REACH_DIST 0.17 m of the PICK
  can at decision 0 in 5/10 episodes (9/10 within 42 decisions; min tool–can distance median 0.108 m) — trivially
  satisfied from demo ICs. Stage-1b needs a target that random exploration hits occasionally but not at t=0.
- Third probe variant (held actions, 25-decision persistence, corrected world) still running.

## 2026-09-03 11:25 — stage-1 s1 training done (0 touches); probes complete; stage-1b machinery staged
- **s1 r2dreamer touchgoal STATE s1 (3233332): training done at 299k, 0 goal touches (~270 episodes), entropy 9.93.**
  In-job fresh eval running (patched tree): first 10/15 hold episodes all `timeout, r=0.0`. s0's standalone eval
  (3234609, CPU node pax157) at 2/15, also timeouts, ≈245 s/episode there (≈45 s on the GPU nodes).
- Probe, held actions (25-decision persistence, corrected world): touched 0/10, min tool–goal median 0.328 m, best
  **0.158 m**; pick-can reach(<0.17) hit at decision 0 in 5/10. Summary of all three probes: no policy without
  direction reaches the goal can; persistent actions get to ≈16 cm at best.
- Walker sanity (r2d): s0 **894 @260k**, s1 **872 @280k** (gate ≥600 by 500k) — passing on the curve; LAST at 500k
  pending. dv3 cartpole s1 at 189k: 663 (gate 800 @200k about to read out); s0 at 125k: 697.
- Stage-1b machinery (staged, NOT run, NOT yet registered): a `reach_goal` scope in a PRIVATE copy of the env code
  (`$LAB/wm_fix_2026-09-03/gp_root` = baselines/*.py + rl/*.py + eval_ics.json + can_pos_recovery/*.py +
  trial_placements.json, 14-line patch `reach_goal_patch.py`; the shared `$LAB/genesis_pickaplace` untouched):
  +1 and terminate when the TOOL is within `REACH_GOAL_DIST` (env var, REQUIRED) of the GOAL can. Both adapters map
  `reach_goal → reached_goal` for task_success; eval_genesis too. Threshold calibration probe (R=0.30, uniform)
  running locally. Point of 1b: a sparse goal-directed reward that undirected exploration hits occasionally but not
  at t=0, so a trainer that learns will show it, on both ports identically.

## 2026-09-03 11:24 (cluster clock) — dv3 cartpole s1 MISSES the stage-0 gate; dv3 touchgoal s1 cancelled (my job)
- **dv3 (cluster tree) cartpole_balance s1 (3232176): LAST eval 581.3 @197k (best 699.9 @157k; 20 evals of 5 eps).
  Gate LAST ≥ 800 by 200k: FAIL.** s0 (3232175) at 125k: 697.3, endpoint ≈ 12:20. Per the registered disconfirm branch
  the current dv3 tree is "port suspect"; the upstream NM512 reference (3234321/22, same overlay venv, same launcher,
  2 compat lines) is the bisect: at 16–24k steps so far (11 fps).
- Cancelled **3233334** (dv3 touchgoal STATE s1, my job): 4.9 fps on pax047 ⇒ 17 h to 300k, and the three probes show
  the touchgoal reward is unreachable by undirected exploration, so its only remaining value was symmetry with s0
  (3233333, 12.9 fps, continues as the dv3 stage-1 record; 0 touches at 60k). Freed GPU goes to stage 1b.
- Clock note 2: entry stamps from 10:00 on drifted ≈5–10 min ahead of the cluster clock; job-output timestamps
  (`# … DONE`, `sacct`) are authoritative. Stamps from here on are taken from the cluster (`date` in the ssh).
- Local cross-check r2d cartpole s0 (RECONSTRUCTED tree, this box, env_num 4): done 196k, LAST 190k **998.6** — agrees
  with the cluster record trees (996/997).
- 11:30 — Stage 1 verdict + Stage 1b registered (PLAN §7). Submitting 1b now (job ids in the next entry).

## 2026-09-03 11:26 (cluster) — STAGE 1b SUBMITTED, all six RUNNING within 75 s, variant line in every launcher header
- r2dreamer reach_goal STATE R=0.30: s0 **3234950** (pax008), s1 **3234951** (pax008); R=0.25: s0 **3234952** (pax047),
  s1 **3234953** (pax141). dv3 reference R=0.30: s0 **3234954** (pax182), s1 **3234955** (pax183). 300k steps each,
  `GENESIS_PICKAPLACE_ROOT=$LAB/wm_fix_2026-09-03/gp_root` (private env copy with `reach_goal`), `REACH_GOAL_DIST`
  exported per job, corrected world exported, fresh-process eval at the end.
- 11:25 (cluster) — **Stage-1 registered primary score, r2dreamer s1 (3233332, COMPLETED rc 0): fresh-process eval,
  sample mode, 15/15 hold ICs @1200, `[sim-variant] gc_kp4_riser3_shelf6` present (8 lines): `touched_goal` 0.0,
  timeout 1.0, mean_reward 0.0** (`runs/s1_r2d_touchgoal_state_s1/fresh_eval_hold15/metrics.json`; state_obs=True and
  `state` in obs_keys confirmed in the eval header). s0's standalone eval (3234609) still running on the CPU node.
- Calibration probe FINAL (local, corrected world, uniform random, scope reach_goal, R=0.30, 10 eps): **reached 5/10**
  (episode ended at decisions 22, 42, 69, 126, 174), min tool–goal distance median 0.311 m, best 0.283 m in the non-hit
  episodes; pick-can reach(<0.17) at decision 0 in 5/10. ⇒ R=0.30 is the "learnable, dense-enough" rung (random
  baseline 0.5, ≈70 decisions median to hit); R=0.25 is expected at ≤1–2/10 for random (best distances 0.231–0.283
  across probes). PLAN §7 1b text said "3/4 so far" — final is 5/10.
- 11:37 (cluster) — **dv3 (cluster tree) cartpole s1 (3232176) COMPLETED: run continued to 205k (dv3 counts steps before
  action_repeat); last eval ≤200k = 581.3 @197k → gate FAIL; supplementary final eval 709.0 @205k; best 709.0.**
  s0 (3232175) at 145k: 629 — endpoint ≈ 12:15. Upstream reference at 32k (325 / 318 @25k).

## 2026-09-03 11:50 (cluster) — STAGE 1b: the port LEARNS then COLLAPSES (critic past max return); stage 1c registered
- Per-10k trace (`collapse_trace.py`), r2d R=0.25 s0: 20k succ 0.88 (n96, len 36, ent −3.33, val 0.61, rewfr 13) → 30k 0.89
  (len 38, ent −3.82, val 0.91, ret_replay_max 1.38, value_replay_max 1.21) → 40k 0.47 (len 164, ent −4.49, **val 1.22**,
  ret_replay_max 1.48, value_replay_max 1.50, ret_replay_mean 1.13) → 50k **0.00** (len 300, **ent 9.83**, a_std 0.78,
  val 0.26). R=0.30 s1: 10k 0.87 (ent −0.05, val 0.38) → 20k 0.75 (ent 9.25, value_replay_max 1.23) → 30k 0.00 (ent 9.93)
  and never recovers (val decays to 0.01 by 90k; no reward frames after 40k). Cont head 0.79–0.95 during buildup, 1.0 after.
- Interpretation: λ-return targets exceed the maximum attainable return (1.0) — bootstrapping continues past the reach
  terminal (terminal state is not distinct in the 17-dim state; the reward model keeps paying near the goal in imagination)
  → critic overestimates → the dynamics-gradient actor loses its signal → entropy bonus drives std to max → the learned
  policy is destroyed within 10k steps. Stage 1c (PLAN §7) submits four single-lever ablations; job ids below.
- 11:50 (cluster) — Stage 1c SUBMITTED (all RUNNING within 60 s; lever confirmed in each launcher header): r2d R=0.25
  150k — clamp1 (`env.return_clamp=1.0`) s0 **3235522** / s1 **3235523**; repdreamer (`model.rep_loss=dreamer`) s0
  **3235524** / s1 **3235525**; ent3e5 (`env.act_entropy=3e-5`) s0 **3235526** / s1 **3235527**; dv3 reference R=0.25
  150k s0 **3235528** / s1 **3235529**. Run names `s1b_r2d_reachgoal_state_r0.25_<tag>_s<seed>` (launcher TAG).
- Code reading (cluster dreamer.py = record; lines 476–556, 603–640): imagination `start` = the posterior at EVERY replay
  row (B·T, terminal rows included); continuation in imagination is `imag_cont = frozen_cont(feat).mean` (a learned
  prediction — the stored `is_terminal` is NOT consulted), `weight = cumprod(imag_cont·disc)`, and the imagined
  λ-return `ret[:, 0]` is the `boot` for the REPLAY value targets. The cont head is trained with `cont = 1 − is_terminal`
  (one terminal row per success; the row 26 cm from the goal is a positive continuation) and the reward head with the
  single +1 on that same row. In a 17-dim state with no tool position, "reached" is a smooth boundary for both heads
  ⇒ rollouts through the goal region neither stop nor stop paying ⇒ imagined returns > 1 leak into every earlier row's
  target (value_replay_max 1.5, ret_replay_mean 1.13 at 40k) ⇒ the dynamics-gradient actor sees a flat, inflated
  return landscape and the entropy bonus takes over. Replay-side targets themselves are correct at real terminals
  (`live = (1 − is_terminal)·disc` ⇒ the pre-terminal row's target is exactly 1.0). The stage-1c levers test this:
  clamp1 caps the leak (predicted: no collapse or a much slower one); repdreamer/ent3e5 should not change it if the
  leak is the driver; dv3 (same structure) should collapse too.
- 11:58 (cluster) — three stage-1c jobs (3235522/23 clamp1, 3235526 ent3e5 s0) died at the first update:
  `torch._inductor.exc.InductorError: SubprocException … kernel.precompile(warm_cache_only=True)` — each shared its
  node with a sibling compiling the same kernels into the SAME inductor cache (pax110 ×2, pax113 ×2). Infrastructure,
  not config. All launchers now export a per-job `TORCHINDUCTOR_CACHE_DIR=$W/inductor_cache/$SLURM_JOB_ID`
  (new trap for §4). Empty logdirs removed; resubmitted: clamp1 s0 **3235738** / s1 **3235739**, ent3e5 s0
  **3235740** (RUNNING). Surviving originals: repdreamer 3235524/25, ent3e5 s1 3235527, dv3 R=0.25 3235528/29.
- 12:05 — **Random baseline at R=0.25 (local, uniform, 20 eps × 300 decisions): reached 4/20 = 0.20** (at decisions 40, 64,
  111, 266), min tool–goal median 0.306 m, best 0.221 m. Stage-1b r2dreamer at R=0.25 reached 0.82–0.89 per bin with
  ≈37-decision episodes before collapsing — learning well above baseline, then destroyed. (R=0.30 baseline: 5/10.)

## 2026-09-03 12:08 — stage 1c early bins (R=0.25; random baseline 0.20)
- **ent3e5 s1** (act_entropy 3e-5): 10k 0.78 (len 70) → 20k **0.88** (len 44, ent −4.70, val 0.96) → 30k **0.00** (n7, len 265,
  **ent −4.45 = still committed**, **val 1.50**, a_mean 0.19). The policy fails while confident, right as the critic
  passes the max return ⇒ the entropy explosion seen at 3e-4 is a downstream symptom; the primary event is the inflated
  critic misdirecting a committed actor (consistent with imagination paying "near the goal" without terminating).
- **repdreamer** (rep_loss=dreamer): s1 0.14 → 0.71 (ent −0.8, learning); s0 0.50 → 0.00 → 0.32 (ent 0.40) → 0.20 (ent 9.91)
  → 0.00 with val ≤ 0.26 — an entropy blow-up WITHOUT an inflated critic. ⇒ representation loss is not the lever; and the
  randomization can also fire at low value (flat imagined-return landscape suffices).
- **clamp1**: 8–10k steps only (0.38 / 0.30, ent 9.2) — not readable yet. Original R=0.25 s1 shows bistability: 100k bin
  **0.53** (n40) after two zero bins, then 0.00 again at 125k.
- dv3 R=0.25 s0/s1 at 10k, dv3 R=0.30 at 20–26k: no signal yet (11–12 fps).

## 2026-09-03 12:10 (cluster) — stage-1 fresh eval s0 in; walker at the bar; tg diagnostic submitted
- **Stage-1 registered primary score, r2dreamer s0 (standalone eval 3234609, COMPLETED rc 0, 57 min on the CPU node):
  15/15 hold ICs, sample mode, 1200 steps, variant line present: `touched_goal` 0.0, timeout 0.93 (1 tipped),
  mean_reward 0.0.** With s1's 0/15 (11:25) the stage-1 verdict of record is **FAIL 2/2 by reward unreachability**
  (PLAN §7 verdict entry stands).
- Walker sanity (r2d, 500k budget, jobs still finishing): s0 LAST 936.1 @460k (best-of-last-3 956.3), s1 936.5 @490k
  (961.8); both first ≥600 at 150k. Gate (≥600 by 500k on ≥1/2, ≥400 on 2/2): passing; final entry when the jobs end.
- Stage-1c (e) `tg` (21-dim state with tool xyz + tool–goal distance) submitted 12:08: s0 **3236061** (pax183), s1
  **3236062** (pax047), R=0.25, 150k. Standalone test: reach fired at decision 41, distance feature = env distance.

## 2026-09-03 12:17 (cluster) — STAGE 1c: the return clamp at the TRUE max return removes the collapse; nothing else does
- **clamp1 (`env.return_clamp=1.0`, R=0.25, random baseline 0.20): s0 0.79 (n96) → 0.98 (n682) → 0.98 (n576) at
  0/25/50k, entropy −3.67; s1 0.34 (n32) → 0.97 (n343) → 0.94 (n318), entropy −3.88.** Hundreds of episodes per 25k bin
  ⇒ ≈9-decision reaches (random: 40–266 decisions). Two consecutive bins ≥ 0.94 after ignition on 2/2 seeds; still
  running to 150k + fresh eval (registered "no collapse" criterion needs every 10k bin ≥ 0.6 through 150k).
- Collapsed (2/2 seeds each): base (0.87/0.82 → 0.00 by 50k; s1 re-ignited 0.53 at 100k then 0.00); **ent3e5** (0.81/0.68
  → 0.00 by 50k, entropy 9.93); **repdreamer** (0.86 → 0.15 → 0.00; s0 bistable: 0.84 at 75k → 0.00 at 100k, entropy
  −5.67 then 9.93). dv3 reference: R=0.30 s0 0.89 (n9, entropy 1.11) at 25k–40k — learning; R=0.25 no success yet
  at 30k (11 fps, few episodes). `tg` at 9k (too early).
- Reading: the mechanism is λ-return targets exceeding the maximum attainable return (imagination through an
  indistinct terminal keeps bootstrapping/paying); capping the target at the KNOWN max stops it. Consistent with
  the port's own 08-11 diagnosis, and it explains the paper's dead endpoints: no pick run ever had (sparse reward,
  reward_scale 1, clamp = max return) together — the frozen block's clamp 100 sat under shaped returns of 500–1000
  (CONFOUNDS row 13), A32 C2000 was far above max, RS1 had no clamp, and the sparse arms never found the reward
  without demos. Stage 2 (pick WITH demos, sparse, scale 1) + `return_clamp 1.0` is the first run of that combination.
- 12:25 (cluster) — **Stage 0 walker_walk (r2dreamer, 500k): s1 (3233330) COMPLETED rc 0, LAST 936.5 @490k (best-of-last-3
  961.8); s0 (3233329) at 496k, LAST 933.0 @490k (940.8); both ≥ 600 from 150k. Gate PASS 2/2.** With cartpole 996/997:
  **r2dreamer passes stage 0 on both DMC tasks** — the port's learning machinery is sound on reference tasks; the
  pick-endpoint deaths are not "a broken trainer" but the terminal-leak/clamp-scale mechanism found in stage 1b/1c.
- clamp1 at 75k–80k: s0 0.95 (n133, entropy risen to 4.19 — watch), s1 0.99 (n139, entropy −3.34). tg at 14–17k: 0.41 /
  0.20 (entropy 3.2 / 4.5, dropping) — early.

## 2026-09-03 12:28 (cluster) — dv3 fork vs upstream on cartpole: the FORK is the problem (interim, 65k)
- Upstream NM512 `dreamerv3-torch` @6ef8646 (+2 documented compat lines), same overlay venv/launcher/env count: cartpole
  eval **796.7 (s0) / 924.6 (s1) at 65k**. Cluster fork (`$LAB/dreamerv3-torch` @2a80452 + 139 dirty): at 65k ≈ 381 (s0) /
  ≈ 270 (s1); s1 LAST-by-200k 581, s0 498 @189k. ⇒ the current dv3 tree learns a stock DMC task 2–3× slower than upstream
  and misses the registered gate; "port suspect" is now evidence-backed. Bisect target = the fork's 633/201/448-line
  diffs in dreamer.py/models.py/tools.py (upstream envs/dmc.py identical). Every dv3 genesis number in the paper's
  diagnostics inherits this flag (on top of the base-world confound).

## 2026-09-03 12:36 (cluster) — stage-1c fresh evals: clamp1 s1 15/15 (7 decisions/reach); repdreamer 0/15 ×2
- **clamp1 s1 (3235739, COMPLETED rc 0, 150k, 35 min): fresh-process eval, sample mode, 15/15 hold ICs @1200, 6 variant
  lines: `reached_goal` 1.0 (15/15), mean_steps 7.0, mean_reward 1.0.** Train curve: 0.97–1.00 in every 25k bin from
  25k to 150k (n up to 1085 episodes per bin), entropy −2.7 at the end — no collapse. Random baseline 4/20 at 40–266
  decisions.
- repdreamer s0/s1 (3235524/25, COMPLETED): fresh eval `reached_goal` 0.0 (15/15 timeouts) both — collapsed policies.
- clamp1 s0 (3235738) finishing (0.97–0.99 through 125k); ent3e5 s0/s1, tg s0/s1, dv3 R=0.25/0.30 still running.
- 12:42 (cluster) — **dv3 (cluster tree) cartpole s0 (3232175): last eval ≤200k = 513.3 @197k (best 697.3 @125k) → gate
  FAIL; with s1's 581 the current dv3 tree FAILS stage 0 on cartpole 2/2.** Upstream reference (797/925 @65k) and the
  fork-with-upstream-defaults bisect (3236667/68, started 12:29) decide code-vs-recipe.
- 12:39 (cluster) — **tg (distinct terminal, 21-dim state) COLLAPSES too**: s0 0.78 → 0.94 (n170) → 0.67 → 0.00 @75k;
  s1 0.78 → 0.64 → 0.00 @50k; entropy 9.93 both. Registered reading (e): the leak is in the imagination/bootstrap path
  itself, not (only) head smoothness at an indistinct terminal; the return clamp at the known max is the lever.
  ent3e5 s0/s1 finished 150k at 0.00 since 50k (fresh evals pending in-job).
- **Stage-1b registered gate (base recipe, R=0.30, 300k, fresh eval): s0 0/15, s1 0/15 (all timeouts, sample mode) — FAIL
  2/2, and worse than the 5/10 random baseline: the collapsed actor actively avoids the goal region.** Stage 1b/1c verdict
  of record: the base recipe cannot hold a learned policy; `return_clamp = max return` can (clamp1 s1 15/15; s0 pending).

## 2026-09-03 12:43 (cluster) — clamp1 s0 fresh eval 3/15 (OPEN train/eval discrepancy); STAGE 2 dH SUBMITTED; repeat evals
- **clamp1 s0 (3235738, COMPLETED rc 0): fresh eval on the same 15 hold ICs, sample mode: `reached_goal` 0.20 (3/15),
  tipped 2/15, timeout 10/15, mean_steps 212** — while its last 40 TRAINING episodes (146.8k–148.7k) score 0.95 at 6.6
  decisions and `latest.pt` is the end-of-run save (12:33:37). s1's identical procedure gave 15/15 at 5–9 decisions.
  Open: eval-vs-train discrepancy on one seed (candidates: IC-set sensitivity of a fast/aggressive policy — 2 tips —,
  sampling fragility, CPU-vs-GPU numerics, checkpoint identity). Six repeat evals submitted on the gpu partition
  (cpu device): per seed {hold sample eval-seed 1; hold MODE seed 0; sel sample seed 0} = 3236980–3236985.
  Reading rule (pre-stated): if s0 stays ≤ 5/15 across all three while s1 stays ≥ 12/15, the clamp1 result is
  "1/2 seeds robust" and stage 2 needs its own 2-seed fresh evals read strictly; if s0 recovers under `mode` or on
  `sel`, it is sampling/IC fragility of the policy, not the checkpoint.
- **STAGE 2 dH submitted 12:43 (all RUNNING on pax151, demo-gate PASSED: 58 tapes, gc_kp4_riser3_shelf6, stride 4,
  with_state, terminal 1.0): clamp1 s0 3236976 / s1 3236977 (`env.return_clamp=1.0`); clamp0 (registered stock control)
  s0 3236978 / s1 3236979.** 1M steps, buffer 5e5, sparse both sides, fresh evals (hold 15 + rnd 30) at the end.
- Upstream dv3 reference at 75k: **867.5 / 937.2** (fork: 474 @205k / 709 @205k). Fork-with-upstream-defaults bisect
  (3236667/68) at 15k.
- 12:55 (cluster) — **MY silent default #2 today: `wmfix_eval.sbatch` exported `GENESIS_PICKAPLACE_ROOT=$LAB/genesis_pickaplace`
  (the shared repo, whose full_env.py has NO `reach_goal` scope) instead of the training env copy `gp_root`** ⇒ the six
  repeat evals 3236980–85 ran a scope the env does not know (no terminal, no reward): the one that finished showed
  0/15 "timeout/tipped" — INVALID, cancelled, outputs removed. The IN-JOB evals (wmfix_s1b.sbatch, gp_root +
  REACH_GOAL_DIST inherited) are valid: clamp1 s1 15/15, s0 3/15 stand. Launcher fixed (root = gp_root, 6th arg
  REACH_GOAL_DIST exported, refuses reach_goal runs without it, header prints `rgd= root=`); resubmitted:
  s0 {hold/sample/es1 **3237337**, hold/mode/es0 **3237338**, sel/sample/es0 **3237339**}, s1 {**3237340**, **3237341**,
  **3237342**}. Trap for §4: evaluators must run in the TRAINING env copy; a scope the env ignores fails silently.
- Stage 2 dH at 48k (all four): picked 0.00 (n12 episodes each), entropy clamp1 s0 −2.5 / s1 9.9 / clamp0 s0 −3.6 / s1 −4.6,
  demo reward frames 4–6 per batch. Too early.

## 2026-09-03 12:58 (cluster) — repeat evals (valid launcher): clamp1 s1 ROBUST (15/15 ×3); s0 fails every variant
- **clamp1 s1: hold/sample/eval-seed 1 = 15/15; hold/MODE = 15/15; sel/sample = 15/15** (all reaches in ≈5–9 decisions;
  evals took < 1 min each). ⇒ the s1 policy is robust to eval seed, action mode and IC set.
- clamp1 s0: first episodes tipped/timeout under all three variants (running) — consistent with its in-job 3/15.
  Its own last 40 training episodes: 0.95 success at 6.6 decisions. ⇒ a genuine checkpoint-vs-training discrepancy on
  one seed. Test submitted (3237391): warm-restart `+resume=true` of s0's `latest.pt` INSIDE the training loop for
  6k env steps — if its first episodes succeed, the eval path differs from the training rollout path (bug to find);
  if they fail, the saved checkpoint is not the policy that trained (save/clone path). Pre-stated: either outcome is a
  finding for §4; stage-2 gates use the same fresh-eval path, so this must be resolved before any stage-2 verdict.

## 2026-09-03 13:10 (cluster) — clamp1 s0 discrepancy: evals final, checkpoint integrity checked, resume-check v2 running
- Repeat evals (valid launcher, gp_root, R=0.25): **s0** hold/sample/es1 **3/15** (10 timeout, 2 tipped); hold/MODE
  **0/15**; sel/sample **1/15**. **s1** 15/15 / 15/15 / 15/15. In-job originals: s0 3/15, s1 15/15.
- Checkpoint integrity: both `latest.pt` (saved step 150000, 160 keys incl. 79 `_frozen_*`): live vs frozen actor /
  encoder / rssm / value parameters max|Δ| = 0.000 on both seeds ⇒ the saved policy IS the acting policy at save time
  (frozen clones share storage with the live params). Not a live/frozen divergence.
- Resume-check v1 (3237391, 6k env steps) inconclusive: the logger flushed once (step 1584, 0 updates) before the run
  ended — no episode row either way. v2 (3237662): 40k env steps, `trainer.update_log_every=1000`; the first ≈4k env
  steps precede any update (fresh buffer must fill), so the first episodes are the loaded policy's own behaviour.
- Remaining candidates for s0: the ≈160 updates between its last logged episode (148.7k) and the end-of-run save
  (150k) in a bistable system; or an episode-start state difference (training carries the previous episode's RSSM
  state through `is_first`, the evaluator starts from `get_initial_state`) that a 6-decision policy is sensitive to.
- Stage 2 dH at ≈104k (all four): picked 0.00 (n ≈ 60 episodes each); entropy clamp1 s0 −5.1, clamp1 s1 9.93, clamp0 s0
  9.92, clamp0 s1 9.93; demo reward frames ≈2 per batch. Three of four actors at max entropy already — watch.

## 2026-09-03 13:09 (cluster) — clamp FLICKER finding; clamp1+ent3e5 submitted (reach rung + stage-2 third arm)
- clamp1 end-of-run rows (5k cadence): s0 entropy 9.93 @111.6k → −4.8 @116.6k, 7.15 @136.6k → −1.05 → −3.7 @146.6k; s1 9.92
  @136.6k, 9.78 @141.6k → −5.22 @146.6k; `ret_replay_max` = 1.000 throughout (clamp holding), `train/val` dips 0.9 → 0.4–0.6
  during flickers. ⇒ with the critic capped, the collapse becomes a recoverable flicker; s0's 150k save sits in the
  unlogged window after 146.6k (its fresh evals 3/0/1 of 15), s1's in a good phase (15/15 ×3). PLAN §7 13:15 entry.
- Submitted: **1c(f) clamp1ent5** (`env.return_clamp=1.0 env.act_entropy=3e-5`, reach R=0.25, 150k): s0 **3237721**, s1
  **3237722**; **stage-2 dH clamp1ent5** (1M): s0 **3237723**, s1 **3237724**. Stage 2 now has three arms on dH
  (clamp1, clamp0 control, clamp1ent5), 2 seeds each.
- 13:15 (cluster) — Stage 2 dH at ≈125k (≈90 episodes each, no online pick yet on any arm; episode length 263–290
  decisions; demo reward frames 2–7 per batch). Diagnostics (25k rows): **clamp0 s0 (control): val 1.13 / target 1.36 at
  75k (> max 1.0) → entropy 9.93 at 100k** — the critic runs past the max on DEMO rewards alone and the actor blows up
  before any online success; clamp0 s1 entropy 9.93 too. **clamp1 s0: entropy −2.5 @25k → 9.8 @50–75k → −4.6 @100k
  → −4.9 @125k (flicker + recovery, val ≤ 0.82)**; clamp1 s1: entropy 9.9 from 25k with a_mean −0.44 on some dims
  (never committed). clamp1ent5 arms just started. The 1M budget continues; ignition in the recipe of record took
  ≥100k–300k.
- 13:11 (cluster) — **dv3 reference (fork tree + world hook, unclamped, same env copy) is LEARNING reach_goal without
  collapse so far:** R=0.30 s0 0.91 (n11) / 0.83 / 1.00, entropy 0.29; s1 0.93 (n45) / 1.00, entropy 1.44; R=0.25 s0
  0.75 (n8) / 1.00, entropy 1.98; R=0.25 s1 0.00, entropy 9.93 (never ignited). (dv3 logs episodes only every 10k
  steps → small n; 11–13 fps.) ⇒ the learn-then-collapse is r2dreamer-specific. Submitted 1c(g) `bnorm`
  (`env.actor_dist=bounded_normal`, no clamp): s0 **3237781**, s1 **3237782** (PLAN §7 13:20).

## 2026-09-03 13:22 (cluster) — clamp1 s0 discrepancy RESOLVED: the end-of-run checkpoint is a flicker-phase policy
- Resume-check v2 (3237662, COMPLETED, 40k env steps, log every 1k): with s0's `latest.pt` loaded into the TRAINING loop,
  the first six episodes (one per worker) all ran to the 300-decision limit; the first update happened only after
  ≈1.6k env steps (≈66 decisions per worker) — a 7-decision reach policy would have finished ≈9 episodes per worker in
  that window. Over 36 episodes: 5 reaches (0.14 ≈ the 0.20 random baseline), lengths 300 except the reaches (3–30).
  ⇒ the saved policy does NOT reach, in the training loop or the evaluator; the evaluator is honest. Combined with
  the 5k-row traces (entropy 7.15 @136.6k → −3.7 @146.6k, then 3.4k unlogged steps to the 150k save) the checkpoint
  captured a flicker that began after the last logged row.
- Consequences: (i) clamp1's registered fresh-eval score is s1 15/15, s0 3/15 — reported as is; (ii) the remaining
  defect is the flicker (actor entropy blow-ups with a capped critic), under test as clamp1ent5 (1c(f) at 34k: 0.77 /
  0.94, entropy −4.2/−4.4 on both seeds); (iii) end-of-run checkpoints are a phase lottery under flicker — the
  stage-2 launchers keep only `latest.pt` (overwritten every 1e5 steps), so no post-hoc phase-aware selection is
  possible for the running stage-2 jobs; the registered end-of-run fresh eval stands and the checkpoint's phase
  (entropy at the last logged row) is disclosed next to it.

## 2026-09-03 13:44 (cluster) — 1c(g) bnorm: the stock actor SURVIVES the critic overshoot; (h)/(i) submitted
- bnorm (stock `bounded_normal`, no clamp) per-10k trace: s0 ignites at 20k (0.89), 0.97–1.00 from 30k to 130k with one
  dip (0.45 @70k, entropy 3.99) and full recovery (entropy −1.5 @130k); s1 0.92 @20k, 0.83–0.97 with excursions
  (entropy 9.15 @90k, 0.53) and recovery (0.91 @130k, entropy −2.1). Critic: val 1.05–1.29, `ret_replay_max` up to
  2.9, `value_replay_max` 2.1 — the SAME overshoot that kills the clipped-actor runs; raw actions sample to ±4.8
  (env clips to ±1). ⇒ two contributors: trigger = critic past the max return (clamp fixes), amplifier = the port's
  `bounded_normal_clipped` (stock actor survives). 1c(f) clamp1ent5 at 140k: 1.00 / 1.00 (n840/689 per bin), entropy
  −5.8 on both seeds — no flicker so far; evals at 150k pending.
- Submitted (PLAN §7 13:48): (h) bnormclamp1 s0 **3238921** / s1 **3238922**; (i) bnormclamp1ent5 s0 **3238923** / s1
  **3238924** (reach R=0.25, 150k).
- 13:46 (cluster) — Stage-2 arm (i) `bnormclamp1ent5` submitted: s0 **3238966** (RUNNING, demo-gate passed), s1 **3238967**
  (PENDING on QOSMaxGRESPerUser). To free a GPU, cancelled **3233333** (dv3 sparse-touchgoal STATE s0, my job): 0 goal
  touches in 177.6k steps (28 episodes; its reward is unreachable by exploration per the three probes); the dv3 stage-1 record is
  therefore "0 touches at 177.6k on s0, s1 cancelled at 27k" — both consistent with r2dreamer's 0/300k ×2.
- Stage 2 dH at 160–340k: clamp1 s0/s1 and clamp0 s0/s1 at entropy 9.93 with 0 online picks (val ≤ 0.06); clamp1ent5
  s1 igniting (0.13 → 0.19 → 0.24 picks per 50k bin, entropy −5.6, val 0.74), clamp1ent5 s0 dead (9.93 @186k).

## 2026-09-03 13:55 (cluster) — reach-rung fresh evals: bnorm 15/15 ×2; clamp1ent5 15/15 + 9/15; (h)/(i) running
- **1c(f) clamp1ent5** (3237721/22, 150k): train 1.00 / 1.00 in the 100k and 125k bins (n840–998 per bin), entropy −5.98 /
  −5.93 at the end, no flicker; **fresh eval s0 15/15, s1 9/15** (timeouts on uids 254, 265, 294, 295, 302, 327; the
  nine reaches in 4–11 decisions). s1's failures are uid-specific with a committed actor — NOT a flicker. Training
  resets draw uniformly from the same 61 success uids (hold ⊂ universe), so a 6/61 per-uid failure rate should have
  shown as ≈0.90 in training, not 1.00: a training-vs-evaluator difference for this policy on these ICs is open.
  Test submitted: the same checkpoint evaluated on 30 random training-distribution uids through the same evaluator
  (`SET=demo`; s1 and s0 as control).
- **1c(g) bnorm** (3237781/82, stock actor, NO clamp): fresh eval **15/15 and 15/15**; train 0.99 / 0.94 in the 125k bin,
  entropy 0.16 / −2.48 — survives the critic overshoot with wobble. Best fresh-eval lever so far at the reach rung.
- Ranking at the reach rung by fresh eval (2 seeds): bnorm 30/30; clamp1ent5 24/30; clamp1 18/30; base 0/30 (R=0.30).
  (h) bnormclamp1 and (i) bnormclamp1ent5 running (3238921–24), read-out ≈ 14:25.
- Stage-1b/1c fresh-eval TABLE (reach_goal R=0.25 unless noted; 15 hold ICs, sample mode, 1200 steps, corrected world;
  base R=0.30 = 300k, others 150k):

  | lever (tag) | s0 | s1 | train at end (per-25k bin, entropy) |
  |---|---|---|---|
  | base R=0.30 (300k) | 0/15 | 0/15 | 0.00 / 0.00, 9.93 |
  | base R=0.25 | 0/15 | 0/15 | 0.00 / 0.00, 9.93 |
  | ent3e5 | 0/15 | 0/15 | 0.00 / 0.00, 9.93 |
  | repdreamer | 0/15 | 0/15 | 0.00 / 0.00, 9.93 |
  | tg (21-dim state) | 0/15 | 0/15 | 0.66 / 0.00, 9.93 |
  | clamp1 | 3/15 (flicker-phase ckpt) | 15/15 | 0.97 / 0.99, −3.7 / −5.2 (flickers) |
  | clamp1ent5 | 15/15 | 9/15 (6 uid-specific timeouts) | 1.00 / 1.00, −6.0 / −5.9 |
  | bnorm (stock actor, no clamp) | 15/15 | 15/15 | 0.99 / 0.94, 0.2 / −2.5 |
  | dv3 reference (unclamped) | running | running | R=0.30: 0.9–1.0 (s1), degrading (s0); R=0.25: 0.86 (s0), 0 (s1) |

  Random baseline: 4/20 at R=0.25, 5/10 at R=0.30.

## 2026-09-03 13:58 (cluster) — train-vs-evaluator discrepancy is NOT only flicker: clamp1ent5 s1 on training-distribution ICs
- clamp1ent5 s1 (train 1.00 in every bin from 75k, entropy −5.9 at the end, no flicker) evaluated on 30 random
  training-distribution uids through the same evaluator: 5 reached / 4 timeout after 9 episodes (running). Its training
  episodes (n950 in the last 25k bin at ≈7 decisions) show no timeouts. ⇒ for this checkpoint the evaluator's rollouts
  differ from the training-loop rollouts on the SAME IC distribution, with a committed actor.
- Ruled out locally (world `gc_kp4_riser3_shelf6`, uids 254/265/294): the reset-returned state and the evaluator's
  re-read `genv._obs()['state']` are bit-identical (max|Δ| 0.0); repeated resets identical; `_dj_target` == measured q.
  Ruled out earlier: live vs frozen params (0.0), episode-start RSSM state (both zeros), preprocessing (none for state).
- Remaining structural difference: NUMERICS — training acts on the GPU under `torch.set_float32_matmul_precision("high")`
  (TF32 matmuls), the evaluator on CPU fp32; the RSSM samples discrete latents (Gumbel), so small differences can flip
  latent samples and, for a marginal fast policy, the trajectory — consistently per uid (seeded). Test submitted:
  the same two checkpoints evaluated with `--device cuda` on hold-15: clamp1ent5 s1 **3239286**, clamp1 s0 **3239287**
  (launcher now takes `DEVICE=`). If CUDA evals recover, the fresh-eval protocol must fix the device (and the
  resume-check's 5/36 for clamp1 s0 — run on GPU — says s0's checkpoint is still bad regardless).
- 14:00 (cluster) — **Early stop of the stage-2 clamp0 controls (3236978/79, my jobs) at ≈400k/1M:** both at entropy 9.93
  since ≤100k with `train/val` ≤ 0.06, 0 online picks in ≈350 episodes each (s1 a lone 0.08 on a 12-episode partial
  bin at 350k). The registered prediction for the control (collapse under the critic overshoot on demo rewards
  alone, `target 1.36 > 1` at 75k) is confirmed; the end-of-run fresh eval would be 0/15 with certainty. Recorded as
  "control: collapsed by 100k, stopped at ≈400k" — no fresh eval; freed 2 GPUs for the queued numerics evals.
- Honesty note on the stopped clamp0 control s1 (3236979): its last partial 50k bin (350k, 25 episodes) showed `train/val`
  rising 0.03 → 0.44 and entropy 9.93 → 9.22 with 0 picks — a possible re-ignition onset (the reach-rung base runs
  re-ignited once at 100k before dying again). Stopped regardless; the control's registered outcome (collapse) stands,
  but "never re-ignites by 1M" is NOT established for the control.
- clamp1ent5 s1 on 30 training-distribution uids (CPU evaluator): 10 reached / 8 timeout after 18 — ≈0.56 vs 1.00 in
  training. CUDA-device eval of the same checkpoint (3239286) now running.

## 2026-09-03 14:05 (cluster) — STAGE 2 IGNITION on dH clamp1ent5 s1 (0.73 online picks); evaluator thread narrowed
- **Stage 2 dH clamp1ent5 s1 (3237724): `train_picked` per 50k bin 0.00 → 0.13 → 0.19 → 0.28 → 0.73 (n135, 200k–250k),
  entropy −6.03, val 0.70** — the first sustained online picking with sparse +1, reward_scale 1, state obs, human demos.
  clamp1ent5 s0 (3237723): dead (entropy 9.92, val 0.06, 0.00 since 100k). clamp1 s0 (3236976) at 437k: entropy −4.01,
  val 0.35, 0 picks — a possible re-ignition; clamp1 s1 dead. bnormclamp1ent5 s0/s1 (3238966/67) at ≈95k: val 0.70/0.76,
  entropy −5.4/−5.0, first picks 0.02 / 0.09. (Stopped controls: see 14:00.)
- Reach rung (h) bnormclamp1: s0 0.97 (n639) → 1.00, s1 0.91 → 0.95, entropy ≈ 3; (i) bnormclamp1ent5: s0 0.96 → 0.96,
  s1 0.92 → 0.96 → 0.98 (n148 @75k), entropy −4.6 / −4.3. Both holding at 50–85k.
- Evaluator thread: clamp1ent5 s1 demo-distribution eval FINAL **14/30**; s0 **30/30**. CUDA-device eval of s1 on hold:
  **9/15 — identical to the CPU eval** (per-uid outcomes reproducible; numerics ruled out). s1's last 40 TRAINING
  episodes (148.7k–149.9k, ending ≈60 env steps before the 150k save) are **40/40** at 7.5 decisions; s0's 40/40 at 5.6.
  Parameters cannot change in 60 steps ⇒ the trainer's acting policy depends on something the checkpoint does not
  carry, or the training-loop rollouts differ from cold-start rollouts. Save/load carries all 160 keys (only
  `return_ema.ema_vals` is non-parameter state; the one non-persistent buffer is the BC lambda, unused). Test running:
  the same checkpoint rolled out INSIDE the training loop (a) with updates (3240214) and (b) with updates disabled
  (`env.train_ratio=1e9`, 3240241) — (b) is a pure cold-start-then-carry-over rollout: first episodes = cold start,
  later episodes = with carried latent state across `is_first` resets.
- 14:08 (cluster) — **The EVALUATOR differs from the training loop for these policies.** clamp1ent5 s1's `latest.pt`
  loaded into the training loop (3240241, updates disabled via `env.train_ratio=1e9`; and 3240214 with updates):
  first 6 episodes (one per worker, cold start from `get_initial_state`) = **6/6 reaches** in both jobs, vs 14/30 on
  the same IC distribution in the evaluator (CPU and CUDA evaluators agree exactly: 9/15 on hold both). Static checks
  exhausted (states identical, params identical live/frozen, no unsaved state, wrappers = TimeLimit+Dtype only,
  packaging equivalent, no train/eval-mode branches). Differential experiment submitted (3240336, one process, one
  world, uids 254/265/294/252 × 2 episodes): evaluator loop vs trainer-faithful packaging vs carried agent state vs
  deterministic mode, with the first actions compared numerically.

## 2026-09-03 14:22 (cluster) — differential rollout (interim) + warm-restart: the checkpoint's MEAN policy is right; SAMPLED
   draws decide; the trainer's rollouts are somehow luckier
- Differential (3240380, one process/world, clamp1ent5 s1, uid 254): with torch seed 100 the three SAMPLED variants
  (evaluator packaging, trainer packaging, carried agent state) all fail identically (timeout@300; first actions
  bit-identical), the deterministic MODE variant reaches in 4; with seed 101 all sampled variants reach in 5. ⇒ no
  packaging/state difference; the outcome hinges on the sampled deviation at the first steps (a0 differs by 0.5–1.0
  per dim between seeds ⇒ the actor's std on REAL reset states is ≈0.5, although `train/action_entropy` (computed on
  IMAGINED latents) reads −5.9). Per-uid determinism in the evaluator = its fixed seed-0 draw sequence.
- Warm restart with updates (3240214): 1175/1188 successes over 40k steps (first 6 cold-start episodes 6/6, then a 5-
  episode dip when updates on the fresh buffer began, then ≈0.99). True no-update rollout (3240468) running (my first
  attempt inverted `train_ratio` — larger = MORE updates in this trainer; §4 trap).
- Open question now precise: why is P(success) ≈ 0.99 for sampled rollouts inside the training loop but ≈ 0.5 in the
  evaluator, when the first actions are bit-identical per seed? Candidates: (1) the sim world's warm state
  (training worlds have run for hours; constraint warm-starts persist across `reset`) — the differential job showed
  all variants failing on the FRESH world's first episode and succeeding on later ones; (2) the RNG stream. The
  no-update rollout (fresh world, frozen policy, hundreds of episodes) separates (1) from (2): if it climbs from ≈0.5
  to ≈1.0 with world age, it is (1).

## 2026-09-03 14:27 (cluster) — RESOLVED: the evaluator is faithful; training-loop success overstates FROZEN performance
- No-update rollout of clamp1ent5 s1's checkpoint inside the training loop (3240468, `env.train_ratio=1e-6` ⇒ 1 update
  total, at step 0): **33/53 = 0.62** (len mean 106; the 6 cold-start first episodes 6/6, then ≈0.55). With updates on
  (3240214, 1 update per 8 env steps = every 2 decisions): **1175/1188 = 0.99**. Evaluator on the same checkpoint:
  14/30 (demo ICs), 9/15 (hold), CPU = CUDA. Differential rollout: sampled variants identical per seed; MODE succeeds.
- Reading: under this recipe the actor is updated every 2 decisions on a buffer that contains the CURRENT episode's
  transitions, so a policy whose sampled first actions are poor ≈40% of the time gets corrected within the episode
  during training (in-episode adaptation). A frozen snapshot cannot; its sampled-action success is ≈0.6 while its mean
  action (mode) reached in 4 decisions on a failing uid. `train/action_entropy` (−5.9) is computed on IMAGINED latents
  and does not describe the std on real reset states (≈0.5 by the first-action spread).
- Consequences: (1) every `episode/train_*` success in this ladder is an upper bound on frozen performance; the
  registered fresh-process evals stand as the numbers of record; (2) clamp1 s0's 3/15 and clamp1ent5 s1's 9/15 are
  real frozen-policy numbers, not artifacts (the flicker note for clamp1 s0 remains a plausible contributor but is no
  longer required); (3) the stage-2 gate (fresh eval, sample mode) is the right statistic, and the action mode of the
  WM eval (sample vs mode) is a protocol question for the main session: DP is deterministic and RLPD's eval uses the
  mean action, so the mode number is the like-for-like counterpart — both will be reported. MODE evals of clamp1ent5
  s1 submitted (hold15 3240770, demo30 3240771).
- §4 trap: in this trainer `train_ratio` LARGER ⇒ MORE updates (`Every(batch_steps/train_ratio·action_repeat)`); to
  disable updates set it tiny, not huge.
- 14:28 (cluster) — **MODE (deterministic) evals of clamp1ent5 s1: hold 15/15, training-distribution 30/30** (sample mode:
  9/15, 14/30). The mean policy is robust; the sampled policy's std on real states is what fails. Protocol implication
  for the main session: the WM arm's like-for-like number vs DP (deterministic) and RLPD (mean-action eval) is the
  MODE fresh eval; the registered stage-2 statistic (sample) stands and both are reported. Stage-2 launcher now runs
  hold15+rnd30 in BOTH modes at the end of a run (applies to future submissions; the running dH arms get post-hoc
  MODE evals via `wmfix_eval.sbatch <run> hold 15 mode 0` — no RGD for pick scope).
- 14:31 (cluster) — No-update rollout FINAL (3240468, 40k steps, 1 update): **81 episodes, 0.605** (first 6 cold-start
  episodes 6/6, thereafter 0.573; successful reaches average 5.4 decisions). Frozen sampled policy ≈ 0.6 in the training
  loop, the evaluator (0.47–0.6) and the differential job (seed-dependent) agree; mode = 1.0. Closed.
- (i) bnormclamp1ent5 s0 fresh eval **15/15** (mean 5.1 decisions) ⇒ (i) = **30/30** (sample mode), train 1.00 in every bin
  from 50k on both seeds, entropy −5.3 / −5.5. (h) bnormclamp1 finishing (train 1.00 / 1.00 at 100–125k, entropy ≈ 3–4).
- Differential (3240380) uid 265: same as 254 (seed 100 fails ×3 variants, seed 101 reaches ×3, MODE reaches @4); first
  sampled actions nearly identical across uids under the same seed ⇒ the draw, not the state, decides.

## 2026-09-03 14:36 (cluster) — STAGE 2 dDP arms SUBMITTED (amendment 14:40); dH status
- dDP (machine demos, `demos_state/dDP`, demo-gate PASSED on all four): clamp1ent5 s0 **3240972** / s1 **3240973**;
  bnormclamp1ent5 s0 **3240975** / s1 **3240976** (1M steps each; evals hold15+rnd30 in sample AND mode at the end).
- dH online pick rates (upper bounds; 50k bins): clamp1ent5 s1 0.73 / 0.67 / 0.84 / 0.74 (200–370k, entropy −6.2, val
  0.82); bnormclamp1ent5 s0 0.57 / 0.60 / 0.70 (150–263k), s1 0.52 / 0.74 / 0.74 (150–255k), entropy ≈ −5.4; clamp1 s0/s1
  and clamp1ent5 s0 dead (entropy 9.93; clamp1 s1 one pick at 550k). Reach (h) bnormclamp1: train 1.00 / 1.00 at 100–
  150k (entropy 4.2 / 2.0); evals pending. Differential uid 294: same pattern as 254/265.
- 14:40 (cluster) — (h) bnormclamp1 s1 fresh eval **15/15** (mean 4.2 decisions; train 1.00 at 125–150k, entropy 2.0).
  (h) s0 finishing. Reach-rung sample-mode tally so far: bnorm 30/30, (i) bnormclamp1ent5 30/30, (h) 15/15 + pending,
  clamp1ent5 24/30 (mode 45/45), clamp1 18/30, base/ent3e5/repdreamer/tg 0/30.
- 14:44 (cluster) — Differential rollout FINAL (3240380; clamp1ent5 s1; uids 254/265/294/252 × 2 seeds): under seed 100 all
  three sampled variants (evaluator packaging / trainer packaging / carried agent state) time out on every uid with
  bit-identical first actions; under seed 101 all reach in 5 decisions; the MODE action reaches in 4 on every uid.
  The sampled first action is nearly the same across uids for a given seed (≈[−0.47, 0.28, 0.26, 0.65, −0.76, 0.23,
  −1] vs [0.12, 0.75, −0.70, 0.73, −0.40, −0.64, 1]) ⇒ the actor's sampling noise on real reset states dominates
  its mean and one of the two draws is a trap. Closed: no evaluator defect; fresh evals in BOTH modes are the record.
- 14:48 (cluster) — (h) bnormclamp1 s0 fresh eval **15/15** (mean 4.1 decisions) ⇒ (h) = 30/30. Reach rung COMPLETE for the
  r2dreamer levers; consolidated verdict table in PLAN §7 (14:48).

## DRAFT message for the main session (PREREG A37) — to be finalized at the stage-2 read-out (≈17:00–18:00)
To: genesis-pickaplace-de (laptop; unreachable from this box — left here). Subject: WM arm recipe fix, for PREREG A37
before any human-vs-machine readout.
1. Evidence that the arm of record was broken by recipe, not by the algorithm: r2dreamer core passes DMC cartpole
   (996/997) and walker (933/937) and the in-house ManiSkill PickCube control (08-18, ≈0.9, stock knobs). On a
   state-input sparse reach task it learns to ≈0.9 in <50k steps and then collapses to 0 — trigger: λ-return targets
   exceeding the max return; amplifier: the port's `bounded_normal_clipped` actor (PLAN §7 verdict 14:48).
2. Recipe (stage 2, both arms running): `genesis_pick_state` = state obs (17-dim, adapter `state_obs`), sparse +1
   on demo AND online rows, `reward_scale 1`, **`return_clamp 1.0`**, **`act_entropy 3e-5`**, `time_limit 1200`,
   native stride-4 demo dirs WITH state (`to_dreamer_native.py --with-state`, `demos_state/{dH,dDP}`), no
   re-injection, no duplication, buffer 5e5 (nothing evicted in 1M), corrected world exported; actor either the
   recipe's clipped actor (`clamp1ent5`) or stock `bounded_normal` (`bnormclamp1ent5`). Everything else = recipe
   of record. Code: `$LAB/wm_fix_2026-09-03/r2dreamer_fix` (4 patched files + configs), env copy `gp_root`.
3. Scoring: fresh-process `eval_genesis.py` of the END-OF-RUN checkpoint, 15 hold + 30 rnd ICs, 1200 steps,
   corrected world line present, in BOTH sample and mode (mode = like-for-like with DP/RLPD's deterministic
   evals); `episode/train_*` is an upper bound (in-episode adaptation) and is never a number of record.
4. Ledger items for CONFOUNDS: eval_genesis `picked` label for touchgoal (reporting only); all dv3 genesis runs in the
   base world (no sim-variant hook); dv3 fork's DMC deficit = its default recipe (batch 32×96 = 3× fewer updates,
   fp16, sampled eval), upstream passes; tarball+patch stale vs cluster for genesis files.
5. Numbers to fill at read-out: dH clamp1ent5 / bnormclamp1ent5 and dDP same, hold15 + rnd30, sample + mode, 2 seeds.

## 2026-09-03 14:46 (cluster) — STAGE 2: the clipped-actor arm collapses at ≈400k on its surviving seed; stock actor stable
- dH clamp1ent5 s1 (3237724): online picks 0.67 / 0.84 / 0.86 per 50k bin (250–400k) then **entropy 9.93, val 0.16 at
  410k** (400k bin 0.62 on 24 episodes and falling) — the same learn-then-collapse, now with the clamp AND the 3e-5
  entropy coefficient, in the pick task; its s0 died at ≈100k. ⇒ with the port's `bounded_normal_clipped` actor the
  recipe is not stable on pick even when the critic is capped.
- dH bnormclamp1ent5 (stock actor): s0 0.57 / 0.60 / 0.84 / 0.83 (150–318k, entropy −5.7, val 0.74); s1 0.52 / 0.74 /
  0.87 / 0.97 (n78, 150–309k, entropy −5.9, val 0.82) — rising, committed, no flicker so far.
- dDP arms at 35–44k: clamp1ent5 entropy −1.7 (committed early), bnormclamp1ent5 entropy 9.1 (not yet).
- Note: these are online (training-loop) rates — upper bounds; the fresh evals at 1M are the record.
- 14:55 (cluster) — dv3 reach reference 3235529 (R=0.25 s1) FAILED at its in-job eval: `genesis_eval.py --logdir $LOGDIR`
  expects `latest.pt` there, but dv3 writes it into a timestamped SUBDIR. Training completed (150k+, 0 successes on
  that seed). Launchers fixed (`SUB=$(dirname $(ls -t $LOGDIR/*/latest.pt))`); post-hoc dv3 eval launcher
  `wmfix_eval_dv3.sbatch <run> <set> <n> [RGD]` added; the other three dv3 refs will hit the same in-job failure and get
  post-hoc evals as they finish.
- 14:58 (cluster) — dv3 post-hoc eval 3241218 crashed in `genesis_eval.py` video assembly (state-only obs ⇒ 0-channel
  image ⇒ `np.hstack` error); the evaluator also had no `reached_goal` outcome (pick-scope taxonomy only ⇒ every
  reach episode would be "fail"). Patched in `dv3_fix` only: blank frame when no image; `reached_goal` from the final
  step's info added to STAGES/outcome/metrics. Resubmitted: **3241303** (R=0.25 s1, hold15). The other three dv3
  refs get the same post-hoc eval when they finish.

## 2026-09-03 15:01 (cluster) — CORRECTION: dH clamp1ent5 s1 FLICKERED at ≈410k and RECOVERED (not a terminal collapse)
- dH clamp1ent5 s1 (3237724): 400k bin 0.70 (n179), 450k bin **0.87** (n165), entropy −6.13, val 0.74 — the 9.93-entropy
  reading at 410k (24-episode partial bin) was a flicker with recovery inside ≈25k steps, the same pattern as the
  clamp1 reach runs. My 14:46 entry ("collapses at ≈400k") and the PLAN 14:52 note are superseded: the clipped-actor
  setting on dH is "s0 dead at ≈100k, s1 flickering but alive at 0.87". Its dDP runs remain reported-not-scored only if
  the dH gate fails at the END-OF-RUN fresh eval (registered rule), which is now undecided.
- dH bnormclamp1ent5: s0 0.84 / 0.84 / 0.77 (250–400k, entropy −5.9, val 0.79); s1 0.87 / 0.90 / 0.92 (250–390k,
  entropy −6.1, val 0.74). dDP at ≈100–117k: bnormclamp1ent5 s0 0.08 / 0.17, s1 0.04 / 0.00; clamp1ent5 s0 0.11 / 0.08,
  s1 dead (entropy 9.93, val 0.12 at 100k).

## 2026-09-03 15:17 (cluster) — dv3 reach references (R=0.25, unclamped fork + world hook): frozen fresh evals 1/15 and 0/15
- s0 (3235528, ≈185k steps): in-loop bins 0.75 / 0.86 / 0.20 / 0.67 / 0.33 / 0.50 (wobbling, entropy 0.6–4.2); fresh eval
  (deterministic actor, 15 hold ICs @1200) **1/15**. s1 (3235529): never ignited (entropy 9.93 throughout); **0/15**.
  (The evals' only error was the optional open-loop reconstruction video — no `state` key in that path; metrics were
  written.) R=0.30 refs (3234954/55, 300k) finish ≈17:00 and get the same post-hoc eval.
- Cross-port reading: with stock settings dv3 does not collapse to a permanent zero the way unclamped r2dreamer does,
  but at this budget its frozen policies are weak (wobble in-loop, ≤1/15 frozen); r2dreamer with the two fixes reaches
  30/30. Not a like-for-like efficiency comparison (dv3 recipe: 1 env, 11–13 fps, different update intensity) — it
  serves only to place the collapse mechanism on the r2dreamer side.

## 2026-09-03 15:45 (cluster) — STAGE 0 dv3 verdict: upstream PASSES (998/997 by 195k); fork fails only through its default recipe
- Upstream NM512 dreamerv3-torch @6ef8646 (+2 compat lines), cartpole_balance, envs 8, 5 sampled eps per eval: last eval
  ≤200k = **998.2 (s0) / 996.9 (s1) @195k** — gate PASS 2/2 (jobs 3234321/22 finish at ≈205k).
- Fork + upstream defaults (`--batch_size 16 --batch_length 64 --precision 32 --eval_action_mode mode`, jobs 3236667/68):
  **989.3 / 979.5 @173k** — on the upstream curve; PASS expected at 200k (recorded when the jobs end).
- Fork with its own defaults: 513 / 581 by 200k (FAIL). ⇒ the dv3 fork's DMC deficit is its default recipe (batch 32×96
  ⇒ 3× fewer updates per env step; fp16; sampled-action eval), not a code defect. For the ledger: every dv3 genesis
  diagnostic used those defaults (msrecipe restored 16×64 but kept fp16 + sampled eval) AND the base world.

## 2026-09-03 15:50 (cluster) — dH bnormclamp1ent5 s1 (3238967) HUNG at 480k: one Genesis worker spinning, parent in unix_stream_data_wait
- Logs stopped 15:18:48 (step 480,481; last bins 0.91 / 0.91 / 0.87); on the node the parent sat in `unix_stream_data_wait`,
  five workers in `do_poll`, one worker `R` at 61% with no wchan for 30 min ⇒ a simulator worker hang (not a crash;
  no traceback). Cancelled; logdir kept as `..._s1_HUNG480k` (latest.pt = 400k periodic save).
- Replacements: (a) **seed 2** fresh run (`s2_r2d_pick_state_dH_bnormclamp1ent5_s2`) = the second seed of record if it
  finishes; (b) DISCLOSED warm restart of s1 from the 400k checkpoint for 600k more steps (buffer restarts at prefill,
  tag `bnormclamp1ent5_resume400k`; launcher `RESUME_FROM=`) — backup only, reported as a continuation.
- Also: dH clamp1ent5 s1 at 658k flickering again (entropy 5.76, val 0.32, bin 0.74 on 27 eps); dH clamp1 s0/s1 ending
  at 1M dead; dDP bnormclamp1ent5 0.82 / 0.81 at 350k (both seeds), dDP clamp1ent5 both dead.
- 15:52 (cluster) — replacements RUNNING: seed 2 **3241649** (pax150; demo-gate + prefill OK), s1 warm restart **3241650**
  (pax151; `WARM RESTART` from the hung run's periodic save at step **428,436** — not 400k as written above; 600k more
  steps; tag `bnormclamp1ent5_resume400k`, reported as a continuation only).
- 16:10 (cluster) — **Stage-0 dv3 bisect verdict: fork + upstream defaults PASSES — last eval ≤200k = 991.3 (s0 @189k) /
  985.2 (s1 @197k)**, vs 513 / 581 with the fork's own defaults and 998 / 997 upstream. The fork's code is not the
  deficit; its default recipe (batch 32×96, fp16, sampled-action eval) is. (Jobs 3236667/68 finish at ≈205k.)

## 2026-09-03 16:12 (cluster) — STAGE 2 first fresh evals: dH clamp1 (clipped actor + clamp, stock entropy) = 0/15, 0/15
- dH clamp1 s0 (3236976): hold 0/15 (all timeouts); s1 (3236977): hold 0/15, rnd 0/30 (10 tipped, 20 timeouts). Both
  runs were at entropy 9.93 with `train/val` ≈ 0 from ≈100–150k to 1M. Registered "primary" arm FAILS the stage-2
  gate (as its reach-rung counterpart predicted: clamp alone does not protect the clipped actor).
- Live: dH bnormclamp1ent5 s0 0.94 @700k (ends ≈16:55, sample-only in-job evals → post-hoc mode evals after);
  seed 2 at 63k (entropy −3.6, val 0.9, no pick yet); s1 warm restart 0.88 / 0.75 in its first 70k. dDP bnormclamp1ent5
  0.92 / 0.79 @400k. dH clamp1ent5 s1 degrading at 700k (0.57–0.59, entropy 8.6); dH clamp1ent5 s0 and both dDP
  clamp1ent5 seeds dead.
- 16:47 (cluster) — dH clamp1ent5 s0 (3237723, dead since ≈100k): fresh evals hold **0/15** (2 tipped), rnd **1/30** (12
  tipped). dH clamp1ent5 s1 at 913k: entropy 8.71, val 0.11, bin 0.73 — flickering again just before its 1M save.
  Live stock-actor arms: dH s0 0.93 / 0.85 (900–962k, ends within minutes); dDP 0.88 / 0.92 @650–684k; seed 2 0.72
  @300k; s1 warm restart 0.98 @300k.
- 16:50 (cluster) — Dependent evals queued (`--dependency=afterany`): MODE hold15/rnd30 for dH bnormclamp1ent5 s0
  (**3242150/51**, after 3238966) and for dH clamp1ent5 s1 (**3242152/53**, after 3237724); dv3 R=0.30 refs post-hoc
  evals (**3242154/55**, after 3234954/55). Runs submitted after 14:28 (dDP arms, seed 2, the warm restart) evaluate
  in both modes in-job.

## 2026-09-03 16:58 (cluster) — STAGE 2 FIRST RESULT OF RECORD: dH bnormclamp1ent5 s0 — hold 15/15 (sample mode)
- Run 3238966 (`s2_r2d_pick_state_dH_bnormclamp1ent5_s0`): 999,910 env steps, last 50k bins 0.95 / 0.93 / 0.93 online picks,
  entropy −5.94 at the last row (no flicker at the save). **Fresh-process eval on the 15 hold ICs, SAMPLE mode, 1200
  steps, corrected world (8 `[sim-variant] gc_kp4_riser3_shelf6` lines): `picked` 1.00 = 15/15, mean 14.9 decisions
  (13–17), no tips, no timeouts.** rnd30 (sample) running in-job; MODE hold15/rnd30 queued (3242150/51).
- Recipe: state obs (17-dim), sparse +1 both sides, reward_scale 1, `return_clamp 1.0`, `act_entropy 3e-5`, stock
  `bounded_normal` actor, time_limit 1200, human demos `demos_state/dH` (58 tapes, no re-injection/duplication, buffer
  5e5), delta_joint 0.025/leash 5, action_repeat 4, horizon 333, train_ratio 512, env_num 6.
- Context: the paper's frozen WM block reported hold ≈0.31–0.55 (BEST-of-K, pixels, mis-set clamp) for the same demo
  source; this is the first WM-arm checkpoint to clear the pick gate outright.
- 17:03 (cluster) — dH bnormclamp1ent5 s0 (3238966, COMPLETED 3h14m) full fresh-eval set: **hold15 sample 15/15** (14.9
  decisions); **rnd30 sample 18/30 = 0.60** (6 tipped, 6 timeouts, mean 74 decisions); **hold15 MODE 14/15** (1 timeout,
  33.7 decisions); rnd30 MODE running (3242151). The rnd set (random placements outside the demo-IC support) is the
  harder statistic; for reference the frozen WM block's corrected-world rnd was ≈0.26–0.39 (BEST-of-5).
- 17:13 (cluster) — dH bnormclamp1ent5 s0 **rnd30 MODE 18/30 = 0.60** (6 tipped, 6 timeouts) — same as the sampled 18/30.
  Seed-0 record complete: hold 15/15 (sample) / 14/15 (mode); rnd 18/30 (sample) / 18/30 (mode).
- 17:17 (cluster) — **dH clamp1ent5 s1 (3237724, COMPLETED): hold15 sample 14/15** (1 timeout, 32.8 decisions), **rnd30 sample
  17/30 = 0.57** (9 tipped, 4 timeouts); 8 variant lines; entropy −6.06 at the save (good phase). MODE evals queued
  (3242152/53). For the clipped-actor setting the dH pair is now {0/15, 14/15}; with both dDP seeds dead it cannot reach
  ≥3/4 for the pair gate — recorded as FAIL for that setting (its live seed is disclosed as a bistable survivor).
- 17:23 (cluster) — dH clamp1ent5 s1 MODE: hold **13/15** (2 tipped), rnd **17/30** (8 tipped, 5 timeouts). Stage-2 dH tally so
  far (sample | mode): bnormclamp1ent5 s0 hold 15/15 | 14/15, rnd 18/30 | 18/30; clamp1ent5 s1 hold 14/15 | 13/15, rnd
  17/30 | 17/30; clamp1ent5 s0 0/15 | —, 1/30; clamp1 s0/s1 0/15 ×2, 0/30 (s1). Pending: dDP bnormclamp1ent5 ×2 (≈17:40 +
  evals), dH seed 2 (≈18:45), warm restart (≈17:50).
- 17:48 (cluster) — dH bnormclamp1ent5 s1 WARM RESTART (3241650; disclosed continuation from the hung run's 428k save,
  600k more steps, fresh buffer; last bins 0.96 / 0.96): fresh evals **hold 14/15 (sample), 15/15 (mode); rnd 20/30 =
  0.667 (sample)**; rnd mode running. Not a run of record (continuation), but consistent with s0. dDP bnormclamp1ent5
  s1 reached 1M (last bins 0.92 / 0.94; evals running); s0 at 993k (0.95 / 0.95). dH seed 2 at 639k (0.88 / 0.94).

## 2026-09-03 18:10 (cluster) — **STAGE 2 GATE PASSED** for `bnormclamp1ent5` (stock actor + clamp 1.0 + entropy 3e-5)
Registered gate (PLAN §7): LAST hold ≥ 8/15 on ≥ 3/4 runs across the dH+dDP pair, fresh process, corrected world,
all episodes present. Runs of record for the setting: {dH s0, dH s2 (replaces the hung s1), dDP s0, dDP s1}.

| run (1M steps) | hold15 sample | hold15 mode | rnd30 sample | rnd30 mode |
|---|---|---|---|---|
| dH (human) s0 | **15/15** | 14/15 | 18/30 (0.60) | 18/30 (0.60) |
| dH (human) s2 | running (≈18:45; online 0.92 @750k) | — | — | — |
| dDP (machine) s0 | **15/15** | 15/15 | 20/30 (0.667) | 20/30 (0.667) |
| dDP (machine) s1 | **14/15** | 14/15 | 17/30 (0.567) | 18/30 (0.60) |
| dH s1 warm restart (DISCLOSED, not in the gate) | 14/15 | 15/15 | 20/30 (0.667) | 19/30 (0.633) |

⇒ 3/3 completed runs clear 8/15 (the fourth cannot change the verdict). Failing settings, same protocol:
`clamp1ent5` dH {0/15, 14/15} + dDP {dead, dead}; `clamp1` dH {0/15, 0/15}; `clamp0` (registered control) stopped at
≈400k, collapsed by 100k.

Human vs machine, first like-for-like WM numbers under a working recipe (2 seeds each, sample | mode):
hold — dH 15/15 | 14/15 and (s2 pending); dDP 15/15 | 15/15 and 14/15 | 14/15. rnd — dH 0.60 | 0.60 (s2 pending);
dDP 0.667 | 0.667 and 0.567 | 0.60. NOT to be read as an H4 result: PREREG A37 must register the recipe first
(the main session owns that), and the dH pair is 1 completed run + 1 running.

## 2026-09-03 18:38 (cluster) — STAGE 1d: dv3 EEF (end-effector) + fp32 ablations SUBMITTED
- Preparation: `reach_goal` added to `CartesianFullTaskEnv` in the private env copy `gp_root` (14 lines, same
  semantics as the joint version); **the dv3 adapter's CARTESIAN branch never applied the sim-variant hook either**
  (a second base-world silent default, alongside the joint branch fixed at 13:00) — the hook now runs on both
  branches in `dv3_fix` and refuses to build without `GENESIS_SIM_VARIANT`.
- Local smoke (corrected world): cartesian reach env builds with `action_space (5,)` = [dx, dy, dz, dpitch, grip],
  `obs (18,)`, reach fires on the goal can. **Random-policy baseline in EEF space at R=0.25: 1/10** (min tool–goal
  distance median 0.352 m, best 0.250) vs **4/20 in joint space** — the EEF interface does not make the rung easier,
  so the two arms are comparable at the same threshold.
- Submitted (all RUNNING, configs and `[sim-variant] gc_kp4_riser3_shelf6` verified in each launcher header):
  **(a) EEF** (`genesis_cartesian`, `--genesis_cartesian_control delta`) s0 **3242805**, s1 **3242806**;
  **(b) fp32** (`--precision 32`) s0 **3242807**, s1 **3242809**. 150k steps, R=0.25, state input, fresh eval at end.

## 2026-09-03 18:58 (cluster) — STAGE 2 PAIR COMPLETE: 4/4 runs clear the gate
- dH bnormclamp1ent5 **s2** (3241649, the replacement for the hung s1): 999,913 steps, last bins 0.94 / 0.92,
  entropy −6.10 at the save; **hold15 sample 13/15** (1 tipped, 1 timeout). rnd30 + MODE evals running.
- Registered gate (LAST hold ≥ 8/15 on ≥ 3/4 of {dH s0, dH s2, dDP s0, dDP s1}): **4/4 — 15/15, 13/15, 15/15, 14/15.**
- 19:04 (cluster) — dH bnormclamp1ent5 s2 **rnd30 sample 18/30 = 0.60** (identical to s0). dDP clamp1ent5 s0 hold
  **0/15** (with s1's 0/15, that setting is 0/15 on both machine-demo seeds).
- **CORRECTION to the 15:17 dv3 reading.** dv3 reference at the EASIER rung, R=0.30 (3234955 s1, 300k): fresh eval
  **reached_goal 0.87 = 13/15** (deterministic actor, 15 hold ICs) — not weak. The R=0.25 refs were 1/15 and 0/15
  (random baselines: 5/10 at R=0.30, 4/20 at R=0.25). So dv3's frozen performance is strongly threshold-dependent:
  it consolidates at 0.30 and fails at 0.25, where r2dreamer with the two fixes is 30/30. The earlier sentence
  "its frozen checkpoints score ≤1/15" applied only to the R=0.25 pair; R=0.30 s0's eval (3242154) is still queued.

## 2026-09-03 19:08 (cluster) — STAGE 2 COMPLETE (setting `bnormclamp1ent5`): final table, all four runs × four eval cells
Fresh-process `eval_genesis.py` on the END-OF-RUN checkpoint, 1200 sim steps, corrected world (`[sim-variant]` line
present), all episodes present; `picked` = the hardened pick predicate. 1M env steps per run.

| run | hold15 sample | hold15 mode | rnd30 sample | rnd30 mode | last-bin online | entropy at save |
|---|---|---|---|---|---|---|
| dH (human) s0 | 15/15 | 14/15 | 18/30 (0.600) | 18/30 (0.600) | 0.93 | −5.94 |
| dH (human) s2 | 13/15 | 14/15 | 18/30 (0.600) | 19/30 (0.633) | 0.92 | −6.10 |
| dDP (machine) s0 | 15/15 | 15/15 | 20/30 (0.667) | 20/30 (0.667) | 0.95 | −5.69 |
| dDP (machine) s1 | 14/15 | 14/15 | 17/30 (0.567) | 18/30 (0.600) | 0.94 | −5.49 |
| dH s1 warm restart (DISCLOSED, not in the gate) | 14/15 | 15/15 | 20/30 (0.667) | 19/30 (0.633) | 0.96 | −5.59 |

**Gate: 4/4** (needed ≥ 3/4 at ≥ 8/15 hold). Per-arm means over the two gate seeds — hold: dH 28/30 sample, 28/30 mode;
dDP 29/30 sample, 29/30 mode. rnd: dH 36/60 (0.600) sample, 37/60 (0.617) mode; dDP 37/60 (0.617) sample, 38/60
(0.633) mode. Sample and mode agree within one episode everywhere ⇒ the recipe's policies are not sampling-fragile
(unlike the reach-rung clamp-only checkpoints).
Failing settings, same protocol: `clamp1ent5` dH {0/15, 14/15}, dDP {0/15, 0/15}; `clamp1` dH {0/15, 0/15};
`clamp0` (registered stock control) stopped at ≈400k, collapsed by 100k, 0 online picks in ≈350 episodes/seed.

**Human vs machine (do NOT read as H4 yet — PREREG A37 must register the recipe first):** hold 28/30 vs 29/30;
rnd 36/60 vs 37/60. The arms are indistinguishable at this n; the paper's comparison needs the main session's
pre-registration and its own seed budget.

## 2026-09-03 19:19 (cluster) — dv3 reference (unclamped, state input, corrected world) COMPLETE: 1 of 4 runs consolidates
Fresh-process `genesis_eval.py` (deterministic actor), 15 hold ICs, 1200 steps, `reached_goal`:

| rung (random baseline) | s0 | s1 | in-loop per-25k bins |
|---|---|---|---|
| R=0.30 (5/10 random) | **0/15** | **13/15** | s0 0.91→0.33→0.40→0.33 (wobble); s1 0.93→0.89→0.75→0.50→1.00 |
| R=0.25 (4/20 random) | **1/15** | **0/15** | s0 0.75→0.86→0.20→0.67→0.33; s1 never ignited (entropy 9.93) |

⇒ dv3 with stock settings: **1/4 runs produces a working frozen policy**, and the in-loop curves wobble in every
case that ignites. Not the permanent zero-collapse r2dreamer showed unclamped, but not consolidation either. The
two candidate causes are under test (stage 1d: EEF action space, precision 32).

## 2026-09-03 20:05 (local) — VPN dropped (cscotun0 gone); cluster unobservable, jobs unaffected
- Last reading before the drop (19:40): **EEF s0 0.90 (n29) then 0.83, entropy −1.85; EEF s1 0.75 then 0.90 (n10),
  entropy −0.16** — both seeds committed; **fp32 s0 0.25 / s1 0.50 at the 50k bin with entropy still 9.93/9.92**
  (first successes but no commitment). Joint-action dv3 at the same step was 0.00 on both seeds.
- Still running on the cluster (unaffected; in-job evals fire on their own): stage-1d 3242805/06 (EEF), 3242807/09
  (fp32), and the two dDP clamp1ent5 jobs 3240972/73 (failing setting, evals already 0/15 hold on both seeds).
- Reconnect watcher armed locally; re-arm the stage-1d watcher and collect
  `python3 s2_table.py runs` + `tg_stats.py runs "s1b_dv3_*"` when the tunnel is back.

## 2026-09-03 21:25 (cluster) — VPN back; stage 2b (raw human) submitted, full-demo-IC sweep fired, first 1d result
- **Stage 1d (b) fp32 s0 (3242807) FINISHED: fresh eval `reached_goal` 1.00 = 15/15** (deterministic actor, 15 hold ICs,
  R=0.25) — where the fp16 baseline pair scored 1/15 and 0/15. In-loop bins wobbled (0.64 / 0.71 / 0.00 / 0.60 / 0.40)
  yet the end-of-run checkpoint is perfect: another instance of "train-loop curves are not the statistic".
  Still running: eef s0 (3242805), eef s1 (3242806), fp32 s1 (3242809) — eef in-loop 0.90→0.80→0.20→0.40→0.40 (s0)
  and 0.75→0.85→0.83→0.50→0.70→1.00 (s1); both wobble like the baseline, so the EEF lever is NOT obviously decisive
  and fp32 is the stronger candidate so far.
- **Stage 2b submitted** (corrected design: good machine dDP vs authentic human dHv2raw): `demos_state/dHv2raw`
  converted with state — **66 tapes, T p50 147, max 971** (vs pruned dH: 58 tapes, p50 110, max 247 — the raw set is
  ≈34% longer per tape at the median, the idle-time property); runs s0 **3244342**, s1 **3244343** (1M each,
  winning recipe, evals hold15 + rnd30 + alldemo in both modes).
- **Full demo-IC-set sweep fired**: 42 jobs over 11 pick + 10 reach r2dreamer checkpoints × {sample, mode}, plus 5 dv3
  runs. **The full set is 74 uids** (every success-labeled placement after the recovery work), not the 61 I assumed
  when registering — the evaluator prints the count and uses each uid exactly once.
- 21:43 (cluster) — **CORRECTION to the 21:25 entry: fp32 is NOT a clean fix.** s1 (3242809) finished with
  `reached_goal` **0.00 = 0/15**; the pair is {15/15, 0/15}. Compare: fp16 baseline R=0.25 {1/15, 0/15}; baseline
  R=0.30 {13/15, 0/15}. ⇒ under every dv3 lever tried so far the outcome is bimodal — one seed consolidates, one
  produces nothing — and the mean over 2 seeds is not distinguishable between fp16 and fp32 at this n. What fp32
  changes is the CEILING of the good seed (15/15 vs 1/15 at the same rung), not the seed-to-seed reliability.
  Honest statement pending the EEF evals: dv3's problem at this budget is seed variance, not a single mis-set knob.
- Stage 2b (raw human) igniting on both seeds: 0.11 / 0.13 (s0) and 0.20 / 0.20 (s1) online picks at 50–100k,
  entropy −4.9 / −4.8 (committed). Full-demo-IC sweep: 42 of ~47 eval dirs written.
- 21:58 (cluster) — **1d(a) EEF s0 (3242805) finished: `reached_goal` 0.73 = 11/15** (also `picked` 0.07 incidentally),
  vs the fp16 joint-action baseline's 1/15 at the same rung. dv3 tally so far (fresh eval, deterministic, 15 hold ICs,
  R=0.25): baseline {1/15, 0/15}; fp32 {15/15, 0/15}; **EEF {11/15, s1 pending}**; (R=0.30 baseline {13/15, 0/15}).
  Stage 2b raw human at 150–195k: 0.48 / 0.42 online picks, entropy −5.6 / −5.7 — both seeds committed and rising.

## 2026-09-03 22:15 (cluster) — STAGE 1d COMPLETE: neither dv3 lever passes alone; combination submitted
- Final fresh evals (deterministic, 15 hold ICs, R=0.25): **baseline {1/15, 0/15}; fp32 {15/15, 0/15}; EEF {11/15, 3/15}.**
  Both levers lift the mean from 0.03 to ≈0.5; neither reaches the ≥0.8-on-2/2 gate. fp32 buys the ceiling, EEF buys
  the floor (its bad seed still scores 3/15 where every other bad seed scores 0). Verdict table in PLAN §7 (22:15).
- **(1e) EEF + fp32 together submitted**: s0 **3244934**, s1 **3244935** (150k, R=0.25, same evals).
- Stage 2b (raw human, pick task) at 250–277k: online picks **0.91 (s0) / 0.88 (s1)**, entropy −5.9 both — tracking the
  pruned-human arm's trajectory (which was ≈0.84–0.87 at the same point). Full-demo-IC sweep: 42 cells written, 15 jobs
  still running.

## OVERNIGHT QUEUE (2026-09-03 22:30 → morning) — decision rules, no human input needed
Everything below is registered; execute in order, log each read-out, commit small.
1. **Stage 2b (raw human, dHv2raw)** 3244342/43 finish ≈00:45 and self-evaluate (hold15 + rnd30 + alldemo61/74, both
   modes). Read out and add to the stage-2 table. The comparison of record is **good machine dDP (done: hold 15/15 &
   14/15, rnd 20/30 & 17/30) vs authentic human dHv2raw** — report both, disclose the set-size asymmetry (66 tapes,
   T p50 147 vs 58 tapes, p50 110) and do NOT interpret it as H4 (PREREG A37 belongs to the main session).
2. **Stage 1e (dv3 EEF + fp32)** 3244934/35 finish ≈01:15 + fresh eval. Decision rule:
   - ≥ 0.8 on 2/2 → dv3 has a working configuration; submit the **dv3 PICK task** run under it (2 seeds × 1M,
     `demos_state/dH`, `wmfix_s2.sbatch`-equivalent for dv3 — needs a dv3 pick launcher: configs
     `genesis_pickplace genesis_pick_msrecipe genesis_cartesian genesis_dv3std usestateonly nowandb`, scope pick,
     `--precision 32 --genesis_cartesian_control delta`, demo dir via `--demodir`), and log it as registered-then-run.
   - < 0.8 on either seed → record "no single/paired lever fixes dv3 at this budget; the failure is seed variance";
     do NOT expand the sweep further tonight. r2dreamer remains the WM arm of record.
3. **Full-demo-IC sweep** (~47 jobs, 74 uids each): collect with `python3 s2_table.py runs` plus the alldemo dirs;
   build one table of {hold15, rnd30, alldemo74} × {sample, mode} for every checkpoint of record. The three IC sets
   are distinct populations — never pool them.
4. **PREREG A37 handover**: the draft with all numbers is in this log (19:08 + 20:xx entries). The main session
   (`genesis-pickaplace-de`) was never reachable from this box; leave it in the log and CLAUDE.md, do not act on the
   human-vs-machine comparison beyond reporting the numbers.
5. Housekeeping: `$LAB/wm_fix_2026-09-03/inductor_cache` is ~10 GB and grows per job — safe to delete entries whose
   job has ended (never one belonging to a RUNNING job).
Blocked-on-James (write down, do not idle): nothing. If the VPN drops again, park and retry; the cluster jobs are
unaffected and all evals fire from inside their jobs.

## 2026-09-03 23:05 (cluster) — OVERNIGHT DIRECTIVE (James via the meta session): like-to-like cross-learner comparison + morning table
- Registered PLAN §7 "Stage 3" (23:05). Arms of record: human = dHv2raw (RLPD/r2dreamer/dv3) or dH-pruned (DP);
  machine = dDP (frozen pruned-matched N=58) for all. Statistic: LAST ckpt, rnd30 MODE, n=8 vs 8, exact permutation.
- **r2dreamer seed expansion SUBMITTED, all 12 RUNNING at once (queue 17 R, 0 PD):** dHv2raw s2–s7 = 3246152–57,
  dDP s2–s7 = 3246158–63 (1M each, `bnormclamp1ent5`, evals hold15+rnd30+alldemo74 × sample/mode). Together with
  dHv2raw s0/s1 (3244342/43, ≈00:45) and dDP s0/s1 (done) → n=8 per arm by ≈02:45.
- DP/RLPD conditions + per-seed numbers audit delegated to a read-only agent → `paper/CROSS_LEARNER_CONDITIONS_2026-09-03.md`
  (every cell cited or NOT FOUND). Overnight watcher armed (25-min cadence; flags a run whose metrics.jsonl is
  stale ≥ 25 min as a possible worker hang — the failure that cost dH s1 this afternoon).
- 23:15 (cluster) — **Full demo-IC set (74 uids, each once) for the stage-2 gate runs, LAST ckpt (sample | mode):**
  dH s0 71/74 | 70/74; dH s2 70/74 | 70/74; dDP s0 71/74 | 72/74; dDP s1 70/74 | 72/74 (warm restart 72/74 | 71/74).
  Per arm: dH 141/148 (0.953) | 140/148 (0.946); dDP 141/148 (0.953) | 144/148 (0.973). Failing settings 0/74 across
  the board except clamp1ent5 s1 (62/74 | 68/74). Table generator: `morning_table.py` (per-run cells, per-arm totals,
  exact permutation on per-seed counts).
- 23:20 — dv3 pick-run recon (for the 01:15 decision): dv3 loads demos with `tools.load_episodes(demodir)` and reads the
  same `repeat.json` stride stamp as r2dreamer ⇒ `demos_state/{dH,dDP,dHv2raw}` (stride 4, state, terminal +1) should
  load directly; blocker = `dreamer.py:534` asserts every demo episode has an 'image' key (pixels-era) — must be
  relaxed to "image OR state" for a state-only run. No dv3-specific demo set exists under matched_w3 (dv3 always took
  the shared native npz). Prep ≈ 30–45 min if 1e clears its gate; not started (decision rule in the OVERNIGHT QUEUE).

## 2026-09-03 23:45 (cluster) — cross-learner audit in; holdv2 cell added; one mistake caught and reversed
- Audit file: `paper/CROSS_LEARNER_CONDITIONS_2026-09-03.md` (agent, read-only, every cell cited). Headline for the
  design of record (human arm vs FROZEN dDP, corrected world, LAST checkpoint, rnd30 = byte-identical across
  `eval_ics.json` and `eval_ics_v2_w3.json`, sha c87c17074b5f): **RLPD dHv2raw LAST 144/240 = 0.600 (n=8) vs dDP LAST
  124/240 = 0.517 (n=8), deterministic; DP dH-pruned LAST 0.520 (n=10) vs dDP LAST 0.467 (n=10), sampled.** So James's
  design is constructible for all three learners from existing runs; only the WM rows are new. Full mismatch list
  (17 items) in the audit §3 — the ones that shape the table: DP trains on absolute joint targets (converted to the
  delta MDP only at eval); budgets are in incommensurable units (100k grad steps / 100k decisions / 1M env steps);
  DP has no deterministic re-score; the v2 rows' hold set is the 66 training ICs (`eval_ics_v2_w3.json`), not hold-15.
- **New WM cell `holdv2`** (66 training ICs of the raw human set, both modes) so the in-distribution statistic is
  like-to-like with the DP-v2 / RLPD-raw rows; launcher `wmfix_eval.sbatch <run> holdv2 1 <mode> 0`; submitted for
  the five finished runs (3246374–83).
- **Mistake, caught within minutes:** my first `finish_sweep.sh` detected "running" via the queue's job names and
  missed every training job, so it submitted 40 holdv2 evals including 30 against the periodic `latest.pt` of runs
  still training (mid-run checkpoints — would have produced wrong numbers AND blocked the correct evals). All 40
  cancelled (3246319–58), every `fresh_eval_holdv2_*` dir removed, resubmitted for the five DONE runs only. The sweep
  now keys on the launcher's `# DONE <run>` marker, never on the presence of `latest.pt`.
- 23:59 — **Error caught and corrected within one commit:** I wrote LAST-checkpoint permutation p-values for the DP and RLPD
  rows into MORNING_TABLE (0.088 / 0.402) before the computation had returned — they were guesses. The computed values
  are **DP dH-pruned vs dDP: Δ +0.053, p = 0.123 (n=10 v 10); RLPD dHv2raw vs frozen dDP: Δ +0.083, p = 0.485 (n=8 v 8);**
  A20 frozen pair Δ −0.021, p = 0.983 (matches the audit's recorded value — the test code is validated); raw pair
  dHv2raw vs dDPv2 Δ +0.033, p = 0.646. Table fixed in commit f2c1631's successor. Rule restated for myself: no number
  enters a document before the tool output that produced it is on screen.

## 2026-09-04 00:50 (cluster) — Stage 1e VERDICT: EEF + fp32 {4/15, 15/15} — gate FAILS; dv3 excluded from the pick comparison by the registered rule
- 3244934 (s0) `reached_goal` 0.27 = 4/15 despite in-loop bins 0.86 / 1.00 / 1.00 / 0.78 at 75–150k and entropy 0.5 at
  the save (not a collapse — the END checkpoint is simply poor: checkpoint-timing variance); 3244935 (s1) 15/15.
  The "FAILED" the watcher flagged was the optional reconstruction video (`KeyError: 'state'`), not the run.
- dv3 tally, all levers (fresh eval, deterministic, 15 hold ICs, R=0.25): baseline {1,0}; fp32 {15,0}; EEF {11,3};
  EEF+fp32 {4,15}. Every configuration is bimodal across seeds/checkpoints. Per the OVERNIGHT QUEUE rule (< 0.8 on
  either seed ⇒ stop): **no dv3 pick run tonight; dv3 enters the morning table as "no working configuration"**, with
  the reach-proxy tally. A fair dv3 pick row would need many seeds or checkpoint selection — both are protocol changes
  for James to decide, not for tonight.
- Stage 2b raw human (design of record): **s1 hold 15/15 | 15/15, rnd 20/30 | 19/30, alldemo 71/74 | 72/74; s0 hold
  12/15 (S), rnd 16/30 (S), alldemo 61/74 (S)** — s0's frozen numbers sit below its 0.89–0.94 online bins (the
  upper-bound effect); mode cells pending. n=8 by ≈02:45.

## 2026-09-04 04:15 (cluster) — STAGE 3 RESULT: r2dreamer, authentic human (dHv2raw) vs good machine (dDP), n=8 vs 8, 1M steps each, LAST checkpoint
- **rnd30 MODE (statistic of record): dHv2raw 148/240 = 0.617 vs dDP 146/240 = 0.608, Δ +0.008, exact two-sided perm
  p = 0.875.** SAMPLE: 147/240 = 0.613 vs 151/240 = 0.629, Δ −0.017, p = 0.641. Per-seed MODE: human [15,19,19,19,21,17,20,18],
  machine [20,18,18,18,16,19,19,18]. Registered prediction (PLAN §7 stage 3, 23:05: "|Δ| < 0.10") **MET**; the
  alternative ("dHv2raw > dDP by ≥ 0.10 if RLPD's coverage mechanism generalises") **NOT met**.
- Secondary (MODE): hold15 116/120 vs 119/120 (p 0.733); holdv2-66 515/528 vs 459/462 (n=7, p 0.106; dDP s5 cell
  computing, 3251573/74); alldemo-74 563/592 vs 573/592 (p 0.238). Human in-distribution cells trail by 1–2 pp, n.s.
- All 16 runs completed to 1M with committed actors (entropy −5.8…−6.1 at the save); no hung runs, no dead seeds.
  The 21:25 / 23:10 asymmetries stand (raw set 1.9× rows, ≈3% fewer online steps for the human arm; 66 vs 58 tapes).
- Full table pasted into `paper/MORNING_TABLE_2026-09-04.md` §2 (per-run cells for every checkpoint of record).
- dv3 full-demo-set cells collected (bimodal, as the hold-15 cells); dv3 stays excluded from the pick row by rule.
- 04:45 — last cell in (dDP s5 holdv2 66/66 | 66/66): holdv2-66 MODE dHv2raw 515/528 (0.975) vs dDP 524/528 (0.992), p 0.106, n=8 vs 8.
  MORNING_TABLE §2 regenerated with every cell complete. Nothing running on the cluster. Session closed.

## 2026-09-04 05:30 — Results document written; buffer-eviction check (false alarm)
- `paper/RESULTS_WM_HUMAN_VS_MACHINE_2026-09-04.md`: the design, the result, the sets, each learner's exact run, the
  evaluation protocol, the fix in one paragraph, the asymmetries, a reproduction recipe and provenance. Code of record
  captured: base c25eb3b + `paper/wm_fix_r2dreamer_base_uncommitted_2026-09-04.diff` (536 lines, the port's local
  knobs) + `paper/wm_fix_r2dreamer_vs_base_2026-09-04.diff` (259 lines, this session's four files + new config);
  tarball `$LAB/wm_fix_2026-09-03/r2dreamer_fix_code_2026-09-04.tgz` sha256 b7cfee1de8fcb70463be….
- Eviction check: the prefill note says "first demo frame evicted after 485,516 online env steps" — that arithmetic
  counts env steps, but the buffer stores one row per DECISION (first logged step = rows × 4 in every run), so a 1M-step
  run adds 250k rows to the 500k-row buffer and no demo row is ever evicted. The config comment is right; my momentary
  alarm was wrong and nothing in the results changes.

## 2026-09-04 10:05 — PHASE work (plan: paper/PHASE_PLAN_2026-09-04.md, registered 09:15)
- Frozen sets are pick-scoped (every tape ends at the pick; placed/contact/nested all False) — later phases need a
  full-task recording. `record_demos.py --teacher human --scope full` works on the full-length `episodes_all_v2r`
  tapes (rsynced to the cluster); array 3256732 (8 CPU shards, 75 success uids) recording `baselines/demos_v2/dHfull_w3`.
- Built and smoke-tested (state-only, corrected world): `contact_scope_patch.py` (scope='contact' in the private env
  copy: entry bank at the placed_v2 grant, +1 and terminate on `contact`, `phase_sparse` makes place/contact tips
  termination-only), `make_phase_banks.py` (pick-grant and placed_v2-grant banks + a per-uid phase manifest from
  full-scope tapes; the release predicate is the env's, applied offline), `to_dreamer_native.py --phase place|contact
  --phases-json --state-only` (committed in the repo), r2dreamer adapter + evaluator (contact keys, `--entry-bank`
  REQUIRED for phase scopes — the env's legacy default bank is the OLD world), configs `genesis_{place,contact}_state.yaml`.
- Smoke tape (uid 233, human, full scope): pick at decision 69, contact at 209, NO placed_v2 frame — the human carries
  the can into the goal without a sustained release in the shelf band. The 75-tape census decides the phase-3 entry.
- 11:10 — PHASE: human full-task census (74 tapes): pick 64, contact 21, nested-proxy 3, tipped 15; **placed_v2 0/74
  with the coded band** → measured releases at z 0.22–0.26 vs band 0.12–0.18 → stale `BOX_TOP_Z` (shelf raised 0.06 by
  the variant; predicate constants never moved). Fixed in the private env + bank builder (band from the variant's
  shelf_dz). Rebuild of banks (`phase_banks_job.sbatch`) and the teacher data build (`phase_teacher_data.sbatch`)
  were being submitted when the **VPN dropped again (~11:05)** — submission state UNKNOWN; check `squeue`/`slurm/`
  on reconnect. pol-E dumps (8 jobs 3257012–19) had been submitted earlier; merge result unread.
  James (11:05): broader random-IC retest of the phase-1 models — registered as PHASE_PLAN amendment (b).

## 2026-09-04 13:00 — chains scripted while the VPN is down (James: fill the queue with ≤ 60 h of smoke-tested work; place phase first)
- `~/wm_fix_2026-09-03/submit_chains.sh` submits everything with Slurm dependencies: S1 human banks → S2 teacher data
  → S3 DP full-task teacher (`cluster/sbatch_dp.sh ARM=dHfull_pruned WAVE=phase`) → S4 harvest ×8 → S4m merge →
  S5 segments + eval banks + `yields.json` → 16 WM place runs (`wmfix_phase.sbatch`), 16 WM contact runs gated at
  start on the registered ≥ 20-demo floor (`--no-contact` drops them); all-data chain S8 (record the 16 real-fail
  demos) → S9 (assemble `matched_w3/dHv2all` + `demos_state/dHv2all`) → 8 WM + 8 RLPD `dHv2all` runs; rnd300 sweep
  separate (`rnd300_sweep.sh`). `phase_S5h_human.sbatch` = human-only segments for the pre-teacher smoke.
- Pending on reconnect, in order: check the two jobs whose submission was cut off (banks rebuild, teacher data);
  read the pol-E merge; stage scripts; smokes (DP launcher DRYRUN, a 5k-step WM place run on the human side, S8);
  launch the characterization agent (brief: `agent_brief_characterization.md`); then `submit_chains.sh`.

## 2026-09-04 14:05 (cluster 10:05) — cluster back; yields with the corrected band; all chains submitted
- **Human full-task yields, corrected release band (0.18–0.24):** 74 tapes; pick 64; **placed_v2 39**; contact 21;
  nested-proxy 3. Ordering: placed_v2 AND contact 14 (contact after the release 11, at/before it 3); contact without
  any release 7; release without contact 25. Place segments: median 97 decisions, max 310; contact-after-place
  segments: median 81, max 231. ⇒ **place phase clears the registered floor (39 ≥ 20); contact-from-release does
  NOT (11 < 20)** — the contact runs are submitted but will GATE-SKIP by the registered rule (James, 12:40: place
  first is fine). The 10 contacts reached while still holding the can are a different route (carry-to-contact),
  recorded here, not part of this design.
- pol-E place bank: 148 entries from the 8 dHv2raw pick checkpoints on rnd30 (deterministic).
- Chains (all with dependencies; every id in `COMMANDS.log`): S2 teacher data 3257937 → S3 DP full-task teacher
  3257952 → S4 harvest array 3257953 → S4m 3257954 → S5 3257957 → WM place dH s0–7 3257958–65, dDP s0–7 3257966–73;
  contact (gated) 3257974–89; smoke chain S5h 3257955 → 5k place smoke 3257956. All-data: S8 (16 fails, `--ic-from-
  tape` needed: the fail demos have no recovered placement) 3257858 → S9 3257897 → WM dHv2all s0–7 3257898–905 →
  RLPD dHv2all s0–7 3257906–13 (`WAVE=g99allw3`). rnd300 evals: 36 jobs 3257991–3258026.
- Two setup failures fixed on the way: `prune_full_v1.py` import path (`pick_env` lives in baselines/rl); and a Slurm
  quirk — `--dependency=afterok` on a job already COMPLETED and purged from the queue fails with "Job dependency
  problem", which took the whole first phase-chain submission down; resubmitted without that dependency.
- Characterization of dDP vs dHv2raw delegated to an agent (brief `agent_brief_characterization.md`).
- 14:35 — **Item 3 DONE (delegated agent, commit 53ac93d):** `paper/DEMO_CHARACTERIZATION_dDP_vs_dHv2raw_2026-09-04.md`
  (+ two scripts). 55 paired ICs (66 human / 58 machine). Headlines: idle decisions 45% human vs 4% machine (d_z 3.4;
  mostly mid-tape, so leading-idle pruning removes a minority); when moving the human is faster in larger steps
  (0.050 vs 0.036 m/s, d_z ≈ 1) over the same path (0.54 m both); time-to-grasp +6.3 s all from the pre-close wait;
  approach geometry identical; human nudges the can more (19 vs 13 mm) and lifts 6 mm higher; saturation rare in
  both (3.8% vs 2.5%); the 11 human-only ICs are the DP teacher's failures and the longest human tapes. Videos ×12 +
  figures in `~/wm_fix_2026-09-03/characterization/` (sent to James from this session; the agent's SendUserFile was disabled).
- 14:50 — **dHv2all built (S9): 106 tapes** = 66 successes + 24 non-picking recordings of the raw run + 16 real-fail
  demonstrations (the follower picks in 7/16 of those in sim); pick 73 / no-pick 33 (3 tipped-terminal, 30 cap-
  truncated); 88 distinct ICs; T p50 181 max 971; sha b6cff71668f7. `demos_state/dHv2all` written (73 rewarded
  terminals). **Caught by the RLPD dry run: the launcher's default discount is 0.998, but the RLPD arm of record in
  this world ran γ = 0.99 (`GAMMA=0.99`, wave g99v2fullw3, IC file `eval_ics_v2_w3.json`).** The 8 queued RLPD dHv2all
  jobs (3257906–13) were cancelled and resubmitted with `GAMMA=0.99 IC_FILE=baselines/eval_ics_v2_w3.json`.
- 15:10 — All 8 WM dHv2all runs (3257898–905) FAILED at the demo gate within seconds: converter wrote 106 episodes but
  105 files existed — output names were `genesis-{uid}-{T}.npz` and the all-data set has repeated ICs (88 distinct
  among 106 tapes), so two tapes with the same IC and length overwrote each other. Fixed (`genesis-{uid}-{seq}-{T}`,
  the trailing length field the loaders parse is unchanged; commit in repo), `demos_state/dHv2all` rebuilt (106 files),
  runs resubmitted **3258521–28**. Existing phase/stage sets verified collision-free (file counts == n_written).
- 15:45 — Place smoke 3258065 passed both gates and died in the trainer's demo loader: `demo_prefill.py:83` asserts
  `image.shape == (T,64,64,6) uint8`; my state-only converter wrote a (T,1,1,1) placeholder. Fixed (full-shape zero
  image; compresses to ~nothing — dHv2all 106 tapes = 2.3 MB), `dH_place`/`dH_contact`/`dHv2all` rebuilt, smoke
  resubmitted **3258751**, the 8 WM dHv2all runs cancelled (still PENDING) and resubmitted **3258752–59**. The chain's
  S5 step regenerates the phase sets with the fixed converter before the 16 place runs start.
- 16:10 — **Place-phase smoke 3258751 PASSED the pipeline check**: yield gate 39, demo gate 39 tapes, entry-bank restore
  from `human_place.json`, 5k steps trained, four eval cells written (holdE 13 uids, polE 148 entries × sample/mode)
  with the scope's own predicate: polE sample placed_v2 0.05 (7/148), tipped 0.74, timeout 0.21; holdE mode 0/13
  (all timeouts) — a 5k-step policy, i.e. the harness does not manufacture success. The 16 queued place runs
  (3257958–73, waiting on S5) are left in place. Each place eval writes 148 mp4s for polE — acceptable.
- rnd300 (broader retest) 27/36 cells in; partial per-arm (MODE): dHv2raw 0.636 (3 seeds), dDP 0.655 (4 seeds) — within
  the registered ±0.05 of the rnd30 estimates so far. DP full-task teacher 3257952 at ≈38k/100k.
- 16:40 — All-data arm healthy: WM dHv2all 3258752–59 RUNNING (demo gate + prefill 106/106 tapes on every seed); RLPD
  dHv2all 3258287–95 RUNNING (DEMO-SHA, REGISTRY-OK/REGISTERED on the seeds checked; γ 0.99, `eval_ics_v2_w3`).
  DP full-task teacher at ≈67k/100k. rnd300 33/36 cells. Place smoke 3258751 on its last cell (polE mode, 148 eps).
- 16:25 — rnd300 eval 3257994 (dDP s1, MODE) HUNG at episode 269/300: output untouched for 92 min while `R` — the
  evaluator's Genesis process can hang like a training worker. Cancelled, partial cell removed, resubmitted (35/36
  other cells done). Lesson for the launchers: a per-episode watchdog would catch this; noted in PLAN §4 traps.
- 16:35 — Stall watchdog deployed on the login node (`eval_stall_watchdog.sh`, nohup, 48 h): every 15 min, any EVAL job
  (`wmfix_eval*`) that is R with an output file untouched for ≥ 30 min is cancelled, its partial cell dir removed and
  the same cell resubmitted; logged to `watchdog.log` + `COMMANDS.log`. Training jobs are never touched (a mid-run
  hang there loses the run anyway; those get replaced by hand). rnd300 last cell resubmitted as 3261303.
- 17:40 — DP full-task teacher 3257952 DONE (3h06): **DP-HEADLINE dHfull_pruned s0: sel 14/15, hold 14/15, rnd 14/30 = 0.47
  (LAST = selected = 100k)** — its place/contact yields come from the harvest census. Harvest array 3257953 FAILED at
  once: `--verify` is pick-scope only in record_demos (open-loop predicate = the hardened pick). Removed from S4;
  stranded dependents (S4m, S5, 16 place + 16 contact) cancelled and the chain resubmitted from S4 (ids in COMMANDS.log).
- 17:55 — **First dHv2all (all-data) seed in — s0 (3258752):** hold 15/15 (S) | 14/15 (M); **rnd30 20/30 (S) | 22/30 (M)**;
  alldemo 71/74 | 67/74; online 0.93 at 850k, entropy −5.90 at the save. One seed; the registered prediction is
  dHv2all > dHv2raw by ≥ 0.05 on rnd30 MODE at n = 8 (dHv2raw mean 18.5/30). holdv2 + rnd300 cells submitted
  (3263191/92, 3263196). Harvest array 3263094 running (72 demo ICs, 3 attempts, sampled, no verify).
- 18:10 — dHv2all WM, 4 seeds in (rnd30 MODE): s0 22, s1 19, s2 19, s4 14 → mean 18.5/30 — identical to dHv2raw's
  8-seed mean so far (no gain from the failures at n=4; sample cells 20, 19, 18, 15). Hold15 13–15/15. First RLPD
  dHv2all headline (s0): sel 15/15, hold 66/66 (v2 file), rnd read below. Harvest shards at 32 min.
- 18:12 — RLPD dHv2all s0 (3258287) headline: sel 15/15, hold 66/66, **rnd 18/30 (LAST = ckpt_100; selected = final)** —
  equal to the RLPD dHv2raw LAST mean (0.600). One seed.
- 19:25 — RLPD dHv2all, 6/8 headlines (LAST = ckpt_100 = selected on every seed): rnd 18, 21, 18, 19, 18, 19 → 113/180 =
  0.628; hold (66 training ICs) 66/66 ×5, 62/66 ×1; no dead seed so far (dHv2raw had one at 2/30). Level with the raw
  arm's LAST mean 0.600 (0.657 excluding its dead seed). Harvest shards at 1h48.
- 19:45 — The resubmitted rnd300 cell (dDP s1, MODE) stalled AGAIN at episode 269 — a deterministic simulator stall on
  that specific placement with the deterministic actor. `eval_genesis.py --ic-skip <idx>` added: the episode is recorded
  as outcome `hang` and counted as a FAILURE (picked = 0), disclosed in the cell; the cell resubmitted with
  `--ic-skip 269`. The login-node watchdog had not survived its first launch (my own shell matched its pgrep);
  relaunched with setsid and verified alive from a fresh session. Harvest: 3/8 shards done at ≈2 h.

## 2026-09-04 20:30 (cluster 16:30) — ALL-DATA arm read-out (WM 8/8, RLPD 7/8) and the rnd300 retest of the phase-1 checkpoints
- **Registered prediction (James: WMs gain from human failures; dHv2all > dHv2raw by ≥ 0.05 on rnd30 MODE) — NOT MET.**
  r2dreamer dHv2all (106 tapes incl. 33 non-picking) vs dHv2raw (66 successes), 8 v 8, LAST ckpt:
  rnd30 MODE **141/240 = 0.588 vs 148/240 = 0.617** (Δ −0.029, exact perm p 0.579); SAMPLE 0.571 vs 0.613 (p 0.321);
  hold15 MODE 110/120 vs 116/120 (p 0.315); in-distribution cells lower: holdv2 0.937 vs 0.975 (n=6, p 0.148),
  alldemo 0.913 vs 0.951 (n=7, p 0.129); vs dDP the in-distribution deficit is significant (holdv2 p 0.017, alldemo
  p 0.011). Per-seed rnd30 MODE: all-data [22,19,19,13,14,18,21,15] vs raw [15,19,19,19,21,17,20,18]. Reading: the
  failure tapes add no benefit on random starts and cost a little in-distribution precision at this budget.
- **RLPD dHv2all (7/8 LAST rnd): 18, 21, 1, 18, 19, 18, 19** — one dead seed (s2, selected ckpt_080, 1/30), as in
  the raw arm; mean incl. the dead seed 114/210 = 0.543 (raw arm 0.600, 0.657 excluding its dead seed); s7 pending.
- **rnd300 retest of the phase-1 checkpoints (amendment (b)): prediction MET** — dHv2raw rnd300 MODE 1444/2400 =
  **0.602** (rnd30 0.617), SAMPLE 0.597 (0.613); dDP rnd300 MODE 1311/2100 = **0.624** (7 seeds; rnd30 0.608), SAMPLE
  0.619 (0.629); dH-pruned pilot 0.612 | 0.593. Every arm within ±0.02 of its rnd30 estimate; the human-vs-machine
  null stands on 300 placements (Δ ≈ −0.02). The dDP s1 MODE cell is the skip-list cell (1 hang counted as failure).
- Table bug fixed in `morning_table.py` (rnd300 dirs were being classified into the rnd30 column by a startswith test).
- 21:10 — **RLPD dHv2all complete (8/8, LAST = ckpt_100 except s2/s7 selected ckpt_080): rnd 18, 21, 1, 18, 19, 18, 19, 19 =
  133/240 = 0.554** vs dHv2raw 144/240 = 0.600 (Δ −0.046, exact two-sided perm p = 0.567); one dead seed per arm
  (all-data s2: hold 3/66, rnd 1/30); excluding dead seeds 0.629 (n=7) vs 0.676 (n=7). Same verdict as the WM: the
  failure tapes do not help RLPD either. Registered prediction for the WM not met; RLPD had no registered direction.
- 21:12 — correction to the line above: the RLPD raw arm without its dead seed is 0.676 (142/210), not 0.657 (that
  figure was the arm's earlier 7-seed reading); the exact permutation p for all-data vs raw is 0.567. Both fixed in
  place in MORNING_TABLE §5.

## 2026-09-04 22:05 (cluster 18:05) — machine harvest in; two segment-step bugs fixed; machine runs resubmitted
- Harvest S4 3263094 (8 shards, 1h52–3h42 each): **195 machine full-task tapes, picked 179, contact 46, nested-proxy 16,
  tipped 32** over 72 ICs. S5 first failed to convert the machine phase sets: the phase manifest and the converter were
  keyed by IC uid, and a harvest has several tapes per IC, so cut indices from one tape were applied to another
  (converter refused the set). Fixed: manifest keyed by tape file, banks written as lists, converter looks up by file.
  Second bug (caught before any run reached its evals): the held-out eval bank was built by dict-indexing the human
  bank, which had just become a list — holdE would have been empty. Fixed; S5 rerun 3270554 (holdE 13 entries).
- Machine sets capped to one tape per IC (PLAN amendment 22:00): **dDP place 63 vs dH 39; dDP contact 25 vs dH 11**.
  Runs: dH place 3263097–104 (RUNNING since 17:18 on the human data, unaffected); dDP place **3270587–94**; contact dH
  3270529–36 (will GATE-SKIP: 11 < 20), contact dDP **3270595–602** (one-armed). yields.json updated.
- 22:30 — The skip-list cell ran all 300 episodes (ep 269 skipped, the rest fast on a normal node) and then crashed in the
  summary on the skipped record's missing `steps` field. Fixed (`steps=0`), resubmitted. dHv2all post-hoc: holdv2
  16/16 done, rnd300 14/16. Contact dDP runs resubmitted 3270619–26 with the polE bank optional (it comes from the
  place-phase policies, which don't exist yet); contact dH 3270529–36 will GATE-SKIP.

## 2026-09-04 22:50 (cluster 18:50) — P100 incident: four machine place runs died at step 0 and my launcher evaluated the untrained checkpoint
- dDP place s1–s4 (3270588–91) landed on **pax077 (Tesla P100, CUDA capability 6.0)**: torch.compile/Triton refuses the
  device → training raised at the first update. `wmfix_phase.sbatch` masked it: the training pipeline ended in
  `|| true`, so `PIPESTATUS[0]` read 0 and the launcher went on to evaluate the prefill-time `latest.pt` (holdE
  placed_v2 0.00 — garbage cells, deleted with the run dirs). Fixes in both launchers: `--exclude=pax077`; the training
  rc is captured before any `|| true`; a run whose last logged step is < budget − 5000 aborts before evaluation.
  Resubmitted: place dDP s1–4 = **3272139–42**; contact dDP s0–7 = **3272143–50**. Unaffected: dH place 3263097–104
  (pax106/142/143/144/026), dDP place s0 3270587 (a100, 2500 updates), s5 3270592 (l40), s6/s7 3270593/94 (rtx_6000,
  cold compile at 0 updates when checked).
- Every node class my runs have touched so far: a100, l40s, l40, rtx_6000, h200 (fine); p100 (fatal). Node class is
  now part of provenance (`sacct NodeList` per job id in COMMANDS.log).
- 23:20 — **Preemption:** contact dDP s4–s7 (3272147–50) and the skip cell 3270635 were PREEMPTED on the `preempt`
  partition and dropped (my launchers had no `--requeue`; 7 of the 16 running place runs are on `preempt` too).
  Fixes: `--requeue` in all three launchers, and a requeued training run clears its partial logdir and restarts
  clean (`SLURM_RESTART_COUNT`); `scontrol update Requeue=1` applied to the 20 running runs. Resubmitted: contact
  dDP s4–7 = **3276571–74**, skip cell **3276575**.
- 23:35 — dHv2all WM cells complete (8 seeds × 10 cells): rnd300 MODE 1422/2400 = 0.593 vs dHv2raw 0.602 (p 0.792);
  SAMPLE 0.574 vs 0.597 (p 0.480); holdv2 0.939 vs 0.975 (p 0.177); alldemo 0.921 vs 0.951 (p 0.215); vs dDP holdv2
  p 0.035, alldemo p 0.044. Verdict unchanged (MORNING_TABLE §5 updated).
- 2026-09-05 00:25 — rnd300 retest COMPLETE (36/36 + the skip cell): dHv2raw MODE 1444/2400 = 0.602 vs dDP 1484/2400 =
  0.618 (Δ −0.017, exact perm p 0.546, n = 8 v 8); SAMPLE 0.597 vs 0.619. The dDP s1 MODE cell = 173/300 with placement
  269 counted as a failure (deterministic stall). Registered prediction (within ±0.05 of rnd30) MET for every arm.
- 2026-09-05 00:50 — first phase results: **place dH s7 holdE 13/13 (sample)**; **contact (machine arm, one-armed) s2
  holdE 11/11 | 10/11, s3 11/11 | 11/11** — contact-from-release is readily learnable (online 0.79 → 1.00 by 800k,
  ≈15k short episodes per 200k steps, entropy −6.0). The rest of the 16 place runs and 6 contact runs finish tonight.
- 2026-09-05 01:25 — place phase, first polE cells (148 policy-generated entries; sample | mode): dH s0 113/148 | 113/148,
  s7 109/148 | 118/148 (0.80), s6 111/148 | —; dDP s5 91/148 | — (0.61). holdE saturates on both arms (13/13 on every
  finished seed, both modes) — the discriminating cell is polE. Early and one machine seed only; n = 8 v 8 by ≈03:30.
  Note the registered asymmetry: polE entries were produced by HUMAN-arm phase-1 policies.

## 2026-09-05 02:10 (local) — VPN window closed (~22:00 cluster); everything remaining runs unattended
- At the last look (21:36 cluster): place dH 3 done / 5 running, place dDP 1 done / 7 running, contact dDP 3 done / 5
  running; every finished place seed holdE 13/13; polE cells dH 0.75–0.80, dDP s5 0.61 (one seed). All remaining runs
  self-evaluate (holdE + polE, sample + mode) inside their jobs; the login-node watchdog covers eval stalls; the
  machine-policy dumps 3280431–38 write `phase_banks/polE_place_from_dDP_s*.json` on the cluster.
- **Morning pickup (in order):** (1) `python3 phase_table.py runs place` and `... contact` → paste into
  `paper/PHASE_RESULTS_2026-09-05.md` §2 with the permutation tests; (2) merge the dDP dumps into
  `polE_place_dDP.json` (the detached `auto_polE_dDP.sh` does it if the VPN is back within its 6 h; else run its
  python block by hand) and run `polE_dDP_sweep.sh` (idempotent) → the polE-dDP columns; (3) score the registered
  learnability floor (≥ 0.5 holdE in ≥ 3/8 seeds) and the prediction (|Δ| < 0.10 on polE); (4) contact = one-armed
  learnability read; (5) refresh CLAUDE.md Agent Status; (6) render the results doc.
- 2026-09-05 09:40 — Positioning assessment (delegated, commit d34006b): `paper/POSITIONING_demo_source_null_2026-09-05.md`.
  Verdict by learner: BC's idle-time sensitivity is established (Diffusion Policy §4.3, robomimic) — ours is a
  quantified re-confirmation; RLfD source-indifference is folk-expected but not directly evidenced (no same-task
  human-vs-machine cell in RLPD/IBRL/JSRL) — ours is the first controlled online one found, and our weakest cell;
  world models have one precedent, MoDem's demonstration-source ablation (same asymptote, policy demos marginally
  faster) — must be cited, no "first" claim; failures-don't-help is new but weak (n=8, 40 tapes). Recommended framing
  F1: "source is second-order once the world is right; idle pruning (BC) and state coverage (value learners) are
  first-order". Cheapest bar-raisers: RLPD/r2dreamer on robomimic's public PH-vs-MG sets; an independent generator;
  TOST equivalence margins.

## 2026-09-05 13:35 (cluster 09:30) — PLACE PHASE COMPLETE: human 0.703 vs machine 0.709 on the policy-generated starts (p 0.878)
- 16 place runs + 8 machine contact runs all COMPLETED overnight (no requeues, no failures beyond the 4 P100 runs
  replaced earlier). polE MODE dH [113,86,112,112,82,99,110,118] vs dDP [92,117,101,113,103,99,112,103]: 0.703 vs
  0.709, Δ −0.006, p 0.878; SAMPLE 0.688 vs 0.701, p 0.748; holdE 104/104 vs 102/104. Registered prediction |Δ| < 0.10
  MET; learnability floor met by every seed. Contact one-armed (dDP): holdE 0.977 | 0.966.
- polE-dDP (machine-policy bank, 149 entries) merged; 32 post-hoc evals submitted (3290111–42) — the symmetry check
  columns land in ≈1 h. Orphan array 3257860 (DependencyNeverSatisfied) cancelled. PHASE_RESULTS §2 written.

## 2026-09-05 14:30 — action-repeat diagnostic (amendment (c) step 1): the sets DO differ at 30 Hz ⇒ repeat-1 pilot triggered
55 paired ICs, source tapes' per-sim-step arm targets (`sim_actions`) and measured joints (`sim_states`), windows of 4:
| descriptor (30 Hz) | human median | machine median | paired d_z | frac H > M |
|---|---|---|---|---|
| within-window target variance | 3.4e-5 | 3.7e-5 | −0.31 | 0.38 |
| per-step target sign reversals | 0.059 | 0.084 | **−1.93** | 0.04 |
| target energy above 3.75 Hz (frac.) | 0.0028 | 0.0048 | **−0.68** | 0.11 |
| measured-joint energy above 3.75 Hz | 0.0109 | 0.0074 | **+0.50** | 0.80 |
| window-end target − window mean | 0.0034 | 0.0047 | **−0.73** | 0.22 |
Reading: the MACHINE teacher's per-step targets flicker more inside a window (the DP's replanning), the HUMAN's measured
motion carries slightly more high-frequency content; both are averaged away by the window-end target at repeat 4.
Registered rule (d ≥ 0.5 on any descriptor) ⇒ build and submit the repeat-1 pilot (r2dreamer, dHv2raw vs dDP, stride-1
demos re-encoded from the per-sim-step arrays, 4 seeds/arm, 1M sim steps = 1M decisions). Design choice to register
with it: keep the PHYSICAL caps per second equal (delta cap 0.025/4 per sim step, leash unchanged in caps).

## 2026-09-05 15:00 (cluster 11:00) — contact phase continued: `carrycontact` (two-armed) + sub-floor human contact runs submitted
- New scope `carrycontact` (private env copy: place-style held-entry restore + contact terminal by either route), adapter/
  evaluator/config/converter/launcher extended (`carrycontact_patch.py`; launcher maps its banks to the place banks and
  takes `YIELD_FLOOR` for disclosed sub-floor runs). Segments: **dH 21 (all 21 contact tapes), dDP 36 (one per IC; 46
  before capping)**. Smoke 3290190 (5k) gates the 16 runs: dH 3290191–98, dDP 3290199–206 (afterok smoke). Sub-floor
  human contact-from-release (11 demos, `YIELD_FLOOR=10`, tag `bnormclamp1ent5_subfloor`): 3290207–14 — disclosed only.

## 2026-09-05 09:55 — repeat-1 clock pilot: registered + built (PHASE_PLAN amendment (c′))

Data correction before the build: the repeat-4 recipe integrates a×cap at EVERY sim step, so the physical per-step cap is already 0.025 and my "0.025/4" wording in (c) was wrong — at 0.00625 the stride-1 demos would be clipped on 23 % (human) / 37 % (machine) of steps. Registered: cap 0.025/step (clips 1.3 % / 0.9 %), horizon 1332, train_ratio 128 (equal updates per sim step), time_limit 1200, batch_length 64 uncompensated. New code: `to_dreamer_native.py --stride1-cap` (two commits), `configs/env/genesis_pick_state_r1.yaml`, launcher `REPEAT`/`ENVCFG`/`EVAL_SETS`/`DEMO_ROOT`, `r1_build.sbatch` (build gate: tape count + total reward equal to the repeat-4 dirs). Chain: build → smoke 5k (hold15) → 8 runs afterok. Predictions P1/P2 in the amendment; my prior P1 (no difference) ≈ 2:1.

## 2026-09-05 10:05 — repeat-1 pilot resubmitted twice (disclosed); END-TO-END arm registered (amendment (d))

- **Slip, caught before any result:** the first repeat-1 chain (3290321–29, 09:47) was submitted WITHOUT the recipe overrides (`env.actor_dist=bounded_normal env.return_clamp=1.0 env.act_entropy=3e-5` — the launcher takes them on the command line). Cancelled at 09:53, resubmitted with the overrides (smoke 3290343 → runs 3290344–51). The smoke then asserted `env.steps must exceed the prefill count`: stride-1 prefill = 57 260 rows (the same physical count as the repeat-4 prefill, 14 389 × 4) > 5 000; resubmitted as 3290358 with 65 000 steps, the 8 runs re-pointed (`scontrol update Dependency`). Build gate passed: 66/58 tapes, rows ratio 3.98/3.96, total reward equal, per-element saturation 3.4 % vs 3.9 % (human) and 2.4 % vs 2.4 % (machine).
- The new full-task launcher `wmfix_full.sbatch` BAKES the actor/entropy overrides in (`RECIPE`), so this cannot recur there; the clamp lives in the config (scope-specific).
- Carrycontact smoke 3290190 FAILED after training (rc 0) and holdE (rc 0): the polE (human-policy) bank eval crashed at entry 900002@12 — `scope=place reset: no entry survived restore (can dropped)`, retried 10× (explicit uid ⇒ single-entry pool). The same bank evaluated cleanly in the place scope (1184 episodes/arm), so the drop is carrycontact-specific; under investigation. The 16 dependent runs 3290191–206 are never-satisfiable until the smoke passes.
- END-TO-END arm registered (PHASE_PLAN amendment (d)); new code: converter `--reward-from-tape` + `--one-per-ic-best`, `configs/env/genesis_full_state.yaml` (scope full, return_clamp 8, 2e6), `wmfix_full.sbatch`, `full_build.sbatch`, `eval_genesis.py` full→nested + `stages` success-by-stage.

## 2026-09-05 10:10 — chains of record (job ids)

- **Repeat-1 clock pilot (c′):** build 3290320 PASSED (gate above); smoke 3290358 (65k steps, `TAG=r1smoke_bnormclamp1ent5`) → runs 3290344–47 (dHv2raw s0–3) / 3290348–51 (dDP s0–3), 1e6 sim steps, `TAG=r1_bnormclamp1ent5`, evals hold15 + rnd30 in-job.
- **Carrycontact:** smoke 3290384 (`TAG=smoke5k`, with the `restore_failed` evaluator guard) → runs 3290191–206 (re-pointed). Sub-floor human contact runs 3290207–14 running.
- **END-TO-END (d):** first build 3290377 converted both sets (human 74: pick 64 / no-pick 10, total reward 118; machine best-per-IC 195 → 72: pick 70 / no-pick 2, total 206) but failed my manifest gate on `scope` (stamped 'pick'); resubmitted with `--scope full` as 3290389 → smoke 3290390 (180k steps, `TAG=smoke_bnormclampS8ent5`) → runs 3290391–94 (dHfull_all s0–3) / 3290395–98 (dDPfull s0–3), 2e6 steps, `TAG=bnormclampS8ent5`.
- Evaluator change (both new chains + every later bank eval): `eval_genesis.py` now records a bank entry that does not survive the entry restore as outcome `restore_failed` (a failure, counted in n) instead of crashing; also `full → nested` success key and a `stages` success-by-stage block in metrics.json.

## 2026-09-05 10:12 — place phase: polE-dDP symmetry columns complete (32/32 cells)

`phase_table.py runs place`: polE-dDP MODE dH 812/1192 (0.681) vs dDP 858/1192 (0.720), Δ per-seed −5.75, p 0.374; SAMPLE 802 vs 848, p 0.434. Registered polE MODE unchanged (0.703 v 0.709, p 0.878). Pasted into PHASE_RESULTS §2.x. Conclusion unchanged: no source effect at the place phase on either entry bank.

## 2026-09-05 10:25 — carrycontact smoke 3290384: polE eval passed with the guard; restore failures quantified

The polE SAMPLE eval of the 5k-step smoke (random-ish policy) completed (rc 0): 148 episodes, contact 0, tipped 0.79, timeout 0.18, **restore_failed 5/148 = 3.4 %** (entries 900002, 900007, 900055, 900058, 900126). The place-scope evals of the same bank (16 checkpoints × 2 modes × 148) had zero restore failures, and the place and carrycontact configs differ only in `scope`/`demo_dir` (diffed); the restore code path is shared. So the failures are history-dependent (scene state left by the previous episode; the smoke policy tips 79 % of episodes), not a property of the entries. Consequence for the carrycontact results: `restore_failed` is counted as a failure for whichever policy it happens to, which mildly penalises policies that tip more — the same direction as their real failures; the rate under trained policies is expected to be far below 3.4 % and is reported per cell by `phase_table.py` (rf column). Disclosed in PHASE_RESULTS when the carrycontact cells are read out.

## 2026-09-05 10:30 — repeat-1 pilot RUNNING; end-to-end smoke resubmitted (launcher log-name slip)

- Repeat-1 smoke 3290358 PASSED (train rc 0; hold15 sample/mode 0/15 picked, 1200 decisions per episode, as expected for 8k online steps); runs 3290344–51 started 10:23.
- End-to-end smoke 3290390 trained to budget (rc 0) but FAILED its own post-train check: `wmfix_full.sbatch` kept `SLURM_OUT=…/wmfix_s2_<job>.out` from the pick launcher while its `#SBATCH -J` writes `wmfix_full_<job>.out`, so the `[sim-variant]` grep read a non-existent file (the lines are present in the real log). A first resubmission (3290503) went out before the fix had applied (sed pattern missed the braces) and was cancelled; smoke of record **3290504**, runs 3290391–98 re-pointed. Nothing about the training recipe changed.
- `phase_table.py` now shows a per-cell `rf` count (bank entries that did not survive restore, counted as failures) so the carrycontact readout carries it; all 96 place cells show rf 0.

## 2026-09-05 11:25 — matched demo counts (PHASE_PLAN amendment (e))

User 11:15: demo count is a confound; test same vs same unless ignition is impossible at the human count. Audit in (e): pick and end-to-end already count-matched; place 39 v 63, contact 11 v 25, carrycontact 21 v 36 were NOT. Actions: the 8 machine carrycontact runs (3290199–206) were held then cancelled before they started (the cc smoke 3290384 had just passed; human runs 3290191–94 running, 95–98 queued); machine sets subsampled to the human counts (`matched_n.py`, seed 0): `dDP_carrycontact_n21`, `dDP_place_n39`, `dDP_contact_n11`; 24 runs submitted (job ids in COMMANDS.log, "MATCHED-N"). Converter gained `--max-tapes/--subsample-seed` for future builds. The §2 place result (39 v 63) is now labelled the uncapped secondary; the matched n39 machine arm decides the number of record.

## 2026-09-05 11:40 — DV3 debugging delegated (user: "use your excess capacity to continue debugging DV3, delegate an agent")

Brief: `~/wm_fix_2026-09-03/agent_brief_dv3.md` (gates G1 reach proxy → G2 pick smoke from dHv2raw → G3 pick n=4v4; port the r2dreamer fix one lever at a time — return clamp at max return, actor distribution, entropy, fp32; ≤ 4 GPU jobs in flight, `dv3dbg_` names, work only in `$W/dv3_fix`/`dv3_dbg`). The agent registers in `paper/DV3_DEBUG_2026-09-05.md`. Robomimic leg parked until Monday (user 11:30).

## 2026-09-05 13:30 — contact-after-release: the human arm IGNITES at n=11 (sub-floor)

First two sub-floor human contact seeds landed (3290211 s4, 3290214 s7): holdE 11/11 sample and 11/11 mode each (`phase_table.py runs contact`). Machine arm at its own yield (25 demos, 8 seeds): 86/88 / 85/88. So ignition at the human count is possible and the matched pair (human n11 3290207–14 vs machine n11 3291451–58) is a valid comparison under amendment (e). Caveat: the holdE contact bank (11 hold-uid entries) is at ceiling for both arms; separating the arms needs a harder, policy-generated contact entry bank (placed_v2 states dumped from the 16 place checkpoints on the human polE bank) — set up next as `polE_contact.json`, scored post hoc on every contact checkpoint.

## 2026-09-05 13:45 — polE_contact bank chain submitted (PHASE_PLAN (f))

16 CPU dumps of the place checkpoints on the human polE_place bank (3293580–95, `phase_dump_place_entries.sbatch`) → merge 3293596 (`polE_contact_merge.sbatch`, pseudo-uids 920000+, source run recorded per entry) → post-hoc sweep 3293597 (`polE_contact_sweep.sh`, re-runnable: scores every finished contact checkpoint lacking `fresh_eval_polE_{sample,mode}`). Contact runs that finish after the bank exists evaluate on it in-job (the phase launcher looks for `phase_banks/polE_contact.json`).

## 2026-09-05 14:45 — review reels (user directive: sample in/out-of-distribution episodes across seeds for every finished condition)

Pipeline: `$W/sample_review.py <label> <run_glob> <in_cell> <out_cell> [K=4] [seed=0]` (cluster; copies K episodes per class drawn uniformly across a condition's seeds into `$W/review/<label>/{in,out}`), rsync → `~/wm_fix_2026-09-03/review/`, `tile_review.py` (one mp4 per condition: row in-dist / row out-of-dist, 4 fps = half speed, 120-frame cap); `review_sweep.sh` re-runs the whole thing for every condition (add a line per new condition). Sent 14:40: place human n39 / machine n63, contact machine n25 / human n11 (holdE only), pick human raw / machine / human all-data.
Visual notes (frames inspected): (1) place episodes take 8–9 decisions from the pick-grant state — the policies lift the can straight from the lower tier onto the shelf; placed_v2 is granted anywhere in the shelf footprint, and policy-bank episodes often set the can down at the shelf's far edge, away from the goal. (2) contact-after-release holdE entries (human placed_v2 states) have the can already adjacent to the goal — contact in 1–3 decisions — which is why every arm saturates that bank; the policy-generated `polE_contact` bank (160 entries, built 14:36 from the 16 place checkpoints, 10 per checkpoint) starts from policy placements that are typically far from the goal and is the discriminating contact statistic (amendment (f)); post-hoc sweep submitted 30 evals at 14:36.
Repeat-1 pilot: 7/8 runs landed by 14:25 (rnd30 MODE so far: human 0.73/0.60/0.60/0.63, machine s1 0.67; hold15 MODE 1.00 on every machine seed seen); the comparison is computed when all 8 are in.

## 2026-09-05 14:55 — repeat-1 clock pilot READ OUT: P1 met (no source effect at the 30 Hz clock)

rnd30 MODE human 77/120 (0.642) vs machine 78/120 (0.650), Δ −0.008, exact p 1.000 (n=4v4); SAMPLE +0.017, p 0.829; hold15 60/60 both. All 8 seeds learned. PHASE_PLAN (c′) verdict + PHASE_RESULTS §6 written. Review reels for the two repeat-1 conditions follow.

## 2026-09-05 15:25 — carrycontact first seed; CORRECTION on the restore failures

Human carrycontact s0 (3290191): holdE 13/13 both modes; polE 112/148 SAMPLE, 116/148 MODE, **rf 5 in both cells** — the same five polE entries that failed in the 5k smoke. So the restore failure is DETERMINISTIC in the carrycontact scope (not history-dependent as I wrote at 10:25 — that inference is withdrawn), while the same entries restore in the place scope. Mechanism still unknown (the restore code path is shared; only `scope` differs between the two configs). Consequence unchanged and symmetric: every arm evaluated in this scope loses the same 5/148 entries, counted as failures; the per-cell `rf` column carries it, and PHASE_RESULTS will report carrycontact polE both as x/148 (registered) and x/143 (restorable entries). First seed: 116/143 = 0.81 on restorable entries.
