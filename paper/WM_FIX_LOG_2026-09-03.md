# WM fix log — 2026-09-03 (GPU box = pop-os, RTX 3060 12 GB; shared with the sim-box world agent, CPU-loaded)

Companion to `paper/WM_FIX_PLAN_2026-09-03.md`. Dated entries; numbers only from `metrics.jsonl` / fresh-eval JSON.
Run tree (outside the repo, never git): `~/wm_fix_2026-09-03/` — `r2d_record/` (code), `runs/<name>/` (logdirs).

## 2026-09-03 ~08:00 — setup, code-of-record reconstruction, what is NOT verifiable yet

**Cluster unreachable at start** (`ssh tufts`: name not resolved; VPN pending). Everything below about "what runs up
there" is inferred from local artifacts and must be re-checked against `$LAB/r2dreamer` and `$LAB/dreamerv3-torch`
once the VPN is up (see "Cluster diff TODO").

**r2dreamer code of record, reconstructed locally.**
- `cluster/r2dreamer_port.tar.gz` (Aug 15) is a 30-file OVERLAY (core: buffer/dreamer/networks/tools/train/trainer/
  distributions + configs + genesis files), not a full tree — no `rssm.py`, no dmc/model-size configs.
- Base commit found by minimum diff: fork `~/workspace/r2dreamer` @ `1fadce4` (2026-03-05, = upstream import; the
  fork's later `main`/`sigreg` work is NOT in the port). Port-specific edits on top: 268 lines across
  dreamer/trainer/networks/tools.
- `cluster/patches/r2dreamer_final_rr.patch` (Aug 31) does NOT apply cleanly to base+tarball (envs/genesis.py hunk 4,
  envs/__init__.py hunk 1 fail) ⇒ the tarball is stale vs the cluster tree; the exact code of record exists only on
  the cluster. The two failing hunks touch genesis-only files, irrelevant to a DMC run.
- Composed tree = `1fadce4` + tarball (patch NOT applied): `~/wm_fix_2026-09-03/r2d_record/`.
- Port defaults on a DMC task resolve to STOCK values: `_base_.yaml` `act_entropy ${oc.select:env.act_entropy,3e-4}`,
  `actor_bc_lambda …,0.0`, `return_clamp …,0.0`, `horizon …,333`, `actor_dist …,bounded_normal`; `dreamer.py`
  clamp/BC branches are skipped at 0. So `env=dmc_vision` runs the port's code paths under stock settings.
- Port changes that DO touch every task (candidates a DMC run exercises): trainer `is_demo` column added to every
  online row; `demo_reinjector` hooks; periodic `latest.pt`; eval envs optional; `buffer.add_chunk` + TensorDict
  `set_` rewrite; `episode_ids` comment changed ("increment" → "kept constant") but NEITHER version increments it —
  no behavior change there.
- `~/workspace/r2dreamer` fork itself (branch `sigreg`, last commit 2026-04-16) has a prior ManiSkill comparison in
  `logdir/` where "DreamerV3 and R2Dreamer both reach >100 reward by 2e5 steps" (fork CLAUDE.md) — that is the
  FORK, not the port, and ManiSkill, not DMC. Does not count for stage 0.

**dreamerv3-torch code of record: cluster-only.** `cluster/patches/dreamerv3-torch-genesis_final_rr.patch` is
against a `configs.yaml` blob (`c508c32`) that exists in NO commit of the local checkout
`~/workspace/dreamerv3-torch/dreamerv3-torch` (fork @ `1ec0641`, dirty), and its context references
`genesis_pixels_vec`/`genesis_pick_msrecipe` blocks the local file lacks. The local checkout can only smoke the
DMC pipeline; the stage-0 dv3 run of record must use the cluster tree (rsync when the VPN is up).
`return_clamp` default 0 in the patch (models.py `_compute_target` clamp only when > 0).

**Environments.** r2dreamer venv `~/workspace/r2dreamer/.venv` (py3.10, torch 2.11, dm_control 1.0.28, mujoco 3.3.0):
DMC cartpole/walker render smoke OK (EGL). dv3 venv `~/workspace/dreamerv3-torch/dreamerv3-torch/.venv` (torch 2.10)
had NO dm_control despite upstream requirements → `pip install dm_control` (→ dm_control 1.0.45, mujoco 3.12.0).
Box: 12 cores (world agent uses ~5), 31 GB RAM, disk 97% full / 27 GB free → env_num 4, small logdirs, no
video_pred_log.

**Cluster diff TODO (when VPN up):** rsync `$LAB/r2dreamer` and `$LAB/dreamerv3-torch` code (no runs/ckpts) →
diff against `r2d_record/` and the local dv3 checkout; record every differing file here; re-run stage 0 on the
cluster trees if the DMC-relevant core differs.

**Smokes launched 08:20 (pipeline only, not gated):** `runs/smoke_r2d_cartpole` (record tree, env=dmc_vision,
cartpole_balance, env_num 4, 8k steps, eval 2 eps @4k) and `runs/smoke_dv3local_cartpole` (local dv3, same knobs).
Purpose: pipeline works end-to-end, throughput, metric names.

## 2026-09-03 08:00 — smoke findings (pipeline)
- r2dreamer stock `dmc_vision` keeps replay on the GPU (`buffer.storage_device=${device}`, max_size 5e5 images) →
  CUDA OOM on the 12 GB card at startup (tried to allocate 3.8 GiB with 6.9 GiB in use). Relaunched with
  `buffer.storage_device=cpu buffer.max_size=2e5|3e5` — the SAME storage choice the port's genesis config makes
  (`storage_device cpu`), so it is not a deviation from the genesis path. Algorithm unchanged (data identical).
- r2dreamer step-0 eval on cartpole_balance (random policy): eval_score 708.9 / length 500 (2 eps). cartpole_balance
  rewards near-upright regardless of policy, so a high random baseline is expected; the gate (≥ 800 LAST by 200k)
  is only ≈ 100 above it — walker_walk is the discriminating task; cartpole mainly checks the pipeline + no
  regression below random.
- dv3 local checkout: wandb ON by default (`wandb_config.use_wandb: True`, project `dreamer_v3`); the smoke synced
  one run there (name `easy-smoke-740`). Gated runs use `--configs dmc_vision nowandb`. Step-5000 eval:
  eval_return 239.7 (2 eps) — below random-ish, but at 5k steps nothing has trained yet.
- Launcher: `~/wm_fix_2026-09-03/launch_stage0.sh <r2d|dv3> <task> <seed> <steps>`; every command is echoed to
  `runs/COMMANDS.log` and the run's `.out` head; refuses to overwrite a logdir.
- Both ports run `torch.compile` at start (dv3 `compile: True`; r2dreamer "Compiling update function") → first
  minutes show fps 0 / no step progress on this CPU-shared box (load avg ≈ 9 with the world agent's 6 workers).

## 2026-09-03 08:25 — smokes complete (pipeline OK on both ports; box too slow for the gate)
- r2dreamer smoke (record tree, cartpole_balance, 8k steps, env_num 4, CPU replay): ran to completion; evals
  [0] 708.9, [4000] 543.7 (2 eps each); train/opt/updates 1249 at step 5528; `fps/fps 5.2` (includes compile).
  Stock `bounded_normal` samples reach ±4 on DMC too (`train/action_min -3.9 / action_max 4.1`) — upstream
  behavior, not port-specific (the port's `bounded_normal_clipped` is opt-in for genesis only).
- dv3 local-checkout smoke (cartpole_balance, 13k steps, envs 4, wandb on): evals [5000] 239.7, [9000] 179.0
  (2 eps); ~8 env steps/s. Pipeline OK; NOT the code of record (see 08:00 entry).
- Throughput verdict: ≈5–8 env steps/s here (CPU shared with the world agent's 6 workers, load ≈ 9) ⇒ 200k-step
  cartpole ≈ 7–11 h per run locally. User (08:20) authorized interactive cluster GPU sessions (and cancelling a
  running job if necessary) → gated runs move to the cluster once the VPN resolves; one local r2dreamer
  cartpole s0 (200k, RECONSTRUCTED tree) launched 08:25 as fallback + cross-machine check
  (`runs/s0_r2d_cartpole_balance_s0`).

## 2026-09-03 ~08:55 — cluster reachable (alias fix), code-of-record diff DONE, stage-0 cartpole jobs submitted
- **Reachability:** the VPN (cscotun0, Tufts DNS) was up the whole time; this box's `~/.ssh/config` has no `tufts`
  alias (that lives on the laptop) — `ssh pax` / `login.pax.tufts.edu` works. All my scripts use `pax`.
- **r2dreamer cluster tree (`$LAB/r2dreamer`, git c25eb3b + 32 dirty files) vs my reconstruction:** every
  DMC-relevant file is IDENTICAL (dreamer/trainer/buffer/networks/tools/distributions/rssm, configs/configs.yaml,
  model/_base_.yaml, env/dmc_vision.yaml, envs/dmc.py). Differences are genesis/isaaclab/maniskill-only
  (demo_prefill 56, envs/genesis 43, eval_genesis 51 lines; cluster-only: `genesis_pick_v5d4c_delta_shaped.yaml`,
  `genesis_touchgoal_v5d4c_delta.yaml`, maniskill/isaaclab configs+envs, eval_ms/ms_control) plus
  envs/__init__.py (maniskill branch + `pick_shaping` kwargs) and envs/parallel.py (+3 lines moving action/done to
  CPU before dispatch — benign). ⇒ the tarball+patch in the repo is STALE vs the cluster for genesis files; the
  cluster tree is the record. Local copy of it: `~/wm_fix_2026-09-03/cluster_r2d/`.
- **dv3 cluster tree (`$LAB/dreamerv3-torch`, git 2a80452 + 139 dirty) vs local checkout `1ec0641`:** dreamer.py
  391 / models.py 65 / tools.py 29 / configs.yaml 221 changed lines; envs/dmc.py, wrappers, networks, exploration,
  parallel identical. Cluster configs.yaml carries `genesis_pickplace/pixels/pick_msrecipe/…/touchgoal/dv3std`
  blocks + `return_clamp: 0` default. Local copy: `~/wm_fix_2026-09-03/cluster_dv3/` (code files only; the repo
  root on the cluster holds timestamp-named run dirs, excluded). ⇒ the local dv3 checkout is NOT the record; dv3
  stage 0 runs from the cluster tree.
- **Cluster envs:** neither `$LAB/r2d_venv` (mujoco 3.3.7, torch 2.8) nor `$LAB/condaenv/genesis` (mujoco 3.3.7,
  torch 2.7) has dm_control; an unpinned install would upgrade mujoco to 3.12 (genesis-world pins <3.4). Base
  envs left UNTOUCHED (verified 3.3.7 after): built overlay venvs `--system-site-packages` from each base python
  (`$LAB/wm_fix_2026-09-03/r2d_venv_dmc`, `…/dv3_venv`) with dm_control (+ mujoco 3.12 shadowed inside the overlay
  only); `suite.load('cartpole','balance')` OK in both.
- **Queue:** my 4 main-session jobs running (rlpd ×3, dp-round-robin); gpu partition had idle L40S/A100 nodes —
  NO job cancelled. Submitted `wmfix_s0` (sbatch `$LAB/wm_fix_2026-09-03/wmfix_s0.sbatch`, `-p gpu,preempt`, 1 GPU
  l40s|a100|l40|h200, 8 CPUs, env_num/envs 8, eval 5 eps every 10k, 200k steps): r2d cartpole s0 **3232145**,
  s1 **3232146**; dv3 cartpole s0 **3232147**, s1 **3232148**. Logs `$LAB/wm_fix_2026-09-03/{slurm,runs}/`;
  every command appended to `runs/COMMANDS.log`. Batch jobs rather than a held interactive TTY (same GPU use,
  survives my ssh).
- 09:03 — jobs 3232145–48 FAILED at 10 s: MY preflight asserted `dm_control.__version__` (attribute does not exist;
  I had seen that on the login node and still shipped it). No GPU time lost; GPUs were allocated within a minute
  (A100 80GB pax106, A100 40GB pax143 ×2, H200 pax009). Preflight now uses `importlib.metadata.version`.
  Resubmitted 09:04 (job ids in the next entry).
- 09:04 — resubmitted, all RUNNING within 45 s: r2d cartpole s0 **3232173** (pax106 A100-80G), s1 **3232174**
  (pax143 A100-40G); dv3 cartpole s0 **3232175** (pax143), s1 **3232176** (pax009 H200). Preflight prints
  `dm_control 1.0.45 mujoco 3.12.0 torch 2.8.0 (r2d) / 2.7.0 (dv3)`. Commands of record in `runs/COMMANDS.log`.

## 2026-09-03 ~09:06 — stage-1 preparation (code drafted + unit-tested; NOT run — gated on stage 0)
- r2dreamer adapter patch (on my local copy of the cluster tree, `~/wm_fix_2026-09-03/cluster_r2d`, diff in
  `~/wm_fix_2026-09-03/state_obs.patch`, 26 changed lines): `GenesisPick(state_obs=False)` flag; when true the
  adapter also emits `obs["state"]` = FullTaskEnv's 17-dim float32 vector (captured from reset()/step() returns —
  the same vector DP/RLPD consume) and adds `state: Box(17,)` to `observation_space`; `envs/__init__.py` passes
  `state_obs=config.get("state_obs", False)`. Default path byte-identical (observation_space unchanged).
  Standalone test in the genesis venv (world `gc_kp4_riser3_shelf6`, scope touchgoal, delta_joint 0.025/5,
  repeat 4): reset → keys image+state+log_*, state (17,) float32; 30 random steps OK, state evolves, no reward,
  sim_variant line confirmed; default-flag env has NO state key.
- Stage-1 config drafted: `configs/env/genesis_touchgoal_state.yaml` = touchgoal recipe of record with exactly the
  plan's changes (mlp_keys 'state', cnn_keys '$^', reward_scale 1, time_limit 1200, act_entropy 3e-4,
  return_clamp 0, demo_dir null, no reinjection); actor_dist stays bounded_normal_clipped (recipe of record).
- dv3 side needs no adapter work: `envs/genesis.py` already has vector mode with a `state` key and
  `genesis_pickplace` config uses `mlp_keys: 'state'`.
- Local r2d cartpole cross-check (RECONSTRUCTED tree, env_num 4): eval 488.6 @30k → 619.6 @40k → 785.1 @50k.

## 2026-09-03 ~09:10 — touchgoal evidence re-checked at the source (plan §1 item 1 STANDS) + one eval label bug
- `eval_genesis.py:242` hardcodes `success_key = "placed_v2" if scope=="place" else "picked"` → for scope
  `touchgoal` the fresh eval reports **"picked"** (0.00 by construction: the episode terminates at first goal
  contact, a pick can never be granted) instead of `touched_goal`. `policy_eval/metrics.json` of
  `touchgoal_v5d4c_delta_s0` indeed has keys `picked 0.0 / tipped 0.0 / timeout 1.0`. Reporting bug only —
  the same JSON's `mean_reward` is 0.0 (a touch pays +100 and terminates), so no touch happened in the eval.
- Decisive: `metrics.jsonl` of both touchgoal seeds (2,814 rows, 2,599 train episodes each, mean length 96.1
  agent steps): `train/data/reward_frames` max **0.0**, `train/data/reward_sum` max 0.0, `episode/score`
  mean 0.0 / max 0.0, `episode/train_task_success` 0.0 in every 100k bin. Not one goal contact in ~2,600
  episodes per seed. The trainer never saw a reward, so this is exploration/reachability under this
  obs+action parametrization, not credit assignment. (dv3 seed 0 found it from 230k — DV3_DIAGNOSIS §6.)
- Consequence for stage 1: the state-input touchgoal run is exactly the right next probe; its fresh eval must
  use the `task_success` / reward accounting, not the evaluator's `picked` summary (fix goes with the stage-1
  patch: scope-aware success key `{"place": placed_v2, "touchgoal": touched_goal, else picked}`).
- ~09:12 Stage-1 tree STAGED on the cluster: `$LAB/wm_fix_2026-09-03/r2dreamer_fix/` = `$LAB/r2dreamer` +
  exactly 3 patched files (envs/genesis.py, envs/__init__.py, eval_genesis.py) + `configs/env/genesis_touchgoal_state.yaml`
  (diff -rq verified; `$LAB/r2dreamer` itself untouched). Launcher `wmfix_s1.sbatch <seed> <steps>` (base
  `$LAB/r2d_venv`, exports GENESIS_PICKAPLACE_ROOT + R2D_SIM_VARIANT=gc_kp4_riser3_shelf6, refuses to run without
  the patch, proves the `[sim-variant] gc_kp4_riser3_shelf6` line in the slurm .out, then a FRESH-process
  `eval_genesis.py` sample-mode 15 hold ICs @1200). NOT submitted — gated on stage 0.
- Local r2d cartpole s0 (RECONSTRUCTED tree): eval **963.0 @60k** (5 eps) — above the 800 bar early; LAST @200k
  is the registered statistic.
- ~09:14 Stage-2 data path scoped (no code yet — gated on stage 1): r2d native sets
  `matched_w3/r2d/{dH,dDP}/*.npz` carry image/action/reward/discount/is_* only (no state); the contract-v1
  sources `matched_w3/dH/*.npz` carry `states (T,17)` + `final_state (17,)` (+ images T+1). Plan when reached:
  `to_dreamer_native.py --with-state` writes `state (T+1,17)` = concat(states, final_state) into a NEW dir
  (`matched_w3/r2d_state/<arm>`), `demo_prefill._load_episode`/`add_demo_set` pass `state` through when present
  (downsample by the same idx; assert (T,17) float32), so demo rows and online rows share the key set.
- Cluster stage-0 throughput: r2d 61–71 fps (env_num 8, A100) → 200k ≈ 50 min; dv3 ≈ 22 fps → 200k ≈ 2.5 h.
- (09:17) Clock note: earlier entry stamps in this log were written from a wrong mental clock (~1 h ahead); corrected
  to real EDT (cluster and box agree: job stamps 09:02/09:04). Entries from here on use `date`.

## 2026-09-03 09:40 — stage-2 data code DRAFTED (opt-in, untested on real training; nothing submitted)
- `baselines/rl/to_dreamer_native.py --with-state` (repo file, +25/−7 lines, default path byte-identical): emits
  `state (T,17) float32` = concat(`states`, `final_state`) aligned with `image`; refuses tapes without
  states/final_state or with non-finite/mis-shaped state; `repeat.json` gains `with_state`, `state_dim`.
- `demo_prefill.py` (staged copy only, `~/wm_fix_2026-09-03/state_prefill.patch`, 19 lines): loads `state` when
  the tape has it (asserts (T,17), downsampled by the same idx), and adds it to demo rows ONLY when
  `env.state_obs` is true (then every tape must have it — loud assert); dropped otherwise, so pixels-only runs
  are unchanged.
- Cluster staged tree `r2dreamer_fix` now differs from `$LAB/r2dreamer` in exactly demo_prefill.py,
  envs/genesis.py, envs/__init__.py, eval_genesis.py + the new touchgoal_state config.

## 2026-09-03 09:55 — user context (dv3 arm result, DayDreamer) → in-house positive controls FOUND on the cluster
- User: has seen dv3 work on an arm control task; DayDreamer shows legged locomotion (dense). Repo evidence:
  MANISKILL_VS_GENESIS.md (08-10) documents the March dv3 ManiSkill PickCube result (pixels, sparse +100, 10
  teleop demos, takeoff 110–140k, ≈200 wandb runs) and its entropy fingerprint (MS: actor entropy → −2 nats by
  17k updates, before the first success; Genesis: 8–9.5 nats flat for 1M steps — the actor never commits).
  DayDreamer also did UR5/XArm visual pick-and-place with sparse rewards, so sparse visual manipulation is in
  the algorithm's demonstrated envelope.
- NEW to the ledger: **r2dreamer has its own ManiSkill positive control on the cluster**
  (`$LAB/r2dreamer/runs/ms_pickcube_s{0,1}`, 08-18, `$LAB/r2d_ms_venv` mani_skill 3.0.0b21): numbers and the
  STOCK knobs it used are in PLAN §7 Amendment (train success ≈0.9 from 50k on both seeds; eval 0.9/1.0 at
  301k). Plan §1's "neither port was ever run on a reference task" is therefore wrong for ManiSkill (true for
  DMC/Atari); the plan's line should be corrected by the main session's doc owner.
- Decisions: stage 1 runs dv3 as a mandatory reference alongside r2dreamer (PLAN §7 amendment); stage 0b is
  satisfied by the on-record runs (no new MS runs unless dv3 fails stage 1 — the current dv3 tree has no
  ManiSkill-capable env: `condaenv/dreamer4` has mani_skill but lacks gym/tensorboard).

## 2026-09-03 10:05 — NEW CONFOUND: every dv3 genesis run ran in the BASE world; dv3 stage-1 launcher staged
- The cluster dv3 tree (`$LAB/dreamerv3-torch`: envs/genesis.py, genesis_eval.py, dreamer.py) contains no
  sim-variant handling at all and `cluster/sbatch_dv3_pack.sh` exports no variant; `FullTaskEnv` is built
  without `sim_variant_hook.apply_pre/post` ⇒ all dv3 genesis runs (touchgoal probe in plan §1 / DV3_DIAGNOSIS
  §6, dv3std/clamp/fix diagnostics, any A32-era dv3 cells) used the base world (base kp, no riser), not
  `gc_kp4_riser3_shelf6`. CONFOUNDS.md mentions dv3 once and not for this — **not on the ledger**; for the
  main session's doc owner (unreachable from this box: ListAgents shows only the sim-box world agent).
  Impact: none on the r2dreamer arm of record; affects any cross-port claim that assumes a shared world.
- dv3 stage-1 reference: adapter patched in a COPY (`$LAB/wm_fix_2026-09-03/dv3_fix`, `dv3_variant.patch`,
  +10 lines) to apply the hook from `GENESIS_SIM_VARIANT` with NO silent default (asserts if unset);
  launcher `wmfix_s1_dv3.sbatch <seed> <steps>` = configs `genesis_pickplace genesis_pick_msrecipe
  genesis_touchgoal genesis_dv3std usestateonly nowandb --time_limit 1200` (state-only obs, delta_joint
  0.025/5, repeat 4, sparse scale 1, clamp 0, train_ratio 512, stock entropy 3e-4), proves the
  `[sim-variant]` line, then fresh-process `genesis_eval.py` on 15 hold ICs @1200 (dv3 evaluator is
  deterministic-actor; r2dreamer's is sample-mode — noted, gate is per port). NOT submitted (stage 0 gate).

## 2026-09-03 10:02 — STAGE 0 readout begins (cluster, code of record)
- **r2dreamer cartpole_balance s0 (job 3232173, pax106 A100-80G): COMPLETED rc 0, 54 min, 196k env steps.**
  Evals (5 sampled eps each, from metrics.jsonl): 0:695 → 10k:201 → 30k:612 → 70k:967 → 100k:989 → 150k:996 →
  **LAST 190k: 996.4** (best-of-last-3 997.0). Gate (LAST ≥ 800 by 200k): **PASS**.
- **r2dreamer cartpole_balance s1 (job 3232174, pax143 A100-40G): COMPLETED rc 0, 59 min, 196k steps; LAST 190k:
  997.0** (best-of-last-3 998.6; 5 sampled eps). ⇒ **r2dreamer cartpole gate PASS 2/2.** dv3 cartpole s0/s1
  (3232175/76) still running (55k @15 fps / 77k @24 fps; last evals 286 / 334) — endpoint ≈ 2 h away.
- Local cross-check (RECONSTRUCTED tree, env_num 4, this box): r2d cartpole s0 at 108k, evals 994–996 from 90k —
  agrees with the cluster trees.

## 2026-09-03 10:05 — SUBMITTED (per PLAN §7 Amendment 10:05): walker sanity + stage 1 both ports
- r2d walker_walk 500k: s0 **3233329** (pax143), s1 **3233330** (pax105). Gate: ≥600 by 500k on ≥1/2, ≥400 on 2/2.
- Stage 1 r2dreamer touchgoal STATE (env=genesis_touchgoal_state, 300k, world gc_kp4_riser3_shelf6 exported,
  tree r2dreamer_fix): s0 **3233331** (pax008 H200), s1 **3233332** (pax106 A100).
- Stage 1 dv3 reference (configs genesis_pickplace+pick_msrecipe+touchgoal+dv3std+usestateonly+nowandb,
  --time_limit 1200, 300k, GENESIS_SIM_VARIANT exported, tree dv3_fix): s0 **3233333** (pax106), s1 **3233334**
  (pax047 H200). All six RUNNING at 10:04; preflights printed genesis 0.2.1 + torch versions.
- dv3 adapter world-hook standalone test (local genesis venv, 09:58): `[sim-variant] gc_kp4_riser3_shelf6: kp=[800,
  800, 600, 400, 240, 240] gc=1.0 riser=0.03` printed; state (17,) float32; 20 random steps OK; with the variable
  unset the adapter REFUSES (AssertionError) — no silent base world in the fix tree.

## 2026-09-03 10:20 — stage-1 health (5 min in) + stage-2 demo sets converted
- Stage-1 r2dreamer s0/s1 (3233331/32): slurm .out has **6 `[sim-variant] gc_kp4_riser3_shelf6` lines each** (6
  workers), `Encoder CNN shapes: {}`, `Encoder MLP shapes: {'state': (17,)}`, 9.60M params — state-only confirmed.
  dv3 s0/s1 (3233333/34): 1 marker line each (single world), still building; no metrics yet. Walker s0/s1 started
  (step-0 evals 21.6 / 28.7).
- Stage-2 inputs prepared (CPU, new dirs, nothing overwritten): `$LAB/wm_fix_2026-09-03/demos_state/{dH,dDP}` via
  `to_dreamer_native.py --with-state --repeat 4 --terminal-reward 1` from `matched_w3/{dH,dDP}`: dH 58 tapes /
  6,985 rows (T p50 110, max 247), dDP 58 / 7,343 rows (T p50 120, max 236); `state (T,17) float32` finite; repeat.json
  `sim_variant gc_kp4_riser3_shelf6, action_repeat 4, with_state true, terminal_reward 1.0, total_reward 58`.

## 2026-09-03 10:40 — stage 1 at half budget: NO goal touch on either r2dreamer seed; probe launched
- r2dreamer touchgoal STATE (3233331/32) at 150k / 128k env steps (≈150 / 125 episodes of 300 decisions):
  `episode/train_task_success` 0.00 in every 25k bin, `train/data/reward_frames` max 0.00, `train/action_entropy`
  9.93 (the maximum for 7 dims) — the same flat-high-entropy fingerprint as the pixel runs. dv3 reference at 30k /
  10k steps (12.9 fps, single world; endpoint ≈ 6 h), 0 touches so far.
- Walker sanity (r2d): 485 @100k (s0) / 469 @110k (s1) — on track. dv3 cartpole: 530 @85k (s0) / 412 @133k (s1) —
  below r2dreamer's pace; gate (≥800 by 200k) at risk for the current dv3 tree.
- Diagnostic launched (local CPU, background): random-policy reachability of the touchgoal reward under the SAME
  adapter/action parametrization (uniform actions and 25-decision-held actions, 10 episodes × 300 decisions, worlds
  gc_kp4_riser3_shelf6 and base): if random exploration essentially never touches the goal can in 300 decisions,
  sparse touchgoal is not a learnability probe for ANY trainer here, and the stage-1 disconfirm branch should be
  read as "reward unreachable by exploration", not "adapter broken" — a dense/`reach`-scope variant would then be
  the honest stage-1 test.

## 2026-09-03 10:55 — upstream dv3 reference submitted (bisect insurance); stage 1 at 200 episodes, still 0 touches
- Cluster dv3 tree vs upstream NM512 `dreamerv3-torch` @ 6ef8646 (2026-03-08, cloned to
  `$LAB/wm_fix_2026-09-03/dv3_upstream`): dreamer.py 633 / models.py 201 / tools.py 448 / networks.py 30 changed
  lines; envs/dmc.py identical. dv3 cartpole (3232175/76) at 95k: 459 / 145k: 412 — likely to miss the ≥800@200k
  gate. Submitted upstream cartpole s0/s1 200k (port `dv3up` in wmfix_s0.sbatch, overlay dv3_venv) so the
  "fork vs upstream" question is answered in parallel (job ids below).
- Stage 1 r2dreamer at 205k / 174k steps (≈200 / 175 episodes): still 0 goal touches, entropy 9.93. dv3 reference
  30k / 10k steps, 0 touches. Random-policy reachability probe (local, corrected world, uniform actions): first 6
  episodes min tool–goal distance 0.23–0.42 m (goal at z 0.262 on the shelf), no touch.
- 11:00 — upstream dv3 reference: first two submissions died at import (3234220/21: `ruamel.yaml.safe_load` removed in
  ruamel ≥0.18; 3234287/88: upstream forces `MUJOCO_GL=osmesa` at dreamer.py:7, no OSMesa on the nodes). Applied
  exactly TWO documented compatibility lines to `$LAB/wm_fix_2026-09-03/dv3_upstream` (safe_load →
  `YAML(typ='safe', pure=True).load`; `os.environ.setdefault('MUJOCO_GL', …)` — the same two changes the fork made).
  Resubmitted: **3234321** (s0, pax142) / **3234322** (s1, pax141), RUNNING.
- **Random-policy reachability (local, corrected world `gc_kp4_riser3_shelf6`, uniform actions in [−1,1]^7, delta_joint
  0.025/leash 5, repeat 4, 10 episodes × 300 decisions, demo ICs): touched 0/10; min tool–goal distance median 0.354 m,
  best 0.231 m; goal can at (0.672, −0.221, z 0.262).** A max-entropy policy does not get within 23 cm of the goal in
  an episode. Base-world and held-action variants running (outputs `~/wm_fix_2026-09-03/probe_*.txt`).

## 2026-09-03 11:10 — STAGE 1 r2dreamer s0 TRAINING DONE: 0 touches in 300k; probe: goal unreachable by random walk in BOTH worlds
- **s1 r2dreamer touchgoal STATE s0 (3233331): train rc 0, 300k env steps, ≈270 episodes × 300 decisions; `train_task_success`
  0.00 in every 25k bin, `train/data/reward_frames` max 0.00, entropy 9.93 throughout.** The in-job fresh eval crashed
  (`KeyError: 'state'` — eval_genesis' `reset_to_uid`/`reset_to_ic` bypass adapter.reset and built the obs dict without
  `state`); patched (both helpers read `genv._obs()["state"]` when `state_obs`), restaged at 11:09:50 (before s1's eval
  starts), and a standalone CPU eval submitted for s0: **3234609** (hold 15, sample mode, 1200 steps). Its result is the
  registered primary score; it cannot exceed what training never saw.
- **Random-policy reachability, base world (uniform, 10 eps × 300 decisions): touched 0/10, min tool–goal median 0.337 m,
  best 0.268 m.** Corrected world (earlier): 0/10, best 0.231 m. ⇒ In neither world does a max-entropy policy come within
  ≈23 cm of the goal can inside an episode; the sparse touchgoal reward is not discoverable by undirected exploration at
  this action parametrization (0.025 rad/sim-step deltas, leash 0.125 rad). The dv3 base-world pixel success (1/2 seeds
  from 230k, DV3_DIAGNOSIS §6) was a rare-discovery event, not evidence that the reward is routinely reachable.
- **`reach` scope is NOT a usable fallback:** in the base probe the tool is already within REACH_DIST 0.17 m of the PICK
  can at decision 0 in 5/10 episodes (9/10 within 42 decisions; min tool–can distance median 0.108 m) — trivially
  satisfied from demo ICs. Stage-1b needs a target that random exploration hits occasionally but not at t=0.
- Third probe variant (held actions, 25-decision persistence, corrected world) still running.

## 2026-09-03 11:25 — stage-1 s1 training done (0 touches); probes complete; stage-1b machinery staged
- **s1 r2dreamer touchgoal STATE s1 (3233332): training done at 299k, 0 goal touches (~270 episodes), entropy 9.93.**
  In-job fresh eval running (patched tree): first 10/15 hold episodes all `timeout, r=0.0`. s0's standalone eval
  (3234609, CPU node pax157) at 2/15, also timeouts, ≈245 s/episode there (≈45 s on the GPU nodes).
- Probe, held actions (25-decision persistence, corrected world): touched 0/10, min tool–goal median 0.328 m, best
  **0.158 m**; pick-can reach(<0.17) hit at decision 0 in 5/10. Summary of all three probes: no policy without
  direction reaches the goal can; persistent actions get to ≈16 cm at best.
- Walker sanity (r2d): s0 **894 @260k**, s1 **872 @280k** (gate ≥600 by 500k) — passing on the curve; LAST at 500k
  pending. dv3 cartpole s1 at 189k: 663 (gate 800 @200k about to read out); s0 at 125k: 697.
- Stage-1b machinery (staged, NOT run, NOT yet registered): a `reach_goal` scope in a PRIVATE copy of the env code
  (`$LAB/wm_fix_2026-09-03/gp_root` = baselines/*.py + rl/*.py + eval_ics.json + can_pos_recovery/*.py +
  trial_placements.json, 14-line patch `reach_goal_patch.py`; the shared `$LAB/genesis_pickaplace` untouched):
  +1 and terminate when the TOOL is within `REACH_GOAL_DIST` (env var, REQUIRED) of the GOAL can. Both adapters map
  `reach_goal → reached_goal` for task_success; eval_genesis too. Threshold calibration probe (R=0.30, uniform)
  running locally. Point of 1b: a sparse goal-directed reward that undirected exploration hits occasionally but not
  at t=0, so a trainer that learns will show it, on both ports identically.

## 2026-09-03 11:24 (cluster clock) — dv3 cartpole s1 MISSES the stage-0 gate; dv3 touchgoal s1 cancelled (my job)
- **dv3 (cluster tree) cartpole_balance s1 (3232176): LAST eval 581.3 @197k (best 699.9 @157k; 20 evals of 5 eps).
  Gate LAST ≥ 800 by 200k: FAIL.** s0 (3232175) at 125k: 697.3, endpoint ≈ 12:20. Per the registered disconfirm branch
  the current dv3 tree is "port suspect"; the upstream NM512 reference (3234321/22, same overlay venv, same launcher,
  2 compat lines) is the bisect: at 16–24k steps so far (11 fps).
- Cancelled **3233334** (dv3 touchgoal STATE s1, my job): 4.9 fps on pax047 ⇒ 17 h to 300k, and the three probes show
  the touchgoal reward is unreachable by undirected exploration, so its only remaining value was symmetry with s0
  (3233333, 12.9 fps, continues as the dv3 stage-1 record; 0 touches at 60k). Freed GPU goes to stage 1b.
- Clock note 2: entry stamps from 10:00 on drifted ≈5–10 min ahead of the cluster clock; job-output timestamps
  (`# … DONE`, `sacct`) are authoritative. Stamps from here on are taken from the cluster (`date` in the ssh).
- Local cross-check r2d cartpole s0 (RECONSTRUCTED tree, this box, env_num 4): done 196k, LAST 190k **998.6** — agrees
  with the cluster record trees (996/997).
- 11:30 — Stage 1 verdict + Stage 1b registered (PLAN §7). Submitting 1b now (job ids in the next entry).

## 2026-09-03 11:26 (cluster) — STAGE 1b SUBMITTED, all six RUNNING within 75 s, variant line in every launcher header
- r2dreamer reach_goal STATE R=0.30: s0 **3234950** (pax008), s1 **3234951** (pax008); R=0.25: s0 **3234952** (pax047),
  s1 **3234953** (pax141). dv3 reference R=0.30: s0 **3234954** (pax182), s1 **3234955** (pax183). 300k steps each,
  `GENESIS_PICKAPLACE_ROOT=$LAB/wm_fix_2026-09-03/gp_root` (private env copy with `reach_goal`), `REACH_GOAL_DIST`
  exported per job, corrected world exported, fresh-process eval at the end.
- 11:25 (cluster) — **Stage-1 registered primary score, r2dreamer s1 (3233332, COMPLETED rc 0): fresh-process eval,
  sample mode, 15/15 hold ICs @1200, `[sim-variant] gc_kp4_riser3_shelf6` present (8 lines): `touched_goal` 0.0,
  timeout 1.0, mean_reward 0.0** (`runs/s1_r2d_touchgoal_state_s1/fresh_eval_hold15/metrics.json`; state_obs=True and
  `state` in obs_keys confirmed in the eval header). s0's standalone eval (3234609) still running on the CPU node.
- Calibration probe FINAL (local, corrected world, uniform random, scope reach_goal, R=0.30, 10 eps): **reached 5/10**
  (episode ended at decisions 22, 42, 69, 126, 174), min tool–goal distance median 0.311 m, best 0.283 m in the non-hit
  episodes; pick-can reach(<0.17) at decision 0 in 5/10. ⇒ R=0.30 is the "learnable, dense-enough" rung (random
  baseline 0.5, ≈70 decisions median to hit); R=0.25 is expected at ≤1–2/10 for random (best distances 0.231–0.283
  across probes). PLAN §7 1b text said "3/4 so far" — final is 5/10.
- 11:37 (cluster) — **dv3 (cluster tree) cartpole s1 (3232176) COMPLETED: run continued to 205k (dv3 counts steps before
  action_repeat); last eval ≤200k = 581.3 @197k → gate FAIL; supplementary final eval 709.0 @205k; best 709.0.**
  s0 (3232175) at 145k: 629 — endpoint ≈ 12:15. Upstream reference at 32k (325 / 318 @25k).

## 2026-09-03 11:50 (cluster) — STAGE 1b: the port LEARNS then COLLAPSES (critic past max return); stage 1c registered
- Per-10k trace (`collapse_trace.py`), r2d R=0.25 s0: 20k succ 0.88 (n96, len 36, ent −3.33, val 0.61, rewfr 13) → 30k 0.89
  (len 38, ent −3.82, val 0.91, ret_replay_max 1.38, value_replay_max 1.21) → 40k 0.47 (len 164, ent −4.49, **val 1.22**,
  ret_replay_max 1.48, value_replay_max 1.50, ret_replay_mean 1.13) → 50k **0.00** (len 300, **ent 9.83**, a_std 0.78,
  val 0.26). R=0.30 s1: 10k 0.87 (ent −0.05, val 0.38) → 20k 0.75 (ent 9.25, value_replay_max 1.23) → 30k 0.00 (ent 9.93)
  and never recovers (val decays to 0.01 by 90k; no reward frames after 40k). Cont head 0.79–0.95 during buildup, 1.0 after.
- Interpretation: λ-return targets exceed the maximum attainable return (1.0) — bootstrapping continues past the reach
  terminal (terminal state is not distinct in the 17-dim state; the reward model keeps paying near the goal in imagination)
  → critic overestimates → the dynamics-gradient actor loses its signal → entropy bonus drives std to max → the learned
  policy is destroyed within 10k steps. Stage 1c (PLAN §7) submits four single-lever ablations; job ids below.
- 11:50 (cluster) — Stage 1c SUBMITTED (all RUNNING within 60 s; lever confirmed in each launcher header): r2d R=0.25
  150k — clamp1 (`env.return_clamp=1.0`) s0 **3235522** / s1 **3235523**; repdreamer (`model.rep_loss=dreamer`) s0
  **3235524** / s1 **3235525**; ent3e5 (`env.act_entropy=3e-5`) s0 **3235526** / s1 **3235527**; dv3 reference R=0.25
  150k s0 **3235528** / s1 **3235529**. Run names `s1b_r2d_reachgoal_state_r0.25_<tag>_s<seed>` (launcher TAG).
