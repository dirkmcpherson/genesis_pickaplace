# HANDOFF — START HERE (2026-09-11 evening, session "bigbox" on pop-os)

For the agent that picks this up on another computer. Branch **`ladder-unify-2026-09-11`** (pushed
to origin; `git pull` first — two lanes may still land commits after this file was written, see §7).
Everything below is on that branch. The cluster was unreachable (VPN down) when this was written;
§5 steps 1–6 were completed by Lane 13 on 2026-09-11 after the VPN returned; only a health monitor (step 7) is per-session.

## 1. What changed, and why (five things)

1. **One code tree, one reward ladder, no env-var gates.** {RLPD} and {r2dreamer} had trained on
   DIFFERENT ladders (`FULLENV_REWARD_X=1` was inert in the RLPD clone), and the (x) ladder's top rung
   could not be paid in training (the episode ended on an unpaid proxy). Both learners now import the
   same `baselines/rl/full_env.py` + `baselines/genesis_can_env.py` + `baselines/stage_predicates.py`;
   every run and every eval cell carries a provenance stamp (`[ladder] …`, `ladder_provenance.json`);
   launchers refuse legacy gates. Why: `paper/E2E_TRAINING_PROBLEMS_2026-09-10.md` §0,
   `paper/E2E_AUDIT_BRIEF_2026-09-10.md` §2/§8 (defects 1–5). Design: `paper/LADDER_UNIFY_BRIEF_2026-09-10.md`.
2. **`nested_proxy` is replaced by `nested_v2`** (released, at rest, within 0.081 m, both upright, not in
   hand; no gripper term, no settle simulation). The proxy's `contact` term was sticky and it reversed
   the human-vs-machine ordering. `nested_v2` = the settled predicate 60/60 on policy episodes.
   `paper/NESTED_V2_PREDICATE_2026-09-10.md`.
3. **The slide predicate is calibrated on the human tapes.** `slide_event` = released → gripper on
   the goal-opposite side within 0.10 m (60° cone) → goalward motion ≥ 1 cm counted only AFTER the
   released can has rested once (the set-down latch; the pilot's `slide_success` fired on set-down
   transients, `can_pos_recovery/videos_slide_smoke_2026-09-11/INDEX.md`). `home` = `slide_event` ∧
   `nested_v2`. On the demonstrations `home` fires on 13/13 human and 14/14 machine sim-slides and no
   other tape. `paper/LADDER_N_DEMO_CHECK_2026-09-11.md`; clips `can_pos_recovery/videos_home_2026-09-11/`.
4. **Tip rule:** threshold 60° and termination are RIGHT (0 of 25 tipped tapes recovers at any
   threshold; a free can passes its tipping point at ~33°); the guard was wrong (`grip < 0.3` fired
   with the can in hand on 6 demo tapes and missed 29 fist-flattened cans). New guard = tracker
   `not in_hand` sustained 4 frames, as an explicit stamped option. `paper/TIP_RULE_2026-09-11.md`.
5. **Demonstration sets locked down:** human `dHfull_all` (74 raw) v machine `dDPfull_first` (72,
   from a Diffusion-Policy teacher trained on the PRUNED full-task human set, first attempt per start);
   matched by phase within 0.06 under the new predicates. `HRI_results/DEMO_SETS_2026-09-11.md`;
   full reproduction chain `HRI_results/RESULTS_PROVENANCE_2026-09-11.md`.

Method change worth knowing: **stage records** — one termination-suppressed re-execution per tape
→ per-frame record → every ladder applied offline in numpy (`relabel_reward.py --from-records`;
offline == direct verified 146/146). Records of RECORD must be produced on the cluster's 64-core
class (this box disagrees on 6/74 + 8/72 tapes after contact).

## 2. What is being tested on the cluster (state at the last check, ~21:00 09-11)

| jobs | what | registered in | state |
|---|---|---|---|
| `lz_rl_staged_*` 3539249–52 | {RLPD} staged ladder, 2v2, 100k | PHASE_PLAN (z) | COMPLETED; cells in `$LAB/gp_unified/baselines/rl/checkpoints/e2e/` |
| `lz_rl_sparse_*` 3539253–56 | {RLPD} sparse (settled contact only), 2v2, 250k | (z) | RUNNING |
| `lz_r2_staged_*` 3539257–60 | {r2dreamer} staged, 2v2, 1M | (z) | COMPLETED (an earlier note here said "s941 sliding in 12 %" — that was the withdrawn `slide_success` column, max 9.8 % in any window; no calibrated `home` cell exists for these runs) |
| `lz_r2_sparse_*` 3539261–64 | {r2dreamer} sparse, 2v2, 4M (moved to preempt) | (z) | RUNNING |
| `dpP_e2e_dHpruned_s100–107` 3562638–45 | {DP} e2e on the pruned human set | (ab) | RUNNING |
| `dpRS_*` 3562831–63 | unified-evaluator re-scores of raw/machine/pruned DP | (ab) | running / chained |

Old 16v16 batch (`e2eL_*`): cancelled by the user 09-11 13:10; completed seeds remain valid
within-learner cells at their achieved n (`paper/LONG_RUN_REGISTRATION_2026-09-08.md`, last section).
Trees of record: `$LAB/gp_unified` @ 92f46b7c (frozen from first job start), `$W/r2dreamer_unified`
@ 903c6a2, `$LAB/gp_dp_e2e` @ 847f635d. Logs: `paper/LADDER_PILOT_LOG_2026-09-11.md`,
`paper/DP_E2E_PRUNED_LOG_2026-09-11.md`. Readout commands are in those logs (§5 of the pilot log).

**Do not quote the pilot's `slide_success` numbers**: that predicate is a set-down-transient
detector on policies. **Re-scored (Lane 11, `paper/PILOT_RESCORE_2026-09-11.md`, 600 sampled
{RLPD} rollouts on this box): the pilot's staged policies do NOT slide** — `dH_s940` (100k final)
and `dH_s941` (40k) produce 0 `home` and 0 `slide_event` in 300 episodes; the only `home` firings
(4/600) come from the OLD-ladder 250k human long-run `dH_s901`. Of 32 old `slide_success`
positives, 4 survive; 27 of the 28 non-survivors had exactly 0.0 mm of credited slide gain. The
calibrated predicate also finds 21 slides the old rung missed (pushes that never arrived, 15 of
them from the machine long-run `dDPfirst_s920`). Lane 11 also found the `nested_ramp` exploit
(place-and-back-off collects the `farside` +1 through the withdrawal band) that forced the ramp
revision in §3. `eval_e2e.py --records-out` now exists; 600 records = 37 MB.

## 3. The Ladder N batch — SUBMITTED 2026-09-11 evening (PHASE_PLAN amendment (aa) + revision 1): jobs 3581558–3581577, log `paper/LADDER_N_PILOT_LOG_2026-09-11.md`; trees `$LAB/gp_ladderN` @ a40c8aa1 and `$W/r2dreamer_ladderN` @ 0cf3d9e (now ALSO do-not-touch while any `ln_*` job runs); sets `_rnrh`/`_rnsh`/`_rzh`; P-aa-7 NOT met on the machine set (home 12/12 on the class of record, not 13/14)

`ln_*`: `nested_ramp` v2 (picked 1 → placed_v2 1 → slide ramp ≤ 3 from the far side after the
set-down latch, span 0.05 m → home 4, max 9; `farside` logged, unpaid — revision 1 closed a
place-and-back-off exploit) 2v2 × both learners; `nested_sparse` (home 1.0 only) 2v2 × both; control = staged ladder +
new tip guard, {RLPD} 2v2. Predictions P-aa-2…7 in the amendment. The user's stated priority is
**a reward that produces the slide (put down → far side → slide → settled contact) as soon as
possible**; this batch is that test.

## 3b. Revision 3 + the world-model milestone cells (2026-09-12 ~03:00, Lane 14, HEAD ee9d449)

- **Revision 3 submitted: jobs 3596021–3596032** — {r2dreamer} `nested_sparse` 4M (s957/958 human,
  s977/978 machine, normal QOS), {r2dreamer} staged + `not_in_hand` guard control 1M (s962/963,
  s982/983, normal), {RLPD} `nested_sparse` 500k (s957/958/977/978, preempt). Stamps verified on the
  ones that started (P1: exactly three distinct `[ladder]` lines across every `ln_*` job ever run).
  **The {RLPD} rev-3 runs live under `baselines/rl/checkpoints/e2e_rev3/`** (seeds s958/s978 collided
  with the batch's control-arm run dirs, whose names carry no ladder) — readout globs must include it.
- **Milestone sweep (`cluster/ln_r2_milestone_sweep.sh`, deployed as `$W/ln14_milestone_sweep.sh`)**:
  copies each `milestones/online_*.pt` and each final `latest.pt` (sha-verified against the sidecar),
  scores rnd30 mode / hold15 mode / rnd30 sampled with the new evaluator on 64/64-core CPU nodes,
  idempotent, ≤ 6 jobs. **No cluster-side scheduler runs it** — the coordinator's session cron runs
  it hourly; a new agent must run it after each milestone lands. Table:
  `python3 $W/ln14_milestone_table.py` → `paper/LN_R2_MILESTONE_CELLS_2026-09-12.md`.
- **First world-model Ladder-N cells (26 cells, 645 episodes, 6 runs): `home` = 0 in every cell.**
  `dHfull_all_rnrh_s950` final 2M: picked 16/30, placed_v2 12/30, farside 11/30, **slide_event 9/30**,
  nested_v2 0/30 — the push happens, the arrival does not. `nested_sparse` machine s975 (1M): picked
  0/45. Three `nested_v2`-without-`home` (drop route) seen.
- **Hardware: "64-core" names two machine classes.** pax006/pax012 are 64 physical / 128 logical
  (SMT on); pax078/019/004/027 are 64/64. Cells written on SMT-on nodes read `picked` ≈ 0 for a seed
  that reads 8/30 (0.5M) and 16/30 (2M) on 64/64 nodes; they are quarantined as `<cell>_smt128` and
  the sweep now asserts both counts. Treat 64/64 as the class of record.

## 3c. 05:00 09-12 readout — {RLPD} ignites on `nested_ramp` (human); {r2dreamer} saturates the ramp (`paper/LN_RAMP_SATURATION_2026-09-12.md`)

- **{RLPD} 250k finals (in-job preview cells, `cluster/ln_cell_census.py`):** human ramp **s950 `home` in all
  six cells** (hold15 MODE 2/15, rnd30 MODE 1/30, spots60 MODE 7/60; 12 in 210 episodes), **s951 3/210**
  (spots60 only) → the human ramp arm satisfies rev 3's ignition clause with the two 500k seeds still
  pending. Machine ramp s970: `home` 0, `slide_event` 0 in 210 episodes; s971 still training. Staged+guard
  controls (s958/959/978/979): `home` 0/4 seeds; s959 nests 9/15 on hold15 MODE by the drop route
  (`slide_event` 1/15) — the behaviour Ladder N exists to refuse. `nested_sparse` RLPD finals land ~05:30.
- **{r2dreamer} 2M:** slides now happen (s950 hold15 MODE `slide_event` 12/15; s971 rnd30 MODE 11/30) but
  episode reward is **exactly 5.000 in 34 of 38 slide episodes** = the ramp (3 × min(1, gain/0.05 m)) is
  saturated at 5 cm and nothing pays until the +4 terminal 2–7 cm further; human idles to timeout, machine
  tips (28/30). `home` 0/270 pooled. Running the 4M extensions longer cannot create a gradient that is not
  there. Candidate amendment for the user (NOT registered): pay the ramp to arrival (per-episode span =
  set-down-to-threshold distance, or 0.12 m), and/or demo re-injection for the world-model learners.
- Videos: `can_pos_recovery/videos_ln_home_2026-09-12/` (2 RLPD `home`, 2 r2dreamer saturated slides), sent.

## 3d. 10:30 09-12 — the counter-vs-checkpoint discrepancy, explained on the local box (`paper/R2D_LIVE_VS_RELOAD_2026-09-12.md`, lane DV3-9)

- Local reproduction: the warm-restart dv3 run's training record showed 22 `home` episodes and windows with
  `placed_v2` 0.72 / `slide_event` 0.31, while its 1M milestone evaluated to placed_v2 0/15 (hold15 MODE), home 0/180.
- **Verdict: not a reload defect, not an evaluator defect.** The reloaded checkpoint run through the trainer's OWN
  `make_envs → ParallelEnv(6) → adapter → FullTaskEnv` loop with the training IC draw gives placed_v2 0.117
  (sampled, n=60) / 0.000 (mode, n=30) — the `eval_genesis` cells. Reload bit-identical (79/79 acting-path tensors;
  0 absent). The policy is **non-stationary on a ~1k-gradient-update timescale**: rolling-60 placed_v2 0.583 →
  0.133 → 0.867 within 9 315 counter steps; the 125-episode window straddles the transient, the capture hit the trough.
  Confirmed on the 0.5M milestone (record last-60 0.150 v hold15 0.200) and the parent's 2M (last-30 0.667 v hold15
  mode 0.667).
- **Rules from this:** (1) compare a checkpoint with the ≤ 60 record episodes ending at its capture, never a longer
  window; (2) one milestone/final checkpoint is a lottery draw for a world-model run — evaluate a checkpoint SERIES
  (the local run now snapshots every `latest.pt` rewrite, 100k counter steps, into
  `~/runs_dv3_local/dv3e2e_ramp_resume_series/ck_<counter>/` with hold15+rnd30 MODE cells and the matching record
  window) or an averaged actor before calling a run ignited or dead; (3) run `probe_live_vs_reload.py` before
  suspecting the evaluator. The cluster s945 case (0.79 → 0/90) is larger than anything measured here — unexamined.
- **Trap fixed the same morning:** `eval_genesis.py` takes the world from `R2D_SIM_VARIANT` (default `base`); two
  resume-run cell sets evaluated without it are quarantined as `*_WRONGWORLD_base/INVALID.txt`. Every eval must
  export it and its log must contain a `[sim-variant]` line.

## 3e. 11:00 09-12 — the ramp resume run HUNG and was killed; its last checkpoint is the best slider on record; `nested_sparse10` launched locally with a world-model warm start

- **Hang:** the warm-restart ramp run (PID 878324) wrote nothing after 09:06:23 (console, metrics, latest.pt), main
  process idle and blocked in a unix-socket read on its `ParallelEnv` workers, five workers in `poll`, no OOM/GPU
  kernel messages. Onset coincides with the DV3-9 probe opening six more Genesis worlds on the same GPU (~09:15).
  Killed at 10:46 (`KILLED_HUNG.txt` in the logdir). **Rule: never run a multi-world probe beside a training
  process on this box** — single-world evaluation cells were fine all week.
- **Its last checkpoint, `dv3e2e_ramp_resume_series/ck_1417866_hang/` (counter 1 417 866 = parent 2M + 1.3M):**
  hold15 MODE picked 15/15, placed_v2 13/15, farside 13/15, **`slide_event` 13/15**, `home` 0/15, tipped 14/15;
  rnd30 MODE picked 21/30, placed 21/30, farside 17/30, slide_event 16/30, **`home` 1/30** (ep 1, reward 9.0),
  tipped 24/30. Matches its own last-60 record window (placed 0.90, farside 0.75, slide 0.53). The behaviour is
  the cluster s971 pattern: slide to the ramp's 5 cm saturation, keep pushing, tip. Videos sent to the user.
- **Launched 10:52: `dv3e2e_sparse10_dHfull_all_rns10h_wm_rlDreamer_s0`** (PHASE_PLAN (ac-local); PID 1024588;
  `~/runs_dv3_local/LAUNCH_sparse10_wm_s0.txt`): `nested_sparse10`, human set built from the LOCAL stage records
  (Σ 130 = 10 × 13 `home`; the class-of-record set has 12), 2M online steps, and `+wm_init=` (new r2dreamer
  `train.py` option, local commit 066162a) loading the encoder/RSSM/decoder/cont + frozen clones from the checkpoint
  above (93 tensors, 12.5 M params); reward head, actor, critics, normaliser, optimizer fresh. Stamps verified:
  `ladder=nested_sparse10 | home=10 | max_return=10`, `return_clamp=10.0 (env and model agree)`, `[wm_init] loaded 93`,
  `[sim-variant] gc_kp4_riser3_shelf6`. Checkpoint series watcher: `~/runs_dv3_local/dv3e2e_sparse10_series/`.
- **Follow-up (unregistered):** the relabel keeps every tape's frames after the paid terminal and never sets
  `is_terminal` on the `home` frame (13/13 `+10`s are mid-tape; `rewarded_terminals: 0` in the prefill report is
  that, not a lost reward). Online, `home` ends the episode. The demos therefore teach "the episode continues after
  home with zero reward" — harmless for the actor's incentive, inconsistent for the continuation head. Same
  construction on the cluster sets. Worth a `--cut-at-terminal` in `relabel_reward.py`, registered, before the next
  set build.

## 3f. 11:10 09-12 — cluster operations move to the other workstation (user: "you won't get the cluster back, we'll monitor from here")

The pop-os box keeps only the local {dv3} work (§3e) and an hourly LOCAL check. Everything cluster-side is now the
other workstation's, in this order:
1. **Milestone sweep, hourly, idempotent:** `bash $W/ln14_milestone_sweep.sh; python3 $W/ln14_milestone_table.py`
   (deployed copy carries the 05:15 dedupe fix; `cluster/ln_r2_milestone_sweep.sh` in the repo is identical). Flag any
   cell with `home` ≥ 1. Instant `FAILED 2:0 00:00:01` `lnms_*` jobs = the SMT guard refusing a node; check the job's
   `ExcNodeList` contains that host before suspecting anything else.
2. **{RLPD} Ladder N finals:** `ssh … "python3 - \$(ls -d $LAB/gp_ladderN/baselines/rl/checkpoints/e2e/e2e_rlpd_* $LAB/gp_ladderN/baselines/rl/checkpoints/e2e_rev3/e2e_rlpd_* <gp_ac clone>/baselines/rl/checkpoints/e2e_ac/e2e_rlpd_*)" < cluster/ln_cell_census.py` (the (ac) RLPD runs live under `e2e_ac/` in the (ac) clone — AUDIT_AC_PACKING B1-related)
   — the four `nested_sparse` 250k finals (s955/956/975/976) and machine ramp s971 were still training at 05:15; they
   decide ramp-vs-sparse alongside `paper/LN_RAMP_SATURATION_2026-09-12.md` §3. Preview cells suffice for the
   ignition read; the pinned re-score (`cluster/sbatch_e2e_rescore.sh`) is what makes them cells of record.
3. **Amendment (ac) smokes then the 2v2 batch** (`cluster/submit_ac_smokes.sh` is theirs): stamps
   `ladder=nested_sparse10 | home=10 | max_return=10 …` and `return_clamp=10.0 (env and model agree)` on both learners
   before any batch; batch = same seeds/budgets/QOS split as the rev-3 sparse arm, fresh gp clone (never `$LAB/gp_ladderN`).
4. **The 4v4 ignition decision** (PHASE_PLAN (aa) rev 3) once the RLPD 500k and r2dreamer 4M seeds land; the 16 v 16
   stays held until then.
5. **s945 (RC2)**: with `paper/R2D_LIVE_VS_RELOAD_2026-09-12.md`'s probe in hand (`probe_live_vs_reload.py`, in the
   full bundle), run it on s945's checkpoint against its last-60 record window before any cluster r2dreamer training
   counter is quoted again.
The local box's hourly cron was replaced by a local-only version at 11:10 (no ssh attempts).

## 3g. 11:40 09-12 — user redirect: PIXEL observations + image augmentation, `nested_sparse10`, persistent world models

- User (verbatim intent): "no i dont want to mess with dv3 training, you should bring in pixel obs and image
  augmentation and try to train sparse10 with some persistent world models." Losses stay stock; inputs change.
- The state-based `nested_sparse10` warm-start run (§3e) was **stopped at counter 357 640 / 230 episodes** to free the
  box for the pixel pipeline (`STOPPED.txt` in its logdir; `latest_at_stop.pt` kept; series snapshots in
  `dv3e2e_sparse10_series/`). Superseded, not failed.
- Facts that fix the design: the recorder's re-execution builds its env with `camera_rig=False`
  (`relabel_reward.py:339`) so every demo set's `image` is zeros; the training adapter already renders the rig
  (top ++ wrist, 64×64×6) every step and discards it; `augment_data` exists on the DreamerPro path; the prefill
  reads real images unchanged. A pixel world model cannot inherit the state encoder, so the first pixel model starts
  fresh and is the one kept (`+wm_init`) for every later ladder variant.
- Lanes (Opus): **PX-1** adds `--images` to the recorder and builds `dHfull_all_rns10h_img` (must reproduce Σ 130 /
  13 `home` / identical action sha; contact sheet for the user) → `paper/PX_IMAGE_DEMOS_2026-09-12.md`. **PX-2**
  adds `configs/env/genesis_full_pixel.yaml` (`cnn_keys: image`), `env.state_slice: 8` (pixels + proprioception
  only: q[:6], gripper motor, grip effort — no privileged can/goal pose), `model.image_aug: shift4` (DrQ random
  shift, one offset per sequence, before encoder AND decoder), verifies `+wm_init` under the pixel config, smokes both
  pixels+proprio and pixels+full-state and reports fps → `paper/PX_PIXEL_CONFIG_2026-09-12.md`. The coordinator
  registers PHASE_PLAN (ad) and launches; the number of concurrent world models is set by the measured pixel fps.

## 3h. 12:42 09-12 — pixel `nested_sparse10` run LAUNCHED (PHASE_PLAN (ad))

- Both lanes landed: PX-1 (`a083664`) — `relabel_reward.py --images`, set `dHfull_all_rns10h_img` (74 tapes, Σ 130,
  every non-image column byte-identical to the state-only set, images real 74/74, rendered past the paid terminal so
  no state pairs with a blank frame; contact sheet `can_pos_recovery/videos_ln_home_2026-09-12/px_contact_sheet_232.png`
  sent to the user). PX-2 (r2dreamer local `43a0e3c`, doc `d01bb90`) — `configs/env/genesis_full_pixel.yaml`
  (`cnn_keys image`), `env.state_slice` (8 = q[:6], gripper motor, grip effort; no can/goal pose), `model.image_aug
  shift4` (random shift before encoder and decoder, one offset per sequence), `[obs]/[image]/[image_aug]` console
  stamps, `+wm_init` refuses missing tensors by name; two smokes clean; **46.7 fps** for both pixel configs vs 81.7
  state-only; a state checkpoint cannot warm-start a pixel run (22 shape mismatches), so the first pixel world model
  is fresh and becomes the bank.
- **Run:** `~/runs_dv3_local/dv3px_sparse10_dHfull_all_rns10h_img_rlDreamer_s0`, PID 1095029, launched 12:42
  (`LAUNCH_px_sparse10_s0.txt`): pixels + proprio, shift4, `nested_sparse10`, human `_img` set, seed 0, 2M online
  (~12 h), milestones 0.5M/1M/2M, `buffer.max_size` 5e5 (image column ≈ 12 GB RAM + ~10 GB latents; the box has 62 GB
  — one run at a time; a second seed starts at 0.5M if `picked` ≥ 0.5, or on another machine once the `_img` set is
  rsynced). Series watcher `dv3px_sparse10_series/` (hold15 + rnd30 MODE per `latest.pt` rewrite + record window).
  Hourly local check reads `~/runs_dv3_local/CURRENT_RUN.txt`.
- Predictions (ad): P-ad-1 hold15 MODE `picked` ≥ 0.5 by 1M (disconfirm < 0.2 → fall back to pixels + full state,
  `env.state_slice=null`, already smoked); P-ad-2 ≥ 1 series cell with `home` by 2M; P-ad-3 a warm-started second
  pixel run reaches P-ad-1 in ≤ half the steps.
- **14:50 — IGNITED at 0.3M** (`paper/PX_SPARSE10_FIRST_HOME_2026-09-12.md`): series checkpoint `ck_421321` hold15 MODE
  picked 14/15, placed 14/15, farside 14/15, slide_event 5/15, **`home` 5/15**, agreeing with its 60-episode record
  window (home 0.37); **rnd30 MODE picked 16/30, farside 16/30, slide_event 7/30, `home` 7/30** (previous best on that
  cell from any learner: 1/30). **16:20 — ck_619574 (0.50M): hold15 MODE `home` 12/15, rnd30 MODE `home` 18/30**
  (picked 15/15 and 21/30; ck_519849 in between read 1/15 and 3/30 — the series, not one checkpoint, is the number).
  P-ad-1 and P-ad-2 met. n = 1; three things changed together (pixels+aug, +10, no privileged pose) —
  the cluster's (ac) state-based arm is the +10-alone comparison. The next pixel world model (seed 1, warm-started per
  P-ad-3) should start once the 0.5M cells are in; the `_img` set (67 MB) is what the other workstation needs to run the
  same arm on the cluster.

## 4. Do not touch

`$W/gp_root`, `$LAB/gp_e2e`, `$W/r2dreamer_fix`, `$W/long_run_candidate_2026-09-08` (old trees;
some completed-batch evals still import them); `$LAB/gp_unified` and `$W/r2dreamer_unified` while
any `lz_*` job runs (their end-of-job eval re-imports the tree); any running `lz_*`/`dpP_*`/`dpRS_*`
job. Never set `FULLENV_REWARD_X`. Never edit `stage_predicates.py` constants without a registered
amendment — every ladder change is a retrain.

## 5. On reconnect, in this order (each step has commands in `HRI_results/RESULTS_PROVENANCE_2026-09-11.md`)

1. `squeue`/`sacct` for the jobs in §2; run `$LAB/e2e_health.py` for the old batch; check disk ≥ 150 GB.
2. Make a fresh clone `$LAB/gp_ladderN` from `origin/ladder-unify-2026-09-11` (do NOT pull into
   `gp_unified`). Apply the r2dreamer bundle (§6) to a fresh `$W/r2dreamer_ladderN`.
3. Regenerate stage records on a 64-core node for both sets (P-aa-7), then build the demo sets
   `dHfull_all_{rh,rn}` and `dDPfull_first_{rh,rn}` from records (action sha256 identical to the
   sources, only the reward column differs; manifests carry the stamp). Compare the `home` counts with
   the local 13/14.
4. Smoke ONE run of each learner per ladder with `TIP_GUARD=not_in_hand` and `LADDER=nested_ramp` /
   `nested_sparse`; the `[ladder]` stamps must be identical within a ladder and differ between ladders
   only in ladder/rungs/max/terminal/clamp. Confirm the evaluator scores a sparse checkpoint with
   `success_key=home`.
5. Submit the 20 `ln_*` jobs per the (aa) table; write job ids into a new `paper/LADDER_N_PILOT_LOG_2026-09-11.md`.
6. Re-score the pilot's staged cells with `slide_event`/`home` (records via `eval_e2e.py --records-out`, Lane 11).
7. Start an hourly health check (the one in this session was session-local and is gone).

## 6. Machine differences

- **This box (pop-os "bigbox") has no rsync-only data** (no `baselines/demos_v2`, no
  `~/wm_fix_2026-09-03`, no census videos). A canonical local copy of the two demo sets and the stage
  records is at `/home/j/data/genesis_pickaplace/` (7 MB + 16 MB) — regenerable from the cluster.
- **The other computer holds uncommitted work** from the 09-07/08 sim-box sessions
  (`render_census.py` fixes, `slide_score.py`, `render_slide_panel.py`, SLIDE docs, CONFOUNDS rows):
  commit and push them there.
- **r2dreamer:** `~/workspace/r2dreamer` here is at the HEAD named by the newest bundle (13 commits past
  6af0ec7: Ladder N, the tip guard, the scope-aware clamp check, the step-accounting preflight fix); its remote is the upstream repo, so they were never
  pushed. They are in `cluster/bundles/r2dreamer_ladderN_2026-09-11d.bundle` (the `…11.bundle`, `…11b`,
  `…11c` are earlier snapshots; use the LATEST letter). On the other computer:
  `cd ~/workspace/r2dreamer && git fetch <path-to-bundle> main:ladderN && git checkout ladderN`
  (the bundle needs 6af0ec7 in the local history; `git bundle verify` says so). Lane 12a's tip-guard
  commit may follow in a second bundle (§7).
- The RLPD-capable local venv is `.venv-eval` (sb3 2.8.0); `~/workspace/genesis_sim2real/venv`
  runs Genesis but has no stable_baselines3.

## 7. Agents that may still be running here when you read this

**Lane 12a LANDED** (merge e80b889): the tip guard is implemented as `tip_guard='grip'|'not_in_hand'`
(required by both launchers, both trainers, the relabel and the annotator; stamped as
`tip=tilt>60deg&not_in_hand@4f`), `paper/TIP_GUARD_IMPL_2026-09-11.md`. The r2dreamer side is in
the newest bundle `cluster/bundles/r2dreamer_ladderN_2026-09-11d.bundle` (13 commits: adds the
scope-aware return-clamp check and the step-accounting preflight fix, both found by the local dv3
pick lane) — always use the latest letter. Its r2dreamer edits were syntax-checked only; a local smoke was dispatched
(see `paper/R2D_LADDERN_SMOKE_2026-09-11.md` if it exists) and the FIRST cluster smoke must show
`tip=…not_in_hand@4f` in the `[ladder]` stamp before any (aa) job is submitted.

DreamerV3 on this box: the pick run passed G2 (`paper/DV3_LOCAL_EVAL_2026-09-11.md`); the e2e run on `nested_ramp` v2 with the human set of record **FINISHED its 2M online steps 2026-09-12 ~03:30** (`~/runs_dv3_local/dv3e2e_ramp_dHfull_all_rnrh_rlDreamer_s0/`; readout `paper/DV3_LOCAL_E2E_COLLAPSE_2026-09-11.md`, final section). Final cells (`~/runs_dv3_local/dv3e2e_ramp_eval_final2M/`): hold15 sampled/mode picked 0.667/0.800, placed_v2 0.400/0.667, farside 0.267/0.600; rnd30 sampled/mode picked 0.533/0.467; **rnd30 MODE episode 29 reached `home` (reward 7.69/9)** — the first calibrated `home` from any RL policy in an evaluation cell (video `fresh_eval_rnd30_mode/ep29_rnd29_home.mp4`, sent to the user). One episode; not a rate. A **warm-restart extension** (weights+optims from `latest.pt`; buffer restarts at prefill, counter fresh — that is what `+resume=true` means in this trainer) is running for a further 2M: PID 878324, logdir `~/runs_dv3_local/dv3e2e_ramp_dHfull_all_rnrh_rlDreamer_s0_resume2M/`, `~/runs_dv3_local/LAUNCH_e2e_ramp_s0_resume.txt`, ~7 h; milestones 0.5M/1M/2M of the resumed counter. Your port at parity is on `~/workspace/dreamerv3-torch` branch `genesis-dv3dbg-local` (`paper/DV3_PORT_PARITY_2026-09-11.md`), not yet run past a smoke.

Still running when this was written: Lane 11 (`eval_e2e.py --records-out` + local re-score of the
pilot's staged checkpoints under the calibrated predicate → `paper/PILOT_RESCORE_2026-09-11.md`), and
the DreamerV3 lanes for the local box (DV3-1 pick run in the r2 port with `rep_loss=dreamer`;
`paper/DV3_LOCAL_2026-09-11.md`, `DV3_TREES_AUDIT`, `DV3_PORT_PARITY`). The coordinator merges and
pushes when they finish; if this file is older than the branch tip, look for those docs.

## 7b. Follow-ups left deliberately unmerged (2026-09-11 evening)

- **r2dreamer trainer-side gate refusal** (lane 12b, `paper/R2D_LADDERN_SMOKE_2026-09-11.md`):
  `train.py` never calls `full_env.refuse_legacy_gates()`, so `FULLENV_TIP_GUARD=1` is refused only
  by the sbatch launchers' preflight, not by the trainer itself. Fix exists on r2dreamer worktree
  branch `lane12b-fullenv-tipguard-fix` (commit 5e3a627); NOT merged into r2dreamer `main` because
  Lane 13 was copying the committed tree (0cf3d9e) to the cluster at the time. Merge it after the
  (aa) batch's tree is frozen, cut a new bundle, and note that the batch ran with launcher-side
  refusal only.
- **CPU `torch.compile`/inductor crash on the first `agent.update()`** in the r2dreamer port
  (lane 12b, CPU smokes only). GPU runs are unaffected (the local DreamerV3 pick run and every
  cluster run use CUDA). Not fixed; do not smoke this port on CPU with `model.compile=True`.
- The set names the suffix map produces: `_rnrh` (nested_ramp + not_in_hand guard), `_rnsh`
  (nested_sparse + guard), staged + guard per lane 12a's map (`_rzh`); Lane 13 records the cluster
  names in `paper/LADDER_N_PILOT_LOG_2026-09-11.md`.

- **{RLPD} episode records lack the Ladder N stages.** `episode_rollouts.jsonl` carries
  `ep_nested_v2` but not `ep_farside` / `ep_slide_event` / `ep_home`; only the evaluator's cells
  carry them. Any "RLPD has 0 slides" read off the records is an ABSENT-KEY artefact (the
  coordinator made that mistake once on 2026-09-11 and corrected it). Read {RLPD} slides from
  checkpoint evaluations (`paper/RL100_READOUT_2026-09-11.md`). For the next batch add the three
  stages to the RLPD episode record (amendment (w) emitter in `full_env.py`) so the training
  curves carry them.

## 8. Open decisions for the user

1. Ramp span 0.05 m (taken) — confirm.
2. `far_release` OFF (measured: removes 2 machine slides, 0 human) — confirm.
3. Whether the 20-job (aa) batch replaces or precedes a 16v16 on the winning ladder.
4. `contact_push` has no tilt clause (pays for shoving a flat can once) — patch in the next amendment?
5. The golden set (blind-labelled trajectories) — still the only thing that validates predicates
   against human judgement rather than against each other.
