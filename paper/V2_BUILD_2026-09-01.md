# A21 v2 demonstration-set build — 2026-09-01 (cluster, via ccmd)

Executes PREREG A21 (+A23 eval protocol; A22 was withdrawn before any use) on the Tufts
cluster. Every state-changing command below was run via `~/bin/ccmd tufts '...'` and is in
`paper/SESSION_LOG_2026-08-23_cluster.md` (rows 09:0x–09:5x EDT 09-01). Frozen sets
(`baselines/matched_v2/{dH,dDP,dR2D,...}`, `baselines/matched_w3/{dH,dDP,...}`) were NOT
touched — the v2 sets are NEW sibling dirs `dHv2` (and later `dDPv2`) under the same roots.
Repo commits for this build: `183fa91` (make_v2_matched.py + dHv2/dDPv2 native-arm
allowlist in sbatch_dp/rlpd), `9dee615` (make_eval_ics_v2.py), `98e7dc2` (the two
generated eval IC files). Cluster repo pulled to each of these before the dependent step.

## 1. Eligible-uid pool (N = 72)

All success-labeled trials with a validated (ok-class) placement after the 08-31 recovery
(`paper/DEMO_RECOVERY_RESULTS_2026-08-31.md`, merged at `a478d95`; verified on the cluster
copy: 233/255/262/267/275/301/321 etc. status ok with the recovery `can_pos`), excluding:

- **303** — unsolved (no video; grid exhausted; control-limited; status `no_shelf_batch`).
- **237** — camera-convicted control-limited (projected seeds sit ON the visible can;
  0/31 candidates lift; `can_pos_recovery/camera_audit/calib/preclose_237.jpg`).
- **294** — solved, but its earlier env-basin flip left it "pending" in A21; excluded as
  registered. (The 09-01 local session later attributed the flip to load; A21's exclusion
  list is kept as registered — 294 can only enter via a further amendment.)

Pool = the 66 frozen-src uids + the 6 newly enabled ones (**236, 239, 244, 255, 261,
331**): 232 233 234 235 236 239 242 243 244 245 246 247 248 250 251 252 254 255 256 257
258 259 261 262 263 265 266 267 269 273 274 275 276 277 278 279 280 281 283 284 286 287
293 295 297 298 299 300 301 302 304 305 306 308 309 311 315 316 317 318 319 320 321 325
326 327 328 329 330 331 333 335.

## 2. Precondition: extend `baselines/episodes_pick_phase_dppruned` 66 → 72

The recorder `--src` dir lacked stride-1 command tapes for the 6 uids above (their July
`episodes_all` tapes were no-pick at the pre-recovery placements; `episodes_all` itself is
devbox-only). Rebuilt on the cluster with the SAME chain that built the original 66
(`collect_all_classified.py` → `make_pick_phase_datasets.py` → `make_dp_pruned.py`,
defaults; METHODS_draft §"Leading-idle pruning"):

- job **3118109** (`v2fill`): `collect_all_classified.py --outdir
  baselines/episodes_all_v2fill --uids 236 239 244 255 261 331` — all 6 collect at their
  recovered placements: 236/239/244/261/331 **placed**, 255 **nested** (so all ≥ picked);
  then `make_pick_phase_datasets.py --src baselines/episodes_all_v2fill --outdir
  baselines/episodes_pick_phase_v2fill --bank baselines/pick_entry_states_v2fill.json`
  → 6 pick-phase tapes (len min/med/max 215/552/1718), 0 skipped.
- job **3118128** (`v2prune`): `make_dp_pruned.py --src baselines/episodes_pick_phase_v2fill
  --dst baselines/episodes_pick_phase_v2fill_pruned` (4022 → 3110 frames, 22.7% pruned)
  then `cp` of the 6 npz into `episodes_pick_phase_dppruned/` → **72 tapes**. The original
  66 are byte-untouched.

## 3. dHv2 collections — fresh ICs at the validated placements, both worlds

`cluster/sbatch_record.sh`, TEACHER=human, SRC=`baselines/episodes_pick_phase_dppruned`,
8 shards, batch partition, CPUS=4 MEM=12g TIME=2:00:00, seed 0, rollout-base 100000.
**`--ic-mode demo` (reset(uid) → current `trial_placements.json` positions); NO
`--ic-from-tape`** — this is the A21 point (kills the ~2 cm training-IC drift).
`--arrival either` kept (the frozen block of record, `dH_either`/`dH_w2`, used it).

- **Old world** (`sim_variant=base`): jobs **3118130–3118137**, EXTRA=`--arrival either`,
  outdir `baselines/demos_v2/dHv2` (+`dHv2_fails`).
- **Corrected world**: jobs **3118138–3118145**, EXTRA=`--arrival either --sim-variant
  gc_kp4_riser3_shelf6`, outdir `baselines/demos_v2/dHv2_w2` (+`dHv2_w2_fails`) — the
  `_w2` recording-name convention matches the frozen `dH_w2` → `matched_w3` pipeline
  (session log rows 117/139). `[sim-variant] kp=800/800/600/400/240/240 gc=1.0 riser=0.03`
  confirmed in shard logs; manifests assert sim_variant uniformity.

**Result: 60 kept / 12 fail of 72 attempted, in EACH world** (jobs all clean, no FATAL).
Merged with `record_demos.py --teacher human --merge` (job **3118179**): dHv2 rollouts=72
kept=60 `records_complete=True` sha `7576d2ea7cbbffd5`; dHv2_w2 rollouts=72 kept=60
`records_complete=True` sha `aaa88a8185f46f0e`. (The fails-dir merges print the known
cosmetic "0 kept records vs N npz" warning — fails dirs contain only non-kept tapes.)
Censuses: `baselines/demos_v2/census_dHv2.md`, `census_dHv2_w2.md` (same job).
Negative control: job **3118209** `--teacher random --ic-mode demo --n 3` → see §7 check.

### Why 60 and not ≈72 (per-uid, no hidden drops)

Keep rate 60/72 = 0.83 ≈ the frozen sets' 0.85 (`dH_either` 56/66) and 0.88 (`dH_w2`
58/66). The A21 "expect ≈70+" was optimistic; every fail is one of the three documented
classes (all deterministic — the frozen 08-23 retry showed identical outcomes):

| class | old world | corrected world |
|---|---|---|
| lying-can IC, tip rule at t=0 (legacy lying-spawn placements; grip open + tilt → terminate n=1) | 234, 318 | 234, 318 |
| over-horizon: tape > 1200-sim-step contract cap (`env_truncated` at 300 decisions) | 245, 286, 293, 295, 299, 300, 331 | 245, 246, 286, 293, 295, 300, 331 |
| follower miss (`adapter_exhausted`, dilation 1.2–1.6; pick never hardens at repeat 4) | 233, 256 | 261, 278, 319 |
| tipped mid-follow | 246 (n=127) | — |

Notes: 233 (the user-validated "perfect" demo) nests at stride-1 replay but its r4
follower run misses in the old world only — it IS kept in the corrected world. 5 of the 6
newly enabled uids are kept in the old world (236/239/244/255/261; 331 is over-horizon in
both), 4 in the corrected (236/239/244/255).

## 4. Matched sets + lerobot datasets (job 3118180)

`baselines/make_v2_matched.py` (new, commit `183fa91`; uses make_matched_sets'
read_dir/write_set = same contract validation, hard links, content sha, sim_variant
FATAL-on-mix, manifest as the frozen builder; refuses frozen names / existing dirs):

- `--src baselines/demos_v2/dHv2 --name dHv2 --out-root baselines/matched_v2`
  → **matched_v2/dHv2: N=60, 60 ICs, rows 7551, len p50 115, sim_variant=base,
  sha `7576d2ea7cbbffd5`**
- `--src baselines/demos_v2/dHv2_w2 --name dHv2 --out-root baselines/matched_w3`
  → **matched_w3/dHv2: N=60, 60 ICs, rows 7129, len p50 107,
  sim_variant=gc_kp4_riser3_shelf6, sha `aaa88a8185f46f0e`**

Lerobot datasets, EXACT frozen invocation (session-log rows 13/157):
`convert_to_lerobot.py <set> <set>/lerobot 8 4 none image` → 60 episodes each, **fps 7.5**
(= 30/action_repeat 4), finalized + metadata verified. Both pass sbatch_dp's provenance
gates (episode count == npz count, fps gate, manifest contract v1, sim_variant match).

## 5. A23 evaluation IC files (committed)

`baselines/make_eval_ics_v2.py` (commit `9dee615`) generated from the dHv2 manifests
(commit `98e7dc2`): `baselines/eval_ics_v2.json` (old world) and
`baselines/eval_ics_v2_w3.json` (corrected). Keys keep the launcher contract so
`IC_FILE=<file>` is the only knob: **sel** = first 15 of the sorted kept training ICs
(A23 "fixed 15-IC training subset", mirroring the frozen sorted-prefix sel), **hold** =
ALL 60 kept training ICs (A23: reported strictly as IN-DISTRIBUTION, not a holdout),
**rnd** = byte-copy of `baselines/eval_ics.json` rnd-30 (the headline statistic,
comparable to A16/A20). sel old = [232 235 236 239 242 243 244 247 248 250 251 252 254
255 257]; sel w3 = [232 233 235 236 239 242 243 244 247 248 250 251 252 254 255].

## 6. DP teacher trainings (submitted; the only GPU jobs of this build)

Mirrors the frozen teacher recipe exactly: 2 pilot seeds per world through
`cluster/sbatch_dp.sh` (100k grad steps, batch 64, diffusion, action_repeat 4, in-job
select/confirm on the DEFAULT `baselines/eval_ics.json` — same as the frozen pilots),
teacher then chosen per PREREG §3.1 (median in-dist seed; 2 seeds → the lower; ckpt by
sel). Run names carry v2: `dHv2_DP-pilotv2_s{0,1}`, `dHv2_DP-pilotv2w3_s{0,1}`.

| job | arm/wave | DEMO_ROOT | SIM_VARIANT | status at handoff |
|---|---|---|---|---|
| ~~3118190~~ → **3118203** | dHv2 s0 WAVE=pilotv2 | matched_v2 | base | 3118190 died at 36 s (CUDA "device busy" on **pax007**, the known bad node A20 also excluded) → resubmitted `DUPLICATE_OK=cuda-transient-retry --exclude=pax007` |
| **3118191** | dHv2 s1 WAVE=pilotv2 | matched_v2 | base | training |
| **3118192** | dHv2 s0 WAVE=pilotv2w3 | matched_w3 | gc_kp4_riser3_shelf6 | training |
| **3118193** | dHv2 s1 WAVE=pilotv2w3 | matched_w3 | gc_kp4_riser3_shelf6 | training |

All four passed DEMO-SHA / PROVENANCE-OK / REGISTRY gates (registered in
`cluster/RUN_REGISTRY.jsonl`, git 183fa91, demo shas above). ~4–5 h train + in-job
select/confirm each; DP-HEADLINE lines land in `dp_<jobid>.out`.

## 7. What remains (the documented chain — NOT run in this build)

1. **Check** (first reader): `grep -h "DP-HEADLINE" dp_311819*.out dp_3118203.out` and the
   negctl merge (`baselines/demos_v2/_negctl` must have kept ≈0).
2. **Teacher selection** per §3.1 from the four DP-HEADLINE lines (per world: lower of the
   two seeds at the median-in-dist rule; selected ckpt from its SELECT-RESULT).
3. **dDPv2 harvest** (per world, mirroring rows 138/13:37 of the session log; GPU policy):
   ```
   TEACHER=dp CKPT=baselines/outputs/dp_pilotv2/dHv2_DP_s<K>/checkpoints/<SEL>/pretrained_model \
     OUTDIR=baselines/demos_v2/dDPv2 SHARD_N=6 GRES=gpu:1 PARTITION=gpu CPUS=4 MEM=16g TIME=2:00:00 \
     EXTRA="--ic-mode demo --attempts 3 --mode sample --verify" bash cluster/sbatch_record.sh
   # corrected world: CKPT from dp_pilotv2w3, OUTDIR=baselines/demos_v2/dDPv2_w2, TIME=2:30:00,
   #   EXTRA += " --sim-variant gc_kp4_riser3_shelf6"
   ```
   (No `--ic-from-tape`: every pool uid is resettable now. IC pool = `--uids-from` default
   `episodes_pick_phase_dppruned` ∩ success_uids = the 72.) Then `--merge` each.
4. **dDPv2 matched sets** (per-IC matched to dHv2, loud on shortfall):
   ```
   python baselines/make_v2_matched.py --src baselines/demos_v2/dDPv2 --name dDPv2 \
       --out-root baselines/matched_v2 --base baselines/matched_v2/dHv2
   python baselines/make_v2_matched.py --src baselines/demos_v2/dDPv2_w2 --name dDPv2 \
       --out-root baselines/matched_w3 --base baselines/matched_w3/dHv2
   python baselines/convert_to_lerobot.py baselines/matched_v2/dDPv2 baselines/matched_v2/dDPv2/lerobot 8 4 none image
   python baselines/convert_to_lerobot.py baselines/matched_w3/dDPv2 baselines/matched_w3/dDPv2/lerobot 8 4 none image
   ```
5. **Learner blocks** (A21 + A23; fresh seed ids per A9; 20 DP + 32 RLPD GPU jobs — QOS-pace):
   ```
   # DP, n=5/arm/world, seeds 50-54
   for A in dHv2 dDPv2; do for S in 50 51 52 53 54; do
     ARM=$A SEED=$S WAVE=v2final DEMO_ROOT=baselines/matched_v2 SIM_VARIANT=base \
       IC_FILE=baselines/eval_ics_v2.json sbatch -p gpu cluster/sbatch_dp.sh
     ARM=$A SEED=$S WAVE=v2finalw3 DEMO_ROOT=baselines/matched_w3 SIM_VARIANT=gc_kp4_riser3_shelf6 \
       IC_FILE=baselines/eval_ics_v2_w3.json sbatch -p gpu cluster/sbatch_dp.sh
   done; done
   # RLPD sparse γ0.99 UTD10, n=8/arm/world, seeds 50-57 (statistic = A16: LAST-ckpt rnd-30 + divergence)
   for A in dHv2 dDPv2; do for S in 50 51 52 53 54 55 56 57; do
     ARM=$A SEED=$S REWARD=sparse GAMMA=0.99 UTD=10 WAVE=g99v2 DEMO_ROOT=baselines/matched_v2 \
       SIM_VARIANT=base IC_FILE=baselines/eval_ics_v2.json sbatch -p gpu cluster/sbatch_rlpd.sh
     ARM=$A SEED=$S REWARD=sparse GAMMA=0.99 UTD=10 WAVE=g99v2w3 DEMO_ROOT=baselines/matched_w3 \
       SIM_VARIANT=gc_kp4_riser3_shelf6 IC_FILE=baselines/eval_ics_v2_w3.json sbatch -p gpu cluster/sbatch_rlpd.sh
   done; done
   ```
   `hold` numbers from these runs are IN-DISTRIBUTION (60 training ICs) per A23; headline =
   rnd-30. Check each .out for FATAL within minutes (this build hit 1 CUDA-transient in 21
   jobs). `world_of` in `analysis/results_table.py` must map waves v2finalw3/g99v2w3 →
   corrected before any table.

## 8. Deviations / judgment calls (disclosed)

- Kept 60/72 per world, not the "≈70+" A21 hoped for — §3 itemizes all 24 fails into the
  three pre-existing failure classes; nothing new appeared, nothing was retried into the
  kept set (human follower is deterministic; the frozen block's retries proved it).
- Teacher trainings: **2 seeds per world** (4 jobs), mirroring the frozen pilot recipe
  (`dp_pilot` s0/s1, `dp_pilotw2` s0/s1) rather than the literal "one per world" — §3.1
  needs ≥2 seeds to apply its selection rule.
- Recording used `--arrival either` (the frozen block-of-record setting) — the ONLY
  intended difference vs the frozen recordings is the IC source (validated placements via
  reset(uid) instead of `--ic-from-tape`), plus the 6 extra uids.
- Corrected-world naming: `dHv2_w2` recording → `matched_w3/dHv2` (the existing `_w2` →
  `matched_w3` convention), not `dHv2@gc_kp4_riser3_shelf6`.
- 3118190's pax007 CUDA failure resubmitted as 3118203 with `--exclude=pax007`.

---

# Addendum (same day): A24 → A25 — dHv2raw becomes THE human arm

**A24** (per-learner split) then **A25** (supersedes A24: RAW human set for ALL learners;
teacher trains on raw; DP seeds raised to 50-57 = equal n=8) landed after §1-6 above.
Executed:

## A. `episodes_pick_phase_all` correctness fix (unpruned command source)

The unpruned src (`baselines/episodes_pick_phase_all`, 91 files — the `dHunpruned` src)
carried STALE fail/no-pick tapes for the 6 recovery-enabled uids (truncated at 1200
frames at the pre-recovery placements — e.g. 239's 2756-frame demo cut to 1200; the
recorder would have followed a truncated command stream). Replaced those 6 entries with
the fresh unpruned pick-phase tapes from §2's `episodes_pick_phase_v2fill` (236/239/244/
255/261/331, label success, cut at grant+2); originals backed up in
`baselines/episodes_pick_phase_all_stale0901/`. Still 91 files; the other 85 untouched.

## B. dHv2raw collections (both worlds)

Same protocol as §3 (`--ic-mode demo`, no `--ic-from-tape`, `--arrival either`, 8 shards,
seed 0) but SRC=`baselines/episodes_pick_phase_all` **and an explicit `--uids <the 72>`**:
without it the recorder's plan picked up 74 ICs — pick_phase_all also holds success-
labeled tapes for **237 and 294**, which A21 excludes (their placements make them
resettable). First submission (jobs 3118232-47) was cancelled ~3 min in for exactly that
reason and resubmitted with the uid list (old world **3118262-69**, corrected
**3118270-77**); outdirs were wiped between waves; merge rollout counts (72 each) confirm
no stray tapes survived.

**Kept: old world 55/72, corrected 52/72.** Merges (jobs 3118370/3118371)
records_complete=True; censuses `census_dHv2raw.md` / `census_dHv2raw_w2.md`. Fails are
the same three classes as §3, with MORE over-horizon (idle frames make raw tapes longer —
a source property per A25, disclosed not matched away):

- old (17): env_truncated ×14 (245 246 250 286 287 293 295 299 300 315 320 328 330 331),
  lying-can tipped ×2 (234 318), adapter_exhausted ×1 (233).
- corrected (20): env_truncated ×14 (same list), lying-can ×2 (234 318),
  adapter_exhausted ×4 (261 278 301 319).

## C. Sets, datasets, eval files, arm registration

- **matched_v2/dHv2raw: N=55, rows 7745, len p50 132, sim_variant=base,
  sha `24b539f7c07a1e7f`** + lerobot (55 eps, fps 7.5, finalized).
- **matched_w3/dHv2raw: N=52, rows 6810, len p50 123, sim_variant=gc_kp4_riser3_shelf6,
  sha `4f413c190c88c186`** + lerobot (52 eps, fps 7.5, finalized).
- DRYRUN provenance gates pass for ARM=dHv2raw on sbatch_dp.sh AND sbatch_rlpd.sh in both
  worlds. Arm registered: `b54e817` (rlpd), `172a811` (dp).
- `eval_ics_v2.json` / `eval_ics_v2_w3.json` REGENERATED from the dHv2raw manifests
  (commit `fe419b4`; hold = 55 / 52 training ICs, A23 in-dist semantics; rnd-30
  unchanged). The §5 versions (from pruned dHv2) were never consumed by any run.
- Main session notified (sets + shas + templates); the human-arm learner matrix is
  submitted from the main session, not this build.

## D. Teachers (§6 superseded)

The four pruned-dHv2 teacher jobs (3118191/92/93, 3118203) were **cancelled** ~1.5 h in
(A25: nothing trains on pruned dHv2 by default) and resubmitted on dHv2raw, fresh waves,
`--exclude=pax007`:

| job | run name | DEMO_ROOT | SIM_VARIANT |
|---|---|---|---|
| 3118386 | dHv2raw_DP-pilotv2raw_s0 | matched_v2 | base |
| 3118387 | dHv2raw_DP-pilotv2raw_s1 | matched_v2 | base |
| 3118388 | dHv2raw_DP-pilotv2raww3_s0 | matched_w3 | gc_kp4_riser3_shelf6 |
| 3118389 | dHv2raw_DP-pilotv2raww3_s1 | matched_w3 | gc_kp4_riser3_shelf6 |

Teacher selection per §3.1 unchanged (in-job select/confirm on the default
`baselines/eval_ics.json`, as the frozen pilots). §7's remaining chain updates to:
dDPv2 harvest CKPT comes from `dp_pilotv2raw`/`dp_pilotv2raww3`; dDPv2 matched sets use
`--base baselines/matched_v2/dHv2raw` / `--base baselines/matched_w3/dHv2raw`; learner
arms are dHv2raw vs dDPv2, DP seeds 50-57 (A25 raised from 50-54), RLPD seeds 50-57.
The pruned dHv2 sets (§4) remain on disk as documented artifacts (optional continuity
leg, launched only under a further decision).

## E. Artifacts ledger (v2 build, cluster)

| path | N | sha16 | role |
|---|---|---|---|
| baselines/demos_v2/dHv2 (+_fails) | 60 (+12) | 7576d2ea7cbbffd5 | pruned-src recording, old world (artifact) |
| baselines/demos_v2/dHv2_w2 (+_fails) | 60 (+12) | aaa88a8185f46f0e | pruned-src recording, corrected (artifact) |
| baselines/demos_v2/dHv2raw (+_fails) | 55 (+17) | 24b539f7c07a1e7f | RAW recording, old world (ARM SOURCE) |
| baselines/demos_v2/dHv2raw_w2 (+_fails) | 52 (+20) | 4f413c190c88c186 | RAW recording, corrected (ARM SOURCE) |
| baselines/matched_v2/dHv2raw (+lerobot) | 55 | 24b539f7c07a1e7f | human arm, old world |
| baselines/matched_w3/dHv2raw (+lerobot) | 52 | 4f413c190c88c186 | human arm, corrected |
| baselines/matched_v2/dHv2 (+lerobot), matched_w3/dHv2 (+lerobot) | 60/60 | 7576d2ea/aaa88a81 | pruned artifacts |
| baselines/demos_v2/_negctl | 0 kept expected | — | random-teacher negative control (§7 check) |

---

# Addendum 2 (same day): A26 — no horizon cap, completed pools

**A26** (supersedes the A25 sets before any readout): every demonstration enters v2; the
A25 55/52-tape launches (32 learners, by main) and my 4 raw teachers (3118386-89) were
cancelled — verified CANCELLED in sacct, queue clear.

## A. Re-collection of the missing tapes (jobs 3118707-11 old / 3118712-16 corrected)

Same recorder protocol, into the SAME `demos_v2/dHv2raw{,_w2}` dirs with
`--rollout-base 100200` (the recorder names shard manifests by shard AND base, so waves
coexist; npz stems can't collide), `--uids` = that world's missing uids, and
**`--max-sim-steps 12000`**. Sizing note of record: the longest raw source tape is 295 at
3695 frames — the follower's own dilation cap (3.0 × n frames) bounds useful horizon at
~11.2k sim steps, and the initially floated 3600 would have re-truncated 295/300/293/286
(295's kept rollout used 4974 sim steps). Per-tape `max_sim_steps` is a stamped npz scalar
read by `validate_tape` (n ≤ ms//4+1) — mixed-cap sets need NO gate changes; the EVAL
horizon stays 1200 (A26; training tapes may exceed it, disclosed).

- **Over-horizon class: 14/14 recovered in BOTH worlds** (245 246 250 286 287 293 295
  299 300 315 320 328 330 331; kept rollouts 1253-4974 sim steps).
- **adapter_exhausted class: 0/5 recovered, as predicted** — the human follower path
  hard-forces `attempts = 1` (record_demos.py IC plan; `--attempts 8` was passed and is
  inert for human) and is deterministic: 233 (old), 261/278/301/319 (corrected)
  reproduced adapter_exhausted with near-identical decision counts. Recovering them would
  take a protocol change (follower tol/settle/dilation), not retries — reported, not done.
- 234/318 remain excluded per A26(iii) (genuine lying starts; metric-scope exclusion).

## B. Completed sets (jobs 3118777/3118778) — the sets of record for the v2 matrix

- **matched_v2/dHv2raw: N=69** (of 72; missing 233 + excluded 234/318), rows 15791,
  len p50 153, sim_variant=base, **sha `c7b45edd65c65b2c`**, lerobot 69 eps fps 7.5.
- **matched_w3/dHv2raw: N=66** (missing 261/278/301/319 + excluded 234/318), rows 14323,
  len p50 146, sim_variant=gc_kp4_riser3_shelf6, **sha `bd77c9c31291c2cf`**, lerobot 66
  eps fps 7.5.
- Merges records_complete across both waves (87/90 rollouts); censuses refreshed; DRYRUN
  gates pass on both launchers in both worlds; `eval_ics_v2{,_w3}.json` regenerated
  (hold = 69/66 training ICs; commit `969e79e`). Main notified with N/shas + the
  v2full/v2fullw3 (DP) and g99v2full/g99v2fullw3 (RLPD) templates — the 32-job matrix
  relaunch is main's.
- The A25 N=55/52 sets no longer exist on disk (rebuilt in place, `--force`); their shas
  (24b539f7c07a1e7f / 4f413c190c88c186) remain in this doc, the registry rows of the
  cancelled jobs, and the recorder manifests' history.

## C. Teachers, round 3

pilotv2full s0/s1 = **3118787/3118788** (matched_v2, base); pilotv2fullw3 s0/s1 =
**3118789/3118790** (matched_w3, corrected), `--exclude=pax007`. Selection per §3.1
unchanged; harvest chain (dDPv2 → per-IC matched vs the N=69/66 bases → lerobot →
renders) as documented above, with `--base baselines/matched_{v2,w3}/dHv2raw` now meaning
the completed sets. Review renders topped up idempotently (v2_review/v2_{old,w3}_dHv2raw
{,_fails}; job 3118824 for the A26 wave).

---

# Addendum 3: round-3 teacher results + §3.1 selection (decision of record)

All four pilotv2full teachers completed clean (DP-HEADLINE, selected = by sel on the
default `baselines/eval_ics.json`, as the frozen pilots):

| run | selected ckpt | sel | hold | rnd |
|---|---|---|---|---|
| dHv2raw_DP-pilotv2full_s0 (3118787) | 100000 | 10/15 | 11/15 | 7/30 |
| dHv2raw_DP-pilotv2full_s1 (3118788) | 100000 | 11/15 | 10/15 | 8/30 |
| dHv2raw_DP-pilotv2fullw3_s0 (3118789) | 060000 | 11/15 | 12/15 | 5/30 |
| dHv2raw_DP-pilotv2fullw3_s1 (3118790) | 100000 | 10/15 | 12/15 | 8/30 |

Both worlds clear the §3.1 pilot bar (sel ≥ 0.5). §3.1 rule "median in-dist seed — with
2 seeds, the lower, NOT the best": ranked by **hold at the selected ckpt** (the
confirmation in-dist number the frozen decision notes quote), tie-broken by sel@selected
(the w2-pilot precedent tied on hold AND rnd, so it does not discriminate the tiebreak;
disclosed):

- **Old-world teacher: seed 1, ckpt 100000** (hold 10 < 11) →
  `baselines/outputs/dp_pilotv2full/dHv2raw_DP_s1/checkpoints/100000/pretrained_model`
- **Corrected-world teacher: seed 1, ckpt 100000** (hold 12 = 12 tie → sel 10 < 11) →
  `baselines/outputs/dp_pilotv2fullw3/dHv2raw_DP_s1/checkpoints/100000/pretrained_model`

Alternative readings (sel-ranked; hold-tie → seed id) would pick s0 in one or both
worlds; all four candidates sit within 1/15 of each other in-dist, and the rule's
purpose (don't cherry-pick the best) is served by any of them — the chosen seeds are not
the best-rnd seeds in either world... in the old world s1 IS better on rnd by 1/30
(8 vs 7); disclosed. Teacher rnd (5-8/30) sits below the frozen pruned-set pilots
(15-18/30): raw tapes + the expanded IC pool (harder recovered starts now in sel/hold's
universe is unchanged though — eval used the SAME frozen eval_ics.json) — the drop is a
teacher property, disclosed, not gated (§3.1 bar is sel ≥ 0.5).

Harvests submitted (frozen flags, no --ic-from-tape; IC pool = the 72 resettable uids,
per-IC matching to the dHv2raw bases trims to 69/66 at set build): old world jobs
**3120436-41** (CKPT = old teacher above, `--ic-mode demo --attempts 3 --mode sample
--verify`), corrected **3120442-47** (+ `--sim-variant gc_kp4_riser3_shelf6`).
A27 pre-work done: `baselines/matched_w3/r2d/dHv2raw` (66 eps, terminal 1.0, stamped) —
job 3119766; `matched_w3/r2d/dDPv2` follows the dDPv2_w2 set.

---

# Addendum 4 — §E: PREREG A31 chain (cluster-resident, 2026-09-01 22:07 EDT)

A31 corrects the teacher clause: the dDPv2 teacher trains on the **PRUNED** dHv2 sets (N=60/60), not
raw. The addendum-3 raw-teacher harvest is **archived, not used**: `baselines/demos_v2/{dDPv2,dDPv2_fails,
dDPv2_w2,dDPv2_w2_fails}` renamed to `*_rawteacher_archive` (43/34/36/40 npz; logs untouched). The whole
chain runs as slurm `--dependency=afterok` jobs from `cluster/a31_chain/` (commit `ae9ac33`), needs no
laptop, and appends every stage result to `/cluster/tufts/shortlab/jstale02/ddpv2_chain.log`
(per-stage .out files in `$LAB/ddpv2_chain_logs/`, shard logs in `baselines/demos_v2/logs/`).

## Scripts (paths; verbatim in the repo at `ae9ac33`)

| file | stage | what |
|---|---|---|
| `cluster/a31_chain/common.sh` | — | world table (old: pilotv2P / matched_v2 / base / eval_ics_v2.json / dDPv2 / v2fullP / g99v2full; w3: pilotv2Pw3 / matched_w3 / gc_kp4_riser3_shelf6 / eval_ics_v2_w3.json / dDPv2_w2 / v2fullPw3 / g99v2fullw3), `clog` |
| `cluster/a31_chain/launch_chain.sh` | 0 | run once on the login node; submits everything below with afterok links |
| (teachers) `cluster/sbatch_dp.sh` | 1 | `ARM=dHv2 SEED={0,1} WAVE=pilotv2P{,w3} DEMO_ROOT=… SIM_VARIANT=… IC_FILE=… sbatch --exclude=pax007 -p gpu` (fresh waves; the cancelled 3118191-93/3118203 outputs under `dp_pilotv2{,w3}` are not reused) |
| `cluster/a31_chain/select.sh <world>` | 2a | §3.1 selection from the two `sweep/HEADLINE.txt` lines → `$TDIR/TEACHER_SELECTED.txt` + harvest IC pool `$TDIR/HARVEST_UIDS.txt` |
| `cluster/a31_chain/shard.sh <world> <K>` | 2b | one of 6 harvest shards (gpu, gres 1, 4 cpu, 16g, 4 h): mirrors `sbatch_record.sh`'s dp/GRES command with `--ic-mode demo --attempts 8 --mode sample --verify --uids <pool> [--sim-variant gc_kp4_riser3_shelf6]`, seed 0, rollout-base 100000, `MUJOCO_GL=egl`; CKPT read from TEACHER_SELECTED.txt at run time |
| `cluster/a31_chain/merge.sh <world>` | 2c | `record_demos.py --teacher dp --merge` (success + fails dirs) + census → `MERGE-RESULT` line |
| `cluster/a31_chain/build.sh` | 3 | adapted `$LAB/ddpv2_build.sh`: per world `make_v2_matched.py --name dDPv2 --base <dHv2raw>` and `--name dDPv2p --base <dHv2>` (strict first, `--allow-short` only on the per-IC-shortfall FATAL, as before); `convert_to_lerobot.py … 8 4 none image` for dDPv2p ×2; `to_dreamer_native.py --repeat 4 --terminal-reward 1 --scope pick --force` for matched_w3/dDPv2 → `matched_w3/r2d/dDPv2`; `SET-RESULT` lines |
| `cluster/a31_chain/submit_learners.sh` | 4 | DRYRUN gates (dp dDPv2p ×2 worlds, rlpd dDPv2 ×2) → abort on FATAL; else DP `ARM=dDPv2p SEED=50-57 WAVE=v2fullP{,w3}` + RLPD `ARM=dDPv2 SEED=50-57 REWARD=sparse GAMMA=0.99 UTD=10 WAVE=g99v2full{,w3}` (`--exclude=pax007 -p gpu`); `scontrol release 3120763-68` (A28); if `matched_w3/dDPv2` N ≥ 45: r2d DRYRUN per arm then A27 packs `ARM={dHv2raw,dDPv2} PACK_SEEDS=(90 91)…(100 101) DEMOSET=w3 R2D_SIM_VARIANT=gc_kp4_riser3_shelf6 CONFIG=genesis_pick_v5d4c_delta_shaped TIME_LIMIT=400 sbatch -p gpu -n 16 --mem 90g --time=30:00:00 cluster/sbatch_r2dreamer_pack.sh` (12 packs); else `A27-SKIPPED-LOW-N` |

Launcher registrations (same commit): `dDPv2p` added to `sbatch_dp.sh`'s native-arm case; `dHv2raw|dDPv2`
added to `sbatch_r2dreamer.sh`'s ARM case (DEMOSET=w3 → `matched_w3/r2d/<ARM>`, the path addendum 3 built
for dHv2raw), with the n-band floor lowered to 45 for `dDPv2` only (the A31 chain's A27 gate). `sbatch_rlpd.sh`
already had `dDPv2`. DRYRUN proof on the cluster before launch: teachers both worlds DEMO-SHA n=60
(7576d2ea… / aaa88a81…) + PROVENANCE-OK; r2d `dHv2raw` n-band [50,66] sha 3ba55720…, `dDPv2` n-band [45,66]
(dir absent until build, as expected). Nested `sbatch` from a batch node verified (job 3137229 → NESTED-SBATCH-OK).

## Selection rule as implemented (select.sh)

Parse `DP-HEADLINE … selected=<ckpt> sel=a/b hold=c/d rnd=e/f` for seeds 0 and 1. Candidates = seeds with
sel ≥ 0.5 at the selected ckpt (§3.1 pilot bar; none → `TEACHER-BAR-FAILED`, chain halts — nothing harvests
from a broken teacher). Rank by **hold@selected ascending** (two seeds → the LOWER = "median, not best",
addendum 3's decision of record), tie → **sel@selected ascending**, tie → **lower seed id**. The task brief's
alternative fallback ("higher sel") is NOT used — the documented rule is unambiguous. Choice, rule, and both
candidate lines are written to `baselines/outputs/dp_pilotv2P{,w3}/TEACHER_SELECTED.txt` and the chain log.
Teacher select/confirm runs on the A23 v2 IC files (`IC_FILE=eval_ics_v2{,_w3}.json`, hold = 69/66 training
ICs of the raw base) per the brief — a disclosed difference from the frozen pilots' default `eval_ics.json`.

Harvest IC pool = union of the raw base ICs (A31: ALL dHv2raw ICs, 69/66) and the pruned base ICs (60/60), so
the dDPv2p view can be per-IC exact too (w3 adds 301, which is in pruned but not raw); 234/318 (lying-can,
excluded from every v2 set) are therefore NOT wasted on ×8 attempts. Old-world pool = 69 uids, corrected = 67.

## Job ids (submitted 22:06 EDT; all accepted, squeue shows PD/Dependency for every stage)

| stage | old world | corrected world |
|---|---|---|
| teachers (dHv2_DP-pilotv2P{,w3}_s0/s1) | **3137348, 3137349** | **3137358, 3137359** |
| select (afterok both teachers) | 3137350 | 3137360 |
| harvest shards rec_dDPv2{,_w2}_0-5 (afterok select) | 3137351-3137356 | 3137362-3137367 |
| merge (afterok 6 shards) | 3137357 | 3137368 |
| build (afterok both merges) | 3137369 | |
| submit learners + A28 release + A27 packs (afterok build) | 3137370 | |

Teachers were PD on `QOSMaxGRESPerUser` at launch (behind the running human-arm v2 matrix), so "training
within 10 min" could not be observed; their .out files are `dp_313734{8,9}.out`, `dp_313735{8,9}.out`. The
learner/pack job ids are only known when 3137370 runs — read them from `ddpv2_chain.log`
("machine-arm learners submitted=N/32 ids:…", "A27 packs submitted=P/12 …"). Expected N: DP 16 + RLPD 16 = 32.

Not cluster-resident: nothing in the chain. Left for the operator: the pre-existing `~/purge_0901.sh`
(fs 85 %) and the `harvest` readout jobs 3137006-08 (read-only) are untouched.
