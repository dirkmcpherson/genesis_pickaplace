# NEXT — checklist for the next session (written 2026-08-28 16:20, before context compaction)

Read first: AUDIT_INDEX.md → LEARNER_HEALTH_2026-08-28.md → PREREG A11–A15 → SESSION_LOG tail.
Rule set in force: A14 (learner health before any source comparison), A15 (no divergence from standard
implementations; deviations are tested and disclosed, never silently kept), A9 (seed id = one run),
A12 (matrix = {dH,dDP} × {succ, +own fails} × {DP,RLPD,r2dreamer,dv3}), A13 (world decided by the
corrected-world WM gate).

When the VPN is back (`ssh tufts`), in this order:
1. RLPD fix factorial (WAVE=rlpdfix_{g99,g99u5,u5}, 18 jobs, `rlpd_*.out`): for each, max critic_loss,
   Q-WATCHDOG trip count, peak & final ep_rew_mean, SWEEP-HEADLINE selected vs final. Criterion (N18):
   watchdog silent after 30k, max critic_loss < 1, final ≈ selected. If g99 passes → cancel the 24 held
   old-recipe RLPD jobs, resubmit the RLPD matrix under GAMMA=0.99 (old + corrected world, dH/dDP ×
   {succ, +own fails} × n=10 ≈ 80 jobs, 5 h each). If not → N18 falsified; report RLPD-as-published fails.
2. dv3: touchgoal tg_none_s0 (learned 0.92 @234k), tg_none_s1, tg_clamp_* (were stuck at step 10000 —
   check state/run.log); dense diagnostics final_dH_DV3-dense-{,clamp,fix,std}_s2x under
   dreamerv3-torch/logs_cluster/genesis_final/<tag>/<timestamp>/metrics.jsonl (value_mean, train_success_rate).
   Under A15 only `std` can enter the matrix.
3. r2dreamer standard arm (runs/pick_v5d4c_delta_shaped_STD_{dH,dDP}_s12{0,1}): entropy floor/ceiling
   flips, value vs realized, ckpt_scores.tsv stability. Corrected-world gate (dH/dDP s80-83, DEMOSET=w3).
   Last-checkpoint hold re-scores: baselines/outputs/n12_rescore/*_LAST_hold_*.log (BEST vs LAST).
4. Only after a learner passes its criterion: release its held jobs (`scontrol release`) as PACKED pairs
   (cluster/sbatch_r2dreamer_pack.sh, validated >2x per GPU) or resubmit under the new recipe.
5. Env fix d2c391f (pick scope pays one terminal) applies to all jobs started after 08-28 13:25; earlier
   runs carried the +2 double grant identically across arms (disclosed).
Held: 35 DP, 29 r2d (`scontrol hold`), 24 old-recipe RLPD. Watch disk (~480 GB free) and the QOS cap (10 GPUs).
Tooling: analysis/stats.py (Welch + exact permutation), analysis/results_table.py (dedup, world map, RESCORE parsing).

## Added 08-29 (post-scout)
- **WM fallback gate (user 08-29: "MoDem is an interesting idea if everything else fails").** If neither dv3-std (s20-22 @1M, s23-25 @3M packed) nor r2dreamer NOCLAMP (s122/123 dH/dDP, repval kept) shows a sustained pick with a bounded value estimate by ~1M sim steps, start the MoDem adapter via the DEMO3 codebase immediately (do not wait for 3M). Needs PREREG amendment A16: WM arm = MoDem-as-published incl. BC-initialised actor (confound stated; RLPD also sees demos directly). See paper/WM_CANDIDATES_2026-08-29.md §3.
- Check every pack .out for `rc=1` within the first hour of start (three r2d packs died silently 08-28).
- Pending readouts: dv3 std/clamp/fix ≥500k; tg_clamp s2/s3; r2d w3 gate at 3M (s80-83); RLPD g99 matrix (released 00:30 / 08:30).
- 08-29 11:00: r2d w3 gate ignites (see log). TODO before any table: `analysis/results_table.py world_of` must map r2d dH/dDP seeds 80-83 → corrected world; read `n12_rescore/*_W3_*` (24 jobs).
