# CONFOUNDS — living ledger (started 2026-09-02; reference on EVERY claim edit)

Rule: every result sentence in RESULTS/brief/paper must be checkable against this table. A confound is
"closed" only when a within-table control exists; "disclosed" means it is stated with numbers but not
removed; "open" means an experiment is running. Update the status column whenever a block reads out.

| # | confound | threatens | control / status | evidence / where |
|---|---|---|---|---|
| 1 | **World fidelity** (old vs corrected) | any cross-world comparison; the RLPD headline | CLOSED for within-learner tables (A19: one world per table); cross-learner figure is all-corrected-world; RLPD run in BOTH worlds → world-dependence is a *finding* (A16 met, A20 null). **A34 (09-02): old world DROPPED by user decision — corrected world is the only world of record; old-world numbers survive only as a one-sentence sensitivity disclosure** | RESULTS §2.1/§2.3, fig1/fig5, fig6 (coverage mechanism) |
| 2 | **Checkpoint rule per learner** (DP selected / RLPD LAST / WM BEST) | the cross-learner gradient | **REOPENED 09-02 → see row 25 (WM BEST-of-K is not a valid estimate; "DISCLOSED" understated it).** Previously: DISCLOSED + sensitivity table: DP, RLPD rule-invariant; **WM effect exists only under BEST-of-K** → WM claim = learnability under selection, not endpoint | brief §2b; A32 tests endpoint stability |
| 3 | **Reward density** (DP none / RLPD sparse / WM dense) | the cross-learner gradient | **RESOLVED 09-02 night, unexpectedly: A33 RLPD-dense (corrected world, n=8v8) COLLAPSED in both arms (selected rnd 0.12 v 0.09; LAST 0.05 v 0.02) vs sparse ≈ 0.6 → density alone flips a learner from working to dead; every cross-learner row that differs in density is uninterpretable; WM dense shaping now suspect on its own (RESULTS §2.4).** Was: OPEN: A33 (RLPD-dense, corrected world, frozen sets, n=8v8) + A32 SPARSE-RS1 (WM sparse pilot). Prior: old-world RLPD-dense n=6v6 +0.08 ns (shaping hurt both) | PREREG A33/A32; RESULTS §2.2 dense |
| 4 | **Human tape format** (pruned vs raw) per learner | DP rows; any "human" label | DISCLOSED + quantified: frozen blocks ALL pruned (inconsistency, A24/A25); **CORRECTED 09-02 (AUDIT_approach f17):** frozen blocks ALL pruned — including the frozen WM block s80-87 (`matched_w3/dH`) and the A33 RLPD-dense human arm (`matched_w3/dH`, N=58); RAW human tapes only in v2 RLPD (dHv2raw s60-67) and A27 (dHv2raw); v2 DP pruned (`human*`); raw costs DP +0.16/+0.27 (fig11); pruning removes time not space (fig12) | fig11/fig12, `human*` convention (colstyle), RESULTS §1 |
| 5 | **Demo-set N / IC support** | every human-vs-machine contrast | CLOSED within contrasts (per-IC, per-N matching: make_matched_sets / make_v2_matched); v2 raw 69/66 vs pruned 60/60 handled by two machine views (dDPv2 vs raw base; dDPv2p vs pruned base, A31). DISCLOSED 09-02: old-world dDPv2 built --allow-short, N=68 vs raw base 69 (IC 286 unharvestable, 0/attempts); w3 dDPv2 66/66, both dDPv2p 60/60 | manifests (shas), V2_BUILD |
| 6 | **Frozen-set selection bias** (recoverable-only trials) | generality of frozen results | DISCLOSED; removed in v2 (full 74/75 pool, validated placements) | DEMO_RECOVERY_RESULTS, CRITIQUE_demo_recovery |
| 7 | **Machine-demo generator identity** (dDP = different DP teacher per world; teacher saw all ICs) | "machine demos" as one thing; per-seed holdouts | DISCLOSED: teacher must train on PRUNED human (A31, corrected after A25 error); generator exposure symmetric with the human demonstrator (A22 note, withdrawn design) | PREREG A25/A31, fig6e |
| 8 | **Row count / dataset size** | fails-arm effect; raw-vs-pruned | CLOSED for fails (row-matched dup controls, A17 rule); DISCLOSED for raw (2× rows, fig12a) | RESULTS §2.2, fig12 |
| 9 | **Training-IC placement drift** (frozen tapes vs recovered truth) | "re-executions of real trials" framing | DISCLOSED: median 2 cm, 5/19 > 3 cm, max 13 cm (319); common-mode across arms → no reruns | RESULTS claim 10 |
| 10 | **Infrastructure incidents** (NFS-handle kills from wandb_cache purge; bad node pax007; OOM packs) | seed integrity | HANDLED: fresh seed ids on relaunch (A9); re-eval from archived ckpts (no retrain); pax007 excluded; wandb_cache removed from purge; a31 build.sh `python\|tee` masked a FATAL (10th silent-default) -> submit gate FATAL, nothing lost, rebuilt + resubmitted 3164885 | SESSION_LOG 09-02 |
| 11 | **Multiple comparisons** | DP +0.06 (one unadjusted test); 38-metric screen | DISCLOSED; screen is hypothesis-generating; causal test A28 | brief §6, WM_METRIC |
| 12 | **Eval-IC overlap** (frozen hold-15 ⊂ training ICs) | "generalization" wording | DISCLOSED (A8/A16: hold never the statistic); rnd-30 fixed and shared across ALL blocks; v2 hold = all training ICs, labelled in-dist (A23) | PREREG A16/A23 |
| 13 | **WM critic-target scale** (return_clamp=100 vs shaped returns ~500–1000) | all WM ENDPOINT claims (both arms equally) | **A32 dH READ OUT 09-03: C2000 and RS1 endpoints ALL die (LAST 0.00 ×8), sparse never ignites → clamp is not the cause of collapse (RESULTS §3.3).** A32 (C2000, RS1, SPARSE-RS1) now BOTH arms (A36, dDP s140-151 + dH sparse top-up s152-153; A27 cancelled). Ignition count CORRECTED 09-02 to 6/8 v 3/8 (registered BEST-hold criterion; Fisher 0.315); "ignition unaffected by the clamp" WITHDRAWN per row 16 (AUDIT_approach f11) | RESULTS §3.2, fig13 |
| 14 | **Load / machine sensitivity of replay** (borderline demos flip under load; official numbers idle box ×3) | placement funnel numbers | DISCLOSED: funnel stages are single-collection; learner evals are fresh-process per episode | 07-20 finding; DEMO_RECOVERY_RESULTS |
| 15 | **Metric operationalization** (pause_frac fragile; idle_frac gripper-column bug; act_hf_frac demoted) | mechanism claims | **REOPENED 09-02 (AUDIT_zero_action):** idle_frac human 0.19 vs machine 0 is PROVENANCE, not behaviour (DP teacher emits continuous (q*-q)/0.1 deltas -> exact zeros impossible; human follower re-issues reached waypoints -> exact zeros). Strict-stop gap lives entirely below 0.3 mm/decision (88x at 0.05 mm, 1.0x at 2 mm); dwell-run structure identical (424x8.5 vs 419x8.7) -> "stops-vs-creep" = bit-exact rest vs sampler jitter floor, NOT different dwell behaviour. fig7 real side = measured /joint_states, not commands; real idle threshold ~80x looser. Pruner != metric (30 Hz abs stream, 7 cols, pre-pick window). TODO: tolerance sweep on cluster npz; relabel figs/claims; A28 stays the causal test | paper/AUDIT_zero_action_2026-09-02.md, WM_METRIC addenda, FIGURES fig6/7/8/10/12 notes |
| 16 | **WM actor gradient under the return clamp** (λ-return target clipped at 100 before ReturnEMA → advantage ≈ critic residual; entropy coef 3e-5) | every WM source claim INCLUDING the ignition asymmetry (row 13 said "unaffected" — withdrawn) | OPEN: ignition = discovery under a saturated critic (reach-only gradient + sampling + demo-learned contact dynamics), not value learning; A32 runs dH only → the source pair under a corrected scale has not been run | AUDIT_approach f11; port dreamer.py:499-506,551-553, networks.py:397-405 |
| 17 | **WM demo rows unshaped, online rows shaped ×100** (`to_dreamer_native.py:65-72` refuses non-sparse; port demo_prefill scales the terminal only; each demo tape under-reports return by ≈60–100) | A27 (row count differs by arm: dHv2raw 14,323 rows vs dDPv2 ≈8k, ×4 dup); fig13 | OPEN: no control; RLPD closed the same asymmetry with `--demo-shaping` (train_rlpd.py:161,205-208) | AUDIT_approach f12 |
| 18 | **WM train horizon 400 sim steps vs eval 1200**; median demo pick at 107–146 decisions > the 100-decision online episode; RLPD trains at 1200 | cross-learner rows; A33 "same reward" comparison; §3.2 return-scale numbers | DISCLOSED by PREREG A35 (never amended before 09-02); no TL-1200 WM cell exists | AUDIT_approach f13; submit_learners.sh:30,35; METHODS_draft:365 |
| 19 | **Success-horizon asymmetry inside the raw pair** (14/66 dHv2raw tapes recorded at `--max-sim-steps 12000` vs every dDPv2 tape at 1200; human success = pick within ~3000 decisions, machine within 300) | RLPD-v2 (dHv2raw vs dDPv2), A27 | OPEN: report on the per-IC subset excluding the 14 ICs (both arms) or re-harvest dDPv2 at 12000 with the same attempts; add `max_sim_steps` to the census | AUDIT_approach f14; V2_BUILD:298-312; shard.sh:13 |
| 20 | **A33 comparator** (A33 human arm = `matched_w3/dH`, PRUNED; its sparse comparator is A20 s40-47, not the raw v2 RLPD) | row 3 resolution; any "RLPD raw" wording | DISCLOSED (row 4 corrected 09-02) | AUDIT_approach f17 |
| 21 | **Attempts / verify / grasp-margin asymmetry** (machine tapes = best-of-8 sampled, `--verify`; human = single deterministic replay at ICs searched to make replay succeed; load-sensitive borderline tapes) | every human-vs-machine contrast | OPEN: first-attempt yield per IC beside every dDP result; per-tape grasp-margin stat in the census | AUDIT_approach f20; record_demos.py:804; shard.sh:13 |
| 22 | **Crashed episodes leave the denominator** (`rate = picked/present`; incomplete sel sweep → −1, skipped; crash rate node-dependent) | any headline printed with `(expN)`; checkpoint selection | OPEN: refuse a headline unless present == expected, or count missing as 0 and say so | AUDIT_approach f23; eval_sweep.sh:144-157; results_table.py |
| 23 | **Selection / hold IC file differs by learner in v2/w3** (WM: frozen `eval_ics.json` sel/hold-15; DP/RLPD v2: `eval_ics_v2_w3.json`, sel ⊂ hold = all 66 training ICs) | cross-learner hold rows; A27 ignition co-primary | DISCLOSED by PREREG A35 (A27 ignition on the frozen hold-15; rnd byte-identical across files) | AUDIT_approach f25 |
| 24 | **Divergence statistic** ("max critic loss ≥ 1" = grep of SB3's `critic_loss`, one sample per dump, summed over E=10 members; not a running max; threshold not comparable under dense reward) | RESULTS §2 Fisher rows; A33 health read | OPEN: per-member loss normalised by target variance, logged as a running max; re-derive the Fisher rows | AUDIT_approach f27; rlpd_sac.py:275-276 |
| 25 | **WM BEST-of-K = selection on the reported statistic** (launcher scores every `latest.pt` write, ~28 snapshots/run, 15 sampled episodes each, inside the training job; reports the MAX over the table as BEST hold and calls BEST hold ≥ 8/15 "ignition"; K varies per run — W3 block: 29 for s80-83 both arms, 37–58 for s84-87 (OOM-resume re-polling: dH 58/37/37/58, dDP 37/58/58/58), i.e. unbalanced across seeds AND arms; no confirmation pass; DP/RLPD select among exactly 5 on sel ICs then confirm on hold/rnd) | ALL WM BEST/ignition numbers (0.554 v 0.308; 6/8 v 3/8) and the cross-learner comparison | **CLOSED 09-03 (BEST-of-5 fresh-process numbers of record in RESULTS §3.1; old BEST-of-K numbers superseded).** Noise alone: at true hold rate 0.35 a 15-episode score reaches ≥ 8 with p ≈ 0.11, so P(any of 28 does) ≈ 0.96 vs ≈ 0.44 over 5; ignition was near-guaranteed for any non-dead run and its count depends on K. Fix in flight: fresh-process BEST-of-5 (fraction ckpts 20–100 %) on hold-15 + rnd-30, all episodes present (agent, 09-02 pm) → puts WM on the DP/RLPD rule; still a max, disclosed for all three. Only LAST is selection-free, and LAST is dead in both WM arms. No WM source claim until A32 yields live endpoints | AUDIT_approach f10/f23/f26; WM_BEST5_RESCORE_2026-09-02.md (pending) |
| 26 | **Machine generator = self-distillation of the human set** (extends row 7): dDP = DP trained on the same pruned human tapes (teacher = "hold@selected ASC, lower of 2 = median-not-best" rule, `TEACHER_SELECTED.txt`: w3 seed 0 ckpt 080000, sel 15/15 hold 63/66 rnd 16/30), rolled out at the same ICs, best-of-8 success-filtered. **7/66 dDPv2 ICs (245 246 286 293 295 300 331) are OUTSIDE the teacher's training set** (pruned dHv2 lacks them) and are exactly over-horizon raw-only trials (row 19) → at those ICs human = slow over-horizon tape, machine = out-of-training-distribution teacher success inside 1200 steps | every "human vs machine" sentence (external validity: distillation, not an independent generator); rows 19/21 | DISCLOSED (DRAFT_robomimic revised 09-02: "policy re-execution"); the 7-IC overlap is NEW 09-02 → OPEN: same exclusion subset as row 19 for the raw pair | AUDIT_approach f1; manifests matched_w3/{dHv2,dDPv2} ic_uid_histogram (cluster, read 09-02) |
| 27 | **A28 ablation arms are not per-IC matched to their anchors**: dDPretimed N=52, dHsmoothed N=56, dDPnoised N=55 vs frozen dDP/dH N=58 (manifests, 09-02) — re-execution failed on 6 / 2 / 3 ICs, so each intervention arm also differs from its anchor in IC support and row count | A28 predictions ignition(dDPretimed) > ignition(dDP), ignition(dHsmoothed) < ignition(dH), ignition(dDPnoised) ≈ ignition(dDP) | OPEN: score anchor and arm on the common IC subset (or match the anchors down with make_matched_sets); list the dropped ICs in WM_METRIC addenda before readout; A28 is also under the row 13/16 clamp | manifests matched_w3/{dDPretimed,dHsmoothed,dDPnoised}/manifest.json (N, ic_uid_histogram) |
| 28 | **Statistical framing** (extends row 11): (a) the cross-learner "gradient" has no test — source×learner interaction p 0.20–0.27, ordering non-monotone (DP +0.06, RLPD −0.02, WM +0.25); (b) the registered Holm (PREREG §7) is not applied in RESULTS — DP rnd 0.041 → 0.082, WM rnd/ignition → 0.27/0.32; (c) §7 says paired permutation, `stats.py` is unpaired (seed index pairs nothing); (d) Bayesian P(Δ>0) under a flat prior ≈ 1 − one-sided p, not independent evidence; (e) means over bimodal seeds hide that conditional on a seed being alive Δ = +0.06 / +0.06 / +0.07 (DP/RLPD/WM) — the only source-dependent quantity in the corrected world is the WM ignition rate (row 25 caveat) | "scales with use beyond imitation"; DP "small human edge"; "directional on every view" | DISCLOSED in `analysis/DECOMPOSITION_2026-09-02.md`; RESULTS/brief prose not yet aligned (main session) | AUDIT_approach f2, f5, f7; decomposition (a)(b)(d) |
| 29 | **Power / detectable effect**: seed SD (rnd) DP 0.06, RLPD 0.26, WM 0.31 → 80 %-power MDE at n=8: RLPD 0.36, WM 0.43; at n=12: 0.30 / 0.35; RLPD CrI [−0.25, +0.20] does not exclude the old-world +0.21; PREREG A27's "~80 % power" was ≈0.5 at its own Δ 0.25 | every "null" / "indifferent" / "no effect" sentence | DISCLOSED (decomposition c); wording rule: "no effect ≥ MDE detectable" | AUDIT_approach f6 |
| 30 | **Registration timing** (post-hoc reversals): P-R2D (§6) and PAPER_PLAN H4 predicted machine ignition / WM indifference → failed → A27 registered the reversed prediction after the full frozen n=8v8; A16 after s30-32 of the 8 seeds it was scored on; A29 after 2 seeds; ignition threshold "informed by" the same data | "pre-registered" wording for the WM row; A16 (old world, now sensitivity only) | DISCLOSED: PREREG A35 seeds-seen table; rule: amendments list seeds read, confirmatory stats reported on unseen seeds | AUDIT_approach f8; PREREG A35 |
| 31 | **rnd-30 reused as the decision set** (γ restart A17, clamp verdicts, pruning A29, recipe choices all read on rnd for weeks) and rnd is an IN-support sample (uniform can xy in the demo bounding box + 1 cm, goal fixed, `ic_sampling.py:14-31`), not "off-demo-support" (A23 wording) | optimism of every headline; "generalization" wording | OPEN: `rnd2` (new RNG seed, same sampler) scored once on the checkpoint of record of every arm (main session queued); correct A23 | AUDIT_approach f9 |
| 32 | **Learner-eval partition / node / launch-time asymmetry** (distinct from row 14 = replay): human RLPD-v2 re-scored on `-p batch` CPU nodes, machine dDPv2 in-job on `-p gpu`; WM selection evals run concurrently with two trainings per 16-core pack; v2 human arms launched 09-01, machine arms 09-02 after the harvest chain (different nodes/load). Frozen DP block node census (HEADLINE `node=`): dH {pax026 ×4, pax064 ×4, pax183, **pax007** (the node later excluded as bad, row 10)} vs dDP {pax064 ×4, pax051 ×2, pax023, pax052, pax141, pax183}. July: borderline episodes flip under load; WM online training rollouts under pack load feed the replay buffer, so training data itself is load-sensitive | any human-vs-machine contrast whose arms were scored on different partitions; WM selection | OPEN: stratify headlines by node class post hoc (node is in every HEADLINE / eval json); re-score both arms on one partition if it moves | AUDIT_approach f24; rlpd_select_confirm.sh:41; submit_learners.sh:22; sbatch_r2dreamer_pack.sh:23,42 |
| 33 | **Action-selection regime at eval differs by learner**: RLPD deterministic (`wandb_eval.py:236`); DP sampled with a fixed per-IC seed (`:108`); WM sampled — `eval_genesis.py` default `--mode sample`, `--max-steps` in sim steps (1200), `success_key = picked` (cluster port read 09-02) — for selection AND every re-score (`r2d_rescore.sh` passes no `--mode`) | cross-learner rows; WM BEST-of-K noise (row 25) | DISCLOSED (PREREG §5); fix = one deterministic (`--mode mode`) re-score of the WM/DP checkpoints of record | AUDIT_approach f26; $LAB/r2dreamer/eval_genesis.py:48-50,239 |
| 34 | **DP budget fixed in gradient steps, not epochs**: 100k steps for every set while raw pools carry 2.0–2.1× the rows (w3 dHv2 7,129 vs dHv2raw 14,323) → the raw arm sees each row half as often; the PREREG §1 300k "epoch-matched view" was cut and never ran (METHODS [CHECK 19]) | A29 raw-vs-pruned DP (+0.27): under-training per row is an untested alternative to "idle spans degrade DP"; any DP contrast with unequal rows | OPEN (cheap): one raw-arm leg at 200k steps, 3 seeds, corrected world | PREREG:34,44; METHODS_draft:311,538; V2_BUILD:102,323 |
| 35 | **K per WM run varies with restarts** (extends row 25): `ckpt_scores.tsv` rows, W3 block: s80-83 = 29 in both arms; s84-87 = 37–58 (OOM resume re-polled the archive: dH s84 58, s85 37, s86 37, s87 58; dDP s84 37, s85-87 58) → BEST-of-K optimism differs by seed and is not balanced across arms | WM BEST hold/rnd, ignition | OPEN: the BEST-of-5 fraction re-score (main session) removes it; report K per seed next to every BEST number | cluster `$LAB/r2dreamer/runs/pick_v5d4c_delta_shaped_{dH,dDP}_s8?/ckpt_scores.tsv` (read 09-02) |
| 36 | **Demo sampling weight / demo share differs by arm** (extends rows 8/17): RLPD samples the offline half uniformly per transition → raw human tapes (p50 146 decisions) weigh 1.2–1.8× per tape vs dDPv2 (p50 ≈120) at lower rewarded-row density; WM `demo_duplicate 4` on ≈14.3k vs ≈8k rows | RLPD-v2 and A27 (raw pair) | OPEN: log `train/demo_rew_per_batch` and demo share per arm; report beside the contrast | AUDIT_approach (RLPD+WM audit LOW 8); rlpd_sac.py:184-185 |
| 37 | **Number and identity of human demonstrators not recorded** in any paper doc: if one operator, every "human" claim is one teleoperation style (robomimic's PH); if several, per-operator skill is a hidden factor (MH) and per-operator tape counts are unbalanced across ICs | external validity of "human demonstrations"; A28 burstiness mechanism (operator-specific rhythm) | CHECK: bag metadata / trial log for operator id per uid; then DISCLOSE in Methods (and, if >1, report per-operator counts) | `trial_reader.py`; inthewild_trials/ |
| 38 | **Human sim tapes are follower-mediated re-executions** (extends row 15): `HumanFollower.act` emits (cmd − target)/0.1 deltas toward the bag's *measured* joint stream with arrival / dwell / dilation logic (`record_demos.py:400-463`, dilation cap 3.0× n frames), attempts forced to 1 → the human tapes' temporal profile (exact zeros, dwell runs, stretched segments) is partly the follower's, not the human's; the DP teacher's tapes have no such layer | A28 "burstiness" mechanism; fig7 real-vs-sim; any "human motion statistics" sentence | DISCLOSED here; CHECK: stamp per-tape fraction of decisions spent in follower dwell/dilation and report it beside pause/stop metrics | AUDIT_approach (matching audit A8, RLPD audit 6); AUDIT_zero_action |
| 39 | **Old world dropped after both worlds' results were known** (extends row 1): A34 decided 09-02 14:30; A20 (corrected-world null) read 08-31, old-world +0.21 ×3 read 08-30/31 — a reader will ask whether the dropped world was the one with the effect | selective-reporting exposure of the one-sentence sensitivity note | DISCLOSED: the sensitivity sentence must state that the corrected world was designated after both readouts and why (bag-measured fidelity, A34 rationale) | PREREG A34; RESULTS §2.1/§2.3 |
| 40 | **Eval-pipeline dependence of replay stages** (sim-improvement agent, 09-02: same world, predicates and box, fresh-process-per-episode vs sharded scene-reuse disagree on 12/74 per-uid stages, nested +5) | any stage-level replay number compared across pipelines; the nested floor (~5/74 cross-pipeline) | DISCLOSED: every DP/RLPD headline comes from ONE harness (eval_core.run_eval via wandb_eval.py, one process per block, env.reset per IC = scene reuse); WM via r2dreamer eval_genesis.py (one process, reset); recording via sharded scene reuse; the fresh-process pipeline produced no headline. Within-comparison consistent; cross-pipeline stage numbers must not be mixed. OPEN: pick-stage disagreement count (pick is the headline metric) |
| 41 | **ts5 world declared but never used** (sim_variants.py comment "WORLD OF RECORD from 2026-08-26" = gc_kp4_riser3_shelf6_ts5, commit 7c8195d; only matched_w4_pilot/dH + dp_pilotw4 ever ran on it; every frozen w3 set, harvest and arm is gc_kp4_riser3_shelf6 WITHOUT ts5; no PREREG entry reverts it) | "world of record" wording; any citation of gripper_lab ts5 numbers as properties of the reported world | DISCLOSED 09-02: by omission, not decision (pick recreation 57 base vs 58 ts5 = wash; E5: ts5 stiffens can-goal/can-shelf 1.6x). User decision pending on rebuild (A) vs keep-and-disclose (B, recommended) | METHODS_draft rows 123/164/248, AUDIT_INDEX E5, gripper_lab §8 |
| 42 | **WM re-scores of record ran in the BASE world** (cluster/r2d_rescore.sh never exported R2D_SIM_VARIANT; adapter default `base`; 0/2289 n12_rescore logs carry a [sim-variant] line, incl. all 32 `*_s8?_W3_*` behind RESULTS §3.1) | EVERY frozen-block WM hold/rnd/ignition number (0.554 v 0.308; 6/8 v 3/8) — policies trained at kp×4/gc/riser scored at base kp/no riser | **CLOSED 09-03: re-scored in the corrected world (WM_BEST5_RESCORE §5): BEST-of-5 rnd 0.39 v 0.26 (+0.13, p 0.30), ignition 4/8 v 1/8 (p 0.28); direction unchanged, size smaller, still ns.** Fresh-process BEST-of-5 re-score in the corrected world running (cluster/r2d_best5_submit.sh, refuses episodes without the variant line). 12th silent default (env-var default `base`) | WM_BEST5_RESCORE_2026-09-02.md §0(a) |
| 43 | **WM training budget unmatched across seeds** (OOM warm restarts re-ran the full step budget: s80-83 3M; dH s85/86 + dDP s84 ≈3.9M; dH s84/87 + dDP s85-87 6M; the "F100" fraction checkpoint of five runs is a ~112k-step post-restart snapshot named by a stale metrics step) | frozen WM block seed comparability; BEST-of-5 selection for s84-87 | DISCLOSED; re-score reports pass/internal step per checkpoint; consider excluding restarted seeds or reporting 3M-pass-1 only | WM_BEST5_RESCORE §0(b), §1 |
| 44 | **Observation-modality asymmetry: the WM arm is PIXELS-ONLY** (r2dreamer configs genesis_pick_v5d4c_delta{,_shaped}: `mlp_keys: '$^'`, `cnn_keys: 'image'`; obs = 64×64×6 two-camera rig, NO joint state, NO can pose, NO goal xy; adapter envs/genesis.py:164-171) while DP and RLPD train on the ground-truth state vector `[q(6), grip, effort, can xyz, can quat, goal xy]` | every WM number; the cross-learner comparison; the "WM cannot learn this task" reading (RESULTS §3.3) | **OPEN 09-03 — never on the ledger before.** The WM must localize a small can from 64×64 pixels with no proprioception; a different (harder) problem than the other two learners solve. dreamerv3-torch already has a state config (configs.yaml:368 `mlp_keys: 'state'`); r2dreamer has none. Also: neither port has ever been validated on a reference task (no DMC/Atari run in any log); touchgoal (no demos) = r2d 0/0 over 900k both seeds, dv3 1/2 seeds. Fix ladder: state-input WM leg (modality parity) → reward-consistency fixes (rows 17/18/16) → reference-task sanity of both ports — **RUNG 1 DONE 2026-09-07 for BOTH ports: the WM arm is now state-input, not pixels-only.** r2dreamer runs on the 17-dim state vector (the same one DP and RLPD see), removing this asymmetry from every phase and end-to-end number produced since 09-04; dv3 was ported to state input as well and passed its registered gate (G2, sampled actions, `DV3_DEBUG_2026-09-05.md` §8: human 0.700 / 0.700 out-of-distribution vs a 0.5 threshold, in-distribution 15/15 and 14/15), which also retires the sub-claim that neither port had a working configuration. Rung 3 (reference-task validation on DMC/Atari) is STILL OPEN for both ports — no such run exists in any log, so "validated implementation" must not be claimed | r2dreamer configs/env/*.yaml, envs/genesis.py; DV3_DIAGNOSIS §5-6 |
| 45 | **Replay-path reset is history-dependent** (sim-improvement agent, 09-03: every replay-path reset — `replay_harness.rollout`, `world_search.score_variant`, `fulltask_fidelity_lab`, `gripper_lab`, `render_*`, `remeasure_contact`, hence `cpu_research` placements — teleported the arm with `set_dofs_position` but left the PD target at the PREVIOUS tape's last command (fresh process: CTRL_MODE.FORCE, zero torque), so a tape's stage depends on which tape ran before it in the same process; `GenesisCanEnv.reset` was fixed for this on 08-14 (P2 FIX), the replay path was not) | every replay-path stage number (gripper_lab tables incl. the ts5 evidence, fidelity-lab funnels, world-search fit/holdout, recovery-campaign placements, rendered outcomes); the MECHANISM behind rows 14 (load = different partition) and 40 (fresh-process vs scene-reuse = FORCE-mode vs stale-PD reset step) | **DISCLOSED + FIXED 09-03 for the replay path** (`replay_harness.reset_arm()`, all 7 sites; `can_pos_recovery/leak_probe.py`: uid 242 nested/nested/placed by predecessor, prox 0.068/0.069/0.081 → bit-identical 0.0756 with the fix). Scale: same θ, idle box, two shard partitions of the fit-50 differed on 7/50 stages (−9 c+n) — the size of the world-search headline gain (+8 holdout c+n), which is being re-scored on the fixed harness with a 1 mm jitter noise floor (WORLD_SEARCH_PREREG Amendment 3). Env path (eval_core/wandb_eval, record_demos, r2dreamer eval) unaffected — DP/RLPD/WM headlines safe; comparisons made WITHIN one partition were fair but their noise was unmeasured ("×2 exact" replayed the same history). OPEN: magnitude on the gripper_lab/fidelity tables (head agent's call whether to re-run) | WORLD_SEARCH_PREREG_2026-09-02.md Amendment 3; replay_harness.py reset_arm docstring |
| 46 | **Human tapes are replayed on a fixed clock that is not the tapes' clock** (sim-improvement agent, 09-03: `HumanFollower` consumes exactly 4 tape frames per 0.12 s decision = 33.3 fps, but `trial_reader.py` emits a frame per 1/60 s window only when joint_states AND base_feedback (both 40 Hz) land in the same window — a beat artifact that drops 2–26 % of the robot's samples irregularly → tapes run 29.6–39.2 fps per bag (median 32.8; long 100–120 s demos 286/287 at 38 fps); the tapes carry no timestamps) | every human-arm tape (dH/dHpruned in both worlds): each demo is replayed 2–7 % faster or slower than the human moved (duration ratio p10/p50/p90 0.94/0.98/1.06 from the clock alone; 25/74 full-scope tapes end >2 s off, 286/287 +16 s); measured real2sim timing (`real2sim_fidelity.py`): median \|lag\| 0.72 s → 0.25 s once the clock term is removed (pick scope 0.36 → 0.15); the residual 1–2 decisions is follower/PD lag + the 4-frame grip quantization (sim closes 0.12 s early, lifts 0.1 s late, picked grant +0.06 s after the human's lift-off). Machine-arm demos (env-clock rollouts) are unaffected; pause structure (STOPS-vs-CREEP) is scaled uniformly per demo, not reordered | **DISCLOSED 09-03; frozen banks not changed** (earlier user: no change). Isolated 09-09 development EEF probe uses matched bag timestamps and joint/grip interpolation for 233/234/235: 233 remains complete, 234/235 still tip before upright release; see `paper/eef_recovery_2026-09-09/timestamp_probe/`. This is not a frozen recorder change. Full-pool timing follow-up is running (`paper/eef_recovery_2026-09-09/timestamp_full_pool/`): 237 newly completes with exact independent EEF replay and selected-phase visual review, separately packaged as a timestamp reconstruction; final gap 0.548 mm, not sustained endpoint contact. A ROS1 timestamp-conversion rounding bug in the new reader was found and fixed: it affected one averaged joint sample each in 254/295/300, now exactly reproduced. Full-pool collection now finished: 10/74 complete versus 8 matched fixed-clock controls, with 237 and 317 newly verified; four other trials gain upright release and five regress earlier. Timing and interpolation change together. See `timestamp_full_pool/FINAL_COLLECTION_READOUT.md`; this remains a December-18 development comparison, not an early-day result or calibrated fidelity proof. Fix if ever wanted = schedule waypoints by `inthewild_trials/<uid>_timed.npz` `t_frame` (consume until t_frame[k] ≥ decision time) → duration ratio 1.00 by construction; recorder change → all human sets rebuilt (cluster) | can_pos_recovery/extract_real_timed.py; real2sim_fidelity.py; scratchpad lag_anatomy/lag_resid (numbers in WORLD_SEARCH_PREREG A2.3 addendum) |
| 47 | **Two stage proxies are stale in every shelf world** (sim-improvement agent, 09-03): (a) `placed` (genesis_can_env.py:267) tests the can centre in `[BOX_TOP_Z+0.01, +0.07]` = [0.12, 0.18] with the UN-shifted BOX_TOP_Z 0.11, i.e. inside the solid shelf (top 0.17) → **0/222 census tapes** ever stamp `placed` in shelf6 worlds (the recorder manifest's non-zero `placed` count is a cumulative-flag artifact); (b) the recorder / training `nested` proxy (full_env.py:800: hard contact ∧ grip cmd < 0.3 ∧ tilts < 20) requires a full release that humans do not make (release grip 0.33–0.98) → on w3 full scope 74 uids it fires on **5** tapes where the honest settled `_nested()` (picked ∧ centre dist ≤ 0.081 ∧ both upright after 100 settle steps, no grip test) gives **16** (13 under, 2 over — proxy fired, settle drifted to 8.4/9.5 cm) | (1) every recorder-stamped stage table (census_*, demos_v2 manifests, `_stage.json`) — `placed` reads 0 by construction and `nested` is ~3× under-counted; the honest funnel of record for w3 is picked 69 / placed 66 / set-down 46 / contact 26 / nested 16 (tipped 32, upright-short 21); (2) **training-reward asymmetry in the corrected-world learners**: the staged reward `picked +1, placed +1, contact +2, nested +4` (full_env.py:4) — `placed +1` is UNEARNABLE in w3 (every RLPD/WM corrected-world run trained on a 3-stage reward), and `nested +4` is missed on human partial releases (relabelled human tapes, RLPD demo buffer, r2d demo reward) but not on machine demos, whose teachers release fully → a source-dependent reward on identical outcomes; (3) place-phase datasets / place-entry banks built from the `placed` flag | **DISCLOSED 09-03 (honest re-score tool `can_pos_recovery/honest_rescore.py`, bit-exact re-execution 74/74, `baselines/demos_v2/census_ts5w4_0903/full_gc_kp4_riser3_shelf6_honest.json`); NOT FIXED in the predicates** (fix = shift the band with `sim_variants.shelf_top`, and either drop the grip test from the proxy or score nested by the settled test at episode end; both change training rewards → head-agent decision, retrain). Report `nested` from the honest re-score only; quantify the reward asymmetry per arm before any learner claim leans on `nested`/`placed` | SHELF_HEIGHT_PREREG_2026-09-03.md §1 F1; honest_rescore.py; census logs |
| 48 | **The sim shelf is ~4 cm lower than the real one** (sim-improvement agent, 09-03): differential bag measurement (`inthewild_trials/<uid>_timed.npz`, base_feedback tool z at release − tool z at the pick closure; grasp height and tool offset cancel), 43 demos with > 8 cm lift and a full release: release height **median 15.8 cm above the table** (p25 14.3 / p75 17.0 / sd 2.4; end-of-demo slide height 15.4) vs the sim shelf6 top 12.0 cm (shelf10 = 16.0). w3 hides it: its penetration grasp lets the can slip 2.5 cm down the pads during the carry (ts5 0.9) so it is released 1.6 cm above the shelf; the stiff ts5 grasp releases 3.5 cm above → the "falls from height" tip class (w3 11 / ts5 10 / ts5_w4 13 of 32/26/22 tipped tapes) | every corrected-world place/slide number (human tapes re-executed 4 cm short of their shelf; `contact` and honest `nested` counts; the ts5 "−2 picks / −4 contact" census verdict of 09-03 was read against a mis-set shelf); the real2sim fidelity claim for the place phase (tool z matches by construction — the follower tracks joints — but the can lands on a lower surface) | **WITHDRAWN 09-03 17:20 as a shelf-height claim** (SHELF_HEIGHT_PREREG §6): all three rows NOT ADOPTED — P1 shelf10_ts5 68/18/29/17, shelf10 70/20/28/24, shelf8_ts5 69/20/17/24 (picked/contact/tipped/nested) vs w3 69/26/32/16; contact FELL in every row (prediction falsified, wrong sign). The 15.8 cm tool differential was read with the stale `replay_harness.BOTTLE_HEIGHT` 0.075 (world can 0.101) and contains real in-hand slip + grasp push-down + lift-while-releasing; the human carries enter the shelf footprint with the can bottom p10 2.8 cm above shelf6, so a 16 cm shelf clips the real paths (27/67 tapes never upright over the footprint at shelf10). Best estimate of the real shelf: 12–13 cm = shelf6. Keep w3. User tape-measure still requested (prediction now ~12–13 cm, not 16) | SHELF_HEIGHT_PREREG_2026-09-03.md §1 F3/F4, §2–4; scratchpad real_shelf_dz.json / tip_anatomy.py; real2sim_follower_lab_2026-08-23.md §9.7 |
| 49 | **Contact-constraint creep: the sim grasp is a viscous joint, not a stick** (sim-improvement agent, 09-03): Genesis 0.2.1 regularises every contact constraint by R ∝ (1−imp)/imp (`constraint_solver_decomp._func_add_contact`, `imp_aref`, engine default dmin/dmax 0.9/0.95), so a sustained tangential load BELOW the friction-cone limit yields a constant constraint velocity ∝ timeconst·(1−imp)/imp. Pinch lab (`can_pos_recovery/pinch_lab.py`, two fixed pads, can pinched 3 cm off-centre = 0.10 N·m): droop grows linearly in time (3.7/19.4/34.1° at 0.1/0.5/1 s), nearly independent of pinch force (59° at 0.5 mm vs 43° at 2 mm penetration) and of contact count (split can seg6 45° vs seg1 62° — the "free hinge" hypothesis is the wrong lever); ∝ timeconst (ts5: 21.5°); impedance 0.99 → 8.5° / slip 0.30 → 0.03 cm (stiffness ×5.6); 0.999 → 0.8° (×55, rejected). Release lab (20 w3 tipped tapes, branch at the release onset, pre-branch bit-exact 20/20): control 0/20 upright, no friction/knuckle/timeconst condition above 10/20, snap-open 17/20 → the can rotates in the slowly-opening pinch, not at the drop | every corrected-world number that depends on holding the can: in-grasp slip during the carry (w3 2.5 cm, ts5 0.9 cm — ratio 2.8 ≈ the lab creep-rate ratio 2.7), in-hand tips, the 32/74 tipped tapes (release class), the contact/nested funnel (26/16); it is an ENGINE regularisation artifact common to every arm (human and machine demos alike), so it biases the sim→real fidelity of the place/slide phases, not the human-vs-machine contrast | **P1 FALSIFIED 09-03 17:30 (wrong sign on every metric)** — `gc_kp4_riser3_shelf6_imp99` (0.99 on all 16 geoms), recorder path, 74 uids, honest re-score exact 74/74: picked 68 / contact **20** / tipped **43** / nested 13 / set-down **23** vs w3 69/26/32/16/46 (noise floor 5.3; holdout same wrong sign: contact 7→6, tipped 13→14, set-down 13→6). NOT ADOPTED. Tip anatomy (`tip_anatomy.py`, correct can height): 25/43 tips are cans EJECTED from the closed hand during the carry at the human's hold level (grip cmd 0.47–0.53) — the finger PD sets the penetration from the contact stiffness, so a 5.6× stiffer finger–can pair is a shallower, harder pinch that squirts the cylinder; w3's grasp is a penetration grasp (gripper lab §6.3) and the pinch lab could not see this (pads position-fixed). Disclosure: the August gripper lab already had this row (`g_dmax99`: slip 3.79→5.71 mm, tilt 4.8→10.8°, gripper_lab_2026-08-25.md §4) and I missed it when registering. The creep (F5) stands as an engine property but is NOT the recorder-path tip mechanism; the impedance lever is dead for the grasp. C1 `gimp99` (grasp geoms only) and B1 `ts5_imp99` finish as registered (verdicts in CREEP_PREREG §5). Next lever under test (release lab first, then a prereg): a follower-side unloading-aware release (the real Kortex reading over-travels 0.38→0.67 on the can while the pads barely move, gripper lab §1.6; the sim's linear map retracts the pads through the whole 8 mm penetration with the can still caged during the human's slow release) **ALL THREE ROWS NOT ADOPTED 09-03 18:30** (CREEP_PREREG §5): P1 imp99 contact 20 / tipped 43, C1 gimp99 (grasp pair only) 23 / 38, B1 ts5_imp99 20 / 29, vs w3 26 / 32 (noise floor 5.3; holdout sign fails in all three). C1 reproduces half of P1s damage on every metric = the site is the finger-can pair, not the world contacts; B1 shows grasp_timeconst 0.005 recovers the tips (43->29) and the nested (17 vs w3 16) but not the contact. Disconfirm branch (c): the creep is a real engine property but NOT the recorder-path mechanism; the impedance lever is dead for this grasp. w3 remains the world of record. Registered fallback ladder (imp 0.95/0.97) NOT taken (its trigger was picks dropping, and picks held at 67-69). | CREEP_PREREG_2026-09-03.md §1 F4/F5, §2–4; pinch_lab_imp_results.json, pinch_lab_results.json; `census_ts5w4_0903/release_lab_gc_kp4_riser3_shelf6/`; sim_variants.py `impedance` block |
| 50 | **ADOPTED (recorder path): fast-open release filter `gc_kp4_riser3_shelf6_og4`** (sim-improvement agent, 09-04 00:15, prereg-first): a causal grip filter in record_demos.HumanFollower amplifies OPENING moves 4x below the running hold plateau (closing/holds never altered — unit-tested on all 91 v2r streams); the WORLD is bit-identical to w3. Census (74 uids, honest exact 74/74): picked 69 / contact 28 / **tipped 32→20** / **honest nested 16→23** / set-down 46→50; holdout tipped 13→5 with contact 7→9; the CLEAN holdout (17 uids the release lab never saw) confirms (tipped 6→4, nested 4→8, contact 5→7). Passed every pre-registered clause; ognow (K=inf) also passed but og4 preferred by the registered margin rule. **CONFOUND TO CARRY: this is mechanism-UNfaithful — the bags prove the human release is a ~1.7 s ramp (no independent gripper cmd topic; g_frame==fb_grip) and the filter shortens it to ~0.4 s, so og4 tapes are NOT faithful re-executions of the human grip stream (median 21.5% of decisions altered); and machine-arm harvests do NOT get this shaping unless the same filter is applied to teacher grip outputs — any human-vs-machine comparison on og4-built data must either neutralise or disclose that asymmetry.** Head agent/user decide the retrain. | RELEASE_OPEN_PREREG_2026-09-03.md; census_og_0903; record_demos.HumanFollower._filter_grip; sim_variants og rows |
| 51 | **Two census ICs are unwinnable by construction (bogus lying-can placements)** (sim-improvement agent, 09-07): `trial_placements.json` gives uids **234 and 318** a 90 deg can_quat at z 0.085 (a can lying on its side) — the only two such entries in the file, and the same two flagged as "2 bogus 90 deg @ z=0.085 artifacts" on 2026-07-20. The full-task tip rule (terminate when a free can lies tilted > 60 deg with the grip open) therefore fires at DECISION 1: both tapes are 1 decision long, end_reason `terminated`, stage `none`, in EVERY census (og4, ognow, w3, jit, imp99, gimp99, ts5_imp99, shelf rows). **The real footage shows both cans standing UPRIGHT at t=0** (cam4 frame 0, checked by eye against uid 233 as the control; both source demos are label success, 234 with 899 decisions) — so this is a placement-recovery artifact, not a world or controller failure. Impact: every "n=74" funnel of record carries 2 impossible uids in its denominator (picked 69/74 = 0.93 is 69/72 = 0.96 on the winnable set). It does NOT move any comparison: both are `none` in every row, one in fit and one in holdout, so all deltas and the og4 adopt-rule verdict are unchanged (verified row by row). DEVELOPMENT FOLLOW-UP 09-09: isolated upright-quaternion/z corrections with unchanged xy and commands restore pickup in both EEF replays, but both fail supported upright release; see `paper/eef_recovery_2026-09-09/upright_ic_probe/`. Frozen placements remain NOT FIXED: correcting the two ICs would change the initial-condition set for every frozen arm and census, so it is a head-agent/user call; the recovery path is the documented camera_audit click pass (an automated Hough lid-detect attempted here is NOT trustworthy — it returned nothing on the good control 233 and a point 30 cm off for 234, consistent with the camera README's "every blob heuristic failed once"). | trial_placements.json trials 234/318; census_og_0903/full_gc_kp4_riser3_shelf6{,_fails}; real_videos_480p/{234,318,233}_real.mp4; CAN_STARTING_POSITION 2026-07-20 note |

| 52 | **Node core class is a real divergence source; hold it fixed within a comparison (THIRD independent confirmation)** (place/slide agent, 09-08): the phase re-score audit and the tables pipeline's node join had already reached this from different data; this lane reached it a third way, by RE-RUNNING cells on pinned hardware. The five DP `spots60` cells that had run on 48-core nodes (`dH_DP_s25-29`) were re-run pinned to 64-core and reproduced their per-seed counts **exactly** (53/51/55/50/50 both times), while the single cell that had run on a **36-core** node (`rlpd_g99v2fullw3_dHv2raw_s61`) moved **+2 of 30** on sampled rnd30 and **-1 of 60** on deterministic spots60; the 40-core seed (s60) was unchanged. That matches the established pattern: 53/53 same-core-count comparisons bit-identical, all 19 disagreements with a 36-core box on exactly one side. Effect on conclusions: NONE here (the pick source comparison stays null, p 0.658 sampled rnd30 after the fix vs 0.813 before), but both of this lane's comparisons HAD been core-heterogeneous before the fix (machine arm 100 % 64-core against a human arm with 36- and 40-core cells; DP dDP 100 % 64-core against dH with 48-core cells). Mitigation now standing: every cell written by `baselines/wandb_eval.py` and `baselines/eval_place.py` stamps `node.cores`; `analysis/rlpd_sampled_table.py`, `analysis/dp_spots60_table.py` and `baselines/place_table_all.py` print a per-arm core-class census and flag a class present on one side only; the pre-fix cells are preserved as `*_mixedcore`. OPEN and unidentifiable on this cluster: which instruction-set/microarchitecture difference actually causes it. Disclosed gap: the 2026-09-08 world-model place `_v2` re-score records `bank_version`/`entries_pinned` but NO node or core field, so its hardware pin cannot be verified from the cells. | `*_mixedcore` dirs under `baselines/rl/checkpoints/rlpd_g99v2fullw3_dHv2raw_s6{0,1}/sweep/` and `baselines/outputs/dp_w2final/dH_DP_s2{5..9}/sweep/`; jobs 3362808-14 (pinned re-runs); core census in the three table scripts |

## Standing rules
1. Never compare across rows of this table without naming the confound in the caption/text.
2. A new block gets a PREREG amendment BEFORE readout, and a row here if it changes any control.
3. When a status changes (OPEN → CLOSED/DISCLOSED), edit this file in the same commit as the result.
| 53 | **Replay-consistency selection on machine demonstration sets (CHECKED AND DISMISSED 2026-09-08)** — `record_demos.py --verify` keeps a harvested machine tape only if it replays consistently open-loop, a guard the human sets never pass through. On an independent benchmark this mechanism is large: tapes surviving one perturbed replay survive *independent* perturbations at 0.63–0.66 against 0.385 for the rest, so replay-filtering there selects a well-behaved subpopulation and any arm built that way is quality-selected as well as smaller | would be an asymmetric confound favouring the machine arm, of the same shape as the disclosed best-of-3 end-to-end selection — a property of how the set was *built* rather than of who produced it | **DISMISSED on measurement, not argument.** The guard rejected **3 tapes ever** across all harvests (dDPv2 1 of 80, dDPv2_w2 0 of 70, demos_v1/dDP 2 of 80) — about **1.4 %** of the kept pick set. It was active on **exactly one arm pair** (`matched_w3/dDP`, pass 58/58; the human pick arm shows n/a), **symmetric at place** (absent from both arms), and **structurally impossible end-to-end**, where `--verify` is fatal with full scope — so it cannot compound with the best-of-3 selection. Human sets are not unfiltered either: they are kept only if one sim execution reaches the stage, so the true asymmetry is the *second, independent* replay check, which removed 1 tape of 69. **Why the magnitude differs from the benchmark by two orders of magnitude is identifiable rather than lucky:** our machine tapes are a trained policy rolled out in the same world it is replayed in, deterministically, so the replay is near-exact — unlike cross-checkpoint rollouts replayed under perturbation | harvest manifests' `rejected_by_verify` counter; per-tape `verify` stamp in every kept npz; `record_demos.py:772` |
| 54 | **Hardware-class numerical divergence (CLOSED 2026-09-09, user ruling: irrelevant at our effect sizes)** — same physical core count reproduces bit-identically (119/119 comparisons); across classes 16 of 192 re-scored cells differ, movement ≤ 0.058 and human-arm-only, with one cross-class cell moving `contact` by 0.100. The minority 36-core machines are the divergence source | every cell's exact numbers; comparability of cells produced on different machines | **CLOSED — do not re-run for hardware alone.** Our comparisons are dominated by seed variance, not machine variance: per-seed counts range 8–19 of 30 *within* one arm, and the minimum detectable effects run 0.10–0.50. A ≤ 0.10 hardware wobble sits well inside that and is not the binding constraint on any conclusion. Cells stamp `node`/`cpu_model` automatically so the question stays answerable, and a cell produced off-class is USABLE rather than discarded. Prior guidance to pin, and the archived `*_wrongcpu` cells, are superseded | user ruling 2026-09-09; REVIEW_GUIDE addenda 2026-09-08 |
| 55 | **Potential missing passive distal-finger adaptation** (09-09): user reports real outer fingers curling in response to a can seated deeper toward the wrist. Current replay uses directly controlled tip joints plus fixed URDF mimic constraints. Fresh trial-118 yaw16 replay: maximum deviation from `q_tip = -0.676*q_driver + 0.149` is 0.00044 rad (~0.025°) across 1,760 frames, so independent contact-driven distal rotation is effectively absent. | real2sim fidelity of deep grasps, retention and release; claims that simulated linkage tests validate real finger behavior or rule out linkage compliance | **OPEN, manufacturer corroboration 09-09.** Kinova user guide p21 Figure 9 illustrates a distinct inward-curled 62 mm grasp alongside a straight-finger small-object grasp (source and figure in review README). This supports the reported grasp configuration; passive transmission/compliance parameters remain unresolved. Raw cam0 views do not isolate passive motion at matched proximal angles. Official Kinova URDF uses the same fixed coupling, so URDF conformity alone does not validate loaded-grasp fidelity. No causal attribution of drops, effect on source comparisons, or simulator fix established. A separate two-trial soft-mimic/zero-tip-PD probe permits up to 2.9 degrees independent motion on 233 but rescues neither 235 treatment; not adopted or calibrated (`paper/eef_recovery_2026-09-09/PASSIVE_FINGER_PROBE.md`). Existing sets unchanged. | `paper/finger_curl_review_2026-09-09/README.md`, `118_joint_trace.json`, `118_real_contact_release.jpg`; gripper_lab §2.1 |
| 56 | **Early recovery omits the variant post-build hook** (09-09 source audit): `recover_early.py` and `render_recovered.py` apply mount/shelf geometry via `apply_pre`, then call `build_world`, but never `apply_post` / `post_build`. The omitted hook sets arm gains and shifted goal spawn height; gravity compensation is already applied by the pre-hook. | named-world provenance of the 88 early winners and their rendered stage labels; using those initial positions in full w3+yaw recorder worlds | **OPEN.** Full-hook uid 118 fails to pick in both joint and EEF replay, whereas its archived video is labeled picked. This is a world-path discrepancy, not evidence that EEF causes the failure. Separate full-length pre-only diagnostic reproduces picked=true/contact=false/nested=false for uid 118; full-hook joint and EEF runs both have picked=false. This isolates the hook package, not its individual components. Do not extrapolate a corrected corpus count or overwrite existing winners. Yaw corrections remain necessary and separate. | `paper/eef_recovery_2026-09-09/README.md`; `can_pos_recovery/eef_recovery_probe.py`; recovery/render source call sites |
| 57 | **Small EEF IK errors can change recovered contact outcomes** (09-09): two trial-242 initial positions complete the full physical sequence in raw-joint replay but fail with ordinary EEF IK, despite passing a 0.1 mm / 0.1 degree p99 geometric gate. | claims that low arm/IK error guarantees grasp or final-slide equivalence; EEF bank acceptance | **PILOT REPAIR VERIFIED, broader validation open.** A local float64 URDF-Jacobian refinement, using only desired Cartesian pose and the IK branch seed, restores the successful measured trajectory exactly at the +10 mm x placement. Fresh saved-EEF-action replay is bit-identical and video confirms release and slide. Trial 242 is the independently rescued source; its alternative with added push is explicitly motion-repaired, not another human demo. A subsequent 74-trial census has 73 bit-identical float32 arm-target streams and one differing only at numerical rounding scale; eight unchanged-motion complete sequences pass independent EEF action replay and visual review. Historical mixed packaging reached eleven independent source UIDs (NOT eleven faithful recoveries; default manifest now contains eight unmodified-command reconstructions, with fitted-IC and modified-command manifests separate): eight unchanged-motion conversions plus disclosed repairs of 242, 275 (12.5 mm added push), and 308 (adopted og4 opening correction plus 0.3 s terminal hold). New repairs pass independent replay and visual review; 308 retains slight tilt and robot contact at the endpoint, with a separate 3 s retention check (`paper/eef_recovery_2026-09-09/REPAIR_FOLLOWUP.md`). The raw 8/74 census is not the previously adopted og4 recorder performance. This validates action conversion in this pool, not corpus-wide physical recovery or passive finger compliance. | `paper/eef_recovery_2026-09-09/SLIDE_RECOVERY_PROGRESS.md`; `baselines/demos_eef_recovery_2026-09-09/manifest.json`; `baselines/eef_delta_control.py` |

| 58 | **Early fitted initial positions intersect the full-world solid shelf** (09-09): 12/89 archived winner/seed positions overlap the shelf cylinder–box geometry. Trial 131 is pushed upward by the shelf at initialization and later receives a picked flag despite zero sampled robot-can contacts. | interpreting early picked/released counts as physical recovery; attribution of all failures to finger control | **OPEN, DISCLOSED.** Keep the declared census and flag invalid initial geometry; do not permanently reject real recordings or move positions merely to increase yield. Real initial placement and shelf geometry need reconciliation. | `paper/eef_recovery_2026-09-09/early_yaw_pool/initial_geometry_audit.json`; `early_yaw_pool/SLIDE_DIAGNOSTIC.md` |
| 59 | **Recovery contact diagnostic differs from the supplied slide metric of record** (09-09). The strict release/recontact/solver-contact diagnostic gives 10/74 timestamp completions; the unchanged supplied `slide_predicate.py` gives 21/74 with physical tilt mapping at the original 120 ms cadence. Recorder tipped flags are grip-gated; their proxy additionally admits physically tipped trial 295. | mixing metrics, cadences, tip definitions or cohorts; claiming rescoring itself improved simulation | **DISCLOSED.** Original64 result reproduced exactly (64 released, 64 pushed, 15 arrived). Paired December18 physical-tilt scores: fixed 19/74, timestamp 21/74; five gains and three losses. Keep exact predicate, disclose adapter and both tip mappings, retain stricter contact evidence separately. Additional metric passes undergoing independent EEF replay. | `paper/eef_recovery_2026-09-09/slide_metric_of_record/README.md`; `can_pos_recovery/score_recovered_slides.py` |

| 60 | **Slide predicate can release before pickup** (09-09): stationary initial can plus moving approaching tool satisfies release; subsequent carry can satisfy gain. In 162 completed EEF traces, every metric pass (21 December18, nine raw early) has release before the legacy picked flag. | treating supplied metric pass as proof of pick–place–supported slide | **OPEN, DISCLOSED.** Keep the user's exact metric and thresholds. Report independent physical sequence evidence separately. Early raw passes include invalid initial-overlap cases; legacy picked flag is not ground truth. | `paper/eef_recovery_2026-09-09/slide_metric_of_record/release_order_audit.json`; `audit_release_order.py` |

| 61 | **Adaptive-hand contact softness is uncalibrated and changes release as well as grasp** (09-09): compliant rigid contact permits solver overlap; finger treatment includes all four finger geometries, and manipulated-can treatment also changes table/shelf/goal contact. | equating improved grasp appearance with realistic material deformation or improved full-demo recovery; generalizing two selected trials to early/late populations | **DISCLOSED; physical calibration OPEN.** Six fixed runs (113/233 × baseline/softer fingers/softer can) completed with identical source commands and exact baseline reproduction. In 233, loaded cap-relative rim error falls from about 14 px to 1–5 px, but final center distance worsens from 67.75 mm to 183.24/185.29 mm; neither soft treatment passes the supplied metric or strict sequence. In yaw-corrected 113, neither rescues carry or supported shelf release. Soft 233 median carry overlap is 8.92/9.63 mm, not measured pad compression. One softness level, no material calibration, no bank adoption. | `paper/eef_recovery_2026-09-09/adaptive_softness/README.md`; `plan.json`; `summary.json`; `233_softness_real_comparison.png` |

| 62 | **Pad subdivision changes the contact manifold even with an unchanged exterior** (09-09): four finger collision geometries become twelve; the rigid divided control adds simultaneous pad/backing/joint-region contacts. | attributing a replay change to pad softness rather than geometric contact generation; treating overlap as validated pad compression | **DISCLOSED, candidate rejected.** All four divided-geometry 233 runs (rigid control plus three pad softness settings) fail upright supported release and finish tipped about 90 degrees. Reconstructed single-hull control remains within 0.63 mm final XYZ of the prior adaptive trace. A second surface-material implementation preserves the original collision geometry and exactly reproduces the prior adaptive trace at unchanged contact parameters, but all three softened settings also fail the full sequence. Stronger return-spring comparisons are separate exploratory mechanics tests; no pad configuration has yet established an end-to-end recovery improvement. | `paper/eef_recovery_2026-09-09/localized_pads/INITIAL_READOUT.md`; `initial_plan.json`; `surface_plan.json`; `spring_plan.json`; `summary.json` |

| 63 | **Hand mechanics fitted to one loaded grasp need not transfer across closure regimes** (09-09): actuator coordinate, force saturation, return springs, damping and distal travel are uncalibrated together. Motor feedback is not a passive-joint measurement; mirroring the raw CAD right-tip range is a symmetry hypothesis, not a verified left-axis conversion. | interpreting a good curled-grasp video or a paired pad benefit as general end-to-end recovery improvement | **OPEN, DISCLOSED.** Across 48 calibration replays on three demos, the reduced-damping soft-pad hand improves 233 loaded rim errors and yields a supported 24.97 mm final push. Its .03 s pad passes the unchanged metric (70.19 mm center distance) while the same-hand .02 s control fails (179.33 mm). It still misses goal contact by 4.19 mm, which the original fixed-coupling replay achieved; placement image errors worsen. Both early calibration demos fail pickup under both pad settings. Frozen reserved 176/185/237 comparisons are complete. Across all six selected demos, original fixed / matching rigid / soft have supplied-metric passes 3/6, 2/6, 3/6 and strict completions 3/6, 0/6, 1/6. Soft 176 retains goal contact; 185 still fails and 237 loses final retention. No bank adoption or population claim; see `localized_pads/VALIDATION_READOUT.md`. | `paper/eef_recovery_2026-09-09/localized_pads/HAND_MECHANICS_READOUT.md`; `candidate_sequence_audit.json`; `hardware_reference/TRAVEL_REVIEW.md`; `validation_plan.json` |

| 64 | **Generic added joint inertia dominates the experimental passive finger dynamics** (09-09): the pinned URDF loader gives every revolute joint 0.1 kg m² armature, including the now-passive tips (about 18,879× their URDF link inertia about the pivot). | attributing pre-contact curl or slow opening to real passive compliance; using the earlier inertia-free D/K calculation as a settling-time prediction | **VERIFIED IN UNLOADED BENCH; task correction OPEN.** In a fixed-base, zero-gravity, no-contact hand, identical ramps produce 34.46° peak unloaded-relation error and 12.04° worst hold-end error with default armature, versus 10.57° / .011° at 0.0001. Zero or distal-only zero armature triggers the existing instability guard. Default internal mode is underdamped (ratio .069, period 7.50 s), invalidating the earlier overdamped interpretation. Small nonzero inertia is numerical regularization, not measured rotor inertia. Seven low-inertia and six mass-informed return-spring full replays completed; the latter soft candidate completes 233 and improves its four image checkpoints over its rigid control, but both early calibration soft grasps fail. Frozen nine-recording validation is complete and rejects the soft setting: supplied-metric passes original / matching rigid / soft 3/9, 2/9, 1/9; strict completions 2/9, 2/9, 1/9. Soft 224 moves the goal 477 mm and soft 237 57 mm. Seven subsequent limited-compression calibration runs also fail both early pickups. No adoption. | `paper/eef_recovery_2026-09-09/localized_pads/unloaded_armature/README.md`; `source_audit.json`; `mode_readout.json`; `low_armature/plan.json` |

| 65 | **Finger inertial coordinates disagree with their own meshes** (09-09): three published finger COMs lie outside the corresponding watertight mesh bounds; mesh integration corroborates several COM/Ixy sign changes while closely matching published magnitudes. The pinned official raw CAD URDF contains the same values. | interpreting passive finger dynamics as calibrated hardware mechanics, especially after reducing generic armature | **RUNTIME VERIFIED; recovery benefit OPEN.** An isolated candidate changes only inertial coordinate signs and preserves masses/magnitudes, collision geometry and joints. Runtime COM/tensor/mass/quaternion assertions pass; unloaded 951-step bench is stable and symmetric. Peak target error is 2.156 degrees versus 2.116 before, so this is not a peak tracking-error improvement. Uniform-density mesh integration is corroboration, not a measured two-material mass distribution. All seven calibration replays completed. Aligned rigid / soft strict completions are 1/3 / 0/3: 113 has no supported release, 184 no supported push, soft 233 a 2.52 mm ending gap. No general recovery gain or adoption. | `paper/eef_recovery_2026-09-09/localized_pads/finger_inertia_alignment/README.md`; `audit.json`; `bench_comparison.json`; `full_task_plan.json` |

| 66 | **Aligned soft-hand carry drift occurs below the friction limit** (09-09): exact 113 replay with every-substep observation during7–16 s shows median tangent speed3.72/6.54/7.73 mm/s at8–9/9–10/10–11 s, utilization.295/.304/.237, with fewer than.5% loaded samples saturated per interval. Real camera retains the can through18 s while sim drops before12 s. | attributing carry loss to insufficient physical friction or claiming visual softness is calibrated pad behavior | **VERIFIED DIAGNOSTIC; full-task remedy OPEN.** Isolated Genesis1.4 contact benches preserve input mesh hashes and normal parameters. Centered soft .5 mm-overlap drift over2 s falls3.897→.236 mm from newer pyramidal/ratio1 to elliptic/ratio10; off-center droop50.97→6.51 degrees. Four scenes/eight cases each, not task demonstrations. New analytic mass differs.645% from pinned mesh mass; same-engine comparisons isolate friction formulation better. Historical1.2.1 grasp-gate failure and rejected global-normal-impedance interventions remain disclosed. Four subsequent full-world233 controls all fail pickup; pre-grasp hand oscillation prevents a clean contact-formulation interpretation (row67). No engine adoption. | `paper/eef_recovery_2026-09-09/localized_pads/aligned_contact_diagnostics/README.md`; `engine_contact_review/README.md`; `engine_contact_review/summary.json` |

| 67 | **New-engine parameter equality does not guarantee matched passive-hand dynamics** (09-09): isolated Genesis1.4 port passes finger mass/COM/inertia, armature, limits, damping and normal-parameter readback, but exhibits about15° maximum unloaded-relation error during .3–3 s approach versus .049° pinned, with zero sampled can–hand contacts. | attributing engine-port failure solely to pad softness or friction; treating a fixed-pad bench gain as full-task recovery | **VERIFIED CONTROLLER PORT BUG; fresh-feedback pyramidal soft candidate REJECTED on expanded validation.** In the new engine's MuJoCo-compatible mode, the position getter returns a stale kinematic cache before the external spring callback. Paired zero-collision, zero-gravity951-step benches verify that an explicit forward refresh eliminates feedback error and reduces peak velocity59.70→4.047 rad/s and target error15.41→2.156°, matching the pinned bench without changing physical parameters. Four preceding full233 controls (pyramidal/ratio1 and elliptic/ratio10, each rigid/soft) complete unchanged commands and all fail pickup. Soft runs displace the goal7.015/4.836 m. Principal-inertia representation and recentered material-frame mistakes were corrected with archived attempts; corrected rigid classification reproduces every preceding rigid trace array exactly. Fresh-feedback trial233 now completes under both soft contact settings; pyramidal rigid misses retention and elliptic rigid misses contact. Completed early pyramidal113/184 pairs still fail release; elliptic113 restores carry through20 s under both pad settings but loses descent by22 s. Exact233 saved-action verification passes. The frozen nine-demo fresh-feedback pyramidal panel is now complete (18/18 runs): original / matching rigid / soft supplied-metric passes 3/9 each and strict completions 2/9, 1/9, 1/9. Soft adds no successes over rigid and loses original237; soft185 moves the goal2.533 m. See `localized_pads/g14_validation/README.md`. New-engine neutral self-collision filtering differs with yaw; collision-only re-evaluation finds no opposite-tip contact at all4,187 saved113 soft decision poses, leaving unsampled substeps open. No upgrade, pad adoption or population claim. | `paper/eef_recovery_2026-09-09/localized_pads/g14_full_replay/README.md`; `summary.json`; `approach_readout.json`; `plan_port3.json` |

| 68 | **Monocular seating/placement ambiguity in early113**: camera fitting from approximate CAD cap correspondences leaves focal/depth ambiguity despite small landmark residuals. | treating image mismatch as a measured initial-position or shelf-height correction; fitting placements to recovery success | **OPEN, DISCLOSED.** Clock-matched real frames show upright shelf release, but the provisional impression of support already at20.04s is withdrawn: the real lid drops about26px relative to the opening hand by20.7s. Exact support onset and metric drop height remain unmeasured. Conditional cap-relative rim mismatch is already about24 px near pickup and persists across15 lens hypotheses. A separate vision-only unheld-rim fit spans75–162 mm world height under lens/annotation sensitivity; no placement or height change is adopted. These ranges are not confidence intervals. A subsequent fixed-grasp fit to real rims at10/12/14s gives about2cm greater tool-z seating than simulation, with1–3cm axial disagreement under declared lens/annotation sensitivity; held-out8/18/20.04s rim RMS3.56/6.07/4.00px. This conditional result does not calibrate initial placement, hidden fingers or shelf height. The withdrawn shelf-height claim in row48 remains withdrawn. | `paper/eef_recovery_2026-09-09/localized_pads/g14_release_diagnostic/README.md`; `113_conditional_seating.json`; `113_initial_can_fit.json` |

| 69 | **Spatial pad treatment exposure differs across grasps** (09-10): proximal-only extra compliance is almost inactive in113, despite sustained hand contact. | interpreting a null material intervention as proof that a physical pad property cannot matter; confusing detected contacts with loaded support | **VERIFIED, candidate REJECTED.** Seven frozen elliptic comparisons complete: rigid/proximal strict and supplied-metric passes0/3 each; all-soft233 reproduces its prior completion. Three archived controls match every saved array exactly. A separate exact113 observation replay records8660 distal versus3 proximal detected contacts over1401 scene samples at7–21s. All carry contacts at7–18s are distal and satisfy the region mask; there is no sustained proximal contact being misclassified. The material placement hypothesis does not recover e2e performance; covering thickness/stiffness remain unknown. | `paper/eef_recovery_2026-09-09/localized_pads/proximal_compliance/README.md`; `summary.json`; `contact_location_readout.json`; `diagnostic_execution.json` |

| 70 | **Release failure depends on seating and shelf-edge clearance, not just grip** (09-10): the exact archived113 force replay shows shelf contact supplying the large backward impulse after the fingers separate. | interpreting a failed release as continued finger sticking, or a vision-derived placement improvement as evidence for softer pads | **VERIFIED DIAGNOSTIC; calibration and recovery OPEN.** Two observation replays reproduce every archived113/233 array. In113, last loaded finger contact20.1575s precedes first loaded shelf contact20.21125s; shelf x impulse20.16–20.24s is about−0.167Ns, with momentum residual below0.00005Ns per axis.233 instantaneous force-balance residuals are larger and disclosed. A nominal image-only initial XY17.24mm forward/5.41mm sideways is frozen before three full conditional comparisons. Original fixed / matching rigid / soft all fail the supplied metric and strict e2e sequence. Rigid gains supported release but fails the final push; soft still fails release. Image seating improves under both. Two further exact conditional force replays show soft opening adds downward normal impulse before shelf ejection, whereas rigid retains net upward support and settles. Normal/tangent decomposition and nonzero force-balance residuals are retained; positive contact work is not proof of numerical energy creation. Camera/table assumptions remain unresolved; no pose, shelf, pad or engine adoption. | `paper/eef_recovery_2026-09-09/localized_pads/release_wrenches/README.md`; `vision_initial_probe/README.md`; `g14_release_diagnostic/113_real_drop_check.json` |

| 71 | **Contact damping parameters couple normal and tangential response** (09-10): keeping tc×dampratio fixed preserves the elastic coefficient, but halving tc doubles reference velocity damping in both normal and tangent rows. | treating this as a normal-only damping test, or assuming equal static coefficients preserve the loaded release configuration | **VERIFIED; doubled-damping candidate REJECTED.** All ten corrected replays finish; original-placement matching rigid / soft each score0/3 under both supplied and strict task criteria, and both conditional113 treatments fail release. Undamped soft233 completion is lost. Every treated contact checks the actual engine reference function: static-term error0, maximum velocity-term relative error1.13e−7, time constants above safety floor. Four identity runs across two preserved attempts reproduce all archived arrays. Total12 completed full replays; eight first-attempt compilation failures occur before stepping and are separate. Soft184 moves the goal2.803m. No adoption or physical material calibration. Normal-only damping remains unimplemented and untested. | `paper/eef_recovery_2026-09-09/localized_pads/contact_damping/corrected/README.md`; `corrected/summary.json`; `normal_only_capability.json` |

| 72 | **Near-contact gains and fitted goal geometry do not independently establish fidelity** (09-10): normal-only damping preserves a supplied-metric pass in233 with a0.468mm final surface gap and no goal contacts in saved decision states, but the historical goal estimate itself used233/242 end-contact constraints. | treating a submillimeter strict-contact difference as calibrated real-world error, or a proximity pass alone as full recovery | **DISCLOSED; normal-only calibration complete, broader effect OPEN.** All10 replays finish; two identity controls reproduce every reference array. Every selected normal/tangent row is checked against the installed engine reference, while all other references and contact parameters remain unchanged by the intervention. Rigid / soft original-trio supplied passes0/3 /1/3, strict0/3 each. Soft233 performs a supported40.9mm post-release push and shifts the goal0.636mm; carry image alignment is nearly unchanged and22s release rim error improves40.42→31.36px, still substantial. Both original early pairs and conditional113 pairs fail supported release. Full233 two-camera review verified. No threshold, goal, placement or candidate adoption. A frozen broader elliptic panel is prepared to separate contact formulation, softness and damping; prior broad validation used pyramidal contact. | `paper/eef_recovery_2026-09-09/localized_pads/normal_damping/README.md`; `233_final_slide_diagnostic.json`; `hardware_and_task_check.md`; `elliptic_panel/run.py` |

| 73 | **Broader mechanical gains do not yet improve reconstruction yield** (09-10):33 full panel replays complete; three UID181 verification stops leave eight complete matched groups. | treating executions as independent demos, verification stops as physical failures, or goal-displacing passes as faithful recovery | **NO OVERALL GAIN ESTABLISHED; ninth comparison INCOMPLETE.** Original/rigid1/soft1/rigid2/soft2 metric3/8,2/8,2/8,2/8,2/8; strict2/8,0/8,1/8,1/8,2/8. Soft2 completes the same176/237 as original.176 goal shift improves7.629→0.028mm;237 worsens4.257→47.181mm. Original224 metric-only pass shifts goal86.770mm, so metric counts alone cannot rank fidelity. Verification-stop cause unresolved. Both yaw-corrected early days and late recordings included; selected historical panel, no population estimate or adoption. | `paper/eef_recovery_2026-09-09/localized_pads/elliptic_panel/README.md`; `elliptic_panel/summary.json` |

| 74 | **Cancellation-sensitive reference verification can stop valid physical replays** (09-10): ascending assembled J@qvel and native ancestor-order accumulation differ in float32; output-scaled errors become misleading near cancellation. | scoring an audit stop as physical task failure, or silently weakening checks to finish runs | **RESOLVED WITH DECLARED NUMERICAL CHECK CHANGE.** V2 completes3/4 repeats; V3 completes5/5 using input-magnitude scaling at1e-5 plus independent native Jacobian entry checks at1e-6. Max Jacobian discrepancy5.96e-8 and input-scaled errors<2.72e-7; input/output scales differ up to85x.181 soft1 and176 soft2 reproduce all14 reference arrays exactly. Physical damping arithmetic and both task predicates unchanged; original failures/output-scaled errors retained. Full nine-demo original/latest soft2 metric3/9 vs2/9, strict2/9 vs2/9, no new recovery. | `paper/eef_recovery_2026-09-09/localized_pads/elliptic_panel/verification_retry_v3/README.md`; `identities.json`; `merged_summary.json` |

| 75 | **Repeated material trials cover few distinct recordings; expanded sample inherits source-pose defects** (09-10). | interpreting222 completed replays of12 demos as broad validation, or excluding inconvenient failures | **EXPANSION RUNNING, no result claimed.**36 additional UIDs selected12/day by fixed SHA256 ordering without reading outcomes, from151 eligible existing source records.144 frozen comparisons with eight CPU workers; original references retained. Five current initial poses overlap shelf (220,161,196,151,201), all retained and flagged. Historical source pools, not pristine unseen data or independent participants. No spin-friction change or parameter fitting in this batch. | `paper/eef_recovery_2026-09-09/localized_pads/expanded_panel/plan.json`; `initial_geometry_audit.json`; `README.md` |

| 76 | **Native GPU throughput is not CPU reconstruction equivalence** (09-10): a new device-side hand/pad port supports native environment batching. | counting copied benchmark lanes as new demos or combining GPU/CPU outcomes without controls | **IMPLEMENTATION PROGRESS; adoption OPEN.** CPU port exactly reproduces all arm/finger/can/goal states over962 decisions of233.16 CUDA copies agree exactly over400 decisions; warm aggregate88.6 decisions/s versus14 CPU under concurrent host load. Prototype used unsupported Torch2.7; GPU/CPU differ up to3.42mm can position and0.1404rad finger angle over the prefix. Supported Torch2.8 CUDA stack verified in isolated /tmp overlays; frozen standalone233/262 and mixed16/64 tests running. Gain2, full individual endpoints and contact/EEF exports remain unported. No GPU recovery claim. | `paper/eef_recovery_2026-09-09/localized_pads/native_batch/README.md`; `cpu_full_equivalence.json`; `supported_plan.json`; `supported_stack.json` |

| 77 | **A broader reconstruction gain need not be softness-specific** (09-10):262 newly completes with all four new configurations, including rigid controls. | attributing its recovery to softness alone or generalizing from partial sampling | **262 VERIFIED LOCALLY; sample INCOMPLETE.** Original final distance0.3666m versus soft1 0.065995m, with0.398mm goal movement. Full29.73s two-camera video decoded/selected stages reviewed; independent EEF replay reproduces all14 arrays over991 decisions exactly and completes the strict sequence. At48/144 expanded replays (12 groups), original strict0/12, each new configuration1/12. No admission before the full36-group result and attribution assessment. | `paper/eef_recovery_2026-09-09/localized_pads/expanded_panel/262_review/README.md`; `action_verification/verification.json`; `expanded_panel/summary.json` |

| 78 | **Batch size changes supported GPU trajectories; speed is not verification** (09-10). | treating native GPU throughput as physically interchangeable reconstruction or duplicated lanes as a broader sample | **MEASURED; cause OPEN.** Supported Torch2.8 CUDA, Genesis1.4:16/64 lanes88.52/326.03 aggregate decisions/s on400-decision233/262 prefixes. Within-batch duplicate copies exact, but standalone/batch can trajectories differ up to7.096mm;16/64 differ up to3.002mm. Frozen commands, geometry, collision policy and material match. One-lane shape controls running; no GPU material admission. ExpandedCPU56/144 replays,14 groups:262 gain offset by196 loss, all models strict1/14;196 original goal shift26.50mm and inherited overlap retained. No overall softness gain established. | `paper/eef_recovery_2026-09-09/localized_pads/native_batch/supported_comparison.json`; `lane1_plan.json`; `expanded_panel/summary.json` |

| 79 | **GPU repeatability and extra contact rows can alter task trajectories without an intended material change** (09-10). | attributing across-run GPU differences solely to batching, or zero torsional coefficient being a task-level identity | **MEASURED; causes OPEN.** Exact repeat of standalone GPU233 differs up to3.028mm can position/0.1233rad finger angle over400 decisions, so batch-size causation is not isolated. Separate CPU four-row zero-spin233 controls have zero observed spin torque and passed coefficient audits yet differ from archived three-row traces up to20.34mm rigid/367.86mm soft; soft loses supported release. Eight-case spin pilot retains matched zero controls and frozen physical settings. Expanded72/144 replays,18 groups: original strict3/18, every new configuration1/18 (gain262; losses196/225/232).196 real images show final can contact. No adoption. | `paper/eef_recovery_2026-09-09/localized_pads/native_batch/repeat_single233_comparison.json`; `finite_pad_spin/summary.json`; `expanded_panel/196_review/README.md`; `expanded_panel/summary.json` |

| 80 | **Timing-based GPU solver selection explains a reproducibility failure in the tested batch cases** (09-10). | assuming a fixed random seed fixes numerically different timed solver dispatch, or prefix reproducibility proves e2e recovery | **PREFIX REPEATABILITY RESOLVED; full-task GPU verification RUNNING.** Supported deterministic flag pins prefer_decomposed_solver=1. All saved mixed16 repeat arrays and16/64 physical states are exact for233/262400-decision prefixes;64 throughput299.39 aggregate decisions/s under host load. Standalone/batch233 still differs2.090mm. FullCPU233 diagnostic export exactly preserves962 states/tool/contact/picked/strict outputs. FullGPU repeated endpoint cases pending; no training admission.233 finite-spin real-rim comparison remains mixed and both treated rigid/soft complete, so no softness-specific improvement. | `paper/eef_recovery_2026-09-09/localized_pads/native_batch/supported_comparison.json`; `full_task_cpu_equivalence.json`; `full_task_plan.json`; `finite_pad_spin/233_soft_seating.json` |

| 81 | **Full-task outcome depends on numerical implementation and integration resolution** (09-10). | promoting repeatable GPU prefixes or an8-substep soft success as robust e2e fidelity | **CONFIRMED SENSITIVITY; resolution study RUNNING.** FullGPU233 repeats exactly, including contacts/tool/picked, but tips near23.7s and fails arrival/strict sequence; CPU completes. Final can position differs197.98mm; goal movement51.60 vs9.86mm. FullGPU262 first run completes, so mismatch is episode-dependent. Separate8-substep CPU identity matches all14 arrays, but16-substep233 fails release and differs207.47mm despite unchanged physical/input invariants;32 remains live. Completed8-case finite-spin pilot recovers no new UID and supplies no softness-specific gain. No GPU replacement, material adoption, or threshold tuning. | `paper/eef_recovery_2026-09-09/localized_pads/native_batch/full_task_summary.json`; `full233_repeat_equivalence.json`; `integration_resolution/summary.json`; `finite_pad_spin/summary.json` |

| 82 | **The `rnd30` start distribution OVERLAPS the shelf footprint, so 4 of 30 starts satisfy `placed_v2` at reset** (09-10). | reading any `rnd30` `placed_v2` count as "the robot placed the can". `sim_variants.py:185` puts the shelf at x ∈ [0.55, 0.95], y ∈ [-0.5625, 0.1875]; ICs 6/13/19/26 have can x = 0.556/0.591/0.575/0.561 — all inside — while every other IC has x < 0.55. Perfect separation at the near edge. Those starts are can-in-footprint, upright, gripper open, so after `PLACE_SUSTAIN`=10 frames `placed_v2` grants with the arm still at home. Verified: both arms' `ic13` episodes score `stages={'placed_v2': 1}` and nothing else — no pick, no contact, reward 0.0, full 300 decisions. **This is not a predicate bug; the predicate correctly reports a true state that the task setup created.** Measured over 390 e2e eval episodes (13 fully-trained {RLPD} runs): 26 spurious firings, **symmetric across arms (13 human, 13 machine)**, so it inflates the level of both arms without biasing the contrast. Net of it, placement is human 69/240 (28.8 %) vs machine 1/150 (0.7 %) — the raw 82 vs 14 badly understates the gap. Far-x starts are also outside training support (`DP_PRUNED_GAP`: 0/280 picks at x ≥ 0.52), so these episodes were never winnable. Fix options are NOT equivalent: requiring `picked ∧ placed_v2` changes a REWARD term; excluding the overlapping ICs changes the frozen eval start set and moves every prior `rnd30` number. Neither is actioned. |

| 83 | **Passing a finer resolution is not convergence** (09-10). | selecting32 substeps because it restores233 completion while ignoring16 failure | **NONMONOTONIC SENSITIVITY RETAINED;64 refinement RUNNING.**8/16/32 outcomes pass/fail/pass under unchanged physical/source invariants.8/32 final can positions differ22.53mm;16/32 differ202.08mm. Goal movement9.863/52.964/0.302mm.32 image seating remains mixed despite smaller22s discrepancy.64 is declared before its outcome; no resolution selected by yield. FullGPU262 repeat exact and complete, but fullGPU233 exact repeat fails versusCPU. No adoption. | `paper/eef_recovery_2026-09-09/localized_pads/integration_resolution/summary.json`; `233_ss32_seating.json`; `integration_resolution_finer/plan.json`; `native_batch/full_task_summary.json` |

| 84 | **Full-world divergence is much larger than unloaded hand refinement error** (09-10). | inferring a spring-controller instability from task failures, or adopting a successful timestep/material combination | **DIAGNOSTIC PROGRESS; cause OPEN.** Full233 at8/16/32/64 substeps gives pass/fail/pass/fail;64 leaves the shelf and ends0.99228m from goal,945.58mm from32 endpoint. All source/physical invariants pass. Three951-step no-contact/no-gravity hand schedules complete;8 reproduces both archived arrays exactly, and max16/32 joint difference is0.01526 degrees (8/16 is0.08646). Three local mass/spring linearizations have spectral radii below1; exact proximal opposition and omitted contact/saturation/arm dynamics limit inference. The hand-only check narrows but does not prove the contact-numerics hypothesis. Observation-only full contact-solver trace launched with exact14-array gate. Expanded136/144 runs,34 groups: original and both soft settings metric6/34; original strict3/34 vs each new1/34. No gain or adoption. | `paper/eef_recovery_2026-09-09/localized_pads/integration_resolution_finer/combined_summary.json`; `unloaded_resolution/summary.json`; `integration_resolution/linear_modes/README.md`; `constraint_convergence/plan.json`; `expanded_panel/summary.json` |

| 85 | **The existing CG contact solve sometimes exhausts its iteration cap** (09-10). | interpreting a repeatable passing replay as a sufficiently solved contact model, or all large gradients as failures | **OBSERVATION IDENTITY VERIFIED; remedy OPEN.** Unchanged full233 control reproduces all14 archived arrays exactly. Final improved flag remains true for559/23088 source substeps (2.42%), including244/2400 in20–23s (10.17%). Installed monolithic-loop inspection identifies these as cap exits; actual iteration counts not instrumented. Most other substeps satisfy allowed small-positive-cost-improvement exit, so gradient magnitude alone is not failure. One matched cap1000 follow-up declared and running, retaining float32/tolerance/substeps/physical parameters/source motion. Does not yet explain GPU/timestep differences or establish a better pad. | `paper/eef_recovery_2026-09-09/localized_pads/constraint_convergence/summary.json`; `identity.json`; `constraint_iterations/plan.json` |

| 86 | **Expanded soft-pad metric gain does not establish faithful final-slide recovery** (09-10). | adopting a configuration from aggregate proximity counts or improved grasp appearance | **COMPLETED, NO ADOPTION.** All144 replays/36 additional recordings pass execution/invariant checks. Original/soft1/soft2 metric6/6/7 of36, strict3/1/1. Both soft settings gain262 (also recovered by rigid controls) and lose196/225/232 strict completions. Extra soft2 metric pass191 moves goal178.10mm and has no supported release. Both early yaw corrections retained; five inherited shelf overlaps included. User-requested reassessment recommends a mechanism-independent recovery objective and real-video release/slide diagnosis; no new pad sweep. | `paper/eef_recovery_2026-09-09/localized_pads/expanded_panel/FINAL_READOUT.md`; `paper/eef_recovery_2026-09-09/APPROACH_REASSESSMENT_2026-09-10.md` |

| 87 | **Removing contact iteration-cap exits is insufficient for timestep consistency** (09-10). | equating a better solver termination flag or restoredGPU233 pass with robust physical recovery | **PARTIAL NUMERICAL REMEDY, LIMIT RETAINED.** CG1000 removes observed cap exits and preservesCPU233 completion. Exact nativeCPU identity passes; GPU233 now completes, but CPU/GPU can trajectories differ14.02mm maximum/10.96mm final. AtCG1000,16 substeps still misses the push (102.47mm final distance), with zero cap exits at both resolutions and49.87mm maximum trajectory difference. Float64 CG/Newton references running after exact14-array float32 identity. No engine/material adoption. | `paper/eef_recovery_2026-09-09/localized_pads/constraint_iterations/summary.json`; `constraint_resolution/summary.json`; `native_iterations/summary.json`; `constraint_reference/plan.json` |
