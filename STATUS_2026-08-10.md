# Morning status — 2026-08-10

## The paper: core results FINAL (n=8, Bayesian, committed)

| claim | evidence |
|---|---|
| **Model demos beat human demos for BC, in-distribution — DECISIVE** | P(model>human)=**0.994**; dDP_DP 0.80 (0.67–0.93) vs dH_DP 0.62 (0.40–0.80), n=8 each |
| **The advantage buys ZERO generalization** | random ICs 0.23 vs 0.23, P=0.45 |
| **RLfD is source-indifferent** | SACfD P=0.78/0.44; nominal direction flipped n=3→n=8 (seed noise) |
| **No generational collapse** | lineage 0.67→0.87→0.87 (single-seed, cap-600 caveat) |

Figures (range + SE), per-seed tables, hierarchical stats: `paper/figs/`,
`paper/results_core_matrix.md`, `paper/results_significance.md`. All numbers
re-pulled from wandb at n=8; provenance gap (lineage gen-0 = audit replicate)
still flagged, one cluster eval to close.

## r2dreamer weekend arc (the honest rollercoaster)

1. **v2 breakthrough stands**: repeat-4 + horizon-333 + entropy-3e-5 + re-inject
   → **picked 0.20 eval** — still the only genuine working world-model policy.
2. **v3-ent** (entropy 3e-4 restored): killed per your same-day revision.
3. **v4-r100** (your +100 reward): trained to an apparent 30% pick rate → eval
   1/15 → forensics: **the policy was flinging the can airborne with closed
   fingers** — the pick predicate (z + grip commanded) never checked *holding*;
   148 episodes double-collected pick+place mid-flight. Honest v4 rate: 3–5%.
   The loud reward was a good idea that found a weak predicate.
4. **Predicate hardened** (eef-proximity + 10-frame sustain, replay-validated;
   `genesis_vec_env` copy still needs the same guard before any dv3 retrain).
5. **v5 (loud reward × honest predicate)**: completed 6M overnight — final
   window **0/300**: with the fling un-payable, the +100 config learned nothing.
   Checkpoint eval queued for confirmation, but the verdict shape is clear:
   **v2's recipe remains SOTA; the reward-scale lever fails honest**.

Meta-lesson for the paper's discussion: three independent "successes" this
weekend (v1's 20% train rate, v4's 30%, the z-band 'placed') were all
measurement artifacts caught by paired controls — the honesty machinery is
earning its methods-section keep.

## Eval-harness verification (your skepticism, resolved)
Prev-action conditioning proven correct (bit-identical under garbage action
keys), sampling RNG-matched bit-exact, buffer parity test passes all checks and
is now a permanent guard (`test_buffer_parity.py`, `test_action_conditioning.py`).

## Cluster: needs you (the restart list)
1. `dSACfD` harvest failed → 7 conditions stuck `DependencyNeverSatisfied`.
   Paste `tail -30 paper_smoke_harvest_<id>.out`, then scancel the 7 + relaunch.
2. `dDP_SACfD_s7` crashed mid-train (row is n=7) — one resubmit.
3. Optional: dv3 s0/s1 final ~1M to 5M; literal gen-0 checkpoint eval (provenance).

## Proposed next moves (your call)
- **Cluster port of r2dreamer** (you proposed it): py3.11 venv install script +
  sbatch → seeds × sources × long budgets in parallel. I build it on your go.
- **H4 twins under the v2 recipe**: dH (done: 0.20) vs dDP — still blocked on
  the `genesis_m1all` reverse-rsync.
- **Place via BC-teacher harvest** (two-phase paper option): dH_DP full-demo
  policies place at ~0.5–0.6 → harvestable model place demos without RL-place.
- Paper assembly: skeleton + H2 distributional analysis remain unstarted.
