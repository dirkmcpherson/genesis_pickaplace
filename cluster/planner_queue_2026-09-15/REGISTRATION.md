#### P-MP-20260915 revision 4 — one-shot Slurm submission, no resident dispatcher (2026-09-15, before submission)

The user forbids a cluster-resident dispatcher and explicitly requests putting the motion-planning runs in the queue. Submit all32 jobs, **n=8 per learner**, seeds0–7, via the separately recorded `cluster/planner_queue_2026-09-15/queue_once.py`. Keep the reviewed training preparation immutable (FILES.json SHA256 `82e55cd0c92154f0937e0044968947f86f5856f602d5eb70a75ea55a85db46b9`), all learner commands, datasets, budgets and run paths unchanged. This scheduling-only supplement runs the existing full integrity/data gates and original command builder. It records its own hash and exact sbatch commands beside the submission ledger.

Replace the four-total-pending rule with four start-dependency chains, one per learner: seed k>0 depends on the **start** of seed k−1, and all jobs depend on any reference ah DP jobs pending at submission. There are at most four dependency-eligible planner jobs in the initial scheduling pass; already-started jobs that are subsequently requeued by preemption can add waiting jobs. Slurm enforces the shared preempt20-GPU cap. Submit each new job held; verify the complete32-job dependency graph before releasing these jobs. This permits the full cohort to be queued without a persistent process. Partial submission errors preserve held jobs for explicit inspection; do not blindly resubmit or invent replacement seeds. Training failures no longer automatically stop the already-queued cohort; retain and report failed seeds. No significance-dependent stopping.

The original one-shot queue helper's installed planner priority hook remains active while planner jobs await their turn. Remaining reference RLPD seeds stay held until a later one-shot helper invocation admits them after the planner cohort has started. No queued reference command or run path is changed, and no running job is interrupted. The previous dispatch.py is not started. Milestone evaluation remains a separate one-shot sweep where not already handled by the training launchers. Record actual job IDs and state; queued jobs are not completed results.
